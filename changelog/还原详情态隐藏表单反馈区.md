# 还原详情态隐藏表单反馈区

## 需求

用户（贴客户详情截图）："之前说的详情就不显示错误信息的位置这个得还原回来"。

即撤销 2026-09-18 早前的「详情态隐藏表单反馈区」改动（`changelog/详情态隐藏表单反馈区.md`），恢复 view 态下 `NFormItem` 底部的反馈区占位（错误信息位置）。

## 还原内容

改动仅涉及 `src/components/Form/index.vue`，两处全部还原：

1. `NFormItem` 移除 `:show-feedback="!isView"` 及其注释 —— view 态恢复渲染反馈区（`.n-form-item-feedback-wrapper`，min-height 占位，与编辑态一致）。
2. `NGrid` 移除 `:y-gap="isView ? 10 : 0"` —— 行距恢复由反馈区自然撑开。

其余（view 态只读渲染、mergedRules 空校验、labelPlacement 派生等）均不动；`isView` 在模板与 mergedRules 仍被使用，无死代码残留。

## 验证

- `pnpm typecheck` 通过（0 错误）。
- read_lints 该文件 0 诊断。
- 无 UI 自动化；预期表现 = 详情抽屉每行下方恢复反馈区占位高度，与首次做该需求之前完全一致。
