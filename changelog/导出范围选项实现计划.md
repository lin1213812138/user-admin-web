# 导出组件「数据范围」选项 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 exceljs 字段选择导出弹窗增加「数据范围」选项（全部 / 当前页 / 勾选数据），让用户显式决定导出哪一批行数据。

**Architecture:** 范围选择作为 `FieldSelectDialog` 的**可选 UI 能力**（不传 `scopes` 就不渲染，保证 `DataExport` 零变化），确认时连同字段一起以 `ExportConfirmPayload` 回传；`TableExportAction` 负责按页面提供的三种数据源（`fetchAll` / `data` / `checkedData`）拼装并决定各选项可用性；vxe 原生 `action-export` 管线完全不动。

**Tech Stack:** Vue 3.5 `<script setup lang="ts">`、Naive UI 2.44（`NRadioGroup` / `NRadioButton`）、vxe-table 4.21、exceljs、vue-i18n 11、UnoCSS。

## Global Constraints

（逐条摘自 `AGENTS.md` 与 `MEMORY.md`，每个任务隐式继承）

- 包管理只能用 **pnpm**，禁止 npm / yarn。
- 禁止 `any`；必要时 `unknown` + 类型收窄。
- 注释用**中文**（用户最高优先级偏好，覆盖 AGENTS.md 的英文 JSDoc 约定）。
- 格式化只跑 `pnpm fmt`（oxfmt），**不要手动调格式**：2 空格缩进、单引号、打印宽度 120、无尾随逗号、单参箭头省略括号。
- 新增 i18n key 必须 **三处同步**：`src/locales/langs/zh-cn.ts`、`src/locales/langs/en-us.ts`、`src/typings/app.d.ts` 的 `App.I18n.Schema`（`common` 段在 `app.d.ts:399-416` 附近）。漏改 `app.d.ts` 会直接 typecheck 失败。
- 禁止手改 `src/typings/components.d.ts`（vite 插件生成）。
- ⚠️ 本项目**没有测试框架、没有 test 脚本**。验证手段是 `pnpm typecheck` + `pnpm lint` + `pnpm fmt` + `pnpm build`，不要臆造测试入口。
- ⚠️ 本机 `pnpm build` 可能被 WebStorm `coding-copilot` 的 `node-safe-delete-shim` 拦截 dist 清理（已知坑）。若失败，改用 `pnpm typecheck` + `pnpm lint` 结果作为验收，并在收尾时明确告知用户需自行跑一次 `pnpm dev` 手测。
- `src/components/Export/field-export.vue` 是 `table-export-action.vue` 的**别名文件，禁止删除**（`components.d.ts` 已登记，删除会导致 typecheck 失败）。

## File Map

| 文件                                                             | 职责         | 改动                                                              |
| ---------------------------------------------------------------- | ------------ | ----------------------------------------------------------------- |
| `src/components/Export/type.ts`                                  | 类型定义     | 新增 `ExportScope` / `ExportScopeOption` / `ExportConfirmPayload` |
| `src/components/Export/index.ts`                                 | 统一出口     | 导出新类型                                                        |
| `src/components/Export/field-select-dialog.vue`                  | 纯 UI 弹窗   | 新增可选范围区 + `confirm` 签名改为 payload                       |
| `src/components/Export/data-export.vue`                          | 非表格页入口 | 适配 `confirm` 新签名（行为不变）                                 |
| `src/components/Export/table-export-action.vue`                  | 表格页入口   | 装配三种数据源 + 选项可用性 + 按范围取数                          |
| `src/locales/langs/zh-cn.ts`、`en-us.ts`、`src/typings/app.d.ts` | i18n         | 新增 6 个 key（三处同步）                                         |
| `src/views/system-manage/user/index.vue`                         | 示例页       | 透传 `:checked-data`                                              |

依赖顺序：Task 1 → 2 → 3 → 4 → 5 → 6。Task 3 会临时破坏 Task 4/5 的类型（调用方未适配），Task 5 结束时恢复。

---

### Task 1: i18n 新增 6 个 key（三处同步）

**Files:**

- Modify: `src/locales/langs/zh-cn.ts:87-101`（`common` 段，`export` 系列之后）
- Modify: `src/locales/langs/en-us.ts:87-101`（同上）
- Modify: `src/typings/app.d.ts:401-415`（`App.I18n.Schema` 的 `common` 段）

**Interfaces:**

- Produces: `common.exportScope`、`exportScopeAll`、`exportScopePage`、`exportScopeChecked`、`exportScopeCheckedEmpty`、`exportScopeAllUnavailable`（后续全部任务依赖）。

- [ ] **Step 1: 在 `zh-cn.ts` 的 `common` 段新增中文文案**

在 `exportFailed: '导出失败',` 之后插入：

```ts
    exportScope: '数据范围',
    exportScopeAll: '全部数据',
    exportScopePage: '当前页（{count} 条）',
    exportScopeChecked: '勾选数据（{count} 条）',
    exportScopeCheckedEmpty: '请先勾选要导出的行',
    exportScopeAllUnavailable: '未配置全量取数',
```

- [ ] **Step 2: 在 `en-us.ts` 的 `common` 段新增英文文案**

在 `exportFailed: 'Export failed',` 之后插入：

```ts
    exportScope: 'Data Scope',
    exportScopeAll: 'All Data',
    exportScopePage: 'Current Page ({count})',
    exportScopeChecked: 'Selected Rows ({count})',
    exportScopeCheckedEmpty: 'Please select rows to export first',
    exportScopeAllUnavailable: 'Full data source not configured',
```

- [ ] **Step 3: 在 `app.d.ts` 的 `common` 段补类型声明**

在 `exportFailed: string;` 之后插入：

```ts
exportScope: string;
exportScopeAll: string;
exportScopePage: string;
exportScopeChecked: string;
exportScopeCheckedEmpty: string;
exportScopeAllUnavailable: string;
```

- [ ] **Step 4: 校验类型层的 key 已对齐**

Run: `pnpm typecheck`
Expected: 通过（新增 key 尚未被使用，但类型必须齐，否则后续 `$t(...)` 报 TS2345）。

- [ ] **Step 5: Commit**

```bash
git add src/locales/langs/zh-cn.ts src/locales/langs/en-us.ts src/typings/app.d.ts
pnpm commit:zh
```

---

### Task 2: `type.ts` 定义范围类型并在 `index.ts` 导出

**Files:**

- Modify: `src/components/Export/type.ts`（文件末尾追加）
- Modify: `src/components/Export/index.ts:3`

**Interfaces:**

- Consumes: 已存在的 `ExportField`。
- Produces: `ExportScope`、`ExportScopeOption`、`ExportConfirmPayload`（Task 3/4/5 依赖）。

- [ ] **Step 1: 在 `type.ts` 末尾追加范围类型**

```ts
/** 导出数据范围：all=全部数据，page=当前页，checked=勾选的数据 */
export type ExportScope = 'all' | 'page' | 'checked';

/** 范围选项（弹窗渲染用）：disabled 时灰显，hint 说明不可用的原因 */
export interface ExportScopeOption {
  value: ExportScope;
  label: string;
  disabled: boolean;
  /** 禁用时展示的原因说明（挂在 title 上悬浮可见） */
  hint?: string;
}

/** 字段弹窗确认回传：数据范围（未启用范围区时为 null）+ 勾选且排序后的字段 */
export interface ExportConfirmPayload {
  scope: ExportScope | null;
  fields: ExportField[];
}
```

- [ ] **Step 2: 在 `index.ts` 导出新类型**

把第 3 行改为：

```ts
export type { ExportField, ExportColumnSource, ExportScope, ExportScopeOption, ExportConfirmPayload } from './type';
```

- [ ] **Step 3: 校验**

Run: `pnpm typecheck`
Expected: 通过。

- [ ] **Step 4: Commit**

```bash
git add src/components/Export/type.ts src/components/Export/index.ts
pnpm commit:zh
```

---

### Task 3: `field-select-dialog.vue` 增加范围区 + `confirm` 改为 payload

**Files:**

- Modify: `src/components/Export/field-select-dialog.vue:11-45`（script：props / emits / 本地状态）
- Modify: `src/components/Export/field-select-dialog.vue:200-234`（`handleConfirm` + 模板顶部）
- Modify: `src/components/Export/field-select-dialog.vue:221`（弹窗高度 520 → 560）

**Interfaces:**

- Consumes: Task 1 的 i18n key、Task 2 的 `ExportScope` / `ExportScopeOption` / `ExportConfirmPayload`。
- Produces: props `scopes` / `scope`，emit `update:scope`，新签名 `confirm(payload: ExportConfirmPayload)`。

- [ ] **Step 1: 扩展 import 与 props / emits**

把第 14 行改为：

```ts
import type { ExportConfirmPayload, ExportField, ExportScope, ExportScopeOption, ExportValueMode } from './type';
```

在 `Props` 的 `title?: string;` 之后追加：

```ts
  /** 可选：数据范围选项；不传或空数组则不渲染范围区（DataExport 场景） */
  scopes?: ExportScopeOption[];
  /** 当前选中的数据范围，配合 update:scope 使用；null 表示调用方未启用范围区 */
  scope?: ExportScope | null;
```

`withDefaults` 补充两个默认值：

```ts
const props = withDefaults(defineProps<Props>(), {
  loading: false,
  title: undefined,
  scopes: undefined,
  scope: null
});
```

`Emits` 改为：

```ts
interface Emits {
  (e: 'update:visible', visible: boolean): void;
  (e: 'update:scope', scope: ExportScope): void;
  /** 确认导出：回传选中的范围与勾选排序后的字段 */
  (e: 'confirm', payload: ExportConfirmPayload): void;
}
```

- [ ] **Step 2: 增加本地范围状态与打开时同步**

在 `const visible = computed({...})` 之后追加：

```ts
/** 本地范围值：naive-ui 的 value 是宽类型，收窄时按 scopes 反查，避免类型断言 */
const currentScope = ref<ExportScope | null>(props.scope);

function handleScopeChange(value: string | number | boolean | null) {
  const matched = props.scopes?.find(item => item.value === value);
  if (!matched) return;
  currentScope.value = matched.value;
  emit('update:scope', matched.value);
}
```

在 `watch(() => props.visible, ...)` 的 `if (val)` 分支**第一行**（`pendingDrag = null;` 之前）插入：

```ts
currentScope.value = props.scope;
```

- [ ] **Step 3: `handleConfirm` 改为回传 payload**

把函数体末行的 `emit(...)` 替换为：

```ts
emit('confirm', {
  scope: currentScope.value,
  fields: ordered.filter(item => item.checked).map(toExportField)
});
```

- [ ] **Step 4: 弹窗高度 520 → 560**

把 `style="width: 760px; height: 520px"` 改为 `style="width: 760px; height: 560px"`。

- [ ] **Step 5: 模板顶部插入范围区**

在 `<div class="h-full w-full flex flex-col min-h-0">` 的下一行、`<div class="flex-y-center justify-between pb-8px">` 之前插入：

```vue
<div v-if="scopes?.length" class="flex-y-center gap-12px pb-8px">
        <span class="text-14px">{{ $t('common.exportScope') }}</span>
        <NRadioGroup :value="currentScope" @update:value="handleScopeChange">
          <NRadioButton
            v-for="opt in scopes"
            :key="opt.value"
            :value="opt.value"
            :disabled="opt.disabled || loading"
            :title="opt.disabled ? opt.hint : undefined"
          >
            {{ opt.label }}
          </NRadioButton>
        </NRadioGroup>
      </div>
```

- [ ] **Step 6: 校验（预期会失败，属正常）**

Run: `pnpm typecheck`
Expected: **失败** —— `table-export-action.vue` 与 `data-export.vue` 的 `@confirm` 回调仍是旧的 `ExportField[]` 签名。Task 4 / 5 修复。

- [ ] **Step 7: Commit**

```bash
git add src/components/Export/field-select-dialog.vue
pnpm commit:zh
```

---

### Task 4: `data-export.vue` 适配新签名（行为不变）

**Files:**

- Modify: `src/components/Export/data-export.vue:11`（import）、`44-48`（`handleExport`）

**Interfaces:**

- Consumes: Task 2 的 `ExportConfirmPayload`、Task 3 的新 `confirm` 签名。
- Produces: 无下游依赖。

- [ ] **Step 1: 改 import 与 `handleExport`**

第 11 行改为：

```ts
import type { ExportConfirmPayload, ExportField } from './type';
```

`handleExport` 签名与取数行改为：

```ts
async function handleExport(payload: ExportConfirmPayload) {
  if (exporting.value) return;
  exporting.value = true;
  try {
    await exportRowsToXlsx(props.data, payload.fields, props.filename);
```

- [ ] **Step 2: 确认模板未传 `scopes`**

模板第 61 行保持 `<FieldSelectDialog v-model:visible="visible" :fields="fields" :loading="exporting" @confirm="handleExport" />` —— 不传 `scopes`，范围区不渲染，非表格页行为零变化。

- [ ] **Step 3: 校验**

Run: `pnpm typecheck`
Expected: 仍**失败** —— 只剩 `table-export-action.vue` 一处未适配。Task 5 修复。

- [ ] **Step 4: Commit**

```bash
git add src/components/Export/data-export.vue
pnpm commit:zh
```

---

### Task 5: `table-export-action.vue` 装配三种数据源

**Files:**

- Modify: `src/components/Export/table-export-action.vue:8-13`（import）、`19-51`（props / 状态 / 选项计算）、`53-68`（`handleExport`）、`86`（模板传参）

**Interfaces:**

- Consumes: Task 1 的 i18n、Task 2 的类型、Task 3 的新 `confirm` 签名。
- Produces: props `checkedData` / `showScope`（Task 6 的示例页使用）。

- [ ] **Step 1: 改 import**

```ts
import { computed, ref, watch } from 'vue';
import { $t } from '@/locales';
import FieldSelectDialog from './field-select-dialog.vue';
import { exportRowsToXlsx } from './export-xlsx';
import { toExportFields } from './type';
import type { ExportColumnSource, ExportConfirmPayload, ExportField, ExportScope, ExportScopeOption } from './type';
```

- [ ] **Step 2: 扩展 props**

在 `interface Props` 的 `fetchAll` 之后追加：

```ts
  /** 勾选的数据（表格开了 show-checkbox 时由页面 @selection-change 透传）；不传则「勾选数据」灰显禁用 */
  checkedData?: unknown[];
  /** 是否显示「数据范围」选项；关闭后退化为「有 fetchAll 导全量，否则导当前页」 */
  showScope?: boolean;
```

`withDefaults` 改为：

```ts
const props = withDefaults(defineProps<Props>(), {
  extraFields: undefined,
  fetchAll: undefined,
  checkedData: undefined,
  showScope: true,
  filename: undefined
});
```

- [ ] **Step 3: 增加范围状态与选项计算**

在 `const visible = ref(false);` 之后插入：

```ts
/** 默认范围：有 fetchAll 取全量，否则当前页（与改造前行为一致） */
const defaultScope = computed<ExportScope>(() => (props.fetchAll ? 'all' : 'page'));
const currentScope = ref<ExportScope>(defaultScope.value);
const checkedCount = computed(() => props.checkedData?.length ?? 0);

/** 每次打开弹窗都重置为默认范围，不记忆上次选择 */
watch(visible, val => {
  if (val) {
    currentScope.value = defaultScope.value;
  }
});

/** 范围选项：全部依赖 fetchAll，勾选依赖勾选行数，不可用时灰显并给出原因 */
const scopeOptions = computed<ExportScopeOption[]>(() => {
  if (!props.showScope) return [];
  return [
    {
      value: 'all',
      label: $t('common.exportScopeAll'),
      disabled: !props.fetchAll,
      hint: props.fetchAll ? undefined : $t('common.exportScopeAllUnavailable')
    },
    { value: 'page', label: $t('common.exportScopePage', { count: props.data.length }), disabled: false },
    {
      value: 'checked',
      label: $t('common.exportScopeChecked', { count: checkedCount.value }),
      disabled: checkedCount.value === 0
    }
  ];
});

/** 按范围取行数据；scope 为 null（未启用范围区）时退回默认范围 */
function resolveRows(scope: ExportScope | null): Promise<unknown[]> | unknown[] {
  const target = scope ?? defaultScope.value;
  if (target === 'all' && props.fetchAll) return props.fetchAll();
  if (target === 'checked') return props.checkedData ?? [];
  return props.data;
}
```

- [ ] **Step 4: `handleExport` 改为按范围取数**

```ts
async function handleExport(payload: ExportConfirmPayload) {
  if (exporting.value) return;
  // 勾选项被绕开（如外部数据变化）时兜底提示，且不关闭弹窗
  if (payload.scope === 'checked' && checkedCount.value === 0) {
    window.$message?.warning($t('common.exportScopeCheckedEmpty'));
    return;
  }
  exporting.value = true;
  try {
    const rows = await resolveRows(payload.scope);
    await exportRowsToXlsx(rows, payload.fields, props.filename);
    window.$message?.success($t('common.exportSuccess'));
    visible.value = false;
  } catch (error) {
    console.error('export failed', error);
    // 失败不关闭弹窗，方便调整后重试
    window.$message?.error($t('common.exportFailed'));
  } finally {
    exporting.value = false;
  }
}
```

- [ ] **Step 5: 模板传参**

第 86 行改为：

```vue
<FieldSelectDialog
  v-model:visible="visible"
  v-model:scope="currentScope"
  :fields="exportFields"
  :scopes="scopeOptions"
  :loading="exporting"
  @confirm="handleExport"
/>
```

- [ ] **Step 6: 校验**

Run: `pnpm typecheck`
Expected: **通过**（Task 3 引入的类型断裂到此全部修复）。

- [ ] **Step 7: Commit**

```bash
git add src/components/Export/table-export-action.vue
pnpm commit:zh
```

---

### Task 6: 示例页接入 + 全量校验与收尾

**Files:**

- Modify: `src/views/system-manage/user/index.vue:127-132`

**Interfaces:**

- Consumes: Task 5 的 `checkedData` prop；页面已持有 `checkedRows`（第 49 行 `const checkedRows = ref<UserItem[]>([])`，由第 51-53 行 `@selection-change` 写入）。

- [ ] **Step 1: 用户管理页透传勾选数据**

把 `TableExportAction` 的用法改为：

```vue
<TableExportAction
  :columns="columnConfigs"
  :data="data"
  :checked-data="checkedRows"
  :fetch-all="fetchAllUsers"
  :filename="$t('route.system-manage_user')"
/>
```

- [ ] **Step 2: 类型检查**

Run: `pnpm typecheck`
Expected: 通过。

- [ ] **Step 3: lint + 格式化**

Run: `pnpm lint && pnpm fmt`
Expected: 均通过；若 `pnpm fmt` 修改了文件，再跑一次 `pnpm typecheck` 确认。

- [ ] **Step 4: 构建验证（可能受 shim 影响）**

Run: `pnpm build`
Expected: 成功构建。若因 WebStorm `node-safe-delete-shim` 拦截 dist 清理而失败，记录该情况、**不要强行绕**，以 typecheck + lint 结果验收，并在收尾报告中提示用户自行跑 `pnpm dev` 手测。

- [ ] **Step 5: Commit**

```bash
git add src/views/system-manage/user/index.vue
pnpm commit:zh
```

- [ ] **Step 6: 更新变更记录**

在 `changelog/导出范围选项设计.md` 顶部把「状态：设计已确认，待实现」改为「状态：已实现（待浏览器手测）」，并追加一节「实现结果」，记录：三个范围选项的实际数据源映射、不可用项的禁用表现、typecheck/lint/fmt/build 的实际结果、手测未覆盖项。

同时按项目规则 1，在 `AGENTS_CHANGELOG.md` 的 2026-09-05 分组补一条实现记录链接（复用同一份 `changelog/导出范围选项设计.md` 即可，不新建文档，避免索引重复）。

- [ ] **Step 7: 冒烟清单（交用户手测）**

向用户交付以下手测项：

1. 系统管理 → 用户管理，左侧「导出」按钮 → 弹窗顶部出现「数据范围」单选组。
2. 默认选中「全部数据」（本页传了 `fetchAll`）。
3. 未勾任何行时，「勾选数据（0 条）」灰显不可选。
4. 勾选 2 行后重新打开弹窗，该项变为「勾选数据（2 条）」且可选。
5. 选「当前页（20 条）」导出，文件行数 = 当前页行数。
6. 选「勾选数据」导出，文件行数 = 勾选行数。
7. 关闭弹窗再打开，范围回到「全部数据」（验证不记忆）。
8. 非表格页（若有使用 `DataExport`）弹窗**不应**出现范围区。

---

## 自审结果

- **Spec 覆盖**：spec 第 3.1 / 3.2 / 3.3 / 3.4 / 3.5 / 3.6 分别对应 Task 2 / 3 / 5 / 4 / 1 / 6，无遗漏；spec 第 5 节验证对应 Task 6 Step 2-4 + Step 7。
- **占位符扫描**：无 TBD / TODO /「类似 Task N」等；所有代码步骤均给出可直接粘贴的完整代码块。
- **类型一致性**：`ExportScope`、`ExportScopeOption`、`ExportConfirmPayload` 在 Task 2 定义，Task 3/4/5 使用同名同形；`resolveRows` 在 Task 5 定义并在同一 Task 内使用；`checkedData` / `showScope` 在 Task 5 定义、Task 6 使用，命名一致。
- **中间失败是预期的**：Task 3 Step 6 与 Task 4 Step 3 的 typecheck 失败已在步骤中写明预期原因，不是计划缺陷。
