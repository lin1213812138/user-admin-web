# 分配权限界面隐藏「菜单类型 / 路由地址 / 权限标识」三列

## 需求

用户附「角色分配权限」抽屉截图，红框圈出「菜单类型」「路由地址」「权限标识」三列，要求"分配权限界面不显示这些"。

## 改动

`src/views/system-manage/role/modules/role-permission-drawer.vue`：

- `columns` 由 5 列缩减为 2 列：保留「菜单名称」（树节点列，含图标）与「操作权限」（按钮权限勾选区）；
- 同步删除对应的自定义单元格插槽 `#menuType` / `#routePath` / `#permission`；
- 同步删除仅这三列使用的 `menuTypeTag` / `menuTypeLabel` 两个变量（避免未使用告警）。

**保留未动**：

- 行数据里仍保留 `menuType` / `routePath` / `permission` 字段（`mergeRows` 继续从菜单详情补齐，供按钮权限匹配与「首页必选」判定使用）；
- 顶部搜索框仍按「菜单名称 / 路由地址 / 权限标识」匹配（`matchKeyword` 未改）——如需一并收敛可再调整。

## 验证

- `pnpm typecheck` 0 错误 / oxlint 0 / eslint（.vue 口径）0；
- `pnpm fmt` 已执行；
- 浏览器端到端不可达（本地无后端），未做界面实测；预期表现 = 抽屉表格仅剩「菜单名称」「操作权限」两列（含勾选列）。
