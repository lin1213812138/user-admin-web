# 抓取时间模块先注释隐藏

日期：2026-09-15

## 背景

用户："抓取时间模块先注释一下"——「系统设置 → 轨迹抓取配置」的**抓取时间**子 tab 字段尚未确定（本轮先做「轨迹改造」「轨迹关键词」），先从前端隐藏。

## 改动

`src/views/system-manage/setting/modules/trace-capture/TraceCapture.vue`：

- `configTabs` 数组注释掉 `capture-time` 一项（保留代码 + 中文注释说明「恢复时取消注释即可」），
  tab 栏由 5 项变为 4 项：追踪网络 / 轨迹改造 / 轨迹关键词 / 操作轨迹；
- 数组上方注释同步为「3 个子页共用同一个 tab 数组」。

## 不改动

- `TraceCaptureCategory` 与 `SubTabKey` 类型：保留 `capture-time` 成员，便于恢复；
- `TraceConfigTable.vue`（含 `categoryTabKey` 的 capture-time 映射）：仍服务追踪网络页；
- i18n `subTab.captureTime` 文案与相关 mock 数据：保留未删。

## 验证

- `pnpm typecheck` 0 错误；单文件 oxfmt / oxlint / eslint 0；
- 无浏览器端到端（本地无后端会话），视觉确认由用户进行。
