# 最后更新列只显示时间

> 用户："最后更新就显示时间不要其他的"。

## 改动

`views/system-manage/site/index.vue` 的 `#updateDate` 插槽：去掉 `updateBy - ` 前缀，只渲染 `formatDate(row.updateDate)`（dayjs `YYYY-MM-DD HH:mm`）。「修改人」信息不再在该列展示（后端字段 `updateBy` 仍在实体里，需要时可单独加列）。

## 验证

`pnpm typecheck` 0 / `pnpm fmt` 已跑（纯模板插值删减，无 lint 影响）。
