# 标签设计器移除刻度

> 日期：2026-09-12　模块：`src/views/system-manage/label-designer`

用户不需要画布顶部刻度尺，整体删除（打印设计页 hiprint 自带刻度不受影响）：

- `design-canvas.vue`：删纸张上方 `.ruler` 元素与 `.ruler` 样式块。
- `tool-bar.vue`：删「刻度」显隐切换按钮。
- `store/modules/label-design/index.ts`：删 `showRuler` ref 与导出。
- i18n：删 `page.manage.labelDesign.ruler`（zh-cn / en-us / typings 三处同步）。

验证：`pnpm typecheck` 通过。
