# 2026-08-31

## FormWrap 展开/收起 + 右侧独立按钮区

- 改造 `src/components/Form/index.vue`：`items` 中带 `slot` 的项视为操作按钮（action），抽离到右侧独立按钮区，不计入行数。
- 展开/收起：仅当存在 action 项且字段按 `span` 累加行数 > 1 时显示按钮，收起只显示第 1 行。无需额外 prop。
- 关键坑：本仓库 `GetI18nKey<Schema>` 对庞大的 `common` 联合类型会**截断**，新 i18n 键必须加到 `common` 顶部（及 `src/typings/app.d.ts` 的 `Schema.common` 顶部），否则 `$t('common.xxx')` 报类型错误。本次新增了 `common.expandFilter`/`common.collapseFilter`（顶层 common，注意 lang 文件里另有一个带 `themeConfig`/`fullscreen`/`collapse:'折叠菜单'` 的嵌套 common，不要加错）。
- 验收：typecheck + lint + fmt 均通过。role 搜索区已用此能力。
