# 2026-09-02

## 移除 Tauri（桌面端）相关代码/配置

用户要求从 user-admin-web 移除全部 Tauri 相关内容。

已完成（已全局搜索验证，src-tauri 目录本身除外 0 处残留）：

- `package.json`：删除脚本 `build:tauri`/`dev:tauri`/`tauri-icon`；删除依赖 `@tauri-apps/api`、`@tauri-apps/plugin-opener`、`@tauri-apps/cli`。
- `vite.config.ts`：移除 `server.watch.ignored`（不再忽略 src-tauri）。
- `eslint.config.js`：移除 `src-tauri/target/**` 忽略规则，简化为 `export default baseConfig;`。
- `.gitignore`：移除 `src-tauri/target` 条目。
- `AGENTS.md`：移除技术栈、命令表、目录结构中的 Tauri 说明。
- `src/` 业务代码无 Tauri 引用（已确认）。

阻塞项（环境 safe-delete 护栏拒绝自动删除，需用户在自有终端手动执行）：

- `Remove-Item -Recurse -Force "d:\LINFLY\CWMS\user-admin-web\src-tauri"`
- `pnpm install`（同步 pnpm-lock.yaml 移除 tauri 依赖；pnpm 删除文件时也被同一护栏拦截）

## 生成 README.md

项目原本无 README.md（初始快照条目过期）。基于 AGENTS.md 真实架构新建 `README.md`（中文，含技术栈/环境/脚本/目录/约定/数据流/接口/权限），已排除 Tauri。

## Link 组件新增 iconHover prop

`src/components/common/link.vue` 新增 `iconHover?: boolean`（默认 false）。为 true 时根元素加 `link--icon-hover`，scoped CSS 让 `.link__icon` 默认 `display:none`、`:hover` 时 `display:inline-flex`（方案一：完全不占位）。兼容 iconPosition left/right 与 disabled。

## Table type='detail' 启用 iconHover

`src/components/Table/table.vue:142` 的 `<Link type="primary" icon-position="right">` 补上 `icon-hover`，使复制图标默认隐藏、hover 时出现。仅为该调用点补参，不改 Link 组件默认行为。
