# 2026-09-14

## 基础资料模块重构（完成）

- 将「基础资料」下旧的客户/供应商/商品/商品分类档案**完全替换**为 5 个新子模块：
  - 国家地区 `countryRegion`
  - 邮政路由码 `postalRoute`
  - FBA仓库 `fbaWarehouse`
  - 客户等级 `customerLevel`
  - 客户来源 `customerSource`
- 展示形式：沿用 `ArchiveSwitch` 单页 + 页内左侧子导航，**菜单项与 URL 不变**。
- 改动文件：`views/data-manage/basic/index.vue`、`typings/api/data-manage.d.ts`、`service/api/mock-data-manage.ts`、`locales/langs/zh-cn.ts`、`locales/langs/en-us.ts`、新增 `archives/basic/<kebab>/index.vue` ×5、清理 `typings/components.d.ts` 中失效的旧组件类型声明。
- 约定（适用于 MasterData 归档体系）：`archive` key 用驼峰、组件目录用 kebab-case、i18n 嵌套段用 camelCase。

## 财务资料子模块调整（完成）

- 按用户截图把「财务资料」改为 4 子模块：费用类型 / 结算方式 / 银行账户 / 结算币种及汇率。
- 移除「税率 tax」、新增「费用类型 expense-type」（字段 code+name+状态+备注）。
- 顺序调整 + 标题文案：account「结算账户→银行账户」、currency「币种→结算币种及汇率」。
- 改动：`finance/index.vue` 重排 items、删 `tax/index.vue` 新建 `expense-type/index.vue`、`typings/api/data-manage.d.ts`（`tax`→`expense-type` + FinanceExpenseType）、`mock-data-manage.ts` 工厂、zh-cn/en-us `finance.expenseType` 及标题、`typings/app.d.ts` i18n 类型、`typings/components.d.ts`（`Tax`→`ExpenseType`）。
- 按钮权限 `dataFinance`（/data-manage/finance）整页共用不变；验证 typecheck 财务相关 0 错误（`basic/*` 一批 pre-existing i18n 类型错误无关未处理）。

## 业务资料子模块改造（完成）

- 「业务资料」子模块由仓库/库位/承运商/门店 4 个**完全替换**为 9 个：单号资料管理 waybill / 地址簿管理 address / 申报物品 declaredGoods / 问题类别 problemCategory / 物品类别 goodsCategory / 报关类型 customsType / 出口原因 exportReason / 清关方式 clearanceMethod / 销售条款 salesTerms。
- 沿用 `ArchiveSwitch` 单页 + 页内左侧子导航（子档案不生成路由/菜单），与基础/财务资料同构。
- 字段先按通用「编码 code + 名称 name + 状态 + 备注」，i18n 抽 business 级通用键（code/name/form.codePlaceholder/form.namePlaceholder），9 模块各只加 `.title`。
- 改动：删 `archives/business/{warehouse,location,carrier,store}`、建 9 个 `archives/business/<kebab>/index.vue`、`data-manage/business/index.vue` items、d.ts 删 4 接口+改联合体+加 9、mock factories 删 4 加 9、zh-cn/en-us/app.d.ts business 段重排、清理 `components.d.ts` 失效的 Carrier/Location/Store/Warehouse 声明（dev 重新生成后只追加不清理，手动 prune）。
- 验证：单文件 oxfmt/eslint 0；**业务相关 typecheck 0**；全量 typecheck 仍因 `basic.*` 历史 i18n 类型错误失败（与本次无关，未处理）。

## 权限管理独立顶级菜单重组（完成）

- 用户要求把用户/角色管理从「系统管理」抽到新建的「权限管理」顶级菜单，并确认把菜单管理也一并归入（权限三件套）。
- 目录移动：`views/system-manage/{user,role,menu}` → `views/permission-manage/{user,role,menu}`（整体含 modules/）。
- 路由 meta 集中在 `build/plugins/router.ts` 的 `onRouteMetaGen`：routeIcons / routeOrders 移除 system-manage 的 user/role/menu、新增 `permission-manage`(`ic:round-lock` 父级, order 3) 及三子项（user `ic:round-person` order1 / role `ic:round-supervisor-account` order2 / menu `ic:baseline-menu` order3）。
- i18n：zh-cn/en-us `route` 段删 system-manage_user/role/menu、加 permission-manage 及三子项；`user/index.vue` 两处 `$t('route.system-manage_user')`→`permission-manage_user`。
- 自动生成文件（routes.ts/imports.ts/transform.ts/elegant-router.d.ts/components.d.ts）由 `pnpm dev` 重新生成，禁止手改；`mock.ts` 的 system-manage 演示目录节点属 mock 数据未动。
- 验证：pnpm dev 重新生成成功（父级带 icon/order）、定向 eslint 0 error；typecheck 仅余与本次无关的 data-manage 既有 i18n 类型错误。
- 讨论记录：`changelog/权限管理菜单重组.md` + `AGENTS_CHANGELOG.md` 索引。

## 基础资料 i18n 类型同步修复（完成）

- 现象：全量 `pnpm typecheck` 失败，52 个报错集中在 `basic.*`。
- 根因：`archives/basic/` 的 5 个 `.vue` 与 `views/data-manage/basic/index.vue` 已改用新 key（countryRegion/postalRoute/fbaWarehouse/customerLevel/customerSource），`zh-cn.ts`/`en-us.ts` 也已同步为这 5 个新段；但 `typings/app.d.ts` 的 `basic` 段仍是旧的 customer/supplier/goods/category（手写 `Schema` 类型，非脚本生成，scripts/ 下无 gen-i18n）。
- 改动：仅改 `src/typings/app.d.ts` 的 `basic` 段（旧 4 块→新 5 块），与 `.vue` 及语言包对齐。验证 `basic` 相关 52 个 typecheck 报错清零。
- 残留：全量 typecheck 仍剩 6 个报错，全部来自半成品模块 `system-manage_customer`（`build/plugins/router.ts:46,69` 引用了 `RouteMap` 不存在的 `system-manage_customer`；`service/api/system-manage.ts:10-13` 从 `./mock` 导入未导出的 `mockCustomerList` 等 4 函数）。用户选「保持现状」(option 1)，不碰此模块，待日后决定清理/实现。

## 系统管理新增客户管理模块（完成）

- 用户要求「系统管理新增客户管理模块」，确认落地为 system-manage 独立菜单页（与 site/group 同构、DEV mock 先行，不回归 MasterData 归档）。
- 字段（基础客户档案，用户确认）：客户编号(唯一查重)/客户名称(必填)/客户等级(普通·重要·VIP 静态选项)/客户来源(官网·转介绍·广告 静态选项)/联系人/联系电话/邮箱/地址/状态(启用·禁用)/备注 + 审计字段。
- 新增 `views/system-manage/customer/index.vue` + `modules/customer-operate-drawer.vue`；改 `typings/api/system-manage.d.ts`(Customer 五件套)、`service/api/system-manage.ts`(4 接口)、`service/api/mock.ts`(customers 数据+4 mock+菜单树 id27)、`constants/button-permissions.ts`(customer)、`locales/langs/zh-cn.ts|en-us.ts`+`typings/app.d.ts`(page.manage.customer.\* + route)、`build/plugins/router.ts`(icon `ic:round-business`/order 6)。
- 设计文档 `changelog/系统管理新增客户管理模块.md` + `AGENTS_CHANGELOG.md` 索引。
- 验证：typecheck / lint(0 error) / fmt / build 全通过（顺带清零了前面「基础资料 i18n 类型同步修复」段里残留的 6 个 `system-manage_customer` 报错）。
- 关键范式/坑：
  - i18n `route` 段新增 key 须在 elegant-router 生成路由后写（否则 `route: Record<I18nRouteKey,string>` 字面量 excess property 报错）；先 `pnpm dev` 生成再补 i18n，最后 typecheck 即干净。
  - `pnpm dev` 后台生成若中途 `Stop-Process` 会截断 `routes.ts`（末尾残留 `nMenuRoute(route));`），需重跑 `pnpm dev`/`pnpm build` 覆盖修复。
  - `FormWrap` 支持 `type:'section'` 区块标题（占满 24 栅格）；section 项 `key` 不可与字段 `key` 重复（否则 NGi `:key` 冲突，本模块用 `basic`/`contact`/`remarkSection` 规避）；详情态(mode='view')也渲染区块标题。
  - 状态 0（禁用）是有效筛选值，mock/接口用 `status === 0 || status === 1` 或 `??` 兜底，不用真值判断。

## 渠道报价模块（收货渠道 / 发货渠道）（完成）

- 用户要求新增「渠道报价」模块，含「收货渠道」「发货渠道」两个页面；确认=两个独立一级路由（各自独立页面、互不切换、不套 MasterDataArchive 通用组件）+ 新建独立一级模块 channel-quote（与 data-manage 平级）+ 先通用骨架（code/name/status/remark，业务字段后续补）。
- 改动：新建 `views/channel-quote/{receive,ship}/index.vue`（各自自包含列表页：直接用 Table+useVxeTable+Drawer+NFormWrap，不共享 MasterDataArchive）、`typings/api/channel-quote.d.ts`（Api.ChannelQuote：ChannelQuoteRow/ChannelQuoteArchiveKey=`receive|ship`/ReceiveChannel/ShipChannel/ChannelQuoteList/ChannelQuoteSearchParams）、`service/api/channel-quote.ts`（fetchGet/Create/Update/DeleteChannelQuote，按 archive key 区分，DEV mock / PROD `/channel-quote/:archive/xxx`）、`service/api/mock-channel-quote.ts`（factories 造数 + 4 mock 函数）、zh-cn/en-us `route`+`page.channelQuote`+`typings/app.d.ts` Schema。
- 关键范式：
  - service 的 `request` 分支必须 `as unknown as Promise<T>` 强转（与 data-manage 同款），否则 `useVxeTable` 的 api 返回类型与 FlatResponseData 联合不匹配。
  - 视图中 `searchItems`/`formItems` 用 `computed<FormItemConfig[]>` 注解，否则 `type:'input'` 被推宽为 `string` 报 FormItemConfig 不兼容。
  - 模板里 `row` 是 any，直接传 `openDrawer('edit', row)` / `row.id`，不要写 `row as Api.ChannelQuote.X`（会触发 eslint `vue/no-undef-properties` 误报 Api 未定义）。
  - `route` 段新增 key 须 `pnpm dev` 生成 RouteKey 后再补（否则 excess property 报错）。
- 路由由 `pnpm dev` 自动生成 `channel-quote` 父级 + 两子路由及菜单（i18nKey 已对齐）。
- 设计文档 `changelog/渠道报价模块.md` + `AGENTS_CHANGELOG.md` 索引。
- 验证：typecheck 0 / lint 0 error（仅 link.vue 两个与本次无关的预存 warning）/ fmt / build:test 全通过。

## 渠道报价菜单顺序调整（完成）

- 用户要求「渠道报价放首页下面」并明确是「同一级下面」：即作为**顶级菜单项**紧排在「首页」(home order 0) 下方，与 data-manage/system-manage/permission-manage 同级。
- 改动 `build/plugins/router.ts` 的 `routeOrders`：`home:0` → `channel-quote:1` → `data-manage:2` → `system-manage:3` → `permission-manage:4`（其余模块顺序顺延，子项 order 不受影响）。
- 菜单顺序由 `onRouteMetaGen` 在生成期烘焙进 `routes.ts` 的 `meta.order`，改配置后须重跑 `pnpm dev`/`pnpm build` 重新生成（已用 `pnpm build:test` 重新生成，channel-quote meta 已写入 `order:1`）。
- 顶级菜单项（home/data-manage 等）无自定义 icon 属既有状态，channel-quote 未加 icon 保持一致。
