# 2026-09-17

## 站点管理关联用户/客户查看（公共组件 RelationModal）

- 需求："站点管理表格新增两个字段，关联用户、关联客户，显示查看两个字然后点击查询关联的用户和客户"。
- 决策（用户确认）：Modal 弹窗展示；用户列=姓名/账号/所属站点/状态、客户列=编码/名称/所属站点/状态；弹层无搜索纯查看；追问"能不能封装一个组件"⇒ 封装**公共组件**（用户明确要放公共目录）。
- 后端事实（零改动）：User.siteId 与 Customer.siteId 原生支持，`/user/query` 与 `/customer/query`（queryCommon）都吃 `where.siteId`；Customer 模型冗余 `site` 站点名称。前端 `service/api/customer` 是模板 mock，未动。
- 实施：新建 `components/common/relation-modal.vue`（RelationModal=NModal+通用 Table+useVxeTable，Props=show/title/fetcher/columns/width 720/bodyHeight 480，不感知 siteId、过滤由调用方闭包绑定，immediate:false、打开回第一页重取、defaultPageSize 20、不传 cacheKey）；site service 加 fetchGetSiteUserList/fetchGetSiteCustomerList；typings 加 SiteCustomer/SiteCustomerList；site/index.vue 两列查看按钮+两个 RelationModal 实例+cacheKey 升 `system-manage-site-v3`；i18n 三处 site 段 6 key。
- 坑：① 弹窗 fetcher 解构 `data` 与主表格 useVxeTable 的 data 撞名（oxlint no-shadow）→ `data: res`；② useVxeTable 的 columns 仅 setup 求值一次 ⇒ 单实例切 fetcher/columns 不可行，挂两个实例各持静态列。
- i18n 术语：本仓 `page.manage.user.userName`=「用户账号」、`nickName`=「用户名称」，弹窗姓名列用 nickName。
- 验证：typecheck 0 / lint 0 error / fmt；未跑 playwright。changelog：`changelog/站点管理关联用户客户查看.md`。

## 追踪网络系统类型对接字段配置

- 用户给各系统对接信息清单 + 新增追踪网络截图（红框：网络名称/系统类型/服务器地址/官网查询地址）："框选的这几个是默认有的，其他根据系统类型显示不同名称，配到配置文件里"；随后拍板"直接把后端那个复制一份到前端"。
- 后端事实（`tms-user lib/ext-api/api/` 15 个适配器，trackType=文件名）：kdzs/hl/ydd=账号+密码、sd 速递管家=appToken+appKey、k5=Clientid+Token、kjv5=appKey+appSecret、ry=plantId+plantKey+bankerId、xzh=token、t6/sckj 顺仓=客户编码+API授权码、track17=17token、feia17 17FEIA=apiName+apiToken、nm=icID+key(MD5)、qy 秦远无字段、ups/fedex 无适配器（模型注释 14 码与适配器目录不一致）。
- 决策（用户确认）：映射以后端适配器为准复制到前端；钮门例外按用户清单只显示网址；动态字段全部必填；选项加顺仓/17FEIA/秦远、sd 改名速递管家。
- 实施：`constants/track-config.ts` 重写（TRACK_TYPE_CONFIGS 17 项 + trackTypeFields()，OPTIONS/trackTypeLabel 改派生，列表页零改动）；TrackNetworkDrawer formItems=固定 4 项（name/trackType/url/web）+ 按 trackType 动态生成必填对接字段（label 用配置参数名、切类型只改显隐不清值、channel 不再展示、model/回显/提交不变）；i18n 三处（固定项文案对齐截图：网络名称/服务器地址/官网查询地址，删 account~accountNoPlaceholder 10 key，加 fieldPlaceholder）。
- 验证：typecheck 0 / read_lints 0；未跑 playwright（建议实测显隐/红星/回显）。changelog：`changelog/追踪网络系统类型对接字段配置.md`。
- 同日调整：切系统类型重置校验+清空对接字段——`model.trackType` 加 sync watch（清 account/password/key/accountNo + restoreValidation）；**sync 是关键**：编辑回显 fillFormByRow 同步块先赋 trackType（触发清空）再回填 row 值，pre flush 会把回填值也清掉。
- 同日调整2：列表表头按截图=追踪网络/服务器地址/系统类型/最后编辑/编辑时间（url 移到系统类型前、删账号/渠道列），i18n col.url 改「服务器地址」，cacheKey 升 `trace-config-{category}-v3`。

## 资料管理新增「发货资料」（设计确认，待实施）

- 用户贴思维导图：发货资料 = 服务商(发货/派送/提单)/渠道类别/计泡规则(按公斤·按方)/承运网络，称"有些可能是其他界面的可以直接移过来"。调研：前端无任何现成实现（无可移代码），后端 tms-user 四组 CRUD 全齐（`/provider/*`、`/channel-group/*`、`/weight-rule/*`、`/carrier/*`，分页 `{page,size,keyword,where}`→`{list,total}`，delete 仅单 `_id`）。
- 五项决策（用户逐项确认）：① 资料管理第 5 页 `data-manage/ship` 竖 tab 四项 ② 服务商 1 tab + NRadioGroup 切 providerType（杂支 type3 不管理）③ 「按公斤/按方」= 计泡规则表单字段，后端 weight-rule 模型加 `calcMode`(0-按公斤 1-按方)，后端唯一改动 ④ 字段全量；承运网络 trackConfigId/remoteGroupId 缺数据源暂不展示、关联计泡规则下拉取本页全量 ⑤ 用户拍板"每个界面按照系统设置那样写"（index.vue 壳 + modules/<tab>/<Tab>.vue 独立实现，不做 config 通用组件）。
- service 直连真实接口 DEV 不 mock（export-format 先例）；批量删除=前端逐条调单删接口。设计文档 `changelog/发货资料.md` + 索引已更新；实施计划 `docs/superpowers/plans/2026-09-17-data-manage-ship.md`（11 任务：后端 calcMode → typings → service → i18n → 4 tab → 壳+路由生成 → 接线 → 验证）。
- **已实施并验证**：后端 weight-rule.js 加 calcMode；前端 typings `Api.DataManageShip` + `service/api/data-manage-ship`（16 函数）+ i18n 三处 + `views/data-manage/ship/`（壳 + 4 tab）+ router.ts icon/order + base-layout contentShowPadding + menu-permissions dataShip。typecheck/lint/fmt/build 0 错；playwright 真实后端走查 4 tab CRUD + 类型切换 + 进位规则组 + 名称映射全过，console 0 error，测试数据已清理，未提交。
- 走查发现（后端既有行为）：`/channel-group/query` 走 queryAllCommon 全量返回（忽略 keyword/page/size，order 升序）——数据量小无感知，量大时前端该 tab 要改本地过滤/:pagination null。另：provider keyword 只搜 name（keywordFields:['name']），搜 code 无结果属预期。
- 重生成路由顺带确认：vite dev 起在 9113（9111/9112 被旧 dev 占用），elegant 生成在启动早期完成，35 秒后 taskkill /T /F 杀进程树安全（生成文件完整）。

## 用户编辑页面（编辑跳独立页）

- 需求：贴参考系统截图"用户编辑新加个文件，按照这个"（基本信息卡片 + 顶部 tabs）。五项决策（用户确认）：① 仅编辑跳页面（新增/详情仍走现有抽屉）② 个人档案可编辑+保存（"不编辑放这里干嘛"）③ 配置权限 tab 留空 ④ 账号配置 tab 留空（截图的登录控制/通知配置字段 tms-user UserSchema 均不存在，仅 openId，mongoose strict 会丢弃）⑤ 关联客户 tab 去掉（能力在：/customer/query 支持 userId 按 salesmanId/serviceId/cashierId 过滤）。
- 实施：新增 `views/system-manage/user-edit/`（壳 index.vue=基本信息 NDescriptions 3 列 + 顶部 NTabs + 返回按钮，modules/{profile/Profile,perm/Perm,account/Account}.vue）；Profile=FormWrap 12 档案字段（复用 page.manage.user.\* key），提交 /user/update；i18n 三处 route.system-manage_user-edit + page.manage.user.editPage 6 key；build/plugins/router.ts hideInMenuRoutes+menuActiveKeys；`pnpm dev` 后台轮询生成路由后 user/index.vue handleEdit 改 `routerPushByKey('system-manage_user-edit', {query:{id}})`。
- 关键事实：① **RouteKey 类型生成在 `src/typings/elegant-router.d.ts` 单文件**（非 typings/elegant/ 目录，`@elegant-router/types` 由 vite 插件虚拟解析）② UserUpdateParams=`_id`+Omit<UserCreateParams,'password'>，必填 account/name/siteId，且 /user/update 为全量覆盖语义 ⇒ 局部保存也要把账号属性从 user 原样带回 ③ 重生成路由脚本：Start-Process cmd '/c pnpm dev' + 轮询 routes.ts 出现新 key + 8s + taskkill /T /F。
- 验证：typecheck 0 / lint 0 error（link.vue 2 条既有 warning）/ fmt / read_lints 0；未跑 playwright。changelog：`changelog/用户编辑页面.md`。
- **同日调整 1（用户纠正"不是单独的界面是侧滑弹窗"）**：撤销独立路由页（删 user-edit 目录/还原 router.ts/删 route i18n key/重生成路由），改 `user/modules/user-edit-drawer.vue`（UserDrawer width 70%）+ `modules/edit/{Profile,Perm,Account}.vue`；列表 handleEdit 用独立 editVisible/editRow 开抽屉。
- **同日调整 2（用户贴实拍"做成这样是不想让我修改吗？"）**：基本信息从只读 NDescriptions 改可编辑——新增 `edit/BasicInfo.vue`（account/name 必填、roleIds/groupIds 多选 custom、siteId 必填、status；创建/最后更新只读 custom 格）；Profile 改 expose validate/collect 去内部保存；抽屉 footer 统一提交（collect 判空 → Promise.all 两表单校验 → fetchUpdateUser 全量）成功关抽屉+submitted 刷列表；editPage.save key 已删。
- 坑（重要）：① **template ref 在 v-for 的 NTabPane 内会收集成数组** → 显式写三个 pane ② 档案 tab 必须 `display-directive="show"`（默认 if 切走销毁组件，ref 变 null，其它 tab 点保存静默失败）③ `...ref?.collect()` 展开使字段全可选（TS2322）→ 先取值判空再展开。
- **同日调整**：用户贴截图"服务商类型有四个"⇒ 撤销"杂支不管理"决策，Provider.vue 的 NRadioGroup 与表单均扩为 4 类（providerType 0-3，表单加「服务商类型」必填下拉默认取当前切换值），i18n 三处加 `typeOption.other`/`providerType` 键；typecheck/lint 0 + playwright 实测 4 项生效。

## 操作轨迹配置接入 track-op 接口

- 「系统设置 → 操作轨迹配置」tab 现对接真实 `track-op` schema（原 OperationTrace.vue 是 node/location/drawer 草稿，已覆盖为内联编辑 + 批量保存）。
- 后端：`POST /track-op/query` → `{list}`（固定 5 条 opType 0-4）；`POST /track-op/update` 整表 `{list}` 批量保存。真实接口、DEV 无 mock。
- 前端：`service/api/operation-trace/index.ts`(`fetchGetTrackOpList`/`fetchBatchUpdateTrackOp`)、`typings/api/system-manage.d.ts`(`TrackOpItem`/`TrackOpList`)、i18n `operationTrace` 段已扩展；删除了旧的 `OperationTraceDrawer.vue`（被内联编辑取代）。
- 教训：用户更想要直接动手实现而非多轮提问；需求明确时优先给最小设计直接落地，再用 changelog 补记录。
- 追加：服务地点 `place` 按用户截图改为 NSelect 固定占位项（`[运单出发地]`/`[目的地]`，i18n `operationTrace.placeOption`）；截图「× 标签」是多选形态，因后端 `place: String` 暂按单选，多选待后端确认存储格式。
- 环境注意：本会话期间工作区有并行改动（data-manage 大量文件删除，typecheck/lint 出现 data-manage 相关报错），验证自己改动时需区分是否为本人引入。
- 追加「新增一行」按钮：本地追加空行（`_id:''`），随批量保存 upsert；`opType` 列由只读 NTag 改为 NSelect（新版需选节点）。i18n 三处加 `operationTrace.addRow`（注意 `app.d.ts` 的 export-format 区块另有同名 `addRow` 键，互不影响）。

## 角色权限抽屉去掉「导航菜单」表头复选框

- 用户贴「分配权限」抽屉截图，要删「导航菜单」列头右侧自定义 `NCheckbox`，全选改由左侧 vxe `type="checkbox"` 列表头原生全选框承担（本来就有）。
- 改动（`role-permission-drawer.vue`）：`#nav-header` 模板删 NCheckbox 留 `<span>导航菜单</span>`；删 `navHeaderState` 计算属性与 `toggleCheckAll` 函数。
- 等价性：左侧全选框触发 `checkbox-all`→`handleSelectionChange`，已自动为新勾选菜单补全按钮/子模块权限；首页（必选不可取消）权限保留，比原 toggleCheckAll(false) 清空首页权限更一致。
- `HeaderCheckState`/`buttonHeaderState`/`subMenuHeaderState`/`withHome` 保留（服务另两列表头复选框）。

## 分配权限对接 /role/auths/update

- 用户问分配权限界面是否调 `/role/auths/update`。核对：原 `handleSubmit` 调 `fetchAssignRoleMenu`（DEV 走 mock 不联后端、非 DEV 调过时 `/system/role/assignMenu` 且入参结构不符后端）。
- 后端真实契约：`POST /role/auths/update`，body `{_id, auths: string[]}`（validate.strict，strArr）；`roleService.updateAuths`→`updateOne` 原样存；登录 `fillAuthMap` 把 auths 拍平为 `authMap` 供前端 `hasAuth` 吃。
- 决策（用户拍板）：码体系以「前端定义的码」为准（菜单 id 转字符串 + `system:user:add` 等），后端原样存，`hasAuth` 自洽；不改后端数字码（12/12-1）。
- 改动：`service/api/role/index.ts` 删 `fetchGetRoleMenuTree` + DEV mock 分支、新增 `fetchUpdateRoleAuths({_id,auths})` 直连真实接口；`mock.ts` 删 `mockGetRoleMenuTree`/`mockAssignRoleMenu` 及 MENU_TREE/ALL_MENU_IDS；`role-permission-drawer.vue` 的 `loadData` 按 `props.row.auths` 回显、`handleSubmit` 合并三套状态为扁平 `auths` 提交；`typings/system-manage.d.ts` 删 `RoleMenuNode`/`RoleMenuTree`/`RoleAssignMenuParams`。
- 注意：该 drawer 文件工作区已被外部重构（#nav-header 简化为纯文字、`navHeaderState`/`toggleCheckAll` 移除、残留无用 `allIds` 计算属性已顺手删）。`data-manage/*` 的 typecheck 报错是工作区已删 MasterData 组件所致，与本次无关。
- spec：`docs/superpowers/specs/2026-09-17-role-permission-auths-update-design.md`；plan：`docs/superpowers/plans/2026-09-17-role-permission-auths-update.md`；changelog：`changelog/分配权限对接role-auths-update.md`。

## 资料管理目录拆分对齐系统设置

- 用户贴 `data-manage/modules/archives` 截图（红框）：「把这个拆开，跟系统设置的文件布局一样」。经确认方案 A：拆掉 archives 中间层，basic/business/finance/no-rule 各页自带 `modules/<项>/<Item>.vue` + 各自 `components/MasterDataArchive.vue` 副本（不再公共组件；no-rule 页 NoRule.vue 自包含，不建副本）；shared.ts/types.ts/archive-switch.vue(死代码) 搬进 `data-manage/components/`。
- 实施：PowerShell `Move-Item`+`git add -A` 整目录搬迁，import 按组改写；同步修正 `service/api/data-manage/{mock,index}.ts` 的 types 路径。
- 坑：① PowerShell `Set-Content` 默认 UTF-16 LE 写坏 7 文件（4 index.vue + 3 副本）→ 3 副本 utf16le→utf8 解码恢复，no-rule/index.vue 与 3 index.vue 用 UTF-8 安全重写（basic 5 / finance 4 / business 8 项，business drop 旧 waybill）；② 误用 `git checkout` 回退了 3 个受跟踪 index.vue 的未提交「指向 views/data-manage/modules」改动（2026-09-17 重构未提交），改为按工作树正确内容重建。
- 验证：typecheck 0 / lint 改动范围 0 error（role-permission-drawer 的 allIds 报错、link.vue 2 warning 为预存无关）。未新增 index.vue，无新路由。
- 残留：finance/modules/tax/ 存在但 finance/index.vue 未列 tax（预存孤儿，未动）。changelog：`changelog/资料管理目录拆分对齐系统设置.md`。
- 追加坑（Vite 报「At least one <template> or <script>」）：三份 MasterDataArchive 副本此前被我用 utf16le 误解码反向毁坏（ASCII 按 UTF-16 配对成 CJK 乱码）。**UTF-16 误解码可逆**：`Buffer.from(乱码字符串,'utf16le')` 直接还原原始字节；还原后残留 GBK 非法字节（中文注释被 Set-Content 二次编码弄坏、换行被吞）+ 尾部 1 个 NUL ⇒ 用替换解码写回、手工修复第 55 行注释（`// 默认分页不传，useVxeTable 全局兜底为 50`）、去 NUL 规范结尾。教训：PowerShell 管道读写文件一律改用 Node 显式 UTF-8；typecheck 对「无 script 的乱码 SFC」不报错，不能只靠它把关，需 lint（eslint vue 解析）+ dev 实跑。

## 分配权限回显失败（修复）

- 现象：保存权限后重开抽屉，除首页外菜单/按钮/子模块勾选全不回显。根因：`role-permission-drawer.vue` `loadData()` 回显遍历用 `rows.some(...)`，`Array.some` 回调首次返回 truthy 即短路——顶层第一行「首页」(id=100) 因 `withHome()` 恒写入 auths 必然命中 ⇒ 后续一级菜单整棵子树（按钮、子模块码）不再遍历。
- 已排除：后端 `/role/query` 返回全字段含 auths（Role 无 omitFields、`basicFields` 未定义 → `find(filters, undefined)` 不投影）；前端类型与提交逻辑正常。
- 修复（用户确认）：`walk` 的 `some` 改 `forEach` + 局部 `anyMatched`，保持「父级随子级命中而勾选」传播语义，仅改该函数。typecheck 0。changelog：`changelog/分配权限回显失败.md`。
- 教训：**递归收集 + 向上传播命中结果时禁用 `Array.some` 作遍历骨架**（短路副作用），用 forEach + 局部标记。

## 用户操作抽屉密码加密

- 用户："新增或者修改的时候密码直接加密，加密请看登录那里"。原 `user-operate-drawer.vue` 的 `handleSubmit` 明文提交密码。
- 方案 A（用户确认）：页面提交处加密，与登录 `fetchLogin` 同格式 `md5(md5(plain)+plain)`（工具 `utils/crypto.ts`）；未采纳抽公共函数（动登录代码）/ service 层加密（不符惯例）。
- 实施 2 处（同文件）：import `md5` + 新增/编辑非空分支均改 `md5(md5(model.password)+model.password)`；编辑留空仍表示不修改密码。
- 验证：typecheck 0；changelog：`changelog/用户操作抽屉密码加密.md`。

## 轨迹抓取三个 tab 对接接口

- 用户贴系统设置「追踪网络」截图红框圈异常轨迹/轨迹关键词/抓取时间："这三个界面接口也接一下"。后端：异常轨迹 `/track-err-config/*`（name/timeType 0-2/place/desc/detectDesc，query=queryAllCommon **全量 {list} 无 total**）、轨迹关键词 `/track-status-config/*`（name/common 0-1/configIds[]/detectEvents[]/orderStatus 50|60|70|80，query 分页 {page,size,keyword}→{list,total}）、抓取时间 `/track-schedule/{save,get}` 单文档 upsert exeTimes(0-23 number[])，delete 均仅单 `_id`。
- 三项决策（用户确认）：① 抓取执行记录表后端无接口→保留 mock，只接「抓取时间设置」弹窗 ② 关键词表单完全对齐后端（名称+全局通用开关+关联追踪网络多选+关键词+运单状态，去 enabled/scope）③ 只接三个 tab 追踪网络不动。
- 实施：typings 保留类型名字段对齐后端；service 三目录去 DEV mock 直连（flat 解包 {data,error}）；mock.ts 删 10 个孤儿 mock；表格/抽屉按后端字段重排（关键词挂载拉 /track-config/query 全量做 configIds 名称映射+抽屉多选；detectEvents 逗号串↔数组）；CaptureTimeModal 接 /track-schedule/{get,save}；cacheKey 升 trace-transform-v2 / trace-keyword-v2；i18n 三处（+col.common/trackNetworks、form.common/configIds/configIdsPlaceholder，−scopeOption 块、scope/enabled）。
- **并行冲突（重要教训）**：实施期间另一会话正在同一页面对接追踪网络——接管我新建的 `service/api/track-config/index.ts`（改为完整 CRUD + TraceConfig\* 类型重写为后端模型）、清空 `service/api/trace-config/` 目录、新增 `constants/track-config.ts`、覆盖我在 en-us form 块的改动（已重新套用）。适配：关键词数据源改调并行流 `fetchGetTrackConfigList({})`/复用 TraceConfigList，删我重复的 TrackConfigItem/TrackConfigList 类型与孤儿 trace-config mock。**typecheck 中途报错要先分拣"我的 vs 并行流的"，只修自己的；对并行流文件不碰，验证口径=本人改动文件 0 error。**
- 验证：typecheck 本次文件 0 error（剩余 error 全在并行流进行中文件）/ oxlint 0/0 / eslint 0 error；playwright 走查暂缓（同页 TrackNetwork 组件在并行流未完成）。changelog：`changelog/轨迹抓取三个tab对接接口.md`。

## 追踪网络接真实接口

- 用户贴 tms-user TrackConfigSchema："接一下追踪网络的接口"。界面本就有（系统设置→轨迹抓取→追踪网络，TrackNetworkTable/Drawer 一直走 DEV mock 假字段 serverAddress/systemType），用户明确"没让你改界面，界面本来就有"——任务是换数据源不是建页面。
- 后端事实：`/track-config/{create,update,delete,query,get}` 通用 queryCommon 真分页 `{page,size,keyword}`→`{list,total}`；name 唯一（重复报 102701）；delete 仅单 `_id`；模型 trackType 实为 **String**（用户贴 Number 有出入，确认以 String 为准，14 码 ups/fedex/kdzs/sd/hl/k5/kjv5/nm/ry/ydd/xzh/t6/zgyz/track17）；queryCommon 的 keyword 必须配 keywordFields 才生效。
- 四项决策（用户确认）：trackType=String 码；不新建页面；承运网络 trackConfigId 顺带接上（下拉+名称列）；keyword 由后端 `services/track-config.js` 加 `keywordFields:['name']`（与 provider 一致）。
- 实施：后端 1 行；前端新建 `service/api/track-config`（4 函数无 DEV mock）+ `constants/track-config.ts`（TRACK_TYPE_OPTIONS 14 码 + trackTypeLabel）；删 `service/api/trace-config` + 桶文件悬空导出；TraceConfig\* 类型族改造为模型形状（14 字段，{page,size,keyword}→{list,total}）；TrackNetworkTable 列对齐 7 列（trackType 插槽映射显示名、updateDate 格式化）+ 补 keyword 搜索框 + 补单行删除 + cacheKey 升 `trace-config-track-network-v2`；Drawer 表单 9 字段（name 必填 + trackType 下拉必选 + 7 可选）提交解包 {error}、删无用 category prop；i18n 三处（col/form 增删 + carrier.trackConfig）；Carrier.vue 接 trackConfigOptions(size 500) 下拉 + 名称列。
- 并行会话咬合：轨迹关键词页预先 import 的 `fetchGetTrackConfigList({})`（res.list/item.\_id）与本 service 签名吻合直接接通；mock.ts traceConfig 块、桶文件 './track-config' 导出均为并行会话预备/移除——本次确认无残留。
- 验证：typecheck 0 / lint 0 error（仅剩 link.vue 2 条既有 warning）。changelog：`changelog/追踪网络接口对接.md`。

## 抓取时间列表接 /op-log/query

- 用户贴 tms-user OpLogSchema（opType 注释扩 **4-追踪**）+ /op-log/query 请求示例："抓取时间列表调这个接口，opType 默认传 4"。定位：抓取时间 tab 主表格此前 DEV mock / PROD 调占位假接口 `/system/capture-time/list`（后端无此接口）；`fetchGetOpLogList` 已有封装（flat 解包 `{data,error}`→ret:{list,total}）。
- 四项决策（用户确认）：① 操作日志列映射 name ② 不加搜索栏（YAGNI）③ 清理旧 mock 链路（删 fetchGetCaptureTimeRecordList + captureTimeRecords + timeStampOf + CaptureTimeRecord 类型族，保留 /track-schedule 配置接口）④ 系统日志页不加追踪筛选。
- 实施：typings `OpLogOpType` 扩 4 + 删旧类型族；CaptureTimeTable.vue 改调 `fetchGetOpLogList({page,size,where:{opType:4}})`，列 key 换 OpLog 字段（name=detail 可复制 / client NTag 文案 / opType NTag 4-追踪 warning 色 / creator / createDate），cacheKey 升 `trace-capture-time-v2`；i18n 三处 `opTypeOption.trace`；log/index.vue `opTypeTagType` map 补第 5 位（连锁修复，本页行为不变）。
- 坑：① 重写 CaptureTimeTable.vue 漏了 configVisible/modalVisible 两个 ref 声明（typecheck 报 vue/no-undef-properties + ref unused）② `as const` tuple 用 number 索引时，扩联合类型会报 TS2493（tuple 长度不够），需同步补位。
- 验证：typecheck 0 / lint 0 error（link.vue 2 条既有 warning 无关）；未跑 playwright（需真实后端产生 opType=4 追踪日志）。changelog：`changelog/抓取时间列表调op-log接口.md`。

## 角色管理：超级管理员不允许分配权限

- 用户："角色管理，超级管理员不允许分配权限"。定位：系统「超级管理员」是**用户级** accountType==1（登录后整体跳过权限校验不走角色 auths），Role 模型无超管标记。
- 两项决策（用户确认）：① 识别对象=`roleType === 5`（界面标签「管理员」）的角色行 ② 仅前端禁用（tooltip 提示），后端 /role/auths/update 不拦截；提示文案跟用户原话「超级管理员不允许分配权限」（未改「管理员角色」口径）。
- 实施：role/index.vue 操作列「权限」按钮 `:disabled="row.roleType===5"` + 外包 `NTooltip :disabled="row.roleType!==5"`；**坑：disabled 的 `<button>` 不派发鼠标事件，NTooltip 直接包按钮悬停无反应 ⇒ trigger 套一层 `<span>`**；i18n 三处新增 `page.manage.role.permissionDisabledTip` + app.d.ts Schema 同步。
- 验证：read_lints 4 文件 0 / oxlint 0/0 / eslint role/index.vue 0；typecheck 全量 3 error 均为并行「用户编辑页面」流遗留（editPage back 键、system-manage_user-edit 路由 key 未重生成），与本次无关。changelog：`changelog/超级管理员不允许分配权限.md`。
