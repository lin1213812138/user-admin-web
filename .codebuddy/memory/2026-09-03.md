# 2026-09-03

## 解释：为什么 `pnpm dev` 一启动 `src/router/elegant/routes.ts` 就被"格式化/重排"

- 根因不是格式化工具（oxfmt/prettier 只会调空白，不会重排对象/数组顺序），而是 **elegant-router 的 Vite 插件在每次 dev/build 启动时全量重新生成并重写这 4 个文件**：`routes.ts`、`imports.ts`、`transform.ts`、`typings/elegant-router.d.ts`。
- 插件注册点：`build/plugins/router.ts` 的 `setupElegantRouter()`（`ElegantVueRouter`，来自 `@elegant-router/vue/vite`，本质是 unplugin）。源码（node_modules/@elegant-router/vue/dist/shared/vue.adf39a67.mjs）可见它扫描 `src/views` → magicast 解析既有 `generatedRoutes` 合并 meta → 用 **prettier**（printWidth120/tabWidth2/单引号/无尾随逗号）格式化后 writeFile 覆盖写盘；dev 还 watch views 目录，新增页面也会触发。
- 输出顺序以"本次扫描到的页面顺序"为准（不保留手工排的顺序）→ 所以昨天手工/旧版把 data-manage 放后面，今天启动 dev 后被写回生成器的规范顺序（data-manage 提前）。`nonMenuRoutes`/`menuRoutes`/helper 等附加导出由 AST 保留，不会丢。
- 时间证据：routes.ts 最后提交 2026-09-02 20:15，磁盘 LastWriteTime 2026-09-03 09:23 —— 即今早用户/环境起了一次 dev，生成器重写产生 uncommitted diff（git status 可见仅 routes.ts modified，imports.ts/transform.ts 无 diff）。
- 结论：`src/router/elegant/**` 手改无意义，每次启动都会被覆盖；想控制菜单顺序/图标/标题用 `build/plugins/router.ts` 的 `onRouteMetaGen`（routeIcons/routeOrders）或 `customRoutes` meta。

## 续：用户诉求 = 启动可加 order 但不得打乱已配置的路由块顺序（结论）

- 已翻插件选项类型（vue.9d9f94c9.d.ts）与合并源码（vue.adf39a67.mjs 的 N/F）：elegant-router **没有"保留旧块顺序"开关**；启动全量重写，块顺序 = 生成器按路由名逐层字典序；meta 合并只"填空缺、不覆盖已有 key"。
- 因此 routes.ts 块排列无法人为固化、且与菜单显示无关；显示顺序只由 meta.order 决定（sortRoutesByOrder 升序，缺省 0，同值按原序）。
- 已定案：routeOrders = home:0 / data-manage:1 / system-manage:2（role:1、user:2 不变），菜单稳定为 首页→资料管理→系统管理。
- 陷阱提醒：若某路由的 order 已在 routes.ts 里，改 routeOrders 数字不会覆盖，需先删旧键再重启 dev。
- 用户偏好：不允许把 routes.ts 的路由块顺序改乱；禁止手改/手排 elegant/\*\*；生成器规范版本提交一次后幂等无 diff。
- 文档：changelog/路由菜单顺序与elegant路由生成.md（已挂 AGENTS_CHANGELOG.md 索引）。

## AGENTS_CHANGELOG.md 改为按日期分组

- 用户要求：AGENTS_CHANGELOG.md 按日期分组，日期最新在上；组内按记录先后。
- 已重排完成，并在 AGENTS.md 顶部规则 1 补充"该索引按日期分组，日期最新在上"，后续追加时需放到对应日期组。

## 资料管理子档案按需懒加载

- 问题：data-manage 三模块页（basic/finance/business）打开即构建本页 4 个子档案表格 + 4 请求 → 首屏卡。根因：静态 import + onMounted requestIdleCallback 全部预挂载 + MasterDataArchive setup 即 useVxeTable immediate 拉数据。
- 决策（用户确认）：首次访问才加载 + 界面缓存。**用户明确要求用 `<KeepAlive>` 缓存界面**，不要"隐藏层常驻 DOM"方案。
- 实现：新增共享容器 `src/components/MasterData/archive-switch.vue`（props: items:{key,labelKey,load}[]；defineAsyncComponent 分包 + loadingComponent 全屏 NSpin；内容区 `<KeepAlive><component :is=... :key=activeKey></KeepAlive>`）；三个模块页瘦身为纯配置壳。
- 注意：项目 lint 禁 Options API render（vue/component-api-style）→ loadingComponent 用函数式组件（h 返回），不可 defineComponent({render})。
- 不改：路由/菜单/i18n/mock/MasterDataArchive。typecheck + lint 0 error。
- 文档：docs/superpowers/specs/2026-09-03-data-manage-lazy-load-design.md；changelog/资料管理子档案按需懒加载.md（已挂 AGENTS_CHANGELOG.md）。
- 追加调整（同会话，用户确认）：① archive-switch.vue 内容区外层加 `<Transition :name mode="out-in">`，name 读 `themeStore.page.animate ? themeStore.page.animateMode : ''`（与 layouts/global-content 主内容同一写法，动画 CSS 复用全局 styles/css/transition.css）；② loader 内 `Promise.all([item.load(), sleep(300)])` 保证首次加载转圈至少 300ms 可见（dev chunk 太快会"一闪而过"）。typecheck 0 error。
- 再调整（用户强调"不要整屏 loading，要表格 loading"）：撤销 ② 的强制 300ms 整屏 NSpin；改为 **mock 数据异步化**（mock-data-manage.ts 列表 delay 400ms、增删改 delay 200ms，模拟真实网络），让 useVxeTable 的 loading 在表格区域真实可见。loadingComponent 只作 chunk 加载兜底。typecheck 0 error。
- 用户明确要求 **表格 loading 不用 vxe-table 自带遮罩，用组件库（Naive UI NSpin）**：通用 Table 组件（src/components/Table/table.vue）移除 `vxe-table :loading`，外包 `<NSpin :show :content-style height 100% class h-full w-full>`。NSpin 全局自动注册（components.d.ts），勿显式 import；此改动影响全项目所有 Table。typecheck + lint 0 error。

## archive-switch 两个回归 bug 修复（库位加载不出来 + 横向滚动条）

- **横向滚动条**：fade-slide 动画 `translateX(±30px)` 让 content 溢出右侧/左侧 30px，archive-switch 根与右侧容器原来都缺 `overflow-hidden` → 页面级横向滚动条。已给根 `<div class='flex h-full w-full overflow-hidden'>` 与右侧 `<div class='relative min-w-0 flex-1 overflow-hidden'>` 加 `overflow-hidden` 裁掉动画溢出（与全局内容区 global-content 的 overflow 思路一致）。
- **库位（location）切换后只显示一个小蓝点、内容全空白**：archive-switch 右侧内容区多套了 `<div class='absolute inset-0'>` 包裹 `<Transition>`，配合 `mode='out-in'` + `<KeepAlive>` + `defineAsyncComponent`，让切到非首屏档案（首次加载的 async 组件）时真实组件挂载异常、搜索栏 NCard 与 Table 全部不可见，只剩一个被压扁的小 spinner。仓库能正常加载（默认 activeKey 即首项，未经历 out-in 切回）。已**去掉该多余 absolute inset-0 层**，让 `<Transition><KeepAlive><component :is :key class='h-full w-full' /></KeepAlive></Transition>` 直接放在 `relative flex-1 overflow-hidden` 内渲染（component h-full 100% 父高），动画与缓存仍生效。`pnpm typecheck` 0 error。
- 注意：archive-switch 的 loadingComponent（chunk 加载兜底）保留；动画 name 仍读 `themeStore.page.animate ? themeStore.page.animateMode : ''`（跟随主题）。

## 表格 loading 流畅化（spinner 硬切 / 高度跳动 → fade 遮罩）

- 原实现：`src/components/Table/table.vue` 用 `<NSpin :show="loading" class="h-full w-full" :content-style="{height:'100%'}">` 直接包裹 `vxe-table`。问题：naive-ui NSpin 的 `show` 切换是**硬显隐 spinner（无淡入淡出）**，且 loading 期间 vxe-table 被包在 NSpin 内、高度易跳动 → 用户反馈「出现/消失生硬」。
- 新实现：让 `vxe-table` **始终渲染**（高度固定不跳），外层 div 加 `relative`；loading 显隐改用独立遮罩层 `<Transition name="fade"><div v-if="loading" class="absolute inset-0 z-10 flex items-center justify-center bg-white/60"><NSpin size="large" /></div></Transition>`。复用全局 `styles/css/transition.css` 的 `.fade-*`（`opacity 0.3s ease-in-out`）做平滑淡入淡出。spinner 居中、表格高度稳定、切换顺滑。
- 涉及全项目所有 Table（通用组件），影响面大但纯样式/结构改动。`pnpm typecheck` 0 error（仅 vxe-table 第三方 slot 类型提示 `#trigger/#empty` 的预存 warning，无害）。
