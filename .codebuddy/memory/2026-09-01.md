# 2026-09-01

## 菜单管理操作列新增「增加子菜单」

- 需求:操作列每行加「增加子菜单」按钮,复用 `MenuOperateDrawer`,add 模式 + `parentId` 预置当前行。
- 实现:抽屉新增 `parentId` prop(add 分支覆盖默认 `parentId=0`);`menu/index.vue` 新增 `subParentId` + `openSubDrawer(row)` + `#action` 按钮;i18n 三处加 `page.manage.menu.addSubMenu`。
- 设计文档:`changelog/增加子菜单.md`(已追加 AGENTS_CHANGELOG 索引)。
- 验证:`pnpm typecheck` / `pnpm lint` 通过。用户确认按钮每行显示、父级字段预选且可改。
- 注意:`src/components/common/link.vue:86` 有 2 个 `vue/no-undef-properties` warning(KeyboardEvent/MouseEvent),与本改动无关,先不碰。

## 菜单操作列加宽

- 「增加子菜单」上线后操作列变 3 按钮,原 `:action-width="140"` 不够,已调为 `200`(`src/views/system-manage/menu/index.vue`)。用户管理页仅 2 按钮,140 够用未动。
