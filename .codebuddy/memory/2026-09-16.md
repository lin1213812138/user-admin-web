# 2026-09-16

## 登录页底部区域注释

- 用户截图红框标密码登录表单底部（验证码登录/注册账号切换 + 其他账号登录分割线 + 三快捷账号按钮）："这块内容也注释一下"。
- 仅改 `pwd-login.vue`：模板三块包单个 HTML 注释块；script 连锁注释 `loginModuleRecord` 导入与 `Account`/`accounts`/`handleAccountLogin`；**保留** `toggleLoginModule`/`useRouterPush`——上方「忘记密码」按钮仍用 `toggleLoginModule('reset-pwd')`（首轮误注释被 `vue/no-undef-properties` 警告抓到后恢复）。
- 验证 read_lints 0。记录：`changelog/登录页底部区域注释.md` + AGENTS_CHANGELOG 索引。

## vxe 表格「不需要横向滚动也显示横向滚动条」定位 + 修复

- 用户截图（财务资料 → 结算方式）底部恒有横向滚动条，滑块约 98% 长；用户要求**必须保留 hover 浮现**交互，并选定 `--x-scrollbar-size` 用运行时测量（方案 A）。
- 根因（vxe-table 4.5.9 源码级）：`x: { visible: true }` 是布尔、vxe 只识别 `'visible'/'hidden'/false` → 走自动判定且本页 `overflowX=false`（列宽由 `autoCellWidth()` 按主区宽度分配、总宽=容器宽），vxe 内联写 `visibility: hidden`；项目 `scrollbar.css` 的 `:hover ... visible !important` 把它显示回来；**滑块必然出现的真因**是 handle 恒有 16px 伪溢出（JS 内联轨道宽扣了 8px 纵向滚动条宽，但本项目纵向滚动条已浮动不占布局 + handle 自身 thin 滚动条占位 8px），而 `--scroll-x-space` 宽 = `scrollXWidth` = 容器满宽。另：浮动化后 `.vxe-table--scroll-x-virtual`（absolute 无 width）宽度塌成 0。
- 修复（2 文件增量，无新文件）：`src/plugins/vxe-table.ts` 加 `measureScrollbarSize()`（离屏探针实测 thin 占位 → `:root --x-scrollbar-size`，先于 `injectScrollbarStyle()` 调用）；`src/styles/css/scrollbar.css` 加 3 条：x-virtual `width:100%!important`、x-wrapper `width:100%!important`、x-handle `width: calc(100% + var(--x-scrollbar-size, 8px))!important`。
- 验证：oxlint 0 error / read_lints 0 / `pnpm typecheck` exit 0；dev server 当时未运行（9527 无监听），浏览器效果待用户复核（hover 无滑块；缩窄窗口令列总宽超容器时滑块出现且可拖）。
- 记录：`changelog/不需要横向滚动也显示横向滚动条修复.md` + `AGENTS_CHANGELOG.md` 2026-09-16 分组。

## 后续：无滚动条时隐藏滚动条端点（同一问题第二处）

- 用户新截图（角色管理页）红框标出 hover 时滚动条浮层两端的小色块，问"没有滚动条的时候这个能不能也不显示"。
- 定位（用户实测配合）：① 鼠标移出表格色块消失 → 属滚动条浮层；② 用户右键检查元素 = `.vxe-table--scroll-x-handle > .vxe-table--scroll-x-space{width:1720px}`，1720 与表格实际宽度一致 → **列宽总和==容器宽、无横向溢出**（据此排除四角补块 corner：vxe 与项目 CSS 均已 `display:none`）；③ 真因 = vxe 给两条 handle 设 `overflow-x/y: scroll`（`vxe-table/es/table/style.css:2540-2557`），scroll = 不论是否溢出都渲染滚动条，无滑块时轨道端点仍被画出。
- 修复：`scrollbar.css` 新增 0.4 节 `.vxe-table .vxe-table--scroll-x-handle { overflow-x: auto !important }` + `.vxe-table .vxe-table--scroll-y-handle { overflow-y: auto !important }`（auto = 溢出才渲染；对 vxe 的 overflowX/overflowY 判定无影响）。
- 记录：`changelog/无横向滚动时隐藏滚动条端点.md`；视觉效果待用户复核。

## 角色权限抽屉树表行高自适应

- 问题：「操作权限」列复选框换行被 40px 固定行高裁切；`{ height: 'auto' } as any` 传 `cell-config` 无效。
- vxe-table 4.17 源码结论（对将来的树表自适应有复用价值）：① `cell-config.height` 只接受 number，`'auto'` 被 `toNumber` 吞掉；② vxe **默认不做行高实测**，`calcCellHeight` 的条件是 `!isAllOverflow && (scrollYLoad || scrollXLoad || (treeConfig && treeOpts.showLine))`——树表必须开 `tree-config.showLine` 才逐行实测，写入 `rowRest.height`；③ `isAllOverflow = !!showOverflow`（`table.ts:1754/1791`）——**这是最容易被忽略的一层**：`Table.vue` 写死 `show-overflow="tooltip"` 导致 `isAllOverflow=true`，`calcCellHeight` 直接跳过；④ 行高取值链 `getCellRestHeight`：`cellOpts.height → rowOpts.height → rowRest.height → defaultRowHeight`，要让位给实测值须 `cell-config.height=0`（falsy）；⑤ 单行内容行高 = `Math.max(defaultRowHeight, 实测值)`，不会变矮；⑥ 数据更新/树展开后 vxe 自动 recalculate（reHeight=true → calcCellHeight），业务侧无需手动触发；⑦ 超过 `virtualScrollRowThreshold`（默认 200）启用虚拟滚动后实测停止。
- 方案（用户确认：不用 row-config、只用 cell-config、showLine 连接线用 CSS 隐藏）：
  - `Table.vue` 新增 `showOverflow` prop，默认 `'tooltip'`，模板绑定 `:show-overflow="showOverflow"`（默认行为不变，其他页面无影响）；
  - `role-permission-drawer.vue` 传 `:show-overflow="false"` 让 `isAllOverflow=false`；
  - `autoRowConfig` 改 `{ height: 0 }`（去 as any）；
  - `treeConfig` 加 `showLine: true`；
  - 文件尾 scoped `:deep(.vxe-tree--line-wrapper){display:none}`。
- 设计文档：`docs/superpowers/specs/2026-09-16-role-permission-tree-table-auto-row-height-design.md`。
- ⚠️ git 异常：多次 `git commit` 均 exitCode 0 但**未创建 commit**（HEAD 不变、文件滞留暂存区）——pre-commit hook（typecheck/lint --fix/oxfmt）运行后会中止落盘且每次全量格式化 605 文件污染工作区。设计文档与本次实现改动均未提交，待用户自行处理。
- 验证：read_lints 0 / `pnpm typecheck` 0 错误；浏览器效果待用户复核（按钮多的行撑高、搜索/展开后自适应、无树连接线）。

## 站点管理接入真实接口（mock → /site/\*）

- 用户："站点管理开始改，去掉mock数据改成调用真实接口/site/query"；经 7 轮问答确认：范围=全部 CRUD（query/create/update/delete + get），**状态/站点类型筛选本轮都不做**（"先都不要，帮我把接口先接好"），编辑态先 `/site/get` 取最新数据再回填。
- 后端合约（用户提供 Mongo Schema）：`_id`（字符串 uuid 主键）/ code(必填) / name(必填) / concat(联系人，注意拼写) / phone / workTime / note / startPlace / address / siteType(0-分公司 1-总公司) / creator(Id) / updateBy(Id) / createDate·updateDate（毫秒时间戳）；**无 status 字段**；`POST /site/query` body `{page,size,keyword,fields?,where?}`（其他条件放 where），返回 `{errcode,errmsg,ret:{list,total}}`，实测列表项常只有 `siteType/_id/code/name/createDate/updateDate` → 类型里联系类字段全部可选。
- **重大发现：flat 请求的 PROD 分支此前从未跑通**。`request` = `@sa/axios` `createFlatRequest` → 返回 `{data, error, response}` 扁平结构而非裸数据（`packages/axios/src/index.ts:142-175`）；旧 system-manage.ts 的真实分支直接 `return request(...)`，而各页 `useVxeTable.transform` 读 `r.records` 恒 undefined，一直被 DEV mock 掩盖。**正确范式**（站点页为首个范例，后续模块接入照抄）：页面 api 闭包内 `const { data, error } = await fetchXxx(...)`，`error || !data` 返回空列表（错误已由 request onError 统一提示），成功返回 data；`transform` 按 ret 实际字段写。
- 改动：Site 类型五件套重写（`SiteList={list,total}`、`SiteSearchParams={page,size,keyword?}`）+ system-manage.ts 站点 4 接口去 mock 分支接 `/site/query|create|update|delete` + 新增 `fetchGetSite` + mock.ts 删站点 mock（`sites` 降级为 `SiteFixture{id,siteCode,siteName}`，组别/用户 mock 解析 siteName 仍依赖）+ site/index.vue 重写（keyword 单筛选、`page: current` 映射、dayjs 格式化时间戳、**cacheKey 换 system-manage-site-v2** 防旧列配置残留）+ site-operate-drawer.vue 重写（siteType 下拉默认 0 分公司）+ 连带适配组别/用户 3 处站点下拉（Option<string>、`_id`/`name`）+ i18n 三处换 key + `.oxlintrc.json` no-underscore-dangle `allow:["_id"]`。
- 过渡影响 / 待联调核对：组别/用户 `siteId` 仍 number 与字符串 `_id` 失配（mock siteName 反查空、下拉不回显），待该两模块接真实接口时一并解决；`/site/get` body `{_id}`、`/site/delete` body `{ids:string[]}` 为惯例推断。
- 验证：typecheck 0 / oxlint+eslint 0 error（link.vue 2 条历史 warning 无关）/ fmt 已跑；真实接口联通性待用户联调。记录：`changelog/站点管理接入真实接口.md` + AGENTS_CHANGELOG 索引。

## 站点类型隐藏与总公司保护（站点接真实接口的后续微调）

- 用户："新增的时候站点类型默认为0，不要显示，并且不允许修改和删除总公司"。
- 改动：抽屉删 `siteTypeOptions` + 表单 `siteType` 项（model 固定 0、`fillFormByRow` 仍回填原值保证 update 字段不丢）；index.vue `isHeadquarters(row)` 判定 → 操作列总公司行编辑/删除按钮 disabled（详情不受限）；批量删除 `deletableRows = computed(checkedRows 滤总公司)`，混选只删分公司、全为总公司时按钮禁用。i18n placeholder 留存（表格 siteType 标签列仍用）。
- ⚠️ 再次踩坑：同文件多处编辑放同一批并行调用 → 写入冲突把批量删除 NPopconfirm 整块覆盖丢失（eslint 报 deletableRows unused 才暴露）。**同文件编辑必须串行**（MEMORY 已有此条，本轮实证其后果的真实形态：后应用的编辑基于旧快照、静默吞掉前一批改动区域）。
- 验证：typecheck 0 / lint 0 error / fmt 已跑。记录：`changelog/站点类型隐藏与总公司保护.md`。

## 最后更新列只显示时间

- 用户："最后更新就显示时间不要其他的" → site/index.vue `#updateDate` 插槽去掉 `updateBy - ` 前缀，只渲染 `formatDate(updateDate)`（YYYY-MM-DD HH:mm）。记录：`changelog/最后更新列只显示时间.md`。

## 去掉 header 栏、用户模块移至侧栏底部

- 用户附截图红框两处：整条 header + 右上角用户头像，箭头指向菜单栏底部。三问确认：右侧功能按钮（搜索/全屏/语言/暗黑/主题配置）全部下线、折叠按钮不要（侧栏固定展开）、底部用户区=展开「图标+用户名」/收起仅图标；**补充约束「先不删除文件只做注释」**。
- 关键定位：`@sa/materials` AdminLayout 原生 `header-visible` prop（`showHeader = slots.header && headerVisible`），tab 有 `!showHeader→top-0!` 内置顶格、`sider-padding-top` 仅 showHeader 时附加 → 传 prop 即净，零改 packages。
- 改动（3 文件）：base-layout/index.vue（注释 GlobalHeader/ThemeDrawer 导入、headerProps 块注释、`#header` 插槽与 `<ThemeDrawer />` 注释、AdminLayout 加 `:header-visible="false"`）；global-sider/index.vue（菜单挂载点下新增 56px 底部用户区，跨目录引 `../global-header/components/user-avatar.vue` 不迁移文件）；user-avatar.vue（placement bottom→top + 用户名 `v-if="!appStore.siderCollapse"`）。
- 文件全保留：global-header/（index.vue、theme-button.vue 成死代码）、global-search/、global-breadcrumb/、theme-drawer/；恢复=取消 base-layout 注释。
- 影响：移动端抽屉侧栏失去开关入口、主题抽屉无入口（有意下线）。
- 验证：typecheck 0 / lint 0 error（link.vue 2 条历史 warning）/ fmt 已跑；视觉效果待用户 dev 复核。
- ⚠️ 并行会话坑：AGENTS_CHANGELOG.md 被另一会话插入新条目（「标签栏风格默认改滑块」）致连续两次 old_str 失配 → **追加公共索引前必须重读文件取最新锚点**。
- 记录：`changelog/去掉header栏用户模块移至侧栏底部.md` + AGENTS_CHANGELOG 索引。

## 标签栏右侧加主题模式切换与主题配置入口

- 用户截图红框标 tab 栏右侧（刷新/全屏按钮处）："主题设置和主题配置放这里"；澄清确认=两个按钮（ThemeSchemaSwitch 三态循环 + ThemeButton 开抽屉）。
- 背景定位：接「去掉 header 栏」条目——header 下线后主题入口曾完全消失；ThemeDrawer 组件完好但被注释，`appStore.openThemeDrawer` 仍在。
- 关键事实：`ThemeSchemaSwitch`（src/components/common）自动注册直接用；`ThemeButton` 是 `global-header/components/` 局部组件须显式 import；两者均 ButtonIcon 封装。tooltip 复用 `icon.themeSchema`/`icon.themeConfig`，零 i18n。
- 改动 2 文件：global-tab/index.vue 右侧操作区=刷新→全屏→主题模式→主题配置（跨目录 import ThemeButton）+ base-layout/index.vue 恢复 ThemeDrawer import/渲染（注释文案改「入口在标签栏右侧」）。设计文档 `docs/superpowers/specs/2026-09-16-tab-bar-theme-buttons-design.md`。
- ⚠️ 并行/交叉修改坑再现：AGENTS_CHANGELOG.md 与本日报均被另一会话/用户重写过（上轮「滑块」条目消失、old_str 连续失配）→ **编辑公共记录文件前必须重读取最新锚点**。
- 验证：read_lints 0 / typecheck 0；浏览器效果（按钮渲染、抽屉打开、模式切换）待用户复核。

## tab 栏占位高度 44→50

- 用户截图红框标页签栏："这个的占位高度太低了，能不能调高一点"；候选 44/48/50/52/56 中确认 **50px**。
- 定位：高度唯一来源 = `src/theme/settings.ts` `tab.height`（原 44），经 base-layout `:tab-height` 传 AdminLayout；chrome 页签（PageTab）无固定像素高、SVG `height:100%` 自适应容器 ⇒ 只改一处；dev 下 `initThemeSettings()` 不走 localStorage 缓存、`overrideThemeSettings` 为空 ⇒ 改完刷新即生效。
- 改动：settings.ts `tab.height: 50` + 中文注释。记录：`changelog/tab栏占位高度调高.md` + AGENTS_CHANGELOG 索引（本次编辑前已重读取锚点，无冲突）。
- 验证：read_lints 0 / typecheck 0；视觉待用户复核。

### 更正（同日）：用户要的是页签本体高度，非占位栏

- 用户复核："不是让你调框选的绿色的高度，是里面红色的标签的高度" —— 绿框=占位栏（50px 维持），红框=chrome 页签本体。
- 机制：页签本体=`page-tab/chrome-tab.vue` 根 div，高度由内容行高+`py-6px` 撑开**约 36px**、不随占位栏变高（所以第一轮改 tab.height 页签没变化）；背景 `chrome-tab-bg.vue` SVG `h-full`（symbol viewBox 0 0 214 36）任意高度等比拉伸不变形；`PageTab` 根节点 attrs fallthrough ⇒ 业务侧透传 class 即可落到 ChromeTab 根元素，免动 packages。
- 改动（用户确认 48px）：`global-tab/index.vue` `<PageTab class="h-48px">` + 中文注释。
- ⚠️ 并行会话再现：编辑 global-tab 期间另一会话注释了同文件的 ReloadButton/FullScreen（含 refresh()/appStore 连锁注释）；replace 按当前文件精确匹配故未冲突，但公共文件改前必须重读。
- 验证：read_lints 0 / typecheck 0；视觉待用户复核（页签 48、占位栏 50、上下各留 1px）。

### 第三轮（最终）：「飞上去了」根因 + 全局联动方案

- 用户追问"能不能变成跟标签栏高度一样可以全局配置" + 截图"为什么飞上去了"。
- **playwright 实测根因**（9111 dev、admin/admin@12345 可登录、`data-tab-id` 定位——`.chrome-tab` 是 CSS module hash 类名查不到）：页签 h-48px 生效=48，但链路全 44px——**settings.height 被改回 44**（注释仍在、值矛盾，误改回退）⇒ 48>44、items-end 贴底后顶部溢出 offsetTop=-4 被裁 =「飞上去」。反证：登录页注入同构 DOM 纯 CSS 全正常（tabTop=2 贴底）。
- **最终方案=联动**：`global-tab/index.vue` 页签 `:class="themeStore.tab.mode === 'chrome' ? 'h-full' : ''"` 撑满 tab.height，一个全局配置控制两处、永不溢出；settings.height 恢复 50。slider 本就 100%、button 不受影响。
- 实测：栏 50/页签 50/top 0/overflow false + 截图复核 + typecheck 0；调试浏览器已关、临时截图已删。
- **调试方法论沉淀**：布局问题直接 playwright 上真机实测（登录页可注入同构 DOM 快速验证 CSS；进业务页需登录 admin/admin@12345；登录接口偶发 502 重试即可，session/get 一直通）；改布局前先读「文件当前真实值」——并行会话可能已改回。
- 用户修复后仍见「飞上去」→ 全新浏览器会话实测（site 页）页签 50/top 0/bottomGap 0 全正常 ⇒ 判定用户浏览器为 **HMR 多轮 patch 残留**（页面长期未整刷 + class 从静态改动态绑定），已建议 Ctrl+F5 硬刷新复核；若硬刷后仍异常再回查。

## 刷新/全屏按钮先注释（tab 栏右侧只留主题类按钮）

- 用户截图红框标前两个图标："这两个可以先注释一下" → ReloadButton + FullScreen 注释下线，保留 ThemeSchemaSwitch/ThemeButton。
- **模板注释的 unused 三级连锁**（同文件串行处理）：注释模板两行 → `refresh()` 报 unused → 注释 → `appStore` 报 unused → 注释声明 → `useAppStore` import 注释；各处标注「恢复按钮时一并取消注释」。GlobalContent 用 reloadFlag 不受影响（store 本身还在）。
- 注释格式坑：`vue/html-comment-content-newline` 要求 `<!--` 后、`-->` 前换行，说明文字与代码放同一注释块即可。
- 验证：read_lints 0（含 2 条 comment warning 一并消除）/ typecheck 0。记录：`changelog/刷新全屏按钮先注释.md`。

## dev 启动自动同步 .env.test 的 VITE_SERVICE_BASE_URL IP

- 用户："写个脚本每次启动项目都自动更新.env.test中的VITE_SERVICE_BASE_URL的ip"；定位=后端 tms-user 跑在本机、该 URL 的 IP 即本机局域网 IPv4（192.168.2.28 实测吻合）随 DHCP 变化；本机另有 WSL 虚拟网卡 `172.27.0.1` 必须排除。
- 方案对比采纳 **A=独立脚本 + dev 串联**（B=Vite 插件与构建链耦合、build:test 被牵连，否决；C=predev 钩子 pnpm 默认 `enable-pre-post-scripts=false` 不执行，否决）。
- 新建零依赖 `scripts/update-env-ip.mjs`：`os.networkInterfaces()` + 网卡名黑名单（WSL|vEthernet|VMware|VirtualBox|Hyper-V|Loopback|TAP|TUN|Virtual）+ 过滤 internal/127/169.254，私网段优先级 192.168>10>172.16-31 取首个（多候选打印全列表）；正则按行 `^(\s*VITE_SERVICE_BASE_URL\s*=\s*)(https?://)([^/:\s]+)(.*)$` 只替换主机段、scheme/端口/路径原样保留；IP 未变不写文件（避免 git/mtime 抖动）；找不到行/读失败只警告 exit 0 不阻断启动；可手动 `node scripts/update-env-ip.mjs`。
- `package.json` dev = `node scripts/update-env-ip.mjs && vite --mode test`；不动 `build:test`/`dev:prod`/`.env.prod`。
- 验证：IP 相同输出"无需更新"；临时改假值 10.255.255.1 后正确写回 `192.168.2.28:15001/tms/api/v1/web`；read_lints 0。记录：`changelog/自动更新VITE_SERVICE_BASE_URL的IP.md` + AGENTS_CHANGELOG 索引。
- ⚠️ 工具坑：execute_command 中**分号串联多条 node 命令只执行首条**（判定 isWatchCommand 后提前收包、输出丢失），改用 `&&` 串联正常执行。

## 表格组件新增 search-action 快速搜索栏插槽

- 用户："表格组件帮我加一个slot：search-action，配置部分快速搜索条件"；澄清关键=初版误解为 SearchBar 按钮区加插槽，用户纠正**"跟searchBar.vue没关系，是配置在表格组件里面的单独快速搜索栏"**——业务完全自定义的独立区域，与 SearchBar 组件无关。
- 设计（用户确认）：仅改 `table.vue`，模板最顶部（SearchBar 区块之前）加 `<div v-if="$slots['search-action']" class="mb-12px"><slot name="search-action" :refresh="refresh" /></div>`；内容/容器样式全由业务自定义，不加默认卡片、不生成默认按钮；与 `searchItems`/`SearchBar` 完整搜索栏互不依赖、可并存（插槽在上）；**页面不传 `searchItems` 即彻底无默认搜索/重置按钮**（顺带回应上一条被取消的"主动隐藏操作按钮"诉求）；slot props 暴露 `refresh` 与 operation-left 惯例一致。
- ⚠️ 并行会话坑再现：编辑 AGENTS_CHANGELOG.md 时 old_str 连续失配（另一会话已插入「系统管理菜单重构与系统日志」条目）→ 公共索引追加前重读最新锚点（本次已照做）。
- 验证：read_lints 0 / typecheck 0。记录：`changelog/表格组件search-action插槽.md` + AGENTS_CHANGELOG 索引。

## NInput 设置 width 不生效（纯问答定位）

- 用户："NInput class=\"w-100px\" 设置 width 怎么不生效"；根因=naive `.n-input` 根元素自带 `width:100%`，与 UnoCSS `.w-100px` 同特异性看先后——本项目 `uno.css` 在 `plugins/assets.ts` 启动早期静态 import，naive 样式组件挂载时动态插 `<head>` 恒更晚 ⇒ **必然覆盖**（非偶发顺序问题）。
- 解法：内联 `:style="{ width: '100px' }"`（推荐）/ `class="w-100px!"`（UnoCSS trailing important）/ 外包 div 控宽；flex 容器内固定宽度注意 `shrink-0`。
- 适用面：所有根元素 `width:100%` 的 naive 组件（NSelect/NDatePicker 等）同理。记录：`changelog/NInput设置width不生效.md`。

## 快速搜索栏加阴影并跟随主题圆角

- 用户："快速搜索栏，能不能加点boxshaw"（box-shadow）+ 补充"而且没有使用到全局border-radius"。
- 关键定位：`card-wrapper` shortcut（`uno.config.ts:22`）= `rd-8px shadow-sm`，`rd-8px` 同特异性覆盖 NCard 主题圆角——全局圆角链路 = `themeRadius: 6`（`theme/settings.ts:8`）→ `theme/shared.ts:260` naive `common.borderRadius` → NCard 根元素 `var(--n-border-radius)`；**用 card-wrapper 会让圆角固定 8px 不跟随主题**（与 NInput width 同一覆盖机制）。
- 方案（用户确认）：search-action 区域包 `<NCard :bordered="false" class="shadow-sm" :content-style="{ padding: '12px 16px' }">`——阴影 shadow-sm、圆角交给主题、padding 收紧适配单行内容；业务插槽内容零改动。
- 遗留（用户未表态）：card-wrapper 全项目通用且均未跟随 themeRadius；统一需改 uno.config shortcut，影响面大未动。
- 验证：read_lints 0 / typecheck 0。记录：`changelog/快速搜索栏加阴影.md`。

## 系统管理菜单重构 + 系统日志页面（设计→实施一气呵成）

- 用户需求：用户/角色从权限管理移回系统管理（二级排序 用户1/角色2/分组3/站点4/设置5），新增系统日志（6）；澄清=客户管理独立一级「客户管理」下挂「客户列表」、权限管理只留菜单管理、日志接真实接口 `POST /op-log/query`（用户给 OpLogSchema，后端 tms-user `lib/api-v1-web/op-log` 已核实：queryCommon 通用查询，keywordFields 固定 refNames，setDefaultDate 默认近 12 个月，ret:{list,total}）。
- **实施发现 dev 一直运行**：Move-Item 后 elegant watcher 自动重生成 routes.ts——不必手动启停 dev；流程=移目录→改 `build/plugins/router.ts`（routeIcons/routeOrders）→删 routes.ts 旧值条目（menu order3/group 5/setting 7，触发条件=下一次 views 变化重生成时按 router.ts 值重建）→touch `customer-manage/customer/index.vue` 触发重扫（New-Item+Move 的 fs 事件 watcher 没接住，新一级目录条目缺失，touch 后补齐）→路由生成后补 i18n（I18nRouteKey 派生顺序坑）。
- 最终菜单：一级 首页0/渠道报价1/资料管理1/系统管理2/权限管理3/客户管理4（router.ts 一级 order 对齐 routes.ts 历史生效值，修复两者不一致遗留）；二级 系统管理 用户1/角色2/分组3/站点4/设置5/日志6+隐藏 label-designer/print-design；权限管理 菜单1；客户管理 客户列表1。
- 系统日志页 `system-manage/log/`：**搜索区走 Table `#search-action` 自定义卡片**（SearchBar 只内置 actions 插槽不透传自定义 slot，dateRange 非原生控件；NDatePicker daterange+2 NSelect+NInput+搜索/重置一行 flex），列=名称(detail)/操作人/client NTag/opType NTag(登录 primary/修改 info/删除 error/退出 default)/关联实体 join、/IP/摘要/时间 dayjs，明细抽屉=NDescriptions+NDataTable(logs 变更项/旧值/新值/说明)+NEmpty 兜底；where 只在 opType/client 非空时传（数字等值）；`fetchGetOpLogList` 无 DEV mock 分支；cacheKey `system-manage-log`。
- 类型六件套进 `Api.SystemManage`（OpLogClient/OpLogOpType/OpLogDetail/OpLog/OpLogList/OpLogSearchParams）；i18n 三处 route 段增删 + page.manage.opLog 段 + app.d.ts 类型段。
- 顺手修复既有 oxlint error（`update-env-ip.mjs` 未使用解构变量 `prefix`→`_prefix`，阻断 lint 链）。
- 验证：typecheck 0 / oxlint+eslint 0 error（link.vue 2 条既有 warning 不动）/ fmt 已跑。视觉待用户复核。记录：`changelog/系统管理菜单重构与系统日志.md`。

## 表格分页默认改成 50

- 用户："表格分页默认改成50"；现状=use-vxe-table.ts 兜底 `?? 20` + 17 处页面显式 `defaultPageSize: 20`；关键发现=Table 分页条 `page-sizes=[50,100,200,500,1000,2000,5000]` 50 已是首项而旧默认 20 根本不在选项里（页长显示与下拉脱节），改 50 正好对齐、选项零改动；分页 size 无 localStorage 持久化、改完即生效。
- 方案对比（用户选 **A**）：A=删 17 处显式值+兜底改 50（今后调默认只改一处）；B=17 处全改 50（否决，重复声明依旧）。
- `hooks/common/table.ts`（模板 naive useTable，pageSizes [10,15,20,25,30]）零引用死代码，不动。
- 坑：删行类 replace 的 old_str 带猜测 cacheKey 4 处失配（user/waybill-rule/trace-config/init-data），重读真实相邻行后补齐——删行编辑 old_str 必须带真实上下文。
- 验证：typecheck 0 / lint 0 error / fmt 已跑。记录：`changelog/表格分页默认改成50.md`。
