# AGENTS_CHANGELOG

> 本文件维护所有 Agent 讨论/修复记录的索引，**按日期分组（最新日期在上）**。详细文档见 `changelog/` 目录。

## 2026-09-19

- [清理资料管理死代码 mock（已实施）：用户问「资料管理的 mock 数据删了吗」——此前仅只读排查未动文件；**定位**=`src/service/api/data-manage-archive/index.ts` 的 `declaredGoods` 本地 mock 兜底（mockList/mockCreate/mockUpdate/mockDelete + factories/datasets/idSeq/delay），经确认是死代码：无任何视图 `import MasterDataArchive`、真实的申报物品界面走独立 `@/service/api/declared-goods`（真实 request）；**实施**=删除整个本地 mock 兜底实现段、从 `archiveApiMap` 移除 `declaredGoods` 入口、顶部注释删去「暂走本地 mock 兜底」说明；`account` 早已改走真实 `/trade-account/*`；**验证**=typecheck 0，全仓无悬空引用（其余 `declaredGoods`/`mock*` 均为真实键与无关 operation-trace mock）](./changelog/清理资料管理mock.md)

- [承运网络选择计泡规则自动带出材积除/计泡比率（已实施）：用户要求新增承运网络时「材积除/计泡比率」从关联计泡规则带出（附后端 CarrierSchema 佐证两模型同名字段）；**实施**=`Carrier.vue` 下拉 options 附带 `cubicNum`/`weightOff`，`CarrierOperateDrawer.vue` 的 `weightRuleId` select 挂 `onUpdate` 反查选项回填 `formModel`；**全量同步语义**+**清空修复**（用户报「切规则未清空」）：根因 `NInputNumber` 空值语义是 `null` 而非 `undefined`，原写 `undefined` 不刷新显示；改为未配置写 `null`、表单类型放宽 `number|null`、提交前 `payload` 把 `null` 归一 `undefined` 保住后端口径；清空选择不动已填值；编辑态走 `openEdit` 原值不重算；**验证**=typecheck 0 / lint 0 error（link.vue 既有 2 warning 无关）](./changelog/承运网络带出计泡参数.md)

- [承运网络选择计泡规则自动带出材积除/计泡比率（已实施）：用户要求新增承运网络时「材积除/计泡比率」从关联计泡规则带出（附后端 CarrierSchema 佐证两模型同名字段）；**实施**=`Carrier.vue` 下拉 options 附带 `cubicNum`/`weightOff`，`CarrierOperateDrawer.vue` 的 `weightRuleId` select 挂 `onUpdate` 反查选项回填 `formModel`；**全量同步语义**+**清空修复**（用户报「切规则未清空」）：根因 `NInputNumber` 空值语义是 `null` 而非 `undefined`，原写 `undefined` 不刷新显示；改为未配置写 `null`、表单类型放宽 `number|null`、提交前 `payload` 把 `null` 归一 `undefined` 保住后端口径；清空选择不动已填值；编辑态走 `openEdit` 原值不重算；**验证**=typecheck 0 / lint 0 error（link.vue 既有 2 warning 无关）](./changelog/承运网络带出计泡参数.md)

- [全局状态选项配置（已实施）：用户要求「状态的下拉选择能不能添加一个全局的配置，所有这种的直接取，不要每个界面都配置」；**实施**=①新增 `src/constants/options.ts` 全局注册表（预置 `status`=启用1/禁用0，`getGlobalOptions(key)` 解析时调工厂、支持 `$t`）②新增 `src/composables/use-options.ts` 的 `useStatusOptions()` 组合式 ③`FormItemConfig` 加 `optionsKey`，`NFormWrap` 新增 `itemOptions()` 优先取全局 key（select/checkbox/viewText 均走此），抽屉状态项写 `optionsKey:'status'` 即零配置 ④迁移重复定义：`ProviderOperateDrawer` 改 `optionsKey`；`user/index.vue`、`ProfileInfo.vue`、`provider/Provider.vue`、`ItemNoRule.vue`、`LongNoRule.vue`、`NoPool.vue` 的 `statusOptions` 改 `useStatusOptions()`；`input-format/config.ts` 改 `getGlobalOptions('status')`；4 个 SearchForm 的 `statusOptions` prop 类型放宽为 `SelectOption[]`；**收益**=状态文案/值单一数据源，新增界面状态下拉直接取全局；**验证**=typecheck 0 / lint 0 error（link.vue 既有 2 warning 无关）](./changelog/全局状态选项配置.md)

- [服务商状态字段改下拉（已实施）：用户要求「服务商新增界面状态改成下拉的」；**定位**=`provider/ProviderOperateDrawer.vue` 的 `status` 字段原是 `NFormWrap` 的 `type:'switch'`（启用/禁用 1/0）；**实施**=改为 `type:'select'`，`options` 复用既有 i18n `common.enable`(1)/`common.disable`(0)，`filterable:false`；数据契约不变（`status` 仍为 number、空默认 1、编辑回传 `row.status`、提交零改动），无需新增 i18n key；**验证**=typecheck 0 / lint 0 error（link.vue 既有 2 warning 无关）](./changelog/服务商状态改下拉.md)

- [计泡规则新增/编辑抽屉界面重绘（已实施，待手动冒烟）：用户反馈「记泡规则新增界面没有画好」；本机模型不支持看图，按源码审查定位（见 spec）；**工作流**=agent-skills（using-agent-skills→spec-driven-development 设计闸门，用户「继续」确认→frontend-ui-engineering 实现），spec 见 `docs/superpowers/specs/SPEC-weight-rule-drawer.md`；**问题**=①抽屉 width=420 太窄，进位组三列输入拥挤换行 ②基础字段走 `NFormWrap` 但进位规则是裸 `NSelect`/`NInputNumber`/`LButton` 无 label、风格割裂 ③计泡类型长选项被挤压 ④进位组仅粗糙边框无分隔无空状态；**实施**=重写 `WeightRuleOperateDrawer.vue`：抽屉 width 420→700、`NFormWrap` 用 `type:'section'` 分「基础信息」「进位规则」两块、进位组改卡片（带 label 的「进位类型」NSelect + 删除组按钮、带列标题的 `grid-cols-[1fr_1fr_1fr_auto]` 栅格三列 NInputNumber(:min=0)+删行、加区间虚线按钮）、整段空时 `NEmpty` 空状态+加组按钮；i18n 新增 `weightRule.basicInfo/carryType/carryEmpty/ruleEmpty`（zh/en）+ `typings/app.d.ts` 补类型；**零改动**=列表页/接口/字段契约/提交校验逻辑；**验证**=typecheck 0 / lint 0 error（link.vue 既有 2 warning 无关）；浏览器冒烟待复核](./changelog/记泡规则新增界面重绘.md)

- [结算货币接口对接（财务资料 → 结算货币，已实施，待手动冒烟）：用户提供 tms-user `CurrencySchema`（code/name/countryId/local/rate/status…）与列表截图，把 `finance/modules/currency/Currency.vue` 由通用 `MasterDataArchive`（列仅 code/name/rate/symbol）重构为对接真实 `/currency/*` 的独立页；**已确认**=①接口路径 `/currency/query` ②query 恒传 `scene:1`（否则后端 `if(!scene)where.status=1` 只回启用项）③批量停用走 `/currency/disable {ids}`、**无 delete 路由**（操作列不提供删除，停用·启用走 `/currency/update` 局部 status）④`code` 唯一（uniqField:'code'，编辑/停用须随行回传 code）；**实施**=新建 `src/service/api/currency/index.ts`（query/create/update/disable，无 delete）、重写 `Currency.vue`（useVxeTable+名称搜索+新增/批量停用+列 编码/中文名/国家(useCountrySelect.getName)/是否本币(NTag)/汇率/状态(type:status)/操作(编辑+停用·启用)）、新建 `CurrencyOperateDrawer.vue`（code*/name*/countryId*(国家下拉)/local(switch)/rate*/status(select)）、`FinanceCurrency` 类型重写（去 symbol、加 countryId/local/status+Search/Save/List）、`data-manage-archive` 移除 currency 工厂+`archiveApiMap.currency`+`DataManageArchiveKey`、i18n `currency.*` zh/en 同步扩展（含 common.batchDisableSuccess，并补齐上一任务遗留的 `account.*` Schema 以通过 typecheck）；**验证**=typecheck/lint/fmt 通过，`build:test` 因本机 IDE 安全删除 shim 拦截 `rmSync` 失败（与代码无关）；spec 见 `docs/superpowers/specs/2026-09-19-结算货币接口对接.md`、讨论见 `changelog/结算货币接口对接.md`](./changelog/结算货币接口对接.md)

- [银行账户接口对接（财务资料 → 银行账户，已实施，待手动冒烟）：用户提供 tms-user `TradeAccount` Schema 要求对接真实 `/trade-account/*`；**流程**=agent-skills（spec-driven-development 设计闸门→用户确认→planning→incremental-implementation），先例费用类型（同布局）；**4 问确认**=①搜索 关键字+状态 ②列表严格按截图（别名+内置tag/开户银行/开户名称/开户账号/余额右对齐/备注/状态/操作，无创建人/创建时间）③内置项前端限制停用（按钮 disabled+tooltip、批量自动过滤，编辑不限制）④余额可编辑；**契约三坑（已查 tms-user）**=①query `if (!scene) where.status=1`，管理页必须 `scene:1` 才看得到停用项 ②update 走 `uniqField:'alias'`，局部更新（停用/启用）必须随行回传 `alias` 否则误写 `aliasUpdate` 脏字段 ③disable `{ids}` 批量置 `status=0` 无 buildIn 拦截（前端限停）；无 delete 路由；**实施**=新建 `service/api/trade-account/index.ts`（5 真实接口、解包 `{data,error}`、query 恒 `scene:1`+`page`+失败返 `{list:[],total:0}`）；`FinanceTradeAccount`(+Search/Save/List) 替代 `FinanceAccount`、`DataManageArchiveKey` 移除 `'account'`；i18n 中/en 重写 `account.*`；重写 `Account.vue`（仿 ExpenseType，内置 tag、停用拦截、批量停用、cacheKey v2）；新建 `AccountOperateDrawer.vue`（alias\*/bank\* 必填、余额可编辑、编辑随行 alias）；清理 `data-manage-archive` 的 account 工厂+`archiveApiMap.account`；**不动**=后端、菜单 tab key、权限码、依赖、`MasterDataArchive`（currency/其它仍用）；**验证**=`read_lints` 0、`EnableStatus` 确认为 `0|1` 数值型写法同费用类型类型正确、`FinanceAccount`/旧 i18n key 已无残留；`pnpm typecheck`/`lint`/`build:test` 因审批超时未跑、需用户执行](./changelog/银行账户接口对接.md)

- [AGENTS.md 开发流程从 superpowers 切换为 agent-skills（决策，已实施）：用户要求把强制工作流由 superpowers（`brainstorming` 设计闸门）改为已接入 CodeBuddy 的 `addyosmani/agent-skills`（25 技能，junction 链接 `E:\skills` → `~/.codebuddy/skills/`）；**决策**=规则 2 改为「开发必须使用 agent-skills 工作流且先设计后编码」，设计闸门用 `spec-driven-development`（用户确认 spec 后才进实现）、生命周期 `using-agent-skills` → `spec-driven-development` → `planning-and-task-breakdown` → `incremental-implementation` → `test-driven-development`/`code-review-and-quality`/`browser-testing-with-devtools`，保留「确认前禁止实现」硬约束；**路径约定不变**=`docs/superpowers/specs/`、`docs/superpowers/plans/` 与历史 changelog 不改动；**实施**=仅改 `AGENTS.md` 规则 2 一行（详见 changelog 文档）](./changelog/AGENTS开发流程从superpowers切换为agent-skills.md)

- [费用类型接口对接（财务资料 → 费用类型，已实施，待手动冒烟）：用户提供 tms-user `FeeType` Schema 要求「做成跟收发地址一样布局」；**定位**=前端 `finance/modules/expense-type/ExpenseType.vue` 走 MasterDataArchive + 本地 mock（code/name/scope/remark）与真实 Schema 不符（**无 code、备注字段是 note**），后端 `/fee-type/query|get|create|update|disable` 已就绪但**无 delete**；**契约三坑**=①query `if (!scene) where.status = 1`，不传 `scene` 只能查到「使用中」的记录（看不到停用项）②update 走 `updateCommon({uniqField:'name'})`，内置项（buildIn=1）只 pick `scope/order/note`，且局部更新缺 name 会参与唯一性判断并写入 `nameUpdate` 脏字段 ③disable 是 `{ids}` 批量置 `status=0` 的软停用；**决策（ask_followup 4 问）**=①操作列 编辑+停用/启用、工具栏批量停用、不做删除 ②内置项不允许删除→内置行停用按钮 disabled+tooltip、批量停用自动跳过并 warning、抽屉内名称/状态只读 ③列表 费用名称(+「内置」NTag，同角色管理)/使用范围/排序/状态/备注/创建人/创建时间，抽屉 费用名称\*/使用范围(多选0-4)/排序/状态/备注，**不展示 sysType** ④搜索=关键字+状态+使用范围同一卡片；**实施**=`finance.d.ts` 类型换真实字段(+Search/Save/List，加前端 createTime)、新建 `service/api/fee-type`（current→page + 恒定 `scene:1` + where.status/scope）、`modules/expense-type/constants.ts`（scope 选项 + formatExpenseScope）、新建 `ExpenseTypeOperateDrawer.vue`（内置项只提交 `_id+scope/order/note`）、`ExpenseType.vue` 重写为收发地址同款独立页（`useVxeTable`+search-action 卡片+批量停用+TableColumnConfig；停用/启用走 update 且**随行回传 name**）、i18n 三处同步（`common.batchDisable` + expenseType 键重写，删 code/codePlaceholder）、清理 `data-manage-archive` 的 expense-type mock/archiveApiMap/`DataManageArchiveKey`；**不动**=菜单/路由/权限码、后端、其它 mock 档案（account/settlement/declared-goods）；**验证**=`pnpm typecheck` 0 / `oxlint` 0 warnings 0 errors / `eslint` 0 error（.ts 的 no matching configuration 为既有 warning）/ `oxfmt` 无改动 / `pnpm build:test` Build successful（含 `icon-mdi-block-helper` 构建期解析）；浏览器冒烟待用户复核（详情见 changelog 文档）](./changelog/费用类型接口对接.md)

- [录单格式新增：字段映射默认一个都不勾（已实施，待人工冒烟）：用户要求「打开新增弹窗默认不选择」；**定位**=同日早些时候已把新增态初始勾选从「底稿全量」收窄为「★ 必填 + 锁定字段」（见 `changelog/录单格式新增不再默认全选字段.md`），本次再收窄为**空**；**实施**=`field-list-mapper.ts` 的 `initialMappingOf()` 过滤条件由 `item.required === 1 || item.disabled === true` 改为**只保留** `item.disabled === true`，`input-format-drawer.vue` 仅改一条注释；**为何锁定字段仍勾**=锁定项在编辑器里勾选框不可取消（`f.disabled`），且 `mappingToFieldList` 对锁定项强制保留（`item.disabled !== true` 才允许按「未显示」丢弃），初始不勾会出现「界面看着没选、保存却被带上」的错位；编辑态仍走 `fieldListToMapping` 全量回显不受影响，`mappingToFieldList` 一行未动；**验证**=`npx oxfmt --check`（2 文件）通过 / `npx oxlint`（2 文件）0 error / `pnpm typecheck` exit 0，浏览器冒烟待人工（打开新增抽屉应全空，仅锁定字段保持勾选）](./changelog/录单格式新增默认不勾选.md)

- [录单格式配置提取到 config.ts（已实施）：用户「录单格式的字段怎么没有提出来」；**定位**=上一轮格式弹窗提取的追加只覆盖打印/导出格式（各拆 `form-items.ts` + `table-columns.ts`），录单格式当时已是独立抽屉未被纳入，表单项与列表列仍内联在 .vue；**决策（ask_followup 两问）**=①补齐提取 = 表单项 + 表格列 ②**合一个文件** `input-format/config.ts`（用户选定文件名，不同于 print/export 两文件拆分）；**实施**=新建 `config.ts`（142 行：`buildInputFormatColumns()` 8 列 + `buildInputFormatFormItems()` 6 项，三态下拉选项与 `triStateProps` 随之下沉，`$t` 全部在 builder 函数体内调用）；`InputFormat.vue` 删内联 columns 与 `VxeColumnConfig` type import → `columns: () => buildInputFormatColumns()`（cacheKey `setting-input-format-v3` 与升版本注释留页面）；`input-format-drawer.vue` 删 `SelectOption`/`FormItemConfig` type import 与 `statusOptions`/`yesNoOptions`/`triStateProps`/内联 `formItems` → `computed(() => buildInputFormatFormItems())`；三格式模块消费点写法统一（唯一差异 = 本模块单文件，用户决策）；**行为零变化**（列/表单项字段名/顺序/宽度/对齐/必填/span/placeholder/clearable/filterable 与迁移前逐字一致，`formatDateTime`/`flagTagType`/字段映射文件未动）；**subagent-driven 执行**=每任务独立实现者 + 独立审查者（Task 1 Spec ✅/Approved、Task 2 Spec ✅/Approved/无 findings）+ 最终整分支审查（代码「逐字等价、无死代码/类型问题/行为漂移」，唯一 Important = 缺 changelog 由本文闭环）；**验证**=typecheck exit 0 / oxlint 0-0 / eslint 0 error（config.ts 独立 .ts「no matching configuration」warning 为工程既有）/ read_lints 0；改动留工作区未提交，浏览器冒烟待用户复核；**交付提示**=name 列无 `type: 'detail'`（相对 HEAD，属更早在制品移除的「可点击复制」，本任务零漂移，如需恢复请用户确认）](./changelog/录单格式字段提取.md)

- [通用抽屉点击遮罩可关闭（Esc 保持不可关，已实施）：用户「把 drawer 组件改成点击遮罩层可以关闭」；**定位**=`components/common/drawer.vue` 原 `closeOnMask`（默认 false）同时绑 `mask-closable` 与 `close-on-esc`，全仓 46+ 处使用、无一处显式传值；**决策（ask_followup）**=全局 + 拆开控制：遮罩点击默认可关、Esc 保持原样，需要禁止的显式传 `:close-on-mask="false"`；**实施**=拆为 `closeOnMask: true` + `closeOnEsc: false` 两个 prop，模板分别绑定；关闭链路不变（遮罩点击 → `update:show(false)` → `handleUpdateShow` 双事件、v-model 正常收敛）；**影响**=全部抽屉默认生效（表单类误点遮罩会丢弃未保存内容，属预期取舍）；**验证**=typecheck 0 / oxfmt / oxlint 0/0 / eslint 0 error，浏览器手测待用户复核](./changelog/drawer点击遮罩关闭.md)

- [标签设计切换模板不更新（keep-alive 缓存未重载，已实施）：用户贴打印格式列表截图问「点设计进去，标签设计界面还是之前的，没有更新信息」；**根因**=①业务路由全量 `keepAlive` + elegant inject-name 注入组件 name ⇒ label-designer 命中 `global-content` 的 KeepAlive ②`getTabIdByRoute` 无 `multiTab` 时 query 不进 tab id/key ⇒ `?id=` 变化不换实例 ③页面只在 `onMounted` 读一次 `route.query.id`，无 watch / onActivated；**决策（两问确认）**=保持单页签、点哪条显示哪条；有未保存修改弹确认（用户追加：返回也要拦）；否决多页签 multiTab、路由带 `:id`（等价多开）、关 keepAlive（切回来丢编辑）；**实施**=store 增 `dirty`（commit/updateElement/updateElementOptions/undo/redo/setPaper 标脏，loadFromJson/markSaved/reset 清脏）+ `savedJson` 快照 + `markSaved()/revert()`；页面抽 `applyTemplate`、`loadTemplate` 加 `currentId` 竞态校验并在加载完成 `markSaved`（setPaper 会标脏）、`watch(route.query.id)` 带本页守卫 / 同 id 跳过 / `switchConfirming` 防重入，确认按「确认时刻的 route id」加载、取消 `router.replace` 回滚 URL 至仍在编辑的模板，`handleBack` 确认后 `revert()` 再跳设置页；`tool-bar` 返回改 `emit('back')`；i18n 补 `unsavedTitle/unsavedContent`（zh/en + app.d.ts）；**坑**=Naive UI `onClose` 是「点关闭按钮」语义，防重入解除改用 `onAfterLeave`；**边界**=同一条模板重复点「设计」不重载（同 URL push 被 router 判重复，保留草稿）、顶栏页签切换 / 浏览器后退不拦；**验证**=typecheck / lint / fmt 见文档，浏览器手测待复核](./changelog/标签设计切换模板未更新.md)

- [格式弹窗提取为独立抽屉组件（打印/导出格式，已实施）：用户对 `PrintFormat.vue` 说「把新增弹窗提出来，录单格式和导出格式一样」；**现状**=录单格式已有 `input-format-drawer.vue`，打印格式（新增/复制/查看）与导出格式（新增/编辑）抽屉均内联在页面；**决策（ask_followup 确认）**=两个模块都提取，三格式模块组织统一（对齐录单格式范式：`show` + `row` props、`update:show`/`submitted` emits、成功提示留抽屉内）；**实施**=新建 `print-format/print-format-drawer.vue`（mode create/copy/view + `defaultTemplateType` 承接「新增类型跟随列表筛选」，sizeType=5 宽高联动，create/copy 分别走 create 与 copy/create 接口）+ `print-format/template-type.ts`（页面/搜索/抽屉共用选项与 label）；新建 `export-format/export-format-drawer.vue`（row null=新增，含 InputUpload 上传解析、parsedFields 暂存、「未重传不覆盖」逻辑原样下沉）+ `export-format/template-type.ts`；两页面瘦身 205/209 行只留 openXxx 桥接 + `@submitted="getData"`；**顺手修正**=打印格式原提交未检查 `{ error }`，接口失败也关抽屉+提示成功（双提示），提取后统一 `if (error) return`；**验证**=typecheck 0 / oxfmt 6 文件 / oxlint 0/0 / eslint 0 error，浏览器手测待用户复核；**追加（同日）**=用户「可以把字段也提出来」，确认后把表单项与表格列也各抽成独立 .ts（`form-items.ts` / `table-columns.ts`，builder 内调用 `$t`），抽屉只留交互与提交、页面 `columns: () => buildXxxColumns()`（cacheKey 仍由页面管），typecheck 0 / 10 文件 oxfmt + oxlint 0/0 + eslint 0 error](./changelog/格式弹窗提取为独立抽屉组件.md)

- [tab 切换同步 URL（系统设置 ?tab=xxx 双向同步，已实施）：用户贴截图问「tab 切换这个能不能更新？」；**定位**=设置页只有单向同步（`syncTabFromQuery` 的 URL → tab，服务标签设计器「返回」，即 `打印格式设计跳转标签设计器.md` 里搁置的备选 B），点击 tab 不写回 URL；**决策（brainstorming 两问）**=①范围仅「系统设置」页、能力做成 `useArchiveTabs` 可选参数（资料管理 6 页零改动）②历史策略 `router.replace`（不新增历史，后退直接离开本页）；否决页面内 watch（不可复用）与 `history.replaceState`（route.query 脱节）；**实施**=`types.ts` 增 `ArchiveTabsOptions{urlSync}`；`useArchiveTabs` urlSync 时 URL ⇄ activeKey 双向（`watch(activeKey)` → replace 写回 `?tab=`；`watch(route.query.tab)` → 反向定位，带 `route.path !== selfPath` 守卫防 keep-alive 缓存期误伤，无效/无权限值忽略），两个 watch 以值相等 return 防循环；**配套必要项**=写 URL 时 `tabStore.setTabFullPath(getTabIdByRoute(route), fullPath)`（tab store 新增 action，必须改源数组 `tabs.value`——对外 `tabs` 是浅拷贝 computed），否则从设计器返回后切走再点页签会跳回旧 `?tab=print-format`、丢失最后位置；`setting/index.vue` 删 `useRoute`/`onActivated`/`syncTabFromQuery` 改传 `{ urlSync: true }`；**边界**=裸地址不主动补 `?tab=`、replace 多一次同路由导航（NProgress 可能一闪、页签不新增，缓存 key `getTabIdByRoute` 不含 query 不重挂载）、URL 更新随 `contentDelay` 滞后 ~200ms；**验证**=typecheck 0 / oxlint 0/0 / eslint 0 error（3 个 .ts 的 no matching configuration 属既有现象），浏览器手测待用户复核](./changelog/tab切换同步URL.md)

- [字段映射改为行式布局（已实施，待人工冒烟）：用户圈出「复选框 + ★ 星标」反馈「不太好看，换成其他的」，连续否决 5 个样式方案后选定「行式双开关」；**过程**=用 Node 伴随服务器 + IDE 预览做浏览器实物对比（3 轮 8 个方案，mock 存在 `.superpowers/brainstorm/field-mapping-ui/content/`），第 1 轮 A 文字标签/B 静默必填/C 胶囊循环全否，第 2 轮 前缀红点/柔和必填标签 未采纳，第 3 轮 行式双开关/瓦片墙/色阶 中选行式；**决策**=每行一个字段（字段名 flex-1 省略）+ 两枚带字胶囊（显示＝primary-50/600、必填＝error-50/600，未选灰底，未显示时必填置灰），分组一键操作移入组头（「全选」=显示+必填、「清空」=只清显示且锁定字段保留），卡片头改放汇总「已显示 X/Y · 必填 Z」（必填只算「已显示且必填」）+ 搜索 + 仅看已选，筛选为纯前端、组头动作只作用于可见字段；**落地**=新增 `FieldMappingRow.vue`（旧 `FieldMapping.vue` 一行不改、作回退版本，抽屉只改 import 与模板标签名 2 行）；**未动**=FieldMappingValue 结构 / field-list-mapper.ts / 后端接口 / 组件 props-emits 契约；**顺带**=消除 fill 分支两份重复模板（NScrollbar 常驻）；设计见 `docs/superpowers/specs/2026-09-19-field-mapping-row-layout-design.md`，计划见 `docs/superpowers/plans/2026-09-19-field-mapping-row-layout.md`；**验证**=`pnpm typecheck` exit 0、`pnpm build` Build successful（控制者复跑，dist CSS 已含 `repeat(auto-fill,minmax(280px,1fr))` / `h-19px` / `primary-50` 等新类）、单文件 oxlint/eslint/oxfmt 0 error（未跑全仓 lint/fmt，避免改写用户并行在制品）、三个任务均 Spec ✅ + Quality Approved、整体终审「可以交付，无阻塞项，14 条 Minor 全部判定可延后」、旧组件 `git diff` 为空 / 新组件 untracked / 抽屉 33-25（= 原 31-23 + 本次 2 行）；**未提交**（用户决定改动留在工作区自行提交），浏览器冒烟待用户确认——重点三条：①抽屉内不得出现内嵌滚动条且能滚到「子件信息」末尾 ②「仅看已选」下取消显示该行会立即消失、后字段补位，连点易误改相邻字段 ③筛选状态跨抽屉打开保留（组件常驻）；**实施后修正（用户截图反馈）**=①头部「仅看已选」被压成一字一行竖排，根因是 naive `.n-card-header__main{flex:1;min-width:0}` + `__header-extra` 可压缩，而 `NCheckbox` 的 CJK label 可逐字断行（min-content≈一个字）→ 被压到约 20px；修法=右侧容器 `flex-wrap`+子项 `shrink-0`，`NCheckbox` 换回设计稿的 `NSwitch`+`whitespace-nowrap` 文案，搜索框补 `shrink-0`；②列密度跑成 4 列（`minmax(280px,1fr)`）与设计稿不符 → 新增 `.mapping-grid` scoped 样式 `repeat(auto-fill, minmax(min(420px,100%),1fr))`，抽屉宽度下稳定两列宽行、宽屏 3-4 列、极窄单列不溢出；③顺带修掉自引 bug：外层 div 的 `@click` 与 `NSwitch` 自身切换叠加导致「点开关无效」；**二次修正（用户要求与设计稿 1:1）**=弃用 NCard 的 `title`/`header-extra` 自建 `.fm-head`（标题 14px + 汇总同排居左带 1px 竖线，数字用 `<I18nT>` 具名插槽加粗，`{total}` 走参数）、搜索框补 `icon-uil-search`（build-time 图标免运行时网络）、组头文案改新增键 `fieldMappingGroupAll`（全部，替换复用键「全选」）、分组虚线从「分组底部」移到「组头与字段区之间」、`content-style` 恒 `padding:0` 由头部与内容区各自管内边距、边框色改 `var(--n-border-color,#eef0f3)` 跟随暗色主题；**踩坑**=`oxfmt` 会把 `<i18n-t>` 归一成 `<I18nT>`，vue-i18n 安装时同时注册 `i18n-t` 与 `I18nT` 两个名字故仍可解析；**三次修正**=栅格按用户要求从「自适应两列」改为**固定一行四个** `repeat(4, minmax(0,1fr))`（等分可压缩、字段名走 truncate 省略，窄窗口不横向溢出）；三次修正后 `oxfmt` / `oxlint` / `pnpm typecheck` 仍全 0 error；**四次修正**=分组标题前加蓝色竖条（`i.h-14px.w-3px.rd-2px.bg-primary`，`bg-primary` 跟随主题色/暗色，`.fm-ghead` 左内边距 6px 与字段行 px-6px 对齐）；**五次修正**=胶囊点击热区太小（用户反馈「太小不方便选择」）→ 胶囊 19→26px 高、字 11→12px、左右内边距 8→12px 并改 `inline-flex items-center` 居中（不再靠 line-height），字段行 28→36px 高、圆角 5→6px，行间距 3→4px；**六次修正**=胶囊加悬浮反馈（用户要求「浮动效果」）→ 过渡改 `transition-all duration-150 ease-out`、可用态 `hover:translate-y-[-1px] hover:shadow-[0_2px_6px_rgba(16,24,40,0.12)]`、按下 `active:translate-y-0 active:shadow-none` 回落、底色加深（显示 `hover:bg-primary-100` / 必填 `hover:bg-error-100` / 未选 `hover:bg-#e9ecf1`，暗色 `dark:hover:bg-#ffffff24`），禁用分支不含浮动类；构建产物已回查确认 uno 产出 `.hover\:translate-y-\[-1px\]` 与 `.hover\:shadow-\[…\]` 及 `--un-translate-y:-1px`；**七次修正**=列之间加竖向虚线（`.mapping-grid > div:not(:nth-child(4n+1))::before`，画在格子外侧 `left:-14px` 即 28px 列间距正中，颜色随 `var(--n-border-color)`，不参与 hover 底色），构建产物已确认产出；**八次修正**=行之间加横向虚线（`.mapping-grid > div::after` 画在格子外侧 `bottom:-2px` 即行间距正中，左右各外扩 14px 让同行相邻格子横线在列间距正中首尾相接、与竖线交叉成十字，首列 `left:0` / 末列 `right:0` 防溢出；组末行也保留一条作收口线）；**九次修正**=①组头「全部/清空」放大（字号 12→14px、加 `padding:2px 8px`+圆角 5px 热区、悬停底色 全部 `bg-primary-50`/清空 `bg-#f3f4f6`，间距 14→6px；注：`--primary-color` 是逗号分隔通道值，斜杠透明度写法 `rgb(var(--primary-color)/0.08)` 会失效，故走数字色板类）②**修复真 bug**：卡片汇总「已显示 116/ · 必填 116」中 `{total}` 渲染为空 —— 根因是 vue-i18n `<I18nT>`（`TranslationImpl`）**只认具名插槽作插值值**（`renderChildren` 里 `keys = Object.keys(slots)` + `getInterpolateArg` 只从 slots 取值），`:total="…"` 落进 attrs 不参与插值 ⇒ `{total}` 取 undefined 渲染为空；修法=`:total` 改 `<template #total>`，`#shown`/`#required` 本就是插槽不变](./changelog/字段映射改为行式布局.md)

- [录单格式新增：字段映射不默认全选（已实施）：用户要求「打开新增界面，字段映射不要默认全选」；**定位**=`loadDrawer` 新增分支 `applyBaseFieldList(await fetchDefaultTemplateFieldList())` 把「是否默认」模板底稿整体交给 `fieldListToMapping`，而该函数遵循「项存在=显示」⇒ 底稿几项就勾几项；底稿本身需保留（`mappingToFieldList` 靠它透传 keyMap/options/width 等附加属性，丢了会让后勾字段退化成 fields-config 最小项）；**决策（用户三选一）**=初始只勾底稿中 `required===1` 的 ★ 必填字段，其余不勾；**技术补全**=`disabled===true` 锁定字段一并保持勾选（编辑器里是 disabled 复选框、不可取消，且 `mappingToFieldList` 对锁定项强制保留，初始不勾会造成「界面没选、保存却被带上」的错位）；**实施**=`field-list-mapper.ts` 新增 `initialMappingOf()`（全量映射后按 `required===1 || disabled===true` 过滤各分组 show/required，空分组不落键）；`input-format-drawer.vue` 新增分支改 `baseFieldList=draft` + `fieldMapping=initialMappingOf(draft)`；编辑态仍走全量回显不受影响；**验证**=typecheck 0 / lint 0 error（link.vue 2 既有 warning）/ fmt 通过；边界=无默认模板则映射全空，手动勾选仍按既有行为自动置必填](./changelog/录单格式新增不再默认全选字段.md)

- [录单格式：表单开关改下拉 + 列表快捷操作（已实施）：用户要求抽屉里 switch 改下拉、栅格改一行三个，并指出列表少了「停用/启用」「设为默认」；**决策（用户确认）**=①三态字段 status/customerEnable/isDefault 改 `type:'select'`（filterable:false + clearable:false）②6 项 span 统一 8 ⇒ 2 行 × 3 列（备注也缩 1/3）③配套修 `Form/index.vue` 三处 `item.clearable || true` → `?? true`（原短路让显式 false 失效，副作用是 Shipper/ShipTo 抽屉的 `clearable: !customerId` 开始按预期生效）④列表加「设为默认」（isDefault=1 时 disabled，对齐 PrintFormat）与「停用/启用」（NPopconfirm 二次确认，文案 common.confirmEnable/confirmDisable，对齐 user 列表），action-width 130→260；**后端关键坑**=`/order-template/update` 走 `updateCommon({uniqField:'name'})`，缺 name 时 `$in([undefined])` 长度 1 退化为 `list[0]` ⇒ `findOne({})` 命中任意文档 ⇒ 误报「已存在」，故不能照抄 user 列表「只传 \_id+status」，必须回传 name（等值跳过校验）；「设为默认」无需新接口（后端 isDefault=1 会自动清其它默认），update 路由未配 opLog、updateBy/updateDate 自动补；**实施**=新增 `fetchPatchInputFormat` + `Api.InputFormat.PatchParams` + i18n `setDefault`（zh/en/app.d.ts）；**验证**=typecheck 0 / lint 0 error（link.vue 2 既有 warning）/ fmt 通过，未做浏览器实测](./changelog/录单格式表单下拉与列表快捷操作.md)

- [菜单栏 hover 颜色变化（已实施）：用户问「菜单栏 hover 能不能加个颜色变化」；**根因**=naive-ui 2.44.1 inverted 菜单默认 `itemColorHoverInverted:'#0000'`（hover 无背景）+ hover 文字/图标仅 #BBB→#FFF，深蓝底上几乎不可见（`es/menu/styles/light.mjs` createPartialInvertedVars）；**决策（用户确认）**=背景微亮 + 文字/图标/箭头变主题蓝，落点选「naive 主题变量覆盖」而非 CSS（naive 2.44.1 inverted 不加 `n-menu--inverted` 类名、变量内联注入需 !important，CSS 方案脆弱）；**实施**=仅改 `store/modules/theme/shared.ts` 的 `getNaiveTheme()` 增 `Menu: { itemColorHoverInverted: rgba(255,255,255,0.12), itemTextColorHoverInverted/itemIconColorHoverInverted/arrowColorHoverInverted: getPaletteColorByNumber(primary, 400) }`（#3b82f6→#66a6ff）；刻意不动选中态变量（蓝底白字不可变蓝）与 `itemTextColorChildActiveInverted`（常态色与 hover 共用，改了展开父项常态变蓝）⇒ 展开父项 hover 仅背景微亮、文字保持白（边界已知，如需一致需补 CSS）；**验证**=typecheck 0 / oxlint 0 / oxfmt 通过 + playwright 真实渲染实测（普通项 hover 背景 rgba(255,255,255,0.12)+文字 rgb(102,166,255)，选中项蓝底白字未破坏）](./changelog/菜单栏hover颜色变化.md)

- [乱码文件从 git 历史还原（已实施）：11 个 .vue 工作区损坏（U+FFFD 替换符 / 注释与代码行合并 / :class 多余引号）导致 vue-tsc 大量 TS1005/TS1136；早期按 GBK mojibake 特征搜索 0 命中走弯路，用户纠正"diff 左侧 HEAD 就是完好中文直接还原"；**实施**=搜 `\x{FFFD}` 定位 9 文件→`git show HEAD` 校验字节无 EF BF BD→`git restore`，typecheck 复跑再暴露 2 个不含 U+FFFD 的引号/行合并损坏文件（first-level-menu.vue、ExportFormat.vue）一并 restore；**教训**=①先确认实际损坏字节形态再选搜索特征 ②HEAD 干净时 git restore 是首选 ③U+FFFD 清零后必须复跑 typecheck（损坏形态不止一种）；验证=typecheck 0 / 工作区 U+FFFD 0 命中](./changelog/乱码文件从git历史还原.md)

- [typings 按菜单栏分组（已实施）：用户要求把 `src/typings/api/` 下 18 个平铺 `.d.ts` 按侧栏菜单树两级分组；**决策**=一级菜单建目录、资料管理/系统管理下再按二级建子目录、4 个全局文件(common/upload/auth/route)进新建 `common/`、只移动文件不改 `namespace` 名称；目标=`common/` + `channel-quote/index.d.ts` + `data-manage/{index,ship,basic,business,finance,bl,no-rule,declared-goods}` + `system-manage/{index,system-setting/{company,input-format,print-format,export-format}}`；零 import 引用⇒零业务代码改动，验证=typecheck/lint 0；设计文档见 `docs/superpowers/specs/2026-09-19-typings-api-group-by-menu-design.md`](./changelog/typings按菜单栏分组.md)

- [角色管理新增按钮不显示（权限码多余引号，已修复）：用户反映有 `system:user:add` 权限但按钮不显示，实际根因定位到角色页新增 `system:role:add`；**定位**=`role/index.vue:292` 写成 `<LButton auth="'system:role:add'"`，`auth` 是 LButton 静态 prop（无 `:`）Vue 当字面值、引号原样保留 ⇒ `props.auth`=`'system:role:add'`（带引号），`hasAuth` 查 `permissions.includes("'system:role:add'")` 永远 false ⇒ 按钮恒隐藏，与角色配没配无关；**关键区分**=`v-auth="'x'"` 是指令、值按 JS 表达式求值引号被吃掉（正确），仅组件静态 prop `auth="'x'"` 才原样留引号（错误），全仓 grep 仅此 1 处、其余 8 处 `v-auth="'...'"` 均正确；**实施**=:292 删值两侧单引号（`auth="system:role:add"`）；**附带**=`user/index.vue:282` 用户页新增 `auth="system:user:add"` 写法本身正确，该按钮不显示属角色 auths 未含此码的配置问题；验证=read_lints 0](./changelog/角色页新增按钮权限码多余引号.md)

- [页签切回页面重复查询接口（keep-alive 缓存，已实施）：用户反馈"菜单栏没有缓存，每次切换路由都会重新查询"，确认是顶部页签切回页面重新挂载+列表/详情接口重查；**定位**=`global-content` 的 `KeepAlive :include="routeStore.cacheRoutes"` 仅收 `meta.keepAlive` 路由，原 `routes.ts` 只有 `iframe-page` 设了 keepAlive，业务路由全未缓存；"查询中切不走"非硬拦截（守卫无 next(false)、表格 loading 只盖表格区、App.vue 无全局遮罩），体感卡顿来自切回重挂载+重查+out-in 离场过渡；**决策**=全部非 constant 业务路由开 keepAlive、不改 out-in、不碰请求层；**实施**=`build/plugins/router.ts` 的 `onRouteMetaGen` 对 `!constantRoutes.includes(key)` 路由 `meta.keepAlive=true`（constant=login/403/404/500 排除），删 `.vite-temp` 后 `pnpm build` 重生成 `routes.ts`（7 一级+17 叶子业务路由写入 keepAlive，iframe-page 维持）；`cacheRoutes` 自动收集、每页签 `:key=tabId` 独立缓存；**数据新鲜度**=抽屉 submit 已 getData 刷新、页签刷新按钮走 `resetRouteCache` 强制重挂载（已验证）；**验证**=typecheck 0 / lint 0 error(link.vue 2 既有 warning) / build 通过](./changelog/页签切回页面重复查询接口-keepalive缓存.md)

- [NPopconfirm手写包法收敛为LButton内置popconfirm（已实施）：用户指出 `<NPopconfirm><template #trigger><LButton>` 是重复样板，LButton 已内置二次确认；**范围**=全仓 NPopconfirm 块（排除 LButton.vue 自身实现），48 处/48 文件；**实施**=脚本化转换：trigger 恰为单个 LButton 时，`@positive-click`/`@negative-click` 原样上提（LButton 从 attrs 摘出转交内部 NPopconfirm）、`v-if` 等结构指令移到 LButton、`placement`→`popconfirm-placement`、文案 `{{ $t('common.confirmDelete') }}`→裸 `popconfirm`、`{{ expr }}`→`:popconfirm="expr"`、纯文本→`popconfirm="文本"`；**特例**=10 处批量删除的 `<NPopconfirm :disabled="checkedRows.length===0">` 直接丢弃该 disabled（trigger 按钮自身同条件 disabled，禁用时弹窗本就打不开，行为等价）；**坑**=①属性正则 `[^>]*` 被 `@positive-click` 箭头函数 `=>` 截断导致 10 处静默漏转，改引号感知匹配修复 ②脚本忘了从组装列表排除 `:disabled` 造成重复属性（Vue 编译报错），补去重脚本 ③缩进错乱靠 eslint --fix（vue/html-indent、attributes-order）修正；**注意**=上次删过的 `components.d.ts(160)` LButton.bak 陈旧条目又被写回：`.bak.vue` 文件不存在、9527 无服务，判定是某个运行中的 Vite dev server 内存缓存残留并随 src 变更反复重写该文件，**需重启 dev server 根治**；**验证**=lint 0 error / typecheck 转换文件 0 错误（唯一报错即上述陈旧条目）/ fmt 已跑](./changelog/NPopconfirm收敛为LButton内置popconfirm.md)

- [按钮统一改用LButton并移除size=small（已实施）：用户要求把所有按钮改用 LButton 并去掉 size="small"；**范围**=src 下全部 .vue/.ts/.js（排除 LButton.vue 与 button-icon.vue 自身封装），共 93 文件；**实施**=脚本化 `<NButton>`/`</NButton>`→`<LButton>`/`</LButton>`（正则 `<NButton\b` 的 `\b` 边界避开 `NButtonGroup` 误伤）、`h(NButton)`→`h(LButton)`、仅移除**按钮标签内** `size="small"`（NCard/NTag/NInputNumber 等非按钮组件的 `size="small"` 全部保留）、清理 9 处显式 `import { NButton } from 'naive-ui'`(删 NButton token 余者保留)、为 .ts 的 `h(LButton)` 补 `import LButton`(app.ts)；**例外**=label-designer tool-bar.vue 两处 `<NButtonGroup>` 直接子按钮保留 NButton：naive ButtonGroup 连体边框靠 `.n-button-group .n-button:first-child`/`.n-button + .n-button` 后代与相邻兄弟选择器，LButton 包裹导致内部 NButton 不再是 group 直接子元素⇒连体外观失效；**顺带修**=`components.d.ts(160)` 指向不存在 `LButton.bak.vue` 的陈旧生成条目（阻塞 typecheck/lint/build，该 .bak 文件已不存在）删除以恢复可构建；**坑**=脚本初版给 `directives/auth.ts` 误加未使用 `import LButton`(LButton 仅出现在注释)→lint 报 no-unused-vars 已删除；**验证**=typecheck 0 / lint 0 error(link.vue 2 既有 warning) / fmt 待跑](./changelog/按钮统一改用LButton并移除size=small.md)

- [data-manage 类型按模块拆分文件（已实施）：`typings/api/data-manage.d.ts` 的 `Api.DataManage` 把基础/财务/业务 3 大类 14 接口堆一起过长；方案=利用 `declare namespace` 声明合并按模块拆成 `data-manage.d.ts`(保留 MasterDataRow/DataManageArchiveKey/ArchiveList/ArchiveSearchParams) + `data-manage-basic.d.ts`(BasicCountryRegion/BasicFbaWarehouse) + `data-manage-finance.d.ts`(FinanceAccount/Currency/ExpenseType/Settlement) + `data-manage-business.d.ts`(8 个 Business* + ProblemGroupSearchParams)，命名空间不变 ⇒ 25 处 `Api.DataManage.*` 引用零改动；验证=read_lints 4 类型文件+3 消费文件 0 报错，全仓 typecheck 仍被 components.d.ts(160) LButton.bak 既有错误阻塞；备选=改独立命名空间需改 25 引用未采用](./changelog/data-manage类型按模块拆分.md)

- [问题类别搜索框 `v-model:value` 类型报错（已修复+抽类型文件）：`ProblemCategory.vue` 中 `searchParams` 原声明为 `Record<string, unknown>`，`keyword` 被推为 `unknown` 与 `NInput` 的 `value: string` 不兼容；修复=在 `typings/api/data-manage.d.ts` 的 `Api.DataManage` 下新增命名类型 `ProblemGroupSearchParams`（`type` 别名、含 `keyword: string`，用 type 而非 interface 以便隐式满足 `search-model: Record<string, unknown>`），页面改为 `reactive<Api.DataManage.ProblemGroupSearchParams>({ keyword: '' })`，后续加筛选字段只改该类型；验证=read_lints 0，全仓 typecheck 被 `components.d.ts(160)` 既有非法标识符阻塞，与该修复无关](./changelog/问题类别搜索框类型报错.md)

- [个人中心点击报错 No match（已修复）：非超管且无 system:personalCenter:list 用户点头像下拉「个人中心」报 `No match for {"name":"personal-center"}`；**定位**=个人中心路由被错误挂 `system:personalCenter:list` 权限码，静态路由模式下 `filterAuthRoutesByPermission` 把无该码用户的此路由整体剔除、未 `addRoute`，而下拉菜单对所有登录用户都渲染 → 点击解析不到命名路由；**方案**=从 `build/plugins/router.ts` 的 `routePermissions` 删 personal-center 映射 + 同步删 `router/elegant/routes.ts` 已生成路由的 `permission`，个人中心故意不设权限门禁（页面内用 editInfoCtrl 控可编辑字段）；**边界**=MENU_PERMISSION_TREE 孤立码未删、DEV 下会有无权限控制台 warn（预期）；验证=read_lints 0 报错，typecheck/lint/build 待跑](./changelog/个人中心点击报错.md)

- [6 页去除 MasterDataArchive 全部拆开（已实施）：用户圈住 `<MasterDataArchive :config="config"/>` 要求拆开；问题类别/物品类别/报关类型/出口原因/清关方式/销售条款 各自内联 useVxeTable(api+dayjs transform+列+cacheKey)/搜索/批量删除/列设置/抽屉表单，直接 import 各自 fetch 不走 archiveApiMap，布尔列 slot 直挂页面 Table，去未接线的 detail 模式；修 columns 重名(→buildColumns)与 3 个 fetch 返回 ArchiveList<MasterDataRow> 断言；finance 4 页未在圈选范围仍走公共组件](./changelog/报关类型等四个档案接口对齐.md)

- [档案弹窗去 switch 全改下拉（已实施）：用户圈 问题类别/物品类别/报关类型/出口原因/清关方式/销售条款 6 页面，要求新增弹窗必须有状态下拉、禁用 switch；shared.ts 新增 useArchiveYesNoOptions(是=1/否=0)，6 页 formItems 补 状态下拉(启用/禁用，编辑回显行值)，物品类别 4 个布尔 + 报关类型「仅内部可用」switch→下拉，列表渲染不变；中途修掉 3 页重复声明 statusOptions](./changelog/报关类型等四个档案接口对齐.md)

- [报关类型/出口原因/清关方式/销售条款 接口对齐（已实施）：4 个 Schema 均 name 必填+status+note(备注)，仅报关类型多 inner(仅内部可用 0-否1-是)；**定位**=后端 `/customs-type`、`/export-reason`、`/customs-clear`(前端键 clearanceMethod)、`/sales-terms` 路由均存在、service 全 keywordFields:['name']；**问题**=4 页面原配置带 code 列/code 输入框（Schema 无 code）且表单用 remark 而后端字段是 note（不落库），清关方式原走 mock；**方案**=清关方式新增 4 个 `/customs-clear/*` 真实 fetch 切 archiveApiMap 并删 mock 工厂(真实 6→7/mock 6→5)、修正 3 个既有 fetch 的 current→page 分页错位(原 data:params 致 queryCommon 读不到 page 翻页无效)、4 页面去 code+补 备注列/表单(报关类型加 仅内部可用 switch 列 slot 渲染 是/否)、类型去 code 加 note+报关类型 inner、i18n/App.I18n 补 inner；typecheck/lint/build 通过](./changelog/报关类型等四个档案接口对齐.md)

- [物品类别接口对接（已实施）：ProductGroupSchema（name 必填/sensitive/charged/danger/isDefault 均 0-否1-是/note 说明/status）+ 老系统截图（列：类别名称/说明/是否敏感货/是否带电/是否危险品/是否默认/状态，仅关键字搜索）；**定位**=后端 `/product-group/*` 契约同 problem-group（query 读 page、keywordFields 固定 name、delete 传 ids、create/update 校验 name、setDefault 自动清他项默认），前端 GoodsCategory.vue 原走 mock（code/name/remark）与 Schema 不符；**方案**=data-manage-archive 新增 4 个真实 fetch（current→page 映射+status 拼 where）切 archiveApiMap、GoodsCategory.vue 按截图对齐（布尔列 slot 渲染 是/否，表单 4 switch+说明，status 不进表单默认 1）、类型去 code、i18n/App.I18n 补键](./changelog/物品类别接口对接.md)

- [问题类别接口对接（已实施）：ProblemGroupSchema（name 必填唯一/desc/note/status）+ 老系统截图（类别名称/详细描述/处理办法/状态，仅关键字搜索）；**定位**=后端 `/problem-group/*`（query 读 page、keywordFields 固定 name、status 需放 where、delete 传 ids、create/update 校验 name）已就绪，前端 ProblemCategory.vue 原走 MasterDataArchive mock 且配置含 code/remark 与 Schema 不符；**方案**=data-manage-archive 新增 4 个真实 fetch（list 显式 current→page 映射 + status 拼 where）切换 archiveApiMap、ProblemCategory.vue 配置改 name/desc/note（status 不进表单默认 1）、类型 BusinessProblemCategory 去 code、i18n/App.I18n 补 name/desc/note/namePlaceholder；**备注**=currency 等真实档案透传 current 后端读 page，分页可能未生效（既有问题未改）](./changelog/问题类别接口对接.md)

- [申报物品改造为地址簿布局（已实施）：用户提供 Product/ProductCountry Schema + 老系统侧栏截图（申报物品 → 申报物品库/清关目的地），要求与地址簿布局一致；**定位**=现 `business/modules/declared-goods/DeclaredGoods.vue` 是 MasterDataArchive 占位（code/name/remark）与 Schema 不符，地址簿布局参考 `address/Address.vue`（NTabs 双子页签 + useVxeTable/Table/OperateDrawer）；后端 `/product/*`（delete 传 ids）、`/product-country/*`（delete 传 \_id）已就绪、契约同 data-manage-bl {page,size,keyword,where}→{list,total}；**方案**=新建 typings/api/declared-goods.d.ts（Api.DeclaredGoods）+ service/api/declared-goods + DeclaredGoods.vue 重写为双 tab（申报物品库/清关目的地）+ product 与 product-country 两组 Table+OperateDrawer + i18n 补文案；**待确认**=币种选项来源 / 表单字段是否全保留（imgUrl 上传） / 旧简单档案数据弃用不做迁移](./changelog/申报物品改造为地址簿布局.md)

- [地址簿页面接入收发件人接口（设计待确认）：`data-manage/business/modules/address/Address.vue` 当前走 `MasterDataArchive` 的 `address` 本地 mock（假数据 ADD…），真实接口 `service/api/ship-address` 已就绪（ship-to/shipper 同构 CRUD）；参考实现 `customer-detail/address-tab.vue`+`address-operate-drawer.vue` 已正确接真实接口但客户只读；**用户确认**=收件+发件都接(页面内 tab 切换) / customerId 放新增弹窗里选(全局跨客户视图)；**设计**=Address.vue 重写为自带 useVxeTable 双 tab(where:{} 查全量、加客户列) + AddressOperateDrawer 改客户字段为可选下拉(空 customerId 时) + 清理 archiveApiMap.address mock + i18n 补 tab 文案 + 类型改用 CustomerAddress\*；**待确认**=列表无 customerId 后端是否返全量 / 是否引 useCustomerSelect Hook](./changelog/地址簿接入收发件人接口.md)

- [资料管理 business/finance 接口拆分对接（方案2，已实施）：用户确认把 business(8)+finance(4) 档案像 basic/bl/ship/no-rule 拆成独立真实接口；**定位**=business/finance 共用 MasterDataArchive 硬编码通用 fetchXxxDataManage，DEV 走 mock、PROD 请求 /data-manage/:archive 但后端无此路由必 404；后端 api-v1-web 已存在 currency/sales-terms/export-reason/customs-type 4 个、缺 account/settlement/address/declared-goods/problem-category/goods-category/clearance-method/expense-type 8 个；**契约坑**=后端 queryCommon 返回 {list,total} 且主键 \_id、前端组件用 records 与 row.id 故即便已存在 4 路由也跑不通（当前能显示全靠 DEV mock）；**方案2(设计待确认)**=前端新增 data-manage-business/finance 独立 api 文件 + 组件内 archive→fetch 映射表 + transform 改 r.list + 删除改 row.\_id + 删通用 api/data-manage 与 mock + 合并两份 MasterDataArchive；后端补 8 实体路由；**待确认5问**=id 对齐方式(\_id vs 后端补id) / 缺失8实体新建vs复用fee-type等相近路由 / 组件合并vs保持 / 后端是否由我一并实现 / basic 副本是否漏改一并规范化](./changelog/资料管理business-finance接口拆分对接.md)

- [新增按钮被权限隐藏后左侧残留间隔（用户贴「用户管理」截图红框圈住「批量删除」左边空白，问"这个间隔这么没有去掉呢"；**定位**=不是间隔没去掉，是"按钮隐藏了、包裹它的容器还在"：①`LButton` 的 `auth` 无权限时模板根 `v-if` 整段不渲染 ⇒ 只渲染出一个注释节点 `<!---->`（`LButton.vue:118-137`）②页面按钮组在 naive `NSpace` 里（`user/index.vue:279`），naive `Space.mjs:100-144` 的 `flatten(getSlot(this), false)` 不过滤注释、且**只对"子 vnode 本身是 Comment"跳过包装**（`child.type === Comment ? child : h('div',{role:'none',class:itemClass},child)`）；`<LButton>` 是**组件 vnode**（注释在组件内部渲染）⇒ 照样被套一层无类名的 `<div role="none">`（`itemClass` 未传）③空壳仍是 flex item ⇒ `NSpace` 的 `gap` 照常生效，`space/styles/_common.mjs` 的 `gapMedium='8px 12px'` ⇒ 横向残留 12px；**同类表现**=`v-auth` 的 `el.remove()`（`role/index.vue:292` 新增角色按钮）留下空壳 `<div role="none"></div>` ⇒ 同样残留 12px；**方案待用户选**=A 全局兜底 CSS `.n-space > div[role='none']:empty{display:none}`（`:empty` 按规范忽略注释 ⇒ 空壳不参与 flex 布局、gap 消失，一处覆盖 auth/v-auth、零页面改动，与已有 naive 全局补丁风格一致）/ B 页面改 `v-if="hasAuth('system:user:add')"`（子节点直接成注释 ⇒ NSpace 不包装，但要逐页引入 useAuth 且放弃上轮统一的 `auth` prop）/ C 按钮组改普通 flex div（注释不生成盒子，但要逐页改、偏离 NSpace 约定）；**用户确认选 A 已实施**：`src/styles/css/global.css` 末尾加 `.n-space > div[role='none']:empty{display:none}`，`:empty` 忽略注释 ⇒ `auth`/`v-auth` 两种空壳都不再参与 flex 布局、gap 消失；`[role='none']` 限定只命中 Space 自己生成的包装层，不误伤页面空 div；纯 CSS 改动，`oxfmt` 无变更、typecheck/lint 无新增问题，运行时需浏览器复核](./changelog/新增按钮权限隐藏后残留间隔.md)

- [提单资料-航线表单：补排序、全量国际化、状态改下拉（用户贴「新增航线」抽屉截图要求：补「排序」数字字段，表单 label 硬编码中文`航线类型`/`中文名`/`英文名`/`空运`/`海运`全部改 `$t`；列表 `BlRoute.vue` 同样硬编码的列标题与单元格标签一并国际化；**范围**=仅 bl-route 子档案，其余 5 个子档案与列表状态列/搜索栏状态筛选不动；**状态**=用户先要求去掉，后改口「状态还是加上吧，改下下拉选择」⇒ 恢复为 `select` 下拉（`statusOptions`=`common.enable/disable`，`required:true`，默认值 1，置于排序之后备注之前），原 `switch` 形态弃用；**后端零改动**；**实施**=`locales/zh-cn.ts`+`en-us.ts` 的 `page.dataManage.bl.blRoute` 新增 `nameCn/nameEn/routeType/order/routeTypeOption{air,sea}` 键 + `typings/app.d.ts` 补 `App.I18n.Schema.page.dataManage.bl.blRoute` 对应类型声明（因 `$t` 受 `I18nKey` 强约束）+ `BlRouteOperateDrawer.vue` 新增 order number 项、status 改 select 下拉、`routeTypeOptions`/`formItems`/列全部改 `$t`、`emptyForm`/`openEdit` 含 status/order + `BlRoute.vue` 列标题与 routeType 单元格标签国际化；**验证**=typecheck 0 / lint 0 error（link.vue 2 既有 warning）](./changelog/提单资料航线表单排序国际化.md)

## 2026-09-18

- [按钮权限 v-auth 在多根组件上失效修复（用户贴「用户管理」截图报无新增权限却显示新增按钮、刷新后又回来；**定位**=`v-auth`是 Vue 自定义指令，只作用到组件**单根元素**，挂在**多根(Fragment)组件**上被 Vue 直接丢弃(dev 告警 `Runtime directive used on component with non-element root node`)；`LButton` 模板含 `createReusableTemplate` 的 `DefineButton`(render 返回 undefined→注释节点)+ `NTooltip`/`ReuseButton` 形成 `[<!---->,NButton]` 多根 ⇒ `user/index.vue:280` 新增失效；`NPopconfirm`/`NTooltip` 的 naive Popover `render()` 返回数组(`VBinder` 默认插槽 `[trapDiv?,VTarget,NPopoverBody]`)多根 ⇒ `user:338` 启用停用/`user:346` 删除/`role:354` 删除失效；单根 `NButton`/原生 `span` 上的 v-auth 正常；**附带错码**=`user/index.vue:338` 用 `system:user:enableOrDisable` 但 `menu-permissions.ts:82` 定义 `system:${module}:status`(且 :299 已挂 user.status、角色抽屉也提交 status) ⇒ 恒不匹配；**方案A**=v-auth 只落单根元素、多根组件改组件级 prop 或挂到内部单根触发元素 / `LButton` 新增 `auth?:string|string[]` prop(内部 useAuth().hasAuth,无权限整段不渲染) / `user:280` 改 `auth="system:user:add"` / `NPopconfirm` 上的 v-auth 移入 trigger 内单根 NButton(`user:338` 同时改码 enableOrDisable→status、`user:346`、`role:354`) / **指令语义不动**(el.remove 是 permission-control-design.md §6.1 已确认决策,修复后 v-auth 均在单根元素符合意图) + §6 补「v-auth 仅用于单根元素」规则 / 按钮码沿用裸字符串不引常量；**设计已批准**=spec `docs/superpowers/specs/2026-09-18-v-auth-multroot-fix-design.md`](./changelog/按钮权限v-auth多根组件失效修复.md)

- [界面固定外边距 16px 改 10px（用户要求「每个界面固定加的 p-16px 改成 p-10px」且范围为「所有界面」；**定位=全站固定外边距只有 3 个真正源头**=①`layouts/modules/global-content/index.vue:50` 布局兜底 `p-16px` ②`components/VerticalTabLayout/index.vue:114` 竖排 tab 右栏 `p-16px`（setting/data-manage\*/personal-center 走它）③`components/Table/table.vue:261` `searchActionFlush` 的 `-mx-16px -mt-16px` 抵消父容器 16px 做通栏贴边；其余页面根 div 的 `p-16px` 只是同名原子类视觉不叠加；**决策=方案A原子类直改**（非引入 CSS 变量），`p-10px` 是 UnoCSS 任意值类可直接用；**改动14处+1文档**=3 源头（含 VerticalTabLayout 标题行 px-16px、Table 注释同步）+ 6 页面根 div(user/site/role/log/group/customer-manage) + 设置页内部容器 MasterDetail/FieldMapping/TraceCapture 的 `px-16px py-16px` + channel-quote 两页 `pl-16px` + VerticalTabLayout/README.md 注释；**边界不纳入**=Table 的 NCard `padding:'12px 16px'`、首页卡片、label-designer 预览弹窗、theme-drawer 内部、所有 `text-16px`/`gap-16px` 图标间距与登录页内间距；**验证**=全仓残留 p-16px 均为边界内非固定外边距、typecheck/lint/fmt 待跑、dev 抽查各页外框收紧 10px](./changelog/界面固定外边距16px改10px.md)

- [LButton 按钮组件封装（用户"封装一个按钮组件…默认大小的按钮的高度固定设置为32px，先应用到客户列表的操作栏"；**澄清3问全采纳推荐**=只改默认档高度(未传 `size` 时 32px，tiny/small/medium/large 沿用组件库值，不做全站 themeOverrides) / 客户列表仅换上方工具栏四按钮(行内操作列不动) / 内置 tooltip(disabled 时垫 span 保证提示生效)；**命名**=用户两次修正：`AppButton`→`LButton` 且是**字母 L 非数字 1** ⇒ 文件名 `LButton.vue`，目录由 `components/custom` 改到 `components/basic`；**实施**=新增 `src/components/basic/LButton.vue`（Props extends ButtonProps 保留全部样式能力 + 自有 tooltip/tooltipPlacement/tooltipZIndex；`fixedHeightStyle` 注入 `--n-height:32px`（circle 补 `--n-width`）覆盖 naive 内联变量，**不用 h-32px class**（同优先级不可靠）；text 按钮不干预；插槽 `v-for Object.keys($slots)` 透传；tooltip 分支用 `createReusableTemplate` 免抄三遍）+ 客户列表四按钮改 LButton（新增/列设置去掉手写 size、批量删除删 NTooltip+span 包裹、刷新改 circle 并补 `common.refresh` tooltip）；**坑**=`pnpm build` 报 `Failed to resolve extends base type`（ButtonProps 是派生类型，vue-tsc 能过 SFC 编译器不能）⇒ 改 `extends /* @vue-ignore */ ButtonProps`（未声明属性运行期走 $attrs、编译期类型提示不变）；`vue/no-undef-properties` 告警 ⇒ 用到的 size/text/circle/disabled 在接口显式声明；size 声明后触发 `vue/require-default-prop` ⇒ withDefaults 补 `size: undefined`；模板内直接调 `useAttrs()` 无效须在 setup 内调；**验证**=typecheck 0 / lint 0 error(link.vue 2 既有 warning) / fmt / build 成功；**待提交**=build 重新生成的 `components.d.ts`（新增 LButton 声明）；手动冒烟待 `pnpm dev`）；**追加（用户"table组件里面的也要使用这个按钮组件"）**=`components/Table` 两处 + `components/SearchBar` 全部改 LButton：table.vue 导出/搜索图标按钮去掉 `size="small"` 改 `circle` 并补 `:tooltip`（`common.export`/`common.search`，删掉注释掉的 export 文案与原生 `title`）、table-column-config.vue 列设置弹窗 footer 重置/确认、search-bar.vue 搜索抽屉 footer 重置/搜索；**影响面**=所有用 Table 的页面工具栏右侧两个图标按钮 28px→32px 圆形、弹窗/抽屉确认类按钮 34px→32px；**验证**=typecheck 0 / lint 0 error（link.vue 2 既有 warning）/ fmt / build 成功）](./changelog/LButton按钮组件封装.md)

- [子模块 tab 权限控制（用户贴「单号资料」截图报「这种的子模块没有控制到」，即 VerticalTabLayout 页内竖向 tab 未按角色过滤；**现状**=8 个 VerticalTabLayout 页面的 tab 全由静态 items 驱动、无权限过滤，而 menu-permissions.ts 的 sub() 码（system:{module}:{key}）早已进角色权限树可勾选、登录后 permissions 已含这些码，只是页面从未消费；**实施**=① ArchiveTabItem 加 permission?/component? 字段 ② 新建 views/data-manage/components/useArchiveTabs.ts（hasAuth 过滤 → tabs / 懒加载 asyncComps / activeKey 默认首个 / 全无权限时 NEmpty 占位）③ 6 个资料页（ship/no-rule/finance/business/bl/basic）items 补权限码并接入 composable，注意 basic 页权限码前缀是 dataGeneral ④ system-manage/setting 接入（静态 component 模式，保留 ?tab= query 同步与模板注释）⑤ personal-center 不受控（无权限配置）；**边界**=角色勾了页面菜单但一个子模块都没勾时该页 tab 空白显示 NEmpty，**已有非超管角色需重新编辑勾选子模块权限，否则子模块 tab 全消失**；按钮级 v-auth 仍未实施（另行一轮）]

- [超管判定修正（追加于上条方案A实施之后）：前端 `isSuperAdmin` 去掉 `accountType===1` 条件，仅保留 `roleType===100`；LINFLY 账号 `accountType=1` 但 `roleType=2`，原本被误判超管→走全量菜单分支，是「操作员登录菜单未按权限过滤」的放大根因之一；修正后与后端 `auth.js:22`（accountType==1 跳过接口校验）解耦——前端菜单完全按角色 `permissions` 过滤，接口层仍由后端 `accountType==1` 兜底放行，不会出现「菜单空白但接口 403」死锁；`store/modules/route/index.ts` 的 `initStaticAuthRoute` 现在对 `accountType=1` 的普通操作员也走 `filterAuthRoutesByPermission`]

- [操作员登录菜单未按权限过滤（用户报"我登录的是操作员，怎么没有按权限过滤"；**范围（用户拍板）**=本轮只修菜单/路由层，按钮权限与子模块 tab 另开一轮；**定位=过滤器本身没问题，是换账号后根本没重新过滤**=①**根因1(致命)** `routeStore.resetStore()` 首句 `routeStore.$reset()` 与 `authStore.resetStore()` 的 `authStore.$reset()` 都是 setup store ⇒ Pinia 只支持 options store，dev 抛 `does not implement $reset()`、prod 静默 no-op ⇒ 登出时动态路由不卸载/菜单不清/isInitAuthRoute 恒 true、dev 登出连 toLogin 都走不到 ②**根因2** `login()` 成功只做 applyUser→checkTabClear→redirectFromLogin，若上会话已初始化路由则 guard 里 `isInitAuthRoute` 为 true ⇒ 不重跑 initAuthRoute ⇒ 菜单沿用上一账号权限（checkTabClear 已用 lastLoginUserId 识别账号切换却只清 tab）③**根因3(放大器)** applyUser/initUserInfo 用 `Object.assign` 而 UserDetail 字段不固定 ⇒ 切换账号后旧 `roleType:100`/`accountType:1` 残留 ⇒ `isSuperAdmin` 误判 true ⇒ 走全量分支 ⇒ 菜单全在；**方案(3选1,采纳A)**=A setup store 手写重置+登录按 userId 重建+applyUser 前先清干净 / B 返回值覆盖自定义 `$reset`(依赖 Pinia 内部 assign 顺序且非公开类型) / C 只登录时强制重建(登出后旧路由仍挂在 router，dev 登出抛错依旧)；**实施**=route store 新增 `authRouteUserId`(initStatic/DynamicAuthRoute 成功后写入 userId) + 重写 resetStore(resetVueRoutes→清空 constantRoutes/authRoutes/menus/cacheRoutes/excludeCacheRoutes/authRouteUserId→两 init 标志置 false→await initConstantRoute，置 false 是让 guard 重建菜单的关键) + 导出供 auth 读取；auth store 新增模块级 `createEmptyUserInfo()` 与内部 `resetUserInfo()`(先 delete 所有 key 再赋空壳，只 assign 空壳漏不掉 roleType 这类动态字段)、applyUser/initUserInfo 恢复缓存前均先重置、`resetStore` 用 resetUserInfo+token='' 取代 `$reset`、login 中 `isInitAuthRoute && authRouteUserId !== userInfo.userId` 时 `await routeStore.resetStore()`(在 redirectFromLogin 之前，之后 guard 用新 permissions 重跑 filterAuthRoutesByPermission)；顺带删掉因移除 `$reset` 而不再使用的 `const authStore = useAuthStore()`；**不动**=filterAuthRoutesByPermission/v-auth/hasAuth/子模块 tab/后端；**验证**=typecheck 0；手动冒烟待跑(超管→退出→操作员→F5→换回超管)；**遗留**=同账号重新登录不刷新路由(权限在别处被改需 F5)](./changelog/操作员登录菜单未按权限过滤.md)

- [角色编辑界面：数据权限改多选下拉 + 限制性权限设置配置到角色修改界面（用户贴两张截图：①红框两个复选框「仅查看专属客户业务/仅查看所属组别客户业务」改下拉（追问后明确**多选**）②「限制性权限设置」弹窗 5 项配置到角色修改界面；**现状**=`role-operate-drawer.vue` 的 `dataAuths` 用 `#dataAuths` 插槽裸写 `NCheckboxGroup`（未走 FormWrap）、同抽屉已有 4 个 `type:'switch'`（sendOrderCtrl/sendCtrl/orderColCtrl/editInfoCtrl），第二张图弹窗全仓零命中（是老系统界面）；**后端事实（零改动）**=`tms-user/lib/common/models/role.js` 已有 5 个限制性字段且与第二张图一一对应（sendOrderCtrl 出库后禁止修改/sendCtrl 出库必须称重/orderColCtrl 运单列禁止设置列表字段/editPwdCtrl 用户修改密码/editInfoCtrl 用户修改个人信息，均 0-不允许·不启用 1-允许·启用），`services/role.js:72-92` 取 max 且 roleType==100 强制 editPwdCtrl=editInfoCtrl=1；**唯一缺口**=`editPwdCtrl` 前端类型/model/回填/重置全缺；**组件能力**=FormWrap 原生支持 `select+multiple`（多选值自动 join '、'）与 `type:'section'` 区块标题（蓝色竖条+粗体）⇒ 公共组件零改动；**核心判断**=第二张图不是新功能而是「现有 4 switch 改下拉 + 补齐 editPwdCtrl」并收进 section 区块，直接新增会与现有 4 字段重复；**设计待用户确认**，未写代码](./changelog/角色编辑界面数据权限下拉与限制性权限设置.md)

- [菜单与按钮权限控制（用户问"菜单权限和按钮权限的控制应该怎么处理"；**现状=权限只写不读**=角色页能把码写进 `role.auths`(/role/auths/update) 且后端 `authMap` 能 403，但前端从不消费 ⇒ ①`router/elegant/routes.ts` 无路由写 `meta.roles` 使 `filterAuthRoutesByRoles`(store/modules/route/shared.ts:12-43) 与 `guard/route.ts:29-32` 恒放行 ②`role.auths` 混装数字菜单id(`"601"`)+`system:`码且 `buttons===roles`(后端无 showOps) ③`isStaticSuper` 依赖永不出现的 `R_SUPER` 恒 false ④无 directives/无 app.directive/`useAuth().hasAuth` 零调用/页面 NButton 全裸露；**后端事实**=无 menu 表无 permission 表 `system:` 前缀 0 命中 `/route/getUserRoutes` 是模板死代码 `user.roleType` 由 lib/services/role.js:77 注入(ADMIN=100) `auth.js:22` 用 accountType==1 直通；**决策**=静态路由+meta.permission 过滤 / 统一 `system:xxx` 码(读取只认带 `:` 的 key，逻辑废弃数字 id 不迁数据) / v-auth 指令 + useAuth().hasAuth 并行 / 超管 **`roleType===100`**(与后端 ADMIN=100 对齐)||accountType===1 / **后端零改动**；**不改后端如何避免菜单放行接口403**=后端完全不认 5(auth.js:22 只认 accountType、role.js:89 只认 100) ⇒ 「管理员角色权限树强制全选只读」从兜底升级为**必需**使 auths 完整；**RoleType 已对齐 100**(前后端一致)；**关键实现点**=meta 注入复用 `build/plugins/router.ts` 的 `onRouteMetaGen`(与 icon/order 同处，不动 .vue)、父级目录子级裁光后一并隐藏(现有实现留空目录)、未声明 permission 默认放行+dev 告警(防漏配致菜单消失且保护 VITE_ROUTE_HOME=home)；**设计已落盘**=spec `docs/superpowers/specs/2026-09-18-permission-control-design.md` 已实现（typecheck+lint+build:test 通过，meta.permission 已注入生成的 routes.ts）](./changelog/菜单与按钮权限控制.md)

- [公共上传组件类型限制（用户贴「微信二维码」截图要求上传只能选指定格式；**关键定位**=仓库有**两套**上传实现——`components/Upload` 公共组件(只校验大小不校验格式)与 `Form/index.vue` 内联 NUpload(`image`/`file` 类型，连 accept 都没设、截图字段走这套)⇒ 只改公共组件修不了截图；**澄清4问全采纳推荐**=限制逻辑内聚公共组件内部(不在调用处传 accept) / 所有上传入口复用公共 Upload 组件(FormWrap image/file 也改用语删内联 NUpload) / MIME+后缀双重校验 / 复用后 image/file 由「只存文件名不上传」变「真实上传回写 URL」(用户确认更正确)；**设计已批准**=spec `docs/superpowers/specs/2026-09-18-upload-type-restrict-design.md`；**实施计划**=待 `writing-plans` 生成；**不做**=不动后端/路由/菜单/refId 附件模式；**验证**=typecheck/lint/fmt 全过 + dev 手动选非图片被拦截](./changelog/公共上传组件类型限制.md)

- [合并用户新增/编辑/详情抽屉并改用两张卡片（用户贴编辑抽屉截图要求「个人档案」也用卡片包起来，后拍板「新增和修改都用 `user-operate-drawer.vue`」；**背景**=原两套抽屉：新增/编辑/详情一体化 `user-operate-drawer.vue`（无卡片，含 password/idCard/qrCodeUrl）与只支持编辑的 `user-edit-drawer.vue`（NCard+NTabs，Profile 漏 idCard/qrCodeUrl）；**实施**=`user-operate-drawer.vue` 拆 `formItems` 为 `basicItems`(账号/名称/密码/站点/状态/角色/组别) 与 `profileItems`(真实姓名/性别/生日/身份证/电话/邮箱/微信/其它联系/职位/入职/地址/附件/二维码/备注)、去 section 标题、ref 拆 `basicFormRef`/`profileFormRef` 两个 NFormWrap 共享同一 `model`、模板包两张 NCard(`基本信息`+`个人档案`)、`handleSubmit` Promise.all 校验两张卡片、`:mode` 下传详情只读脱敏、`user/index.vue` 删 UserEditDrawer 导入与 editVisible/editRow 且 handleEdit 改走统一 openDrawer；**删除**=`user-edit-drawer.vue` + `modules/edit/` 下 BasicInfo/Profile/Perm(配置权限 tab)/Account(账号配置 tab) 四个组件；**不做**=不保留两 tab、不新增抽象、不动后端/路由/i18n；**验证**=typecheck 0 / read_lints 0](./changelog/合并用户新增编辑抽屉.md)

- [封装国家选择公共组件（用户贴 FBA 仓库抽屉截图要求国家选择公共组件、下拉显示二字码；**澄清4问全采纳推荐**=组合式 Hook `useCountrySelect` / 名称左对齐+二字码右对齐(naive renderLabel) / 一并替换两处(FBA仓库+客户地址抽屉) / 模块级单例缓存只请求一次 `/country/query`；**设计已批准**=spec `docs/superpowers/specs/2026-09-18-country-select-design.md`；**现状**=两处各自重复调 `/country/query` 映射选项、下拉只显示名称；**实施(已交付 2026-09-18)**=新增 `src/hooks/business/use-country-select.ts`(导出 options/loading/load()/getName(id)，cachePromise 缓存，选项 label=`${nameCn} (${code2})` 兼过滤兜底、value=\_id、renderLabel flex 右对齐 code2，名称优先级 nameCn??name??nameEn??code??\_id) + FbaWarehouseOperateDrawer 删本地 countryOptions/NameMap 改用 Hook 且提交 `country:getName(id)` + address-operate-drawer 删本地加载统一到 data-manage-basic 同源且 watch 回填改用 getName；**坑修复(用户截图复验)**=naive 自定义 option label 是 select 级 prop `render-label` 非 option 字段 ⇒ FormWrap select 加 `:render-label="item.renderLabel ?? fallbackOptionLabel"` 透传(FormItemConfig 新增 renderLabel 配置、fallback 兜底识别 option.renderLabel)，调用方零改动生效；**坑2修复(用户截图二轮复验)**=render-label 节点被 naive 包在 `.n-base-select-option__content` 内且该容器默认不撑满整行 ⇒ global.css 兜底 `.n-base-select-option__content{flex:1 1 auto;width:100%;min-width:0}` 使 justify-between 有空间(纯文本选项视觉无差异)；**坑3修复(截图三轮复验,playwright 实测定位)**=对称失效真因：单选下拉 li padding 实为 `0 32px 0 12px`(naive 为选中行✓预留 20px 右内边距经 `--show-checkmark` 动态注入) + rail 宽 5px 且距菜单右缘内缩 4px ⇒ 修复=行加 `country-option-row` class、global.css 用 `:has(.country-option-row)` 统一右内边距 12px(!important 压动态注入)并隐藏✓、行补 `pr-9px`(=5px rail+4px 内缩)，实测左右留白均 12px 对称、各行二字码同列；**改 FormWrap 仅此一处透传 / 不新增 CountrySelect 组件 / 不做英文过滤**；**验证**=typecheck/lint/fmt/build 全过(lint 仅 link.vue 2 既有 warning)](./changelog/封装国家选择公共组件.md)

- [表格封装搜索栏改为侧滑抽屉（用户："表格组件封装的搜索栏需要改成侧滑的"；**定位**=`Table` 的 `searchItems` 内联折叠 `SearchBar`（`NCard`+`NFormWrap`，grid 行高收展）挤压表格高度、需二次点击；**决策（brainstorming 默认）**=右侧滑出 / 点搜索自动关闭 / 抽屉内纵向堆叠；**实施**=`SearchBar.vue` 整重写复用封装 `CommonDrawer`(`@/components/common/drawer.vue`，即 NDrawer+NDrawerContent，`width=420`,`title=$t('common.search')`)+`NFormWrap`(默认插槽，`#footer` 重置/搜索按钮，`v-model:show`+emit `search`/`reset`/`update:show`，派生 `drawerItems` 把每项 `span` 收敛 24 强制单列，不再裸写 NDrawer 与全站 OperateDrawer 一致) + `Table.vue` 删属性 `searchDefaultCollapsed`/`ref searchCollapsed`(无调用方使用)、新增 `searchDrawerVisible`+`handleSearch`(emit search 并关)/`handleReset`(emit reset 保持开，定义在 emit 之后避 no-use-before-define)、删模板内联折叠块、搜索图标按钮改 `@click="searchDrawerVisible=true"`、根容器前渲染 `<SearchBar v-model:show ...>`；**契约不变**=`searchItems`/`searchModel`/`@search`/`@reset` 不动 ⇒ 23 个调用页零改动，`search-action` 插槽/`searchActionFlush` 不受影响；按钮 title 沿用 `common.search`（无 `common.filter`）；**验证**=typecheck 0 / lint 0 error(link.vue 2 既有 warning) / build:test 成功；手动冒烟待用户 `pnpm dev` 确认](./changelog/表格搜索栏改侧滑抽屉.md)

- [追踪网络「轨迹信息改造」（用户贴两张截图：追踪网络列表操作列改平铺三按钮 编辑/轨迹信息改造/删除，点开弹窗按网络配置字符串替换规则 原始字符串→替换字符串，+添加一行/行内删除/保存；**后端零改动**=tms-user 已有 /track-replace/batch/save(query|get)，模型 track-replace(configId/oriStr/replaceStr)，batchSaveCommon 按 \_id 差集增删改；**澄清4问**=三按钮平铺 / 单元格始终可编辑+末尾统一保存 / 增删改仅本地保存时整表提交 / 两字段可空保存过滤空行；**实施**=typings 新增 TraceReplaceItem/List/BatchSaveParams + service/api/track-replace（fetchGetTrackReplaceList size 拉满 & fetchBatchSaveTrackReplace，真实请求无 DEV mock 解包 {data,error}）+ TrackNetworkTable 操作列三按钮 + 新增 TrackReplaceModal.vue（NModal 只读网络名 + naive NDataTable 改造规则表 行内 NInput 可编辑/行内删除/+添加一行/取消保存，watch(show) 拉规则 保存过滤空行整表提交）+ i18n 三处 traceInfoTransform/transformRules/col.oriStr/col.replaceStr/addRuleRow（save 复用 common.save）；**不做**=其余4 tab / 后端 / 路由菜单；**验证**=typecheck/lint/fmt/build 待跑](./changelog/追踪网络轨迹信息改造.md)

- [资料管理各模块列表与表单拆文件（用户拍板"资料管理下面的模块都要这样做，不要全部都写在一起"=方案 B；**设计已批准**=spec `docs/superpowers/specs/2026-09-18-data-manage-split-forms-design.md`；**范围**=拆 16 个自包含 tab（basic 2 / no-rule 4 / ship 4 / bl 6）的新增+编辑为 `<Tab>Form.vue`（内部自带 Drawer 外壳），business 8 + finance 4 走公共 MasterDataArchive 已拆**不动**；**命名**=`<Tab>Form.vue` 合一（禁用 index.vue 防 elegant 误扫路由）、类型复用 `typings/api/data-manage-*.d.ts` 不硬造 `.model.ts`；**数据流**=列表 `v-model:show`+`:mode`+`:row`+`@submitted="getData"` 挂 `<XxxForm>`，表单 `watch(show)` 回填 + `onMounted` 自取下拉 + 提交成功 `emit('submitted')`；**已知父→子迁移下拉**=FbaWarehouseForm 国家（/country/query）、CarrierForm 计泡/追踪网络；**分批**=basic→no-rule→ship→bl 每批 typecheck/lint/fmt/build 绿灯；**实施计划**=`docs/superpowers/plans/2026-09-18-data-manage-split-forms.md`（16 task）；**未改代码/路由/菜单/权限/i18n 结构**](./docs/superpowers/specs/2026-09-18-data-manage-split-forms-design.md)

- [新增/编辑表单未独立成文件（用户贴「通用资料 → FBA仓库」列表截图问"为什么没有把新增文件独立出来"；**定位**=通用资料两个 tab 页把新增+编辑抽屉内联在页面里——提交 `61eb47c`（09-18 按脑图重构）把 `CountryRegion.vue`/`FbaWarehouse.vue` 从 ~73 行 `MasterDataArchive` 壳重写为 295/343 行自包含页（Table+Drawer+NFormWrap+formItems 同文件），同批 `bl`6/`ship`4/`no-rule`3 同款形态；**根因**=那轮按"一 tab 一自包含页面"最短路径写，未遵守 2026-09-10 确立的"列表页与表单/编辑界面必须拆独立组件文件、类型放 `.model.ts`"原则；**方案（待用户选）**=A 只拆通用 2 个 / B 统一拆 15 个 / C 维持；A/B 共同注意=FBA 抽屉依赖父页面 `countryOptions`/`countryNameMap`（改 props 或子组件自取 `/country/query`）、新文件名禁用 `index.vue`（elegant-router 会扫成路由）；后续用户选 B（全部拆），见上方设计条目；**未改任何代码**](./changelog/新增文件未独立出来.md)

- [费用归类列标签渲染与提交阻塞修复（用户："为什么提交不了呢？typecheck错误"；**定位**=`pnpm typecheck` 实际 0 错误，真凶是 `pnpm lint` 的 error：`ExpenseType.vue` 的 `scopeLabel` 定义了未使用（no-unused-vars）；**根因**=ExpenseType 走 `MasterDataArchive` 包装组件，而该组件只固定渲染 operation-left/operation-right/action 三个插槽、不透传业务插槽给内部 Table ⇒ 自定义单元格插槽无处可用（WeightRule 等直接写 Table 的模块不受影响）；**实施**=MasterDataArchive 用 `useSlots()` + `forwardedSlots`（排除三个 reserved 插槽）做动态插槽透传 `<template v-for="name in forwardedSlots" #[name]="scope"><slot :name="name" v-bind="scope" /></template>` + ExpenseType 用 `#scope` 插槽渲染 `scopeLabel(row.scope)`；**不做**=不新增列类型/formatter、不动 Table/useVxeTable、不新增 i18n；**验证**=typecheck 0 / lint 0 error（link.vue 2 既有 warning）/ fmt 后 `git diff --exit-code`=0，两文件已 git add](./changelog/费用归类列标签渲染与提交阻塞修复.md)

- [资料管理按脑图调整（用户贴三张脑图"资料管理按这个来"；**差距分析**=脑图6组 vs 现状5组：基础资料→通用资料、业务资料→运单资料+地址簿拆收/发、财务费用类型按 scope 拆三分类、新增提单资料整组（bl-route/bl-port/bl-trip/bl-address 9类/bl-unit/track-config）、单号资料补 item-no-rule/no-pool/long-no-rule、发货服务商按 providerType 拆三类；**后端事实**=providerType/scope/addressType 字段均已存在可前端过滤，唯 weight-rule 无"按公斤/按方"字段待确认；**状态**=设计方案待用户确认，4 个待确认问题见文档，未写代码](./changelog/资料管理按脑图调整.md)

- [API配置去掉保存按钮改为即改即存（用户贴详情「API配置」截图红框圈两个「保存」按钮"去掉，修改字段保存的"；**实施**=`api-tab.vue` 删两卡片「保存」NButton，webStatus/apiStatus 下拉改 `@update:value` 直接触发 `handleSave`（v-model 先写回再触发，语义不变），原 saving loading 转为下拉 `:disabled` 防重复触发；顺带删仅此二按钮使用的 `customer.detail.save` i18n key（zh/en/app.d.ts 三处同步）；**验证**=read_lints 0 / typecheck 0](./changelog/API配置去掉保存按钮改为即改即存.md)

- [编辑态行高与详情反馈区说明（用户贴编辑抽屉截图"为什么编辑的时候行高就这么高"；**定位（零改动）**=FormWrap 编辑态每项底部反馈区 min-height≈24px 无错误也占位，行高≈80px 属正常结构；上轮已还原 ⇒ 详情（view 态）现在同样显示反馈区，编辑/详情行高一致；用户确认"详情也要显示反馈区"=维持现状，无代码改动；全库仅 label-designer 两处预存 show-feedback=false 与此无关](./changelog/编辑态行高与详情反馈区说明.md)

- [客户详情行高还原默认尺寸（用户贴详情「业务与财税」tab 截图"为什么还是这么紧凑"；**定位**=biz-tab 与基本信息卡片都是 `NDescriptions size="small"`，不走 FormWrap，与先前还原的 FormWrap 反馈区无关，紧凑根因是 small 尺寸行高最小；**决策（用户确认）**=两处都去掉 `size="small"` 恢复 naive 默认 medium；**实施**=`customer-detail-drawer.vue` + `customer-detail/biz-tab.vue` 各删一个属性；**验证**=read_lints 0](./changelog/客户详情行高还原默认尺寸.md)

- [还原详情态隐藏表单反馈区（用户贴客户详情截图："之前说的详情就不显示错误信息的位置这个得还原回来"；**还原对象**=同日早前的「详情态隐藏表单反馈区」改动；**实施**=仅 `src/components/Form/index.vue` 两处：NFormItem 移除 `:show-feedback="!isView"`（view 态恢复渲染反馈区占位）、NGrid 移除 `:y-gap="isView ? 10 : 0"`（行距由反馈区自然撑开）；其余 view 态只读渲染/空校验/labelPlacement 派生不动；**验证**=typecheck 0 / read_lints 0](./changelog/还原详情态隐藏表单反馈区.md)

- [客户详情页（用户贴客户列表红框圈「客户编码」+ 老系统详情页整页截图："点击弹出这个界面，需要新增一个这个界面"；**需求演进**=首版设计误读 tab 为"邮件地址/邮件管理"，用户补 5 张截图纠正为 业务与财税/收件地址/发件地址/附件管理/API配置/操作日志 六 tab 且 API配置有真实内容（网站下单+API下单）；**二次纠正（历史教训复现）**=独立路由页做完后用户"是弹窗，不是单独的路由界面"——与用户编辑抽屉同款，**详情/编辑类界面默认形态=侧滑弹窗，做路由页前必须先问**；**用户拍板**=写操作做 地址增删改（导入不做）+ 附件上传/删除 + API配置可编辑，网站密码按钮=「重置密码」+popconfirm（后端仅重置为 pwd123456）；**后端事实（零改动）**=/customer/get 全量、/ship-to|shipper/\* CRUD（delete 是 `{ids[]}`、query keyword 必须前端传 keywordFields=[name,phone,zip] 否则 queryCommon 短路不生效）、/upload/files?refId= 批量入库+/file/query（queryAllCommon 无 total→本地分页）+/file/delete 单 \_id、webStatus/apiStatus/appToken/appKey 为 Customer 自有字段经 /customer/update 局部更新（fillUser/fillSite 对空值安全）、/op-log/query keyword 只匹配 refNames→传客户名称；**实施终版（弹窗）**=customer/modules/customer-detail-drawer.vue（公共 Drawer width=80% footer=false，watch show 拉 /customer/get，NDescriptions+NTabs 六 pane 懒加载）+ 列表编码 @detail 置 detailId 开弹窗 + tab 模块移入 customer/modules/customer-detail/（biz-tab 只读+billMode 派生结算周期/结算日期/关联运单状态、address-tab 收/发共用 type 切换+address-operate-drawer 必填对齐后端 validate、file-tab NUpload 批量一次提交、api-tab 双卡片掩码+copyText、oplog-tab）+ 路由页方案全量回退（删目录/撤 hideInMenu/撤 route i18n key/gen-route 重生成）+ 新 service ship-address（含 /country/query 下拉）/customer-file + customer 追加 fetchResetCustomerPassword/fetchUpdateCustomerPartial（局部更新绕开 CustomerUpdateParams 全量必填）+ i18n customer.detail 子块 60+ key（zh/en，app.d.ts Schema 同步）+ Customer 类型补 webStatus/apiStatus/appToken/appKey 与 CustomerAddress/CustomerFileItem/CountryItem 类型族；**坑**=弹窗内表格需 min-h 撑高（h-full 塌陷）、/file/query 无 total 使 useVxeTable 泛型须内联 {list,total}、target=\_blank 必须 noopener noreferrer（lint error）、i18n 同步 app.d.ts 第四次复现；**验证**=typecheck 0 / lint 0 error（link.vue 2 既有 warning）/ fmt / elegant 无路由残留；未跑 playwright](./changelog/客户详情页.md)

- [组别管理关联用户/客户（用户："组别管理列表新增关联用户、关联客户"；**后端事实（零改动）**=用户组别字段为 `groupIds:[String]` 数组（models/user.js，group.js get 用 `userService.count({groupIds})` 证实）⇒ /user/query 用 `where:{groupIds}` 数组成员匹配；客户组别为 `groupId:String` 单值（models/customer.js）⇒ /customer/query 用 `where:{groupId}` 精确过滤；**实施**=group service 新增 fetchGetGroupUserList/fetchGetGroupCustomerList（flat 解包 {data,error}，客户复用 SiteCustomerList 类型）+ group/index.vue 所属站点后新增两列（查看按钮插槽）+ 挂两个 RelationModal 实例（用户列=姓名/账号/所属站点/状态、客户列=编码/名称/所属站点/状态，先探查 size:1 空则 notification 提示不弹窗）+ cacheKey 升 `system-manage-group-v3` + i18n 三处 `page.manage.group.{relationUser,relationCustomer}`（同步 app.d.ts Schema）+ UserSearchParams.where 补 groupIds 声明；**验证**=typecheck 0 / lint 0 error（link.vue 2 既有 warning）；未跑 playwright](./changelog/组别管理关联用户客户.md)

- [角色列表关联用户（用户："角色列表加个关联用户"；**后端事实（零改动）**=tms-user `/user/query` queryCommon 的 where 原样透传，`where:{roleIds:'<roleId>'}` 走 MongoDB 数组成员匹配；**实施**=role service 新增 fetchGetRoleUserList（flat 解包 {data,error}）+ role/index.vue 角色类型后新增 relationUser 列（查看按钮插槽）+ RelationModal 复用（先探查 size:1 空则 notification 提示不弹窗，列=姓名/账号/所属站点/状态）+ cacheKey 升 `system-manage-role-v3` + i18n 三处 `page.manage.role.relationUser`（关联用户/Related Users，同步 app.d.ts Schema）+ UserSearchParams.where 补 roleIds 声明；**验证**=typecheck 0 / lint 0 error（link.vue 2 既有 warning）；未跑 playwright](./changelog/角色列表关联用户.md)

- [页签hover效果消失排查（用户"hover效果又没了"强刷无效；**真正根因**=`node_modules/@sa/materials` 的 workspace Junction 软链丢失，变成 9/13 的 v0.0.4 真实旧目录，应用一直加载旧拷贝——这几天对 `packages/materials` 的修改从未生效；**定位过程**=playwright eval 发现 DOM 类名 hash `_gmu1w_` 与直接抓 `/tms/packages/...` 的 `_wmyri_` 对不上、document.styleSheets 里只有 button/chrome 的 svg-close:hover 没有 slider 的 ⇒ 同一 CSS module 两份编译产物必有路径分叉 ⇒ 资源 URL 指向 node_modules；**修复**=杀 dev server → `pnpm i` 恢复 Junction → 重启 → playwright 端到端复验 hover 背景变主题蓝图标变白；**教训**="改了没生效"必须验证应用实际解析的模块路径（LinkType=Junction+比对两份 package.json version），同名 CSS module 出现两个 hash=路径分叉信号](./changelog/页签hover效果消失排查.md)

- [客户列表接口对接（用户："客户列表的接口帮我对接上"+贴老系统「新增客户」截图"新增客户按这个"；**后端事实（零改动）**=tms-user `/customer/query`（`{page,size,where,keyword,scene}`→`{list,total}`，scene=1 管理列表否则强制 status=1，keyword 匹配 code/name/account）、create（name/account 唯一、code 不传 fillCode 自动生成）、update（需 \_id）、get；**无 delete 接口**⇒删除/批量删除按钮移除（后经用户拍板改为保留禁用+tooltip「删除功能暂未开放」，勾选状态恢复保留）；字典=/customer-level/query、/customer-source/query、/bill-mode/query；**实施**=typings Customer 类型族重写为后端真实字段（\_id/code/name/account/contact/mobile/level/levelId/source/sourceId/updateDate 毫秒…）+ service 去 mock 直连（flat 解包 {data,error}）新增 level/source/billMode 三字典接口 + mock.ts 删 customer 段 + 列表页 scene:1+keyword/levelId/sourceId/status 搜索+列对齐后端+cacheKey 升 customer-manage-customer-v2+字典下拉 onMounted 拉取 + 抽屉按截图三段式重做（基本信息 3 列 code/name*/account*/…tag/taxInfo/note、结算设置 billModeId/withhold/creditLimit/billPerson/contractRange daterange 插槽/billGenMode、专属经办 siteId/groupId/salesman/service/picker/cashier，结算周期/结算日期/关联运单状态为所选 billMode 的派生只读展示不提交，首次打开并行拉 6 组字典）+ i18n 三处 customer 段重写（删 levelNormal 等枚举 key 增 44 key）+ app.d.ts Schema 同步；**坑**=i18n key 必须同步 app.d.ts Schema 否则 TS2345（既有结论复现）、selectedBillMode 须声明在 model 后（no-use-before-define）、NDatePicker daterange 时间戳 v-model 不可传 value-format、派生展示字段提交前剔除；**验证**=typecheck 0 / lint 0 error（link.vue 2 既有 warning）/ fmt；未跑 playwright](./changelog/客户列表接口对接.md)

- [角色管理内置角色不允许修改删除（用户："buildIn=1为内置角色不允许修改和删除"；**现状**=后端 Role 模型已有 buildIn（default 0）但 service update 无校验、delete 仅拦 roleType ADMIN（100203 错误码只用于 ADMIN）；**决策（用户确认）**=① update/delete 均拦截抛 ApiError（修改新码 100204"内置角色不可修改"、删除复用 100203）② /role/auths/update 权限分配不拦截 ③ 前端禁用+tooltip（对齐超管权限按钮写法）④ 批量删除自动跳过内置行 ⑤ name 列加「内置」NTag，由 type:detail 改自定义插槽（Table 插槽优先于 type，detail 的 copy 内部状态无法在插槽复用，放弃 copy 图标）；**实施**=后端 1002-role.js 加 EDIT_ERROR + role.js update 加 buildIn 拦截、delete 投影补 buildIn 并前置校验；前端 Role 类型加 buildIn?: RoleCtrl + role/index.vue（#name 插槽 Link+Tag、编辑/删除 :disabled 包 NTooltip span 桥接、handleDelete 改收 Role[] 过滤+warning 跳过提示）+ i18n 三处 4 key（builtIn/builtInEditTip/builtInDeleteTip/builtInBatchDeleteTip，同步 app.d.ts Schema 防vue-tsc TS2353）；**验证**=typecheck 0 / lint 0 error（link.vue 2 既有 warning 无关）；未跑 playwright](./changelog/角色管理内置角色不允许修改删除.md)

## 2026-09-17

- [用户管理加上关联客户（用户："用户管理加上关联客户跟站点一样"；**关联范围（用户拍板）**=调 `/customer/user/query` 接口；**后端事实（零改动）**=与 /customer/query 共用 ctrl.query，body 带顶层 `userId` 时后端按 salesmanId/serviceId/cashierId 的 $or 过滤（GROUP 数据权限角色追加 groupIds），`scene:1` 管理列表含停用客户并走 fillNames 回填站点名，返回 ret:{list,total}；**实施**=user service 新增 fetchGetUserCustomerList（flat 解包 {data,error}，复用 SiteCustomerList 类型）+ user/index.vue 新增 relationCustomer 列（查看按钮插槽）+ RelationModal 复用（openRelationModal 先探查 size:1 空则 notification 提示不弹窗，列=编码/名称/所属站点/状态）+ cacheKey 升 `system-manage-user-v3` + i18n 三处 `page.manage.user.relationCustomer`（关联客户/Related Customers）+ app.d.ts Schema 同步；**坑**=i18n key 必须同步加 app.d.ts Schema 否则 vue-tsc TS2353/TS2345；**验证**=typecheck 0 / lint 0 error（link.vue 2 既有 warning）/ fmt](./changelog/用户管理加上关联客户.md)

- [关联用户/客户为空提示不弹窗（用户贴站点管理列表截图："如果没有关联的用户和客户就提示为空，不要弹窗"；**定位**=site/index.vue `openRelationModal` 点击「查看」无条件打开 RelationModal，无关联数据时弹空表格；**实施**=该函数改 async：先置 relationRow（fetcher 闭包读该 ref）→ 复用原 fetcher 探查 `page:1,size:1`，`!total && list.length===0` 时 `window.$message?.info($t('common.noData'))` 并 return 不弹窗，否则按原逻辑打开弹窗（弹窗打开后自行重拉第一页）；用户/客户两路径共用；**不做**=RelationModal 组件零改动、后端零改动、不新增 i18n key（复用 common.noData）；**验证**=read_lints 0](./changelog/关联用户客户为空时提示为空不弹窗.md)

- [去掉权限管理和菜单管理（用户："去掉权限管理和菜单管理"；**定位**=侧边栏「权限管理」一级菜单（/permission-manage）仅含「菜单管理」一个子项，属同一模块整体移除；路由 static 模式，删 views/permission-manage 目录 + pnpm gen-route 即可，无需动后端；**实施**=删 views/permission-manage + service/api/menu（含 index.ts 桶导出与 mock.ts 的 mockMenu\*/createMenu/menus 族）+ typings Menu 类型族 + app.d.ts page.manage.menu Schema + i18n 三处 + menu-permissions.ts「权限管理→菜单管理」分组与 menu op 变量 + build/plugins/router.ts routeIcons/routeOrders 两键 + role-permission-drawer.vue 列标题借用 key 改纯文本「菜单名称」+ pnpm gen-route 重生成 elegant；**保留**=角色管理与用户管理分配角色；**坑**=① Remove-Item 被安全删除机制（genie-trash）拦截改用文件级删除 ② sa gen-route 输出交互提示但实际已重生成；**验证**=typecheck 0 / lint 0 error（link.vue 2 既有 warning）/ fmt](./changelog/去掉权限管理和菜单管理.md)

- [角色管理超级管理员不允许分配权限（用户："角色管理，超级管理员不允许分配权限"；**定位**=系统「超级管理员」是用户级 accountType==1（登录后整体跳过权限校验，不走角色 auths），Role 模型无超管标记、roleType=5 即「管理员」角色类型，角色管理页操作列「权限」按钮对所有角色开放；**两项决策（用户确认）**=① 识别对象=roleType=5 的角色行 ② 仅前端禁用（tooltip 提示），后端 /role/auths/update 不拦截；**实施**=role/index.vue 操作列「权限」按钮 `:disabled="row.roleType===5"` + 外包 `NTooltip :disabled="row.roleType!==5"`（disabled 按钮不派发鼠标事件 ⇒ trigger 套 `<span>` 才能悬停出提示）+ i18n 三处新增 `page.manage.role.permissionDisabledTip`（超级管理员不允许分配权限 / Super admin roles cannot be assigned permissions）+ app.d.ts Schema 同步；抽屉与后端零改动；**验证**=typecheck 0 / lint 0 error](./changelog/超级管理员不允许分配权限.md)

- [用户编辑抽屉（用户贴参考系统截图"用户编辑新加个文件，按照这个"：基本信息卡片 + 顶部 tabs；**五项决策（用户确认）**=① 编辑打开侧滑抽屉（新增/详情仍走现有抽屉；先做成独立路由页后被用户纠正"不是单独的界面是侧滑弹窗"，已撤销路由页/路由插件/i18n route key 并重生成路由）② 个人档案可编辑（"不编辑放这里干嘛"）③ 配置权限 tab 留空占位 ④ 账号配置 tab 留空占位——截图的登录控制/通知配置字段 tms-user UserSchema 均不存在（仅 openId，strict 丢弃）⑤ 关联客户 tab 去掉（能力在：/customer/query 支持 userId 过滤）；**二次纠正**=用户贴实拍"做成这样是不想让我修改吗？"⇒ 基本信息从只读 NDescriptions 改 FormWrap 可编辑表单（account/name 必填、roleIds/groupIds 多选 custom、siteId 必填、status；「创建/最后更新」保持只读 custom 格）；**实施**=`user/modules/user-edit-drawer.vue`（UserDrawer width 70% + 打开即 /user/get + loadOptions 刷下拉 + 底部 footer 统一保存：先 collect 判空再 Promise.all 校验 BasicInfo/Profile 两表单，一次 fetchUpdateUser 全量提交，成功关抽屉+emit submitted 刷列表）+ `modules/edit/{BasicInfo,Profile,Perm,Account}.vue`（Profile 去内部保存改 expose validate/collect）+ i18n 三处 page.manage.user.editPage（creator/update/tabPerm/tabAccount）+ app.d.ts Schema；**坑**=① profileRef 在 v-for NTabPane 内变数组→显式三 pane ② 档案 tab display-directive="show" 保留实例（切 tab 后仍可校验提交）③ `...ref?.collect()` 展开字段变可选→先判空再展开；**验证**=typecheck 0 / lint 0 error（link.vue 2 条既有 warning）/ fmt / read_lints 0；未跑 playwright](./changelog/用户编辑页面.md)

- [站点管理关联用户/客户查看（用户："站点管理表格新增两个字段，关联用户、关联客户，显示查看两个字然后点击查询关联的用户和客户"；**五项决策（用户确认）**=① 展示形式 Modal 弹窗 ② 用户列=姓名/账号/所属站点/状态 ③ 客户列=编码/名称/所属站点/状态 ④ 弹层无搜索纯查看 ⑤ 追问"能不能封装一个组件"⇒ 封装**公共组件** `components/common/relation-modal.vue`（RelationModal=NModal+通用 Table+useVxeTable，Props=show/title/fetcher/columns/width/bodyHeight，不感知 siteId、过滤条件由调用方闭包绑定，immediate:false 每次打开回第一页重取）；**后端事实（零改动）**=User.siteId 与 Customer.siteId 均原生支持，`/user/query` 与 `/customer/query`（queryCommon）都吃 `where.siteId`，Customer 模型冗余 `site` 站点名称字段；**实施**=site service 新增 fetchGetSiteUserList/fetchGetSiteCustomerList（flat 解包 {data,error}）+ typings 新增 SiteCustomer/SiteCustomerList（与模板 mock 的 Customer 严格区分）+ site/index.vue 表格新增两列（查看按钮插槽）+ 挂两个 RelationModal 实例（列静态、fetcher 读 relationRow ref）+ cacheKey 升 `system-manage-site-v3` + i18n 三处 site 段 6 key（用户列标题复用 page.manage.user.{nickName,userName,status}，注意本仓 userName=账号/nickName=名称）；**坑**=弹窗 fetcher 解构 data 与主表格 useVxeTable 的 data 触发 oxlint no-shadow 2 处→改 `data: res`；组件 columns 仅 setup 求值一次⇒单实例切列不可行、改挂两实例；**验证**=typecheck 0 / lint 0 error（link.vue 2 条既有 warning 无关）/ fmt；未跑 playwright（需真实后端数据）](./changelog/站点管理关联用户客户查看.md)

- [抓取时间列表接 /op-log/query（用户贴 tms-user `OpLogSchema`（opType 注释扩 4-追踪）与请求示例："抓取时间列表调这个接口：/op-log/query，opType 默认传 4"；**定位**=抓取时间 tab 主表格此前 DEV mock / PROD 调占位假接口 `/system/capture-time/list`（后端无此接口），`fetchGetOpLogList`（flat 解包 `{data,error}`→ret:{list,total}）已有封装可复用；**四项决策（用户确认）**=① 操作日志列映射 name ② 不加搜索栏（YAGNI）③ 清理旧 mock 链路（删 fetchGetCaptureTimeRecordList + captureTimeRecords + timeStampOf + CaptureTimeRecord 类型族，保留 /track-schedule 配置接口）④ 系统日志页不加追踪筛选；**实施**=typings `OpLogOpType` 扩 `4` + 删旧类型族 + CaptureTimeTable.vue 改调 `fetchGetOpLogList({page,size,where:{opType:4}})`（列 key 换 OpLog 字段 name=detail/client NTag/opType NTag(4-追踪 warning)/creator/createDate，cacheKey 升 `trace-capture-time-v2`）+ i18n 三处 `opTypeOption.trace`（追踪/Trace）+ log/index.vue `opTypeTagType` map 补第 5 位 'warning'（OpLogOpType 扩 4 后 tuple 索引 TS2493 连锁修复，本页行为不变）；**验证**=typecheck 0 / lint 0 error（link.vue 2 条既有 warning 无关）；未跑 playwright（需真实后端产生 opType=4 追踪日志）](./changelog/抓取时间列表调op-log接口.md)

- [追踪网络系统类型对接字段配置（用户给各系统对接字段清单+新增追踪网络截图："框选的这几个是默认有的，其他的根据系统类型显示不同名称，配置到配置文件里"，随后拍板"直接把后端那个复制一份到前端"；**后端事实**=`lib/ext-api/api/` 15 适配器（trackType=文件名）实际取用字段：kdzs/hl/ydd=账号+密码、sd速递管家=appToken+appKey、k5=Clientid+Token、kjv5=appKey+appSecret、ry=plantId+plantKey+bankerId、xzh=token、t6/sckj顺仓=客户编码+API授权码、track17=17token、feia17=apiName+apiToken、nm=icID+key、qy 秦远无字段、ups/fedex 无适配器（模型注释 14 码与适配器目录不一致）；**四项决策（用户确认）**=① 映射以后端适配器为准复制到前端 ② 钮门例外按用户清单只显示网址 ③ 动态对接字段全部必填 ④ 选项加顺仓/17FEIA/秦远、sd 改名速递管家；**实施**=`constants/track-config.ts` 重写（TRACK_TYPE_CONFIGS 17 项 + trackTypeFields()，TRACK_TYPE_OPTIONS/trackTypeLabel 改派生，列表页零改动）+ TrackNetworkDrawer formItems=固定 4 项（name/trackType/url/web）+ 按 trackType 动态生成必填对接字段（label 用配置参数名、切类型只改显隐不清值、channel 不再展示、model/回显/提交不变）+ i18n 三处（固定项文案对齐截图 网络名称/服务器地址/官网查询地址、删 account~accountNoPlaceholder 10 key、加 fieldPlaceholder）；**同日调整**=用户"系统类型切换重置表单校验，并且动态字段要清空"⇒ 加 model.trackType sync watch（变化即清 account/password/key/accountNo + restoreValidation，flush:sync 保证编辑回显先清空再由 fillFormByRow 回填）；**同日调整2**=用户贴表头截图"表头按这个显示"⇒ TrackNetworkTable 列改 name/url/trackType/updateBy/updateDate（url 移到系统类型前、删账号/渠道两列）+ i18n 三处 col.url 改「服务器地址/Server URL」删 col.account/col.channel + cacheKey 升 v3；**验证**=typecheck 0 / read_lints 0；未跑 playwright](./changelog/追踪网络系统类型对接字段配置.md)

- [追踪网络接真实接口（用户贴 tms-user `TrackConfigSchema`："接一下追踪网络的接口"；**定位**=界面本就有（系统设置→轨迹抓取→追踪网络 TrackNetworkTable/Drawer），一直走 DEV mock 假字段 serverAddress/systemType，用户明确"没让你改界面，界面本来就有"⇒ 换数据源非建页面；**后端事实**=`/track-config/{create,update,delete,query,get}` queryCommon 真分页 `{page,size,keyword}`→`{list,total}`、name 唯一（重复报 102701）、delete 仅单 `_id`、模型 trackType 实为 String（用户贴 Number 有出入）、queryCommon keyword 须配 keywordFields；**四项决策（用户确认）**=① trackType 以 String 14 码为准 ② 不新建页面 ③ 承运网络 trackConfigId 顺带接上 ④ keyword 由后端加一行 `keywordFields:['name']`（与 provider 一致）；**实施**=后端 services/track-config.js 1 行 + 前端新建 `service/api/track-config`（4 函数无 DEV mock，flat 解包 `{data,error}`）+ `constants/track-config.ts`（TRACK_TYPE_OPTIONS 14 码+trackTypeLabel）+ 删 `service/api/trace-config`（桶文件去悬空导出）+ TraceConfig\* 类型族改造为模型形状（`_id/name/trackType/url/account/password/key/web/channel/accountNo/creator/updateBy/createDate/updateDate`，`{page,size,keyword}`→`{list,total}`）+ TrackNetworkTable 列对齐 7 列（trackType 插槽映射显示名、updateDate 格式化）+ 补 keyword 搜索框 + 补单行删除 + cacheKey 升 `trace-config-track-network-v2` + Drawer 表单 9 字段（name 必填+trackType 下拉必选+7 可选）提交解包 `{error}`、删无用 category prop + i18n 三处（col/form 增删、carrier.trackConfig）+ Carrier.vue 接 trackConfigOptions(size 500) 下拉+名称列；**并行咬合**=轨迹关键词页预接的 `fetchGetTrackConfigList({})`（res.list/item.\_id）与本 service 签名吻合直接接通、mock.ts traceConfig 块与桶文件 './track-config' 导出为并行会话预备/移除，确认无残留；**验证**=typecheck 0 / lint 0 error（仅剩 link.vue 2 条既有 warning）；未跑 playwright（需 /track-config 在线，keyword 与 102701 重名提示建议实测）](./changelog/追踪网络接口对接.md)

- [轨迹抓取三个 tab 对接接口（用户贴系统设置「追踪网络」截图、红框圈异常轨迹/轨迹关键词/抓取时间："这三个界面接口也接一下"；**后端事实（tms-user 源码）**=异常轨迹 `/track-err-config/*`（name/timeType 0-年月日1-时分2-时分秒/place/desc/detectDesc，query=queryAllCommon **全量 `{list}` 无 total**）、轨迹关键词 `/track-status-config/*`（name/common 0-1 全局通用/configIds[] 关联追踪网络/detectEvents[] 关键词/orderStatus 50-转运中60-已送达70-异常件80-已退件，query=queryCommon `{page,size,keyword,where}`→`{list,total}`）、抓取时间 `/track-schedule/{save,get}` 单文档 upsert `exeTimes` 0-23 数字数组（creator/updateBy 由 processData 中间件注入），delete 均仅单 `_id`；**三项决策（用户确认）**=① 抓取执行记录表后端无接口→保留 mock 现状，只接「抓取时间设置」弹窗 ② 轨迹关键词表单完全对齐后端（名称+全局通用开关+关联追踪网络多选+关键词+运单状态，去 enabled/scope）③ 只接红框三个 tab、追踪网络不动；**实施**=typings 保留类型名、字段对齐后端（TraceTransformItem/TraceKeywordItem/CaptureTimeConfig={exeTimes,number[]}|null，TraceTransformList={list}）+ service 三目录去 DEV mock 直连（flat request 解包 `{data,error}`）+ mock.ts 删 10 个孤儿 mock（保留执行记录）+ 表格列/抽屉表单按后端字段重排（关键词表格挂载拉 `/track-config/query` 全量做 configIds 名称映射+抽屉多选共用、detectEvents 逗号串↔数组互转）+ CaptureTimeModal 接 `/track-schedule/{get,save}`（exeTimes 数字↔"00"-"23" 字符串行）+ cacheKey 升 `trace-transform-v2`/`trace-keyword-v2` + i18n 三处增删（col.common/trackNetworks、form.common/configIds/configIdsPlaceholder；删 scopeOption 块、col/form 的 scope/enabled）；**并行冲突（重要）**=实施期间另一会话正在同一页面对接追踪网络：接管我新建的 `service/api/track-config/index.ts` 改为完整 CRUD、`TraceConfig*` 类型重写为后端模型、清空 `service/api/trace-config/` 目录、新增 `constants/track-config.ts`、覆盖了我在 en-us form 块的改动（已重新套用）；已适配=关键词数据源改调并行流 `fetchGetTrackConfigList({})`/复用 `TraceConfigList`，删除我重复的 TrackConfigItem/TrackConfigList 类型与孤儿化的 trace-config mock；**验证**=typecheck 本次改动文件 0 error（剩余 error 全部来自并行流进行中文件：constants/track-config.ts 的 Option.CommonOption 误用、index.ts 悬空 export './trace-config'、TrackNetwork Drawer/Table 引用未添加的 i18n key，均未触碰）/ oxlint 19 文件 0/0 / eslint 0 error；playwright 走查暂缓（TrackNetwork 组件在并行流未完成，待其 typecheck 绿后一并走查）](./changelog/轨迹抓取三个tab对接接口.md)

- [用户操作抽屉密码加密（用户："新增或者修改的时候密码直接加密，加密请看登录那里"；**现状**=`user-operate-drawer.vue` 的 `handleSubmit` 明文提交密码（新增恒传、编辑非空传/留空不改）；**登录约定**=`service/api/auth` 的 `fetchLogin` 用 `md5(md5(plain)+plain)`，工具在 `utils/crypto.ts`；**方案 A（用户确认）**=页面提交处加密，未采纳 B（crypto.ts 抽公共函数并让登录复用，超出范围）/ C（service 层加密，与项目惯例不符）；**实施 2 处**（同文件）=import `md5` + `handleSubmit` 新增分支与编辑非空分支均改为 `md5(md5(model.password)+model.password)`，编辑留空仍不提交密码字段；**验证**=typecheck 0；无 UI/交互变化](./changelog/用户操作抽屉密码加密.md)

- [分配权限回显失败（用户报"分配权限回显失败"；**定位**=`role-permission-drawer.vue` 的 `loadData()` 回显遍历用 `rows.some(...)`，`Array.some` 回调首次返回 truthy 即短路——顶层第一行「首页」(id=100) 因提交时 `withHome()` 恒写入 auths 必然命中 ⇒ some 立即返回 ⇒ 客户管理/渠道报价/资料管理/系统管理/权限管理整棵子树（含按钮、子模块码）不再遍历，勾选全丢只留首页；已排除后端（`/role/query` 全字段含 auths：Role 无 omitFields、`basicFields` 未定义 → `find(filters, undefined)` 不投影）与提交逻辑；**修复（用户确认）**=`walk` 的 `some` 改 `forEach` + 局部 `anyMatched` 标记（不短路），保持「父级随子级命中而勾选」传播语义，按钮/子模块码回显原逻辑不动，仅改该函数约 5 行；**验证**=typecheck 0；手动路径=勾选菜单+按钮+子模块保存后重开抽屉完整回显](./changelog/分配权限回显失败.md)

- [资料管理新增「发货资料」（用户贴思维导图"资料管理新增发货资料，有些可能是其他界面的可以直接移过来"：服务商(发货/派送/提单)/渠道类别/计泡规则(按公斤·按方)/承运网络；**调研**=前端 `src/` 无这 4 块任何现成实现（"移过来"无可移代码），后端 tms-user 四组 CRUD 全齐（`/provider/*` providerType 0-发货1-派送2-提单3-杂支、`/channel-group/*` 无 status、`/weight-rule/*` mode+carryList 嵌套、`/carrier/*` weightRuleId/trackConfigId/remoteGroupId，通用分页 `{page,size,keyword,where}`→`{list,total}`，delete 仅单 `_id`）；**五项决策（用户逐项确认）**=① 资料管理第 5 页 `data-manage/ship`（竖 tab 四项）② 服务商 1 tab + NRadioGroup 切 providerType（杂支不管理）③ 「按公斤/按方」= 计泡规则表单内计算方式字段（后端模型加 `calcMode`，后端唯一改动）④ 字段全量，承运网络关联追踪/偏远因数据源不存在暂不展示、关联计泡规则下拉取本页全量 ⑤ 用户拍板"每个界面按照系统设置那样写"= `index.vue` 壳 + `modules/<tab>/<Tab>.vue` 各自独立实现，不做 config 驱动通用组件；service 直连真实接口 DEV 不 mock（同 export-format 先例），批量删除=前端逐条调单删；**设计文档**=`changelog/发货资料.md`；**实施（已验证）**=① 后端 weight-rule.js 加 `calcMode`(0-按公斤1-按方) ② 前端新增 typings `Api.DataManageShip` + `service/api/data-manage-ship`（16 flat request 函数）+ i18n 三处 + `views/data-manage/ship/`（壳 + provider/channel-group/weight-rule/carrier 四 tab，weight-rule 含 carryList 进位规则动态编辑区、carrier 挂载拉计泡规则全量做名称映射+下拉）+ router.ts icon/order + base-layout contentShowPadding + menu-permissions dataShip 节点，elegant 经 pnpm dev 重生成；typecheck 0 / lint 0 / fmt / build 通过；playwright 真实后端走查：菜单第 5 项 + 4 tab CRUD 全链路（含类型切换过滤、进位规则组编辑、计泡规则名称映射、NPopconfirm 删除）全过、console 0 error、4 条测试数据已清理；**走查发现**=后端 `/channel-group/query` 走 queryAllCommon 全量返回（忽略 keyword/分页，既有行为未改）](./changelog/发货资料.md)

- [资料管理目录拆分对齐系统设置（用户贴 `data-manage/modules/archives` 截图红框："把这个拆开，跟系统设置的文件布局一样"；**决策（确认）**=① 拆掉 `archives` 中间层，basic/business/finance/no-rule 各页自带 `modules/<项>/<Item>.vue`，共享库 `shared.ts`/`types.ts`/`archive-switch.vue`(死代码) 搬进 `data-manage/components/`，对齐 `system-manage/setting` 的 `index.vue + modules/ + components/` 三段式 ② `master-data-archive.vue` 不再当公共组件，每个分组各持一份副本（basic/business/finance 各 1 份；no-rule 页 `NoRule.vue` 本就自包含、不用该组件故不建副本）；**实施**=PowerShell `Move-Item`（Windows `git mv` 报 ENOENT）+ `git add -A` 整目录搬迁（18 档案子文件夹 + NoRule.vue + 3 库文件），`master-data-archive.vue` 复制 3 份并将内部 `./types` 改指向 `components/types`，import 按组改写（`modules/archives/<g>/`→`<g>/modules/`、`modules/master-data-archive.vue`→`<g>/components/MasterDataArchive.vue`、`modules/types`→`components/types`、`modules/shared`→`components/shared`，同步修正 `service/api/data-manage/{mock,index}.ts` 的 types 路径）；**坑（已排）**=① PowerShell `Set-Content` 默认 UTF-16 LE 写坏 7 个文件（4 个 index.vue + 3 份副本），纯 ASCII 的 18 档案文件未受影响 ⇒ 3 副本用 utf16le→utf8 解码无损恢复、no-rule/index.vue 与 3 个 index.vue 用 UTF-8 安全重写（basic 5 项 / finance 4 项 / business 8 项，business drop 旧 waybill）② 误用 `git checkout` 回退了 3 个受跟踪 index.vue 的未提交「指向 views/data-manage/modules」改动（2026-09-17 重构未提交），改为按工作树正确内容重建；**验证**=typecheck 0 / lint 改动范围 0 error（role-permission-drawer 的 allIds 报错与 link.vue 2 warning 为预存、与本次无关）；未新增 index.vue、`routes.ts` 无新增路由；**残留**=finance/modules/tax/ 存在但 finance/index.vue 未列 tax（预存孤儿，未动）](./changelog/资料管理目录拆分对齐系统设置.md)

- [分配权限对接 /role/auths/update（用户问"分配权限界面的接口有调吗？/role/auths/update"；**定位**=抽屉 handleSubmit 原调 `fetchAssignRoleMenu`：DEV 走 `mockAssignRoleMenu`（根本不联后端）、非 DEV 调过时 `/system/role/assignMenu` 且入参 `{roleId,menuIds,buttonCodes,subMenuCodes}` 与后端真实契约不符；后端真实契约=`POST /role/auths/update`、`{_id,auths:string[]}`（validate.strict，roleService.updateAuths→updateOne 原样存，登录 fillAuthMap 把 auths 拍平成 authMap 供 hasAuth 吃）；**决策（用户拍板）**=以"前端定义的码"为准（菜单 id 转字符串如 `505` + 按钮 `system:user:add` + 子模块 `system:setting:basicConfig`），后端原样存、hasAuth 链路自洽，不改后端数字码（12/12-1）；**实施 4 处**=`service/api/role/index.ts` 删 `fetchGetRoleMenuTree` 与 DEV mock 分支、新增 `fetchUpdateRoleAuths({_id,auths})` 直连真实接口（与角色其他接口一致，DEV 也直连）+ `service/api/mock.ts` 删失效的 `mockGetRoleMenuTree`/`mockAssignRoleMenu` 及 MENU_TREE/ALL_MENU_IDS + `role-permission-drawer.vue` 的 `loadData` 按 `props.row.auths` 预勾选回显（递归：菜单 id 或任一后代命中即勾选，父级随子级联动、提交/回显可往返；home 恒勾选）+ `handleSubmit` 把 `checkedIds`/`rowButtonChecks`/`rowSubMenuChecks` 三套状态合并为扁平 `auths`（菜单 id 转 String + 按钮码 + 子模块码，去重）后调 `fetchUpdateRoleAuths` + `typings/system-manage.d.ts` 删失效的 `RoleMenuNode`/`RoleMenuTree`/`RoleAssignMenuParams`；**附带**=清理被外部重构遗留的无用 `allIds` 计算属性（#nav-header 已简化成纯文字）；**验证**=typecheck 0（仅 data-manage/\* 因工作区已删 MasterData 组件导致的既有报错，与本任务无关）/ eslint 该文件 0 / 其余 service·typings 走 typecheck；未跑 playwright 实测（需真实后端 /role/auths/update 在线）](./changelog/分配权限对接role-auths-update.md)

- [角色权限抽屉去掉「导航菜单」表头复选框（用户贴「分配权限」抽屉截图，红框圈「导航菜单」列头右侧的 `NCheckbox`："这个可以去掉，有左边的就够了"；**定位（纯定位）**=`role-permission-drawer.vue` 的 `#nav-header` 模板即该复选框，左侧 vxe `type="checkbox"` 列表头本就自带全选框（`table.vue` 第 330 行）；**三项确认**=① 保留文字「导航菜单」仅删复选框 ② 一并清理 `navHeaderState`/`toggleCheckAll` 死代码 ③ 接受取消全选时首页权限保留（原逻辑会连首页权限一并清空，左侧全选框更一致）；**实施 3 处**（同文件）=模板删 `NCheckbox` 留 `<span>导航菜单</span>` + 删 `navHeaderState` 计算属性 + 删 `toggleCheckAll` 函数；**等价性**=左侧全选框触发 `checkbox-all`→`handleSelectionChange`，其已自动为新勾选菜单补全按钮/子模块权限（`HeaderCheckState`/`buttonHeaderState`/`subMenuHeaderState`/`withHome` 均保留）；**验证**=read_lints 该文件 0 诊断 + 全文搜 `navHeaderState|toggleCheckAll` 0 匹配](./changelog/角色权限抽屉去掉导航菜单表头复选框.md)

- [操作轨迹配置接入 track-op 接口（用户给 tms-user `TrackOpSchema`："这是操作轨迹配置字段，只需要批量保存和列表接口，这个是在表格内编辑"；**设计确认**=① 复用现有 system-manage/setting 的 operation-trace tab（原 OperationTrace.vue 是 node/location/drawer 草稿，同名不同字段，直接覆盖）② 数据固定 5 条不可增删（仅批量保存/列表两接口）③ 表格内编辑 + 顶部批量保存；**实施 6 处**=typings 新增 `TrackOpItem`/`TrackOpList` + i18n Schema 扩展 `page.manage.setting.operationTrace`(col 5 + opTypeOption 5 + timeTypeOption 3 + batchSave/saveSuccess/saveFailed) + zh-cn/en-us 同步 + `service/api/operation-trace` 重写 `fetchGetTrackOpList`(`/track-op/query`)/`fetchBatchUpdateTrackOp`(`/track-op/update`，真实接口无 DEV mock，沿用 createFlatRequest 解包 `{data,error}`) + `OperationTrace.vue` 重写 useVxeTable(`:pagination="null"`) + Table 各列自定义插槽（opType 只读 NTag / timeType NSelect / place·desc NInput / status NSwitch(checked=1)），行对象引用被控件双向绑定、批量保存读整表提交 + 删除被取代的 `OperationTraceDrawer.vue`（其旧导入已随 service 移除否则 typecheck 失败）；**验证**=typecheck 0 / lint 0 error（2 条 link.vue 既有 warning 无关）；未跑 playwright 实测（需真实后端 `/track-op/*` 在线）](./changelog/操作轨迹配置接入track-op接口.md)

- [页签关闭图标 hover 无反馈（用户贴页签栏截图："这个关闭图标hover怎么没有显示边框，都不知道是不是选中了"；**定位（改前实测）**=当前页签模式 `slider`（`src/theme/settings.ts` 的 `tab.mode`），关闭图标 `SvgClose` 是 16×16 `rd-50%` 圆形容器（`packages/materials/.../svg-close.vue`），而 `index.module.css` 只定义了 `.button-tab .svg-close:hover` 与 `.chrome-tab .svg-close:hover` 的 hover 圆形高亮、**缺 `.slider-tab .svg-close:hover`** ⇒ slider 模式下 hover 完全无反馈；**方案 A（用户确认）**=在 `packages/materials/src/libs/page-tab/index.module.css` 末尾补 `.slider-tab .svg-close:hover`（主题色圆形底 + 白 ×）+ `.slider-tab_dark .svg-close:hover`（黑 ×），与 button 模式对齐；验证=仅新增 2 条 CSS 规则、不影响既有 button/chrome 模式，建议 pnpm dev 实测 hover 高亮 + 深色模式对比](./changelog/页签关闭图标hover无反馈.md)

- [角色权限抽屉勾选表头导致树「瞬间收起再瞬间展开」（用户贴「分配权限」抽屉截图、红框圈「导航菜单」表头复选框："这个勾选了为什么要瞬间收起再瞬间展开为什么会这样？"；**定位（未改任何代码，纯实测）**=① 浏览器 console 出 vxe 官方警告 `[vxe ui table v4.21.2] 缺少必要的 "row-config.keyField" 参数`（vxe 源码 `table.js:14456`：只要用 `tree-config.expandRowKeys`、checkbox `reserve` 等按 rowid 索引的状态就必须配 keyField，否则只 warn 不兜底）② 抽屉内 DOM rowid 实测是内部生成的 `row_51`~`row_70`（非业务 id）③ 点击表头复选框后 rowid 整批换成 `row_71`~`row_90`，证明 `toggleCheckAll → applyView → buildView` 把 `viewRows` 全量重建（行对象上的 `_X_ROW_KEY` 随之消失、vxe 重新发号）④ MutationObserver 抓到两帧：点击 305ms → 318ms 可见行 20→**6**（收起）→ 390ms 回到 20（再展开），抖动约 72ms；**机制**=vxe 数据加载后用 `getRecoverRowMaps(treeExpandedMaps)` 按 rowid 恢复展开态，rowid 全变 ⇒ 展开集合清空 ⇒ 瞬间收起；随后 `role-permission-drawer.vue` 的 `syncTreeExpand()`（applyView 内 nextTick `setTreeExpand(rows, true)`）再全部展开 ⇒ 视觉抖动；同类触发点还有搜索关键字 / 勾选行 / 勾选行内按钮权限（都走 applyView）；附带问题＝用户手动折叠的节点也会被 syncTreeExpand 强行展开；**方案（用户确认 A2，已实施）**=`components/Table` 恢复被注释的 `rowConfig` prop + 模板绑定 `:row-config`、抽屉传 `{ keyField: 'id' }`，并把 `syncTreeExpand()` 从 `applyView()` 挪到 `loadData()`（只在打开抽屉时展开一次，勾选/搜索重建不再强制展开）；未采纳 A1（保留强制展开会覆盖手动折叠）/ B（抽屉侧不重建，改动面大且解决不了搜索抖动）/ C；**验证**=typecheck 0 / oxlint 0 / oxfmt / eslint 0 + playwright 复测：rowid 变为业务 id（`100/200/201/300…`）、点表头复选框 MutationObserver 仅 1 次变更且行数保持 20 / rowid 完全不变（无 20→6→20 抖动）、手动折叠「客户管理」后再勾选仍保持 19 行折叠、关闭重开抽屉仍全展开、console 无 `row-config.keyField` 警告、用户管理页回归正常](./changelog/角色权限勾选瞬间收起再展开.md)

- [角色分配权限列表配置化（用户两张截图：先「一级菜单 / 二级菜单 / 操作权限」两列形态，再「导航菜单 / 按钮权限 / 二级菜单权限」三列形态并说明「跟这个一样」；**五项确认**=① 位置=角色管理「分配权限」抽屉 ② 数据源=constants 静态配置全量生成（页面纯渲染）③ 交互沿用现状、不重新设计 ④ 界面=三列 + 表头全选 ⑤ 第三列=仅「系统设置」7 子模块 +「资料管理」三页档案子模块，其余 `--`）；**实施**=① `constants/button-permissions.ts` 重命名 `menu-permissions.ts`（按钮定义全部保留、`getMenuButtons` 索引改从新树构建）+ 新增 `MENU_PERMISSION_TREE`（一级→二级→`buttons`/`subMenus`，按真实菜单层级，顺带修正原配置里 `菜单管理`/`客户管理` routePath 与实际路由不符）+ `getMenuPermissionTree()` 自动生成表格树行（行 id 派生 `(i+1)*100`；无子菜单的一级菜单=首页标记 home 必选不可取消）② 抽屉删 `fetchGetRoleMenuTree`+`fetchGetMenuList` 与 merge/flatten/findHome 逻辑，改三列 + 三列表头全选（含半选/禁用），新增 `rowSubMenuChecks`（勾菜单默认全选 / 取消清空 / 搜索保留 / 勾权限自动勾菜单），移除工具栏全选按钮，宽度 1100→1400，提交体新增 `subMenuCodes` ③ 通用 `components/Table` 加**可选** `headerSlot`（`table.vue` 透传 `#header`、`use-vxe-table.ts` 类型与 columns 透传，向后兼容）；**未动**=service/mock（旧权限树接口与 MENU_TREE 留作死代码待后端对接）、角色列表页；**验证**=typecheck 0 / oxlint 0 / eslint 0 / oxfmt；playwright 实测 dev 9111 真实后端：三列渲染（一级菜单 `--`、用户管理 10 按钮、系统设置 7 子模块、行高自适应）、表头按钮权限全选、表头二级菜单权限全选、提交成功（抽屉关闭 + 列表刷新）、console 0 error（仅既有 2 条 warning）；测试快照/截图/日志已清理；**同日调整**=用户「二级菜单权限方按钮权限左边，并改名为子模块权限」⇒ 列序改 `导航菜单 | 子模块权限 | 按钮权限`、列名与代码/配置注释术语统一为「子模块权限」（`subMenus` / `subMenuCodes` 字段名不动，避免影响后端约定），typecheck / oxlint / eslint / oxfmt 0 + playwright 复测列序与第二列表头全选正常](./changelog/角色分配权限列表配置化.md)

- [导出模板上传控件：图标垂直对齐 + 去掉复制图标（用户贴控件截图"这个组件的图标没有垂直对齐" → 用户"**必须用这个组件**"（保留 naive `NUpload`，不换自管 `<input type=file>`）→ 追加"**复制图标去掉只要上传图标就可以了**"；**定位（改前实测）**=`.n-input__suffix` 是 flex + align-items:center（中心 y=278），复制按钮作为其直接 flex item 正常居中（cy=278），而上传按钮被 `NUpload` 的 `.n-upload` → `.n-upload-trigger` 两层包裹、后者是 **inline-block + line-height:34px**（继承输入框高度），内层 inline-flex 按钮按**基线**落位 ⇒ cy=274、偏高 4px（svg 同）；**修复（方案 A）**=组件内 scoped `:deep(.n-upload), :deep(.n-upload-trigger){ display:flex; align-items:center }`（只影响本组件，不碰公共组件/FormWrap/`components/Upload`）；**去复制图标**=模板只留只读 NInput + 上传按钮，删 handleCopy 与 copyText import、去掉不再消费的 `fileUrl` prop（否则 eslint `vue/no-unused-properties` 告警）并把父级改为 `@update:file-url="formModel.fileUrl = $event"`、i18n 三处删无用 `copyUrlEmpty`；**验证**=oxfmt 无改动 / oxlint 0/0 / eslint 0 / typecheck 0；playwright 实测 dev 9111 + 真实后端：修复后 suffix·按钮·svg 三者 cy 全 278、按钮与 svg 各 1、`.n-upload*` computed display=flex；回归=填表→选发货清单→上传（文件名回显）→保存成功（后端 create 校验 fileUrl 必填、通过即证明事件链路正常）→列表出现 `__上传图标测试`→单条删除恢复原 3 条；console 0 error；测试数据与本次快照/日志、tms-user 临时脚本与 xlsx 已清理）](./changelog/导出模板去掉复制图标.md)

- [通用上传组件补齐拖拽形态（用户贴 naive-ui「拖拽上传」文档截图："拖拽上传类型有没有封装"；**确认**=加进通用组件）；**API**=`list-type` 新增 `'dragger'`（文件列表 + 虚线框拖拽区，内部 naive 映射 `text`）+ 新增 `directory-dnd`（boolean）透传；拖入与点击走同一链路（accept 过滤 / maxSize / 进度 / 失败重试 / v-model 只写 finished）；**关键实现（naive 源码核实）**=① `NUploadDragger` 必须是 `NUpload` 默认插槽**首子节点**（`$slots.default()[0].type[uploadDraggerKey]` 判定，否则 `handleTriggerDrop` 直接 return）② **naive 不提供默认触发内容**（locale 无 Upload 段、NUploadTrigger 直接渲染 default 插槽）⇒ 顺带修复 `file` 形态此前无触发按钮（默认渲染「选择文件」按钮 + `common.chooseFile`）③ image-card 的「+」经 `resolveSlot(slots.default, fallback)` 取插槽 ⇒ 任何非空插槽都会替换原生「+」图标 ⇒ 组件改**双分支渲染**（`v-if="isImageCard"` 不传插槽 / `v-else` 传触发区）+ `v-bind="uploadProps"`（computed 集中绑定，避免 props 重复）④ 坑=`computed` 返回位的三元字面量被拓宽为 `string`（须 `computed<'image-card' | 'text'>` 显式标注）；`directoryDnd` 对象形态不在 naive 类型声明内（收敛为 boolean）；**验证**=typecheck / oxlint / oxfmt 全过 + playwright 首页**动态挂载实例**实测（不碰业务代码；`import('/tms/node_modules/.vite/deps/vue.js?v=...')` + `import('/tms/src/components/Upload/index.vue')` + `createApp`，**dev base 为 `/tms/`，模块 URL 必须带前缀**）：dragger 的 `.n-upload--dragger-inside`=true、394×127 虚线框、48px 图标、文案正确、`dragover` 高亮 / `dragleave` 复位、点击拖拽区 → `/upload?dest=10` → v-model=`[temp url]`（max=5 数组）；file 按钮上传 → string（max=1）；**image-card 回归**（BasicConfig：回显 → 删除清空 +「+」恢复 → 上传回写 +「+」隐藏）console 0 error；**限制**=naive 的 drop 依赖 `webkitGetAsEntry()`（仅真实拖拽可用），程序构造的 `DataTransfer` 无法模拟文件拖入，已用 dragOver 高亮 + draggerInside 判定 + 点击链路上传覆盖等价路径](./changelog/通用公共上传组件.md)

- [通用公共上传组件（用户："按照组件库能不能给我封装一个通用的公共上传组件呢？"；**两问确认**=① 范围 **只做独立通用组件**（新建 `components/Upload/` 供页面按需引入，首个落地=替换 BasicConfig 内联 LOGO 上传；FormWrap 的 file/image 与 ExportFormat 本次不动）② 形态 **单组件覆盖图片+文件**（`list-type: 'image-card'|'file'` + `max`（1=单值，>1=数组）+ `dest`（1..10）；v-model 输出 URL/URL[]；带进度与失败态）；**后端核对（tms-user 源码）**=`POST /upload?dest=<1..10>`（form-data 字段名 file → `{name,size,url}`；dest 1-用户/2-公司/3-打印格式/4-导出格式/5-服务商/6-工单/7-运单/8-交易/9-问题件/10-临时；单文件 dest 必填否则 100030；拒绝 php/js/html/exe；多文件 `/files`→{list:[...]}）；**实施**=① `typings/api/upload.d.ts`（`Dest` 1..10、`Result{name,size,url}`）② `service/api/upload/index.ts` `fetchUpload(file,dest,onProgress?)`（透传 axios `onUploadProgress`→百分比，真实进度）③ barrel 追加 ④ `components/Upload/index.vue`（`custom-request` 走通用接口、成功 `file.url=url` 再 `onFinish()`（本仓 naive onFinish 无参）、受控 fileList 以内部为准+外部值变更重建（pending/failed 不写 v-model 保证上传中提交不丢旧值）、`on-before-upload` 拦 maxSize 提示、`on-remove` 发 remove 事件（仅前端解绑）、image-card 缩略图内置 `object-fit:contain`）⑤ i18n `common.upload.exceedSize` 三处 ⑥ `BasicConfig.vue` LOGO 换 `<Upload v-model:value="logoUrl" :dest="2" :disabled="loading||saving" />` + `logoUrl` computed 中转（组件载荷 `string|string[]` 收敛为字符串）+ 删 `logoFileList`/`handleLogoUpload`/`handleLogoListChange`/`.logo-upload` 样式 ⑦ 死代码清理=`service/api/company` 删 `fetchUploadCompanyLogo`、`Api.Company.UploadResult` 迁 `Api.Upload.Result`；**naive 机制核实（源码）**=custom-request 的 onProgress/onFinish/onError 均经 `doChange → doUpdateFileList` 回抛 `@update:file-list`（受控列表可拿到百分比）；`on-before-upload` 返回 false 的文件不进入列表；`defaultUpload` 默认 true 自动调 customRequest；max>1 必须显式 `:multiple`；**验证**=typecheck 0 / oxlint 10 文件 0 / eslint 0 / oxfmt / `pnpm build` 通过（components.d.ts 登记 `Upload`）+ playwright 实测 dev 9111 真实后端（回显缩略图 `accept=image/*` + computed `object-fit:contain`；选 `public/logo.png` → `/upload` 200 → 卡片回显新图 → 读 BasicConfig setupState.model.logoUrl 同步新地址；hover 垃圾桶删除 → 卡片消失+value 清空+「+」恢复；删除后点提交 → 提示「公司LOGO必填」且未发 `/company/save`（saveSent=false）；console 0 error）；**伪缺陷记录**=验证中期「删除后提交仍保存成功」实为两次操作间隔发生 `goto` 重载、`loadCompany` 回填了 logoUrl（业务字段均未变、仅 updateDate 刷新一次），隔离重测拦截正常；经验=验证「删除→提交」链路勿在中间重新导航、断言直接读组件实例值（`el.__vueParentComponent` 向上找 setupState.model）](./changelog/通用公共上传组件.md)

- [导出格式对接接口（用户贴 tms-user `ExportTemplateSchema`（`lib/common/models/export-template.js`）："这是导出格式字段帮我对接接口" + 目标抽屉表单截图；**四项确认**=① 抽屉**完全按截图** 6 项（模板名称\*/模板类别\*/Excel模板\*/表头起始单元格/数据起始单元格\*/备注），删掉「使用范围 scope」「上传模板 fileName」② Excel模板 = 只读文件名输入框 + 复制地址 + 上传按钮，**该控件单独封装为「特殊上传组件」**（明确不做通用上传）③ 列表列改后端字段 + 行内单条删除（编辑/下载/删除），去掉勾选列与批量删除（后端 delete 仅单条 \_id）④ 解析明细不展示（仅提示条数）、删掉「字段模板下载」按钮；**后端事实（读源码）**=`/export-template/{upload?dest=4,create,update,delete,query,get}`；create 必填 name/templateType/file/fileUrl 且 name 全局唯一（102401）；query body `{page,size,keyword,where}`（keywordFields ['name']，返回 ret:{list,total}，omitFields infoList/fieldList/subList）；类别 9 类 0-系统列表…8-运单详情（前端原 13 分类整体废弃）；upload 由 `parseFile` 解析 xlsx 标记（@信息 / #列表 / ##子列 / $子列表 / %&条码）并回传 file/fileUrl + 全部字段清单；**实施**=typings 重写（CellField/ListField/SubList/Barcode/SubBarcode/ParsedFields/Template/SearchParams/UploadResult/SaveParams）+ service 去全部 DEV mock 接 5 真实接口 + 新增专用组件 `ExportTemplateUpload.vue`（只读 NInput + #suffix 复制（项目 copyText）/上传（NUpload default-upload=false，成功后 emit update:file/fileUrl/parsed 并提示「解析成功：信息字段 x 项 / 列表字段 y 项」））+ `ExportFormat.vue` 重写（搜索栏名称+类别、7 列=模板名称 detail/模板类别/表头起始/数据起始/备注/最后操作·最后更新时间 fixed:right、行内 编辑·下载(window.open fileUrl)·删除 NPopconfirm；抽屉 6 项，file 用 type:'custom' + required 让 FormWrap 按 model.file 自动生成「请上传 Excel 模板」规则；cacheKey 升 `setting-export-format-v3`）+ i18n 三处（新增 type.9 类等 14 key、删旧 13 key）；**关键设计点**＝编辑用列表行数据回填、`parsedFields` 保持 null ⇒ 提交体不含解析字段 ⇒ 后端 updateOne 不覆盖库中原值，重传则整体覆盖；**验证**=typecheck 0 / oxfmt / oxlint 0 / eslint(.vue) 0 + playwright dev 9111 真实后端全链路（列表 3 条真实数据；exceljs 造带标记测试 xlsx → 上传回显文件名；新增后接口查库 fieldRow=2/infoList 2/fieldList 3+columnName/subFieldList 1/subList 1 **解析结果完整落库**；编辑改备注不重传保存后 note 更新且四个解析数组原样保留；真实鼠标点击下载到 94128edb-….xlsx；NPopconfirm 确认后列表回 3 条、query 该名称 total=0；console 0 error；测试数据与本次 .playwright-cli 快照/日志/下载文件、tms-user 临时脚本与测试 xlsx 已全部清理）；坑=custom 项 FormWrap 仍生成必填校验（只过滤 slot 项）、naive NUpload 满额会隐藏触发块故此处不能设 :max；测试期间页面偶发被外部切到 `?tab=basic-config`（history 补丁未捕获调用，疑并行会话共用 default 浏览器所致，非本页代码，已用单条指令内完成操作规避）](./changelog/导出格式对接接口.md)

- [tab 指示条与系统设置不一致（用户贴个人中心左侧 tab 栏截图、红框圈激活 tab 右侧蓝竖线："都是同一个组件为什么跟系统设置的就是不一样呢？"；**根因（实测）**=naive 竖线指示条 `.n-tabs-bar` 是 `position:absolute; width:2px; right:-1px`，**锚在 tab 列表 `.n-tabs-nav` 右边缘而非侧栏右边缘**，而 `VerticalTabLayout` 侧栏是 `w-fit`（宽度由最宽的孩子决定）⇒ 最宽孩子是标题行时（个人中心：标题 102px vs 标签 88px）列表窄于侧栏、蓝条离右边框 15px（306/323）；最宽孩子恰是 tab 文案时（系统设置：标签 115px）列表撑满、蓝条贴边（334/337）；**方案 A（用户确认）**=`VerticalTabLayout` 加 scoped `:deep(.n-tabs-nav){ width: 100% }` 让列表撑满（未加 !important，实测生效；tab 行随之撑满 87→101px、整行可点，文字仍左对齐 16px），未采纳「保持现状」与「把蓝条贴文字」（需覆盖 naive 内部样式）；验证=oxlint/eslint 单文件 0/0 + playwright 实测蓝条 left：个人中心 306→320（侧栏右边缘 323，差 1px 边框）、系统设置 334→334（零变化）、基础资料 →320，三页 console 0 error，截图确认观感一致)](./changelog/tab指示条与系统设置不一致.md)

- [个人中心贴边对齐系统设置（用户贴系统设置页截图并说明「这是靠边的」——"个人中心怎么没有跟系统设置一样？"；**定位（纯定位即结论）**=两页左右布局本来就同源（都用 `VerticalTabLayout`：左「标题 + 贴边竖向 NTabs」+ 右 `p-16px` 内容区），差异只在一处——`base-layout/index.vue` 的 `contentShowPadding` 按路由名排除「贴边页」，名单里有 `system-manage_setting` 而**没有** `personal-center` ⇒ 个人中心页面根被 `GlobalContent` 多注入一层 `p-16px`（`:class="{'p-16px': showPadding}"`），左 tab 栏与内容区都离视口边缘 16px；**方案 A（用户确认）**=`'personal-center'` 加进该排除数组（与系统设置/资料管理三页同机制），未采用页面内 `!important` 覆盖（`VerticalTabLayout/README.md` 规定贴边走 `contentShowPadding`）；实施=仅改 `base-layout/index.vue` 一行 + 注释补「个人中心页」，两个子页（ProfileInfo/ChangePassword 根都是 `h-full overflow-y-auto` + NCard）无 `-mb-16px` 补偿写法故无需连带改动（卡片距左上由 32px 变 16px，与设置页一致）；验证=typecheck 本文件 0 报错（ExportFormat.vue 6 条为其并行未完成改动的报错，未触碰）+ oxlint/eslint 单文件 0/0 + playwright 实测 dev 9111 页面根 padding：个人中心 `0px`、系统设置 `0px`、用户管理 `16px`（回归基线），个人中心左栏 `220,40→323,720`（贴顶/贴底/贴左）、右内容区 `323,40→1280,720`、首卡 `339,56`，切「修改密码」tab 正常渲染贴边保持，console 0 error)](./changelog/个人中心贴边对齐系统设置.md)

- [个人中心（修改个人信息 + 修改密码）（用户："再加个个人中心界面链接在这里，用来修改个人信息和修改密码的"；**后端事实（tms-user 源码 + 数值验证）**=① 改自己密码必被拒：`services/user.js` 的 update 中 `!user.editPwdCtrl && body.password → 100107`，而 `editPwdCtrl` 全库无任何赋值来源（`fillAuthMap` 只合并 `editInfoCtrl`、Role Schema 无此字段）② 改自己信息需 `editInfoCtrl`（admin 自动 =1）③ 改自己密码后端会销毁会话（logout=true）④ 密码格式 `md5(md5(明文)+明文)`（md5(md5('admin@12345')+'admin@12345')=e6b74644a7667245c0298e795506479a=后端常量 ADMIN_PASSWORD）⑤ 无独立改密接口；**三项确认**=① 改密码前端先做、整体置灰提示「后端未开放」② 独立页面 + 两 tab（VerticalTabLayout）③ 可改字段全量（同用户抽屉）；实施 8 处=`views/personal-center/index.vue`（壳，顶层不放注释）+ `modules/profile-info/ProfileInfo.vue`（只读卡 mode=view：账号/站点/角色/组别/状态 + 可编辑 15 字段 + 保存后 `getUserInfo()` 刷新侧栏、`editInfoCtrl=0` 禁用）+ `modules/change-password/ChangePassword.vue`（NAlert + 新/确认密码 + 全禁用，启用步骤写在组件注释）+ `build/plugins/router.ts` hideInMenuRoutes + `user-avatar.vue` 菜单项（DropdownKey 扩展）+ `auth/index.ts` 暴露 getUserInfo + `service/api/user` 新增 fetchUpdateSelfProfile + typings `UserSelfUpdateParams`（Pick 可编辑字段）+ i18n 三处（route 段是 Record<I18nRouteKey> 必须同步）；验证=typecheck 0 / oxlint 0 / eslint 0 / oxfmt；playwright 实测 dev 9111：下拉菜单 [个人中心, 退出登录] → 真实鼠标点击跳 `/personal-center`（Title 个人中心）、左侧菜单不含该项（hideInMenu）、只读回读 admin/总部/-/-/启用、表单回填 超级管理员、改密码 tab 全 disabled + 文案正确、保存链路 `/user/update` 200 + 自动 `/user/session/get` → 切 tab 重建回读测试值 → 改回空（数据已清理）、console 0 error；坑=NDropdown 菜单项 DOM `click()` 不触发选中（须 mousemove+mousedown+mouseup）、FormWrap 自定义 rules 覆盖自动 required（须手写星号+空值校验））](./changelog/个人中心.md)

- [用户区右侧加箭头图标（用户截图红框标用户区按钮并手绘向上 `^`："右侧加一个这样的图标"；**澄清确认**=线形 chevron（`mdi:chevron-up`），「实心小三角 ph:caret-up」「实心粗三角 ph:caret-up-bold」未采纳；实施=仅 `user-avatar.vue` 模板在用户名 span 后加 `<SvgIcon icon="mdi:chevron-up" class="text-14px" />`（`v-if="!appStore.siderCollapse"`、间距沿用插槽内 gap-8px、颜色 currentColor 随文字 82%→hover 纯白、方向朝上同 `placement="top"`、未登录按钮不加）；验证=typecheck 0 / oxlint 0 / eslint 0；playwright 实测 dev 9111：svgCount=2、chevron 14×14 渲染出真实 path、常态 `rgba(255,255,255,0.82)`、hover `rgb(255,255,255)`+按钮 bg 0.16、gap=8px / 垂直中心差 0 / 右缘贴内容最右 176；坑=`btn.querySelector('span')` 会命中 naive `.n-button__content` 容器（假 gap -14），须选 `.flex-center > span`）](./changelog/用户区右侧加箭头图标.md)

- [基础配置接入公司接口（用户贴 tms-user `CompanySchema`：「这是基础配置的字段，这个只要接一个详情和修改的接口就可以」；**四项确认**=① 表单**补齐全字段**（现有 9 项 + Schema 里 8 个业务规则项）② `userWeb`/`customerWeb` 由开关改**文本输入**（Schema 是 String）③ LOGO 走**页面内自定义上传**（FormWrap custom 插槽 + `/upload?dest=2`，公共组件零改动）④ 用户补充截图确认布局=两张卡片（基本信息 / 系统操作控制定义，后者三列）+ 底部按钮居中、文案「提交修改」；**后端核对（读源码）**=`POST /company/get`（无参返回整份文档）/ `POST /company/save`（ajv 必填 name/sysName/address/web/logoUrl，剔除 status/expireDate/sysVersion/authModules/authApis，其余字段透传 commonSave 单文档 `findOne({})` 更新、无需 `_id`）/ `POST /upload?dest=2`（form-data file → {name,size,url}）；实施=typings 新增 `Api.Company`（SaveParams/Info/UploadResult）+ service 新增 `service/api/company`（3 接口直连、无 mock）+ barrel 追加 + `BasicConfig.vue` 重写（两 NCard + 两 NFormWrap 共用 model、必填前端对齐后端、custom 插槽自写 logo 上传+删除、onMounted 逐字段回填、保存成功重拉）+ i18n 三处（common.submitModify / basicConfig.infoCard·ruleCard·rule 8 项·option 7 项）；**坑**=FormWrap 的 `slot` 字段语义是「操作区按钮项」（带 slot 的项被 filter 出表单、不生成校验），自定义单元格只能写 `type: 'custom'`（插槽名自动取 key）——首版误传 slot 致 LOGO 项跑到表单外且无必填校验；验证=typecheck 0 / lint 0 error / fmt + playwright 实测 dev 9111 真实后端（渲染两卡片+三列布局、空表单拦截 4 条必填、get 回填 17 字段全对、logo 上传返回静态 url 并回显、UI 提交后库 updateDate 变化且改写「默认出发地」提交后 startPlace 随之落库、console 0 error；测试环境原无 company 记录，实测写入 1 条测试数据待用户覆盖）；**同日微调**=用户截图「高度太高了」→ 底部操作条 `p-12px` 改 `py-8px` + `-mb-16px`（抵消 VerticalTabLayout 内容区 `p-16px`）贴底，实测 footer 51px、bottom=视口底（720）、按钮上/下留白 9px/8px（改前约 74px）；**上传控件返工（同日）**=用户截图 LOGO 区「这个没有使用上传组件吗？为什么删除这样显示呢？」→ 首版 `:show-file-list="false"` + 自绘虚线框/文字删除按钮改为 naive 标准 `list-type="image-card"`（缩略图卡片 + hover 蒙层预览/删除图标 + 虚线「+」方块；`@update:file-list` 收敛单选并同步 logoUrl，本仓 naive 的 `onFinish` 无参故 url 写 `file.url`；删除后提交正确报「公司LOGO必填」）；随后用户又指「你为什么一定要改成长的？」→ 撤销自加的 160×80 尺寸覆盖、恢复 naive 默认 96×96，仅保留 `object-fit: contain`；**文案**=业务规则卡片标题「系统操作控制定义」改「系统控制」（basicConfig.ruleCard 中英同步）；**第四条反馈**=用户问「如果只允许上传一个右边的是不是就不能显示了？」→ 加 `:max="1"`（naive 满额时自动隐藏「+」上传块，替换=先删再传），实测有图隐藏 / 删除后出现 / 上传成功后再隐藏）](./changelog/基础配置接入公司接口.md)

- [设置模块按文件夹归类（用户贴 `setting/modules/trace-capture` 9 文件平铺截图："怎么不按文件夹区分，这样太乱了，只要文件夹里面不出现 index.vue 就行了"；**源码核实**=`@elegant-router/core@0.3.8` 默认 `pagePatterns=['**/index.vue','**/[*].vue']`（仅排除 `**/components/**`）⇒ 只有 index.vue 才生成路由，任意深度的纯分组文件夹不含 index.vue 就不会被扫到、私有子组件无需挪出 views（用户判断正确，也解释了上次误挪 src/components 的顾虑是多余的）；**两问确认**=① 范围"先只改 setting"（全项目盘点：trace-capture 9 / print-design 7 / home 6 / login 5 / input-format 4，其余暂不动）② 命名按子 tab key（目录 track-network 等 + `TraceConfig*` 改名 `TrackNetwork*`）；实施=`trace-capture/` 下建 4 目录（track-network / track-transform / track-keyword / capture-time）并 `Move-Item` 8 文件（`TraceCapture.vue` 留原位、`setting/index.vue` 引用零改动；除 TraceConfig\* 外文件名不变，Table→Drawer 同目录相对引用随文件整体移动天然有效），仅改 `TraceCapture.vue` 4 行 import + 注释 + 模板标签与 `TrackNetworkTable.vue` 2 处；域命名（`service/api/trace-config`、typings `TraceConfigItem`）不动；**坑**=同文件多处 replace_in_file 同批并行会互相覆盖（import 块被后两处编辑挤掉、typecheck 报 TS2307 才发现）→ 同文件改动改串行后通过；验证=typecheck exit 0 / read_lints 0 / 全文搜 TraceConfigTable|TraceConfigDrawer 0 命中 / 未新增 index.vue、routes.ts 无新增路由）](./changelog/设置模块按文件夹归类.md)

- [侧栏用户区 hover 不明显（用户截图红框标侧栏底部用户区："这个hover不明显"；**定位**=`UserAvatar`→`ButtonIcon` 是 `NButton quaternary`，hover 背景由 naive 按**浅色主题**计算（`--n-color-hover` 极淡深灰、`node_modules/naive-ui/es/button/src/styles/index.cssr.mjs` L93），叠在深蓝侧栏 `rgb(0,20,40)` 上不可见；且此前主题适配加的 `.sider-footer--inverted :deep(.n-button){color:…!important}` 裸 `color` 压过 naive `:hover` 的 `var(--n-text-color-hover)`，连文字变色反馈也被压死；**两轮澄清**=① 热区保持按钮本身（不做整行热区）② 配色白色 16% 背景 + 文字转纯白（「12% 仅背景」「主色蓝半透明」未采纳）；实施=仅改 `global-sider/index.vue` 的 style：删裸 color 覆盖，改为覆盖 5 条 naive 变量（`--n-text-color`/`-hover`/`-pressed` + `--n-color-hover`/`-pressed`，naive 以**内联 style** 设变量故须 `!important`）；仅作用于深色侧栏场景、不影响其它 quaternary 按钮与折叠态；验证=typecheck 0 / oxlint 0 / eslint 0；playwright 实测 dev 9111：`mousemove` 到按钮中心后 computed `bg=rgba(255,255,255,0.16)`、`color=rgb(255,255,255)`，移出恢复 `transparent`/`rgba(255,255,255,0.82)`，console 0 error）](./changelog/侧栏用户区hover不明显.md)

- [轨迹抓取配置新增「抓取时间」子 tab（用户贴两张截图："轨迹抓取配置新增：抓取时间，这是字段，然后操作栏左侧有个按钮弹出这个窗口"；现状=该子 tab 此前因字段待定被注释隐藏（changelog/抓取时间模块先注释.md）；**三项确认**=① 位置恢复为轨迹抓取配置第 4 个子 tab（追踪网络/轨迹改造/轨迹关键词/抓取时间）② 图1 五列（操作日志/操作系统/操作类型/操作人/操作时间）= 主表格列展示抓取执行记录、工具栏左侧按钮打开配置弹窗（图2）③ 数据先走 DEV mock（后端 tms-user 无抓取时间接口）；实施=新增 `CaptureTimeTable.vue`（只读 5 列、无序号/操作列、cacheKey `trace-capture-time`、operation-left「抓取时间设置」按钮）+ `CaptureTimeModal.vue`（NModal 420：说明文字 + 时间行 NTimePicker(HH:mm) 增删、仅剩 1 行删除禁用、空值/重复校验、保存成功关闭）+ `service/api/capture-time`（3 接口 DEV mock + 非 DEV request 占位、barrel 追加）+ `mock.ts` 新增抓取时间段（6 条记录 + 配置 ['03:00']、timeStampOf 辅助）并删旧 capture-time 同构数据（"定时抓取"已无页面使用）+ typings 4 类型 + i18n 三处 `traceCapture.captureTime` + `TraceCapture.vue` 取消注释恢复 4 子 tab；坑=DEV mock 裸值/非 DEV flat 联合返回类型需调用处 `as Api.SystemManage.CaptureTimeConfig` 收窄（TS2339/TS7006）；验证=typecheck 0 / lint 0 error / fmt + playwright 实测 dev 9111：4 子 tab、5 列表格 6 行（时间格式化）、弹窗全链路（空值校验提示/保存回显/单行删除禁用/恢复默认 ['03:00']）、tab 切换、console 0 errors；**同日调整**=用户「抓取时间设置只能选择小时」→「能不能直接用个下拉框」→（首版显示带 :00 被纠正「我不是说了只要小时吗？」）⇒ `CaptureTimeModal.vue` 的 NTimePicker 改为 NSelect（选项 / 显示 / 保存均为纯小时 00~23、移除 dayjs，mock 默认 ['03']），实测下拉选择 08 / 保存 / 重开回显 / 恢复 03 全通过；同期用户将子模块重组为独立目录（track-network/track-transform/track-keyword/capture-time）后 typecheck 仍 0）](./changelog/轨迹抓取配置新增抓取时间.md)

- [操作轨迹配置单独提为一个模块（用户："操作轨迹配置单独提为一个模块"；**两轮澄清确认**=① 层级选 **A 系统设置内的顶级 tab**（B 独立菜单页 / C 仅代码目录 均否决）② i18n 选 **甲 复用现有 key**（列/表单/抽屉文案继续引用 `traceCapture.*`，该域 key 本就共用：`createTitle` 一句四抽屉在用）；实施=① `Move-Item` 挪位并重命名：`modules/trace-capture/OperationTraceTable.vue` → `modules/operation-trace/OperationTrace.vue`、`OperationTraceDrawer.vue` 同迁（组件零逻辑改动、`cacheKey: 'operation-trace'` 保持不变 → 已列配置缓存不丢、目录下不用 index.vue 规避 elegant 路由生成）② `TraceCapture.vue` 删操作轨迹 import / `SubTabKey` 联合类型（`active` 直接用 `TraceCaptureCategory`）/ `operationTabLabel` / 末尾 `NTabPane` ③ `setting/index.vue` 的 tabs 末尾加 `operation-trace` + componentMap 加静态 import（`?tab=operation-trace` 由 `syncTabFromQuery` 自动支持）④ i18n 三处同步新增 `setting.operationTrace.title`、删无引用的 `traceCapture.subTab.operationTrace`；不改 service/mock/类型/路由/菜单；验证=typecheck 0 / lint 0 error / build 通过 + 全项目搜 `OperationTraceTable`、`subTab.operationTrace` 0 命中）](./changelog/操作轨迹配置单独提为一个模块.md)

- [标签设计器保存不再打开 JSON 预览（用户："标签设计界面的报存调用修改接口"；澄清确认=**保存不要开 JSON 预览、只保存**；核查=label-designer 的保存本就是 `POST /print-template/update`（`fetchSavePrintTemplateDesign`），不符点是成功后 `previewTemplateJson()` 用 Blob URL `window.open` 另开新标签预览 designJson（2026-09-12 迭代产物）；实施=index.vue 一个替换块两处：handleSave 成功分支删调用 + 删 previewTemplateJson 函数定义（唯一引用点），保留成功/失败 `$message`；工具栏「预览」按钮与 `store.toJson()` 序列化不受影响；验证=typecheck exit 0 / 单文件 oxlint 0 warnings 0 errors / eslint 0 error / read_lints 0）](./changelog/标签设计保存去掉JSON预览.md)

- [列表显示最后操作与最后更新时间（用户贴列表截图："列表显示最后操作，以及最后更新时间"；**两项确认**=① 列位置选「最后面」（状态之后、操作列之前）② 时间格式选 `YYYY-MM-DD HH:mm:ss` 带秒、列宽 170；实施=InputFormat.vue 加 `dayjs` + `formatDateTime`（空值 `--`）、在「状态」后追加 `updateBy`「最后操作」(w100) 与 `updateDate`「最后更新时间」(w170) 两列（**均 `fixed:'right'`** —— 操作列由 Table 内部 `fixed="right"` 且最后渲染，固定右列按数组顺序排故视觉落在最右）、模板加两个插槽、i18n 三处同步 `lastOperation`/`lastUpdateTime`（app.d.ts Schema 漏改会 TS2345）；**关键坑=cacheKey 必须升 v3**：`useVxeTable` 应用本地列配置是 `cached.map(...)`（以缓存数组为准、默认列仅兜底），默认新增的列不在缓存里就不渲染；验证=typecheck 0 / lint 0 error / fmt；playwright 实测 dev 9111：表头顺序 名称|客户可用|是否默认|排序|备注|状态|最后操作|最后更新时间|操作，行值 超级管理员 / 2026-06-30 20:12:19 与后端一致，console 0 error）](./changelog/列表显示最后操作与最后更新时间.md)

- [打印格式对接后端接口（用户贴 wms-user `PrintTemplateSchema`：「这是打印格式字段，帮我对接接口」；**四轮问答决策**=① 分类口径改用后端 `templateType` 6 类（收货/发货/派送标签、发货/派送发票、提单标签）② 表格沿用现有 8 列结构只换数据源 ③ 去掉勾选列与批量删除按钮、操作列加单条「删除」（后端 delete 仅单条 \_id）④ 表单 = 名称+分类+模板格式 pdf/excel+标签尺寸（自定义时宽高 mm）+默认+备注；**后端核对（读源码）**=`/print-template/{query,get,create,copy/create,update,delete}`（web 前缀 `/tms/api/v1/web` 与前端 baseURL 一致）；query 返回 `ret:{list,total}` 且默认 omit design 等大字段；create/update 必填 `name/templateType/templateMode/sizeType`（update 为 \_id+后三项），create 的 name 全局唯一、isDefault 同 templateType 互斥；update 的 design 非空时 generate=1 且 width/height 取 `design.width/height`；\_id 为 36 位字符串、时间毫秒；**实施设计**=typings 重写（6 类/3 枚举/\_id string）、service 去全部内联 mock 接 6 真实接口（DEV 直连，同站点）、新增共用 `service/api/print-format/size-map.ts`（sizeType↔尺寸↔文案 三处共用）、PrintFormat.vue 改造（真实取数解包、8 列字段映射、分类筛选、去勾选列+行内删除、表单 sizeType=5 动态宽高、cacheKey v3）、label-designer/print-design 适配（query.id string、design 兼容对象/字符串、保存走 update 且 design 顶层补 width/height、paperSize↔sizeType 双向映射；print-design 的 parseLabelSize 加 N×Nmm 正则兜底）；新增文案硬编码中文不做 i18n；**实施**=8 文件（typings 重写 + service 重写去 mock + 新增 size-map + 打印格式页改造 + 两个设计器适配）；**验证**=typecheck 0 / oxlint 0 / eslint(.vue) 0 / oxfmt 通过 + playwright 实测 dev 9112 真实后端全链路（query/get/create/copy/delete/update 全 200：新增体与列表 8 列映射逐字段核对（自定义 80×60→「收货标签/80×60mm/否/否/超级管理员/时间」）、查看抽屉 PDF 文案与宽高回显、删除二次确认、自研设计器保存 design={unit,paper,elements,width,height}、hiprint 保存 design={panels,width,height} 均含顶层 mm 宽高；console 0 errors；测试数据已全部删除清理；唯一未测「设为默认」避免污染既有模板默认状态，该路径走 update 已被覆盖））](./changelog/打印格式对接接口.md)

- [录单格式接入真实接口（用户贴 tms-user OrderTemplate Schema："先把录单格式的查询接口接上…其他的先不管"→追问后"也接上"写操作；**三轮问答共 10 项决策**：① 列表列改后端真实字段（名称/客户可用/是否默认/排序/备注/状态，删「适用范围」）② 名称搜索先不做（后端 query 未配 keywordFields，搜索栏整块去掉）③ 增/改/删也接真实接口 ④ 字段映射接上（保留 FieldMapping UI，清单换 `fields-config.ts` 116 项/5 组，保存时保留 keyMap/options/disabled 等附加属性）⑤ 新建以「是否默认」模板 fieldList 为底稿 ⑥ 去掉批量删除按钮（后端只有单条 delete）⑦ buildIn=1 禁删；**后端事实核对（读源码+登录实测）**=`/order-template/{query,get,create,update,delete}`；query 不传 `scene` 会强制 status=1 并按角色 orderTemplateIds 过滤故管理端必传 scene；列表 omitFields fieldList；「显示」=项是否在 fieldList、`required` 才是必填；fieldType 数字语义 0 运单/1 收件人/2 发件人/3 物品/4 子件（对齐 OJT const.js），本地 fields-config 116 项与之完全对齐无缺失；**实施**=typings 重写（Field 带索引签名透传未知属性）+ service 去全部 DEV mock 接 5 真实接口 + 新增 `field-list-mapper.ts` 纯函数（buildNavGroups/fieldListToMapping/mappingToFieldList）+ FieldMapping 支持 `disabled` 锁定（组取消除外）+ 抽屉重写（基础表单去掉适用范围，新增底稿走 `query{where:{isDefault:1}}`→`get`）+ 列表页改造（scene:1 取数、列调整、去搜索与批量删除、buildIn 行禁删、cacheKey 升 v2）+ i18n 三处同步（app.d.ts Schema 漏改会 TS2345）；**验证**=typecheck 0 / lint 0 error / fmt；playwright 实测 dev 9111 + 真实后端：列表真实 1 条、编辑回显 116 项勾选 53（与后端一致）且 5 个 disabled 字段锁定、新增勾选整组后落库 62 项且**未改动分组逐字节等于系统默认**（keyMap/options/renderName/formatKey 零丢失、新字段追加组末尾）、编辑保存 note/order 生效、删除后库恢复 1 条（测试数据已清理，console 0 error）](./changelog/录单格式接口接入.md)

- [字段映射卡片头部没有左右布局（用户贴录单格式抽屉「字段映射」卡片截图、红框圈出**头部一行**（标题「字段映射」+ 5 个模块按钮）问"这个为什么没有左右布局呢"；**定位（纯问答、零代码改动）**=naive NCard 头部默认 `.n-card-header{display:flex;align-items:center}` + `.n-card-header__main{flex:1;min-width:0}`（`node_modules/naive-ui/es/card/src/styles/index.cssr.mjs` L71-93），靠 `__main` 撑满剩余宽度把 `__header-extra` 顶到卡片最右 = 原生左右布局；而本组件 L212-219 的 `:deep(.n-card-header__main){flex:none!important}`（2026-09-17「系统设置右侧空白排查」为修设置页右侧 886px 窄栏里标题被按钮组挤成 0 宽竖排而加）在禁止压缩的同时也去掉了"撑满剩余宽度"的能力 ⇒ 标题只占内容宽、按钮组紧随其后、行尾留约 400px 空白；该覆盖的原始窄栏场景现已不存在（`FieldMapping` 只在录单格式抽屉 `width="70%"` ≈1300px 使用，控件总宽约 640px 不会挤压）；**用户确认选 A**=`flex:1 1 auto !important; min-width:max-content !important`（保留撑满能力→宽容器按钮组回最右；basis auto + min-width max-content→窄容器标题不竖排、按钮组 wrap）；**验证**=typecheck exit 0 / oxfmt 无改动 / playwright 实测 dev 9111（系统设置→录单格式→新增抽屉）卡片头内容右缘与按钮组右缘同为 1231（贴最右、距卡片边框 25px=24 padding+1 border）、标题块 137px 单行 29px，同页运行时 A/B 改回 `flex:none` 则标题块 71px、按钮组右缘 1164（留 67px 空档，窗口越宽越大）=差异来源确证](./changelog/字段映射卡片头部没有左右布局.md)

- [发件人/子单号/物品信息字段清单生成（用户一次贴三组清单（发件人 15 / 物品信息 20 / 子单号 13）："…这是发件人字段…物品信息字段…子单号字段"；**三项确认**=① 币种 currency=`select`（财务资料币种档案）② 单位 unit=`select`（后端固定枚举 PCS/SET/MTR/KG/ROLL/BOX/PRS/DOZ/PKGS/EA/M2/M3/CM/CM3）③ 产地 producer=`input`（自由输入，用户未采纳 select 项）；fieldType 沿用域约定 shipper/item/product；实施=三组分区共 48 条（发件人与收件人同构：country=select、证照=image；子单号 8 个计算类字段=number（weight/length/width/height/totalLen/wrapLen/volumeWeight/priceWeight）+5 个单号/备注=input；物品信息 6 个 number + currency/unit select + imgUrl image + 其余 input）；验证=node 导入 116 条（order 52/shipTo 16/shipper 15/item 13/product 20）、fieldType.key 组合唯一 116、key 全局唯一 92（跨域短名重复 24 符合预期）、oxfmt/oxlint 通过；**五组 116 条至此全部生成完毕**](./changelog/生成运单的字段.md)

- [收件人字段清单生成（用户贴 16 个收件人字段（FBA仓库代码→证照2）："这是收件人字段"；**两点确认**=① key 用短名（name/phone/fbaCode…），分域由 fieldType 承担 ② `fieldType: 'shipTo'`（对齐后端 shipTo 子文档，后续 shipper/item/product 同规则），另确认补上用户漏写的 customsNo 的 fieldType；背景=用户在本文件首次生成后自行给全部运单字段补 `fieldType: 'order'`、给 packType 加 `component: 'packTypeSelect'` 对接组件；实施=「收件人字段」分区新增 16 条（证照1/2=`image`、国家=`select`（国家档案存名称、与运单层 countryId 存 id 区分）、其余 input；默认 required/show 全 false）+ 补 customsNo 的 `fieldType: 'order'`；验证=node 导入 68 条（order 52/shipTo 16）key 唯一 68、type 新增 image、oxfmt/oxlint 通过）](./changelog/生成运单的字段.md)

- [order 目录新增包裹类型选择公共组件（用户："@src/components/order 在order里面封装一个包裹类型选择的公共组件"；**三项确认**=① 控件 NSelect 下拉（与 fields-config 里 packType 的 type=select 一致）② 文案走 i18n（zh-cn/en-us/app.d.ts 三处同步）③ 单文件 `components/order/pack-type-select.vue`；调研=`components/order` 为用户新建空目录、项目内无 packType 组件、后端 packType 0-包裹/1-袋子/2-文件；实施=① 组件受控封装（props `value?: number|null`/`disabled`/`clearable`/`placeholder`、emit `update:value`、选项 computed 走 i18n、占位默认「请选择包裹类型」、其余 attrs 透传 NSelect）② i18n 新增 `page.order` 段（`packTypeOption.package/bag/file` + `packTypePlaceholder`，插在 page.manage 与 page.dataManage 之间）③ `pnpm build` 自动重生成 components.d.ts 登记 `PackTypeSelect`（顺带清理 MasterData 悬空声明、补 VerticalTabLayout 声明）；验证=typecheck 0 / oxfmt / oxlint 0 / eslint 0 / BUILD SUCCESS）](./changelog/包裹类型选择公共组件.md)

- [运单字段清单生成（用户贴 52 个中文字段清单（客户→备注5）："先帮我生成运单的字段"；**三项确认**=① key 对齐后端 order 模型 Id 字段（channelId/countryId/billModeId/customTypeId/customsClearId/exportReasonId/salesTermsId）② 「类型」= packType（0-包裹/1-袋子/2-文件，uploadField 中文即「类型」）③ required/show 默认全 false；调研=`tms-user/lib/common/models/order.js` + `lib/services/order.js` 的 uploadField 中文对照表，52 字段全部对上（目的地→countryId、内单号→no、运单备注→outNote、备注1→note 无后缀）；实施=`fields-config.js`「运单字段」分区 1→52 条（select 12 / input 26 / number 8 / switch 5 / textarea 1，type 沿用 Form `FormItemType` 子集），其余 4 组注释分区保留不动；验证=node 动态 import 52 条 key 唯一 / oxfmt 单文件通过（末尾补分号）/ oxlint 0 error；文件暂无引用方）](./changelog/生成运单的字段.md)

- [MasterData 移回 views 的 modules 目录（用户："@/components/MasterData 把文件从这里移回 views 里面不需要再其他地方配置" → "你直接改名放在 modules 里面不要叫 index.vue 不就行了"；实施=① 18 个 `archives/<组>/<key>/index.vue` **先改名 PascalCase**（如 `country-region/CountryRegion.vue`）再移动，保证 views 下从未出现过 index.vue（规避 elegant-router 把 index.vue 当页面生成路由）② `src/components/MasterData` 整体 → `src/views/data-manage/modules`（22 文件，含死代码 archive-switch.vue）③ 脚本批量更新 23 个引用文件（3 页面 + 18 档案页 + data-manage service 两文件）④ `components.d.ts` 残留指向旧路径的悬空声明用临时探针文件触发 unplugin 全量重生成后清空⑤ `link.vue` 被 eslint --fix 顺手改了一处缩进（eslint 10 空格 vs oxfmt 12 空格格式分歧、提交前 fmt 会冲突）已恢复原状；菜单/路由/i18n/service 零改动；验证=typecheck 0 / lint 0 error / routes.ts 无新增条目 / `@/components/MasterData` 0 命中 / playwright 三页渲染 + SPA 跳转正常、console 0 errors）](./changelog/MasterData移回views的modules目录.md)

- [资料管理布局统一（用户截图红框标菜单「资料管理」下三个子项："资料管理也要按系统设置界面这么改，保证布局样式统一"；澄清确认=① **保留 3 个子菜单**（菜单/路由/URL 不动，只改三个页面内部布局）② 实现路径 **A：页面直用 VerticalTabLayout**（与 setting/index.vue 同款写法，ArchiveSwitch 退役保留为死代码）；实施=① `base-layout/index.vue` 的 `contentShowPadding` 排除名单加 `data-manage_basic/business/finance`（整页贴边）② 三页 `ArchiveSwitch` → `VerticalTabLayout`（title=`route.data-manage_*`、tabs 由 items 的 labelKey 映射、右侧 defineAsyncComponent 懒加载 + `:key` 重建，**去 KeepAlive/Transition** 与设置页对齐：切 tab 重新取数、无过渡动画）③ `MasterData/types.ts` 新增 `ArchiveTabItem` 类型 ④ `master-data-archive.vue` 去掉自身 `py-8px pl-16px`（避免与右内容区 p-16px 双倍留白）⑤ `VerticalTabLayout/README.md` 接入表 +3 行 + 补充「页面内不要再套 KeepAlive」（vuejs/core#11775）；验证=typecheck 0 / lint 0 error / playwright 实测（基础 5 tab、切 tab 数据切换且卡片位置稳定、业务 9 tab、财务 4 tab、SPA 跳转返回无空白、console 0 errors；左栏 220→323 贴边、右内容区 957 宽、卡片/表格距边 16px）](./changelog/资料管理布局统一.md)

- [基础配置置顶并去掉初始化数据模块（用户："基础配置提到最上面的模块，并且把初始化数据模块去掉"；**两项澄清确认**=① 移除范围 **连同代码一起删除**（modules/init-data 目录、service/api/init-data、i18n initData 段）② 默认选中 **基础配置**（与它排在最上面一致）；实施=① `setting/index.vue` tabs 重排为 6 项（基础配置置顶 → 录单/打印/导出格式 → 运单号规则 → 轨迹抓取配置）、`activeKey` 默认 `basic-config`、componentMap 移除 init-data 并按新顺序重排、`activeComponent` 兜底改 BasicConfig，`?tab=` 同步逻辑未动（旧 `?tab=init-data` 因 `tabs.some` 不匹配自动保持默认）② 删除 `modules/init-data/` 整目录 4 文件（InitData.vue / InitDataCategory.vue / init-data-form-drawer.vue / init-data-info.ts）③ `service/api/init-data/index.ts` + barrel `export * from './init-data'` 删除 ④ `mock.ts` init data 段（createInitData / 40 条数据 / initDataCnNameExists / 4 个 mock CRUD 函数）删除，customer/trace 段共用的 `todayStr`/`CURRENT_OPERATOR` 保留 ⑤ `typings/api/system-manage.d.ts` InitData 6 类型 + `typings/app.d.ts`/zh-cn.ts/en-us.ts 三处 `setting.initData` 段同步删 ⑥ 未动 `MasterDetail.vue`（死代码惯例保留）与路由；验证=typecheck exit 0 / lint 0 error / fmt 已跑 + 全项目（含 components.d.ts、router/elegant）搜 `init-data|InitData|initData` 0 命中 + playwright 实测（dev 9111：6 tab 顺序正确、默认选中基础配置并渲染表单、其余 5 tab 逐个切换渲染正常、离开后带参跳回 `?tab=trace-capture` 正常、console 0 errors）](./changelog/基础配置置顶并去掉初始化数据模块.md)

- [系统设置 tab 切换后跳转其它界面空白（用户："为什么系统设置内tab切换后跳转到其他界面会出现空的" + 截图站点管理空白；playwright 实测复现与收敛=「进出系统设置页后，任何 SPA 内跳转都空白」（页内切不切 tab 均复现、与表格无关：切「基础配置」纯表单页同样复现；与贴边页无关：打印设计正常；用户管理→站点正常）；运行时探针=main 仅剩注释、Vue `BaseTransition`（Transition 真身）subTree 为 `emptyPlaceholder` 形态（clone KeepAlive + children=null）、KeepAlive.slots 已清空 ⇒ **`state.isLeaving` 卡 true 永不复位**（afterLeave 从未执行，手动 update() 无效）；排除项=reloadFlag=true / 路由 matched 与组件均正常 / 0 console error / F5 即恢复；**根因（二次定位修订，推翻首轮结论）=`setting/index.vue` 模板「`<template>` 与根节点之间」写了一行 HTML 注释，dev 下注释被编译成注释节点 ⇒ 页面组件变多根（**Fragment**）⇒ 布局层 `<Transition mode="out-in">` 的 `setTransitionHooks` 只能挂到单个根元素、递归到 Fragment 即中断 ⇒ 卸载走 Fragment 分支 `removeFragment`（不看 `vnode.transition`、`leave` 流程从未启动）⇒ `afterLeave` 永不调用、`state.isLeaving` 卡 true ⇒ 之后任何页面只渲染空占位**；铁证=修复前页面组件 `subTree === Symbol(v-fgt)`（children=[注释, VerticalTabLayout]），修复后为单根组件节点；首轮「页内 KeepAlive」结论**已更正（用户实施的 A 方案去 KeepAlive 实测仍复现）**；修复=注释移入根组件内部（已改并 playwright 复验：离开设置页后 main 84856 正常渲染、长任务 69ms/66ms、console 0 error）；性能实测（同页主线程长任务，单 observer）=切「录单格式」**932ms**（40 行/3660 DOM 节点）> 打印格式 235ms（18 行/2373 节点）> 运单号规则 143ms > 轨迹抓取配置 86ms > 基础配置（纯表单）0ms，卡顿量级≈**行数 × 每行组件数**且被 dev（未压缩 + Vue DevTools）放大；**去掉页内 KeepAlive 使每次切 tab 重建组件、卡的频率上升，根因修复后可安全加回（待确认）**）](./changelog/系统设置tab切换后跳转空白页.md)

- [表格快速搜索栏贴边（用户："tab组件的快速搜索栏如果想要完全贴边怎么处理？"→中途更正"是表格组件"；两项澄清勾选=① 贴边程度 **B 左右+上方都贴边**（卡片顶到内容区顶部与左右边缘，表格与下方间距保持现状）② 范围 **B 做成 Table 组件可选能力**；定位=搜索卡片 16px 留白非 Table 产生、来自父容器内边距（VerticalTabLayout 右内容区 / GlobalContent 均为 p-16px，两者数值一致是负边距方案前提）；方案确认 **A**=Table 新增 `searchActionFlush` 布尔 prop（默认 false 零影响），开启时 search-action 外层加 `-mt-16px -mx-16px` 向上/左/右抵消（B 自定义偏移值把布局数值暴露给业务过度设计 / C 页面 `:deep()` 不满足组件可选能力，均否决）；改动 2 文件=table.vue 三处（Props + withDefaults 默认 false + 模板条件类与注释）+ InputFormat.vue `<Table search-action-flush>`；验证=typecheck exit 0 / read_lints 0 / playwright 实测（dev 9111）录单格式页卡片外层 class `mb-12px -mx-16px -mt-16px`、computed margin-left/-top -16px、margin-bottom 12px，卡片 left/top/right=323/40/1280 与内容区容器完全一致（宽 957，左/上/右三边贴合）+ 站点管理页回归未传 prop margin 0 零影响；**追加变更=用户"改成默认贴边"→ `searchActionFlush` 默认 false→true、录单格式页删冗余 `search-action-flush`，6 个使用页面全量实测（VerticalTabLayout 4 页卡片 left/top/right=337/40/1280 与内容区一致，site/log 卡片=页面根 220/40/1280，无重叠无溢出无报错；GlobalContent showPadding 为 class 注入页面根、与自带 p-16px 同值不叠加）**）](./changelog/表格快速搜索栏贴边.md)

- [设置页纯表格化 + 基础配置独立（用户截图设置页左侧 tab 栏："全部都要使用纯表格的形式，初始化数据里面要把基础配置提出来成为一个单独的模块"；**两次纠正**：① 首轮误把「纯表格」做成「模块内左 tab + 右表格」，用户纠正"跟录单格式一样"→ 每个 tab 页面就是**一张纯表格**、不要左侧分类 tab；② 误把「轨迹抓取配置」4 个子模块也拆成左侧 4 个 tab，用户纠正"我应该只让你把基础配置提出来吧"→ **只有基础配置需要拆出**，轨迹抓取配置撤销回原样；最终实施=① 打印格式/导出格式/初始化数据：分类并入一张表（新增「分类」列 + 搜索区分类下拉筛选 + 新增/编辑表单内可选分类），service 的 `categoryId`/`category` 改可选（不传=全部）+ `keyword` 名称模糊，cacheKey 升 v2 ② 基础配置拆出：`init-data/BasicInfoForm.vue` 迁移改名 `basic-config/BasicConfig.vue`，i18n `initData.info.*`→`basicConfig.info.*` + 新增 `basicConfig.title`，tabs 排 init-data 前（设置页 7 项）③ i18n 三处加各模块 `category` 并删 basicInfoTitle/basicInit/businessInit ④ 撤销越界：`git restore trace-capture/TraceCapture.vue` + setting/index.vue 的 tabs 回 7 项、componentMap 回普通结构 ⑤ `VerticalTabLayout` 子模块接入全部回退（仅设置页壳使用），README 已接入表同步 ⑥ lint 抓出录单格式搜索区按钮缺失致 handleReset 未使用已补回；`MasterDetail` / `InitDataCategory` 为无引用死代码（按惯例保留）；验证 typecheck 0 / lint 0 error / fmt）](./changelog/设置页纯表格化与基础配置独立.md)

- [左侧竖向 Tab 公共组件（用户："能不能封装一个公共组件左侧竖向tab右侧表格"→中途修正"不需要插槽右侧只能是表格"→最终"右侧可以是这样的也可以直接使用表格，应该直接用插槽"；方案=新增独立目录 `components/VerticalTabLayout/`（`index.vue` 组件本体 + `README.md` 使用方法文档，对齐 Table/Form 组织范式）：props `v-model:value`/`tabs:{value,label}[]`/`title`（tab 上方页面名称，竖条 `h-16px w-3px rounded-2px bg-primary` + `text-15px font-600` 复用 Form section 样式），左=贴边竖向 `NTabs placement="left" type="line"`（外包 div 挡 `.n-tabs` width:100%；tab 列表 min-h-0 overflow-y-auto），右=默认插槽 `min-w-0 min-h-0 flex-1 overflow-hidden p-16px`；接入=① setting/index.vue 整页换组件（tabs 由 {key,label} 改 {value,label}、标题 `route.system-manage_setting`、`?tab=` 同步保留）② export-format 由 MasterDetail 换组件（13 分类→`String(id)` tab、activeKey string、watch 切分类重置分页+取数、**顺手补 `@page-change` 修复原实现分页点击不生效**）；组件显式 import 保证 typecheck（不依赖 components.d.ts 自动注册声明）](./changelog/左侧竖向Tab公共组件.md)

- [录单格式改纯列表（用户："录单格式该成纯列表形式，这个直接变成新增、编辑界面"；五项确认=Drawer 900 编辑载体/勾选+批量删除+行内编辑删除/状态只读标签/抽 service DEV 内存 mock/保留名称搜索，顺带修复适用范围 wechat 有数据但表单无勾选项；实施=① 新增 `typings/api/input-format.d.ts`（Scope/FieldMappingValue/Format/List/SearchParams/CreateParams）② 新增 `service/api/input-format`（原 40 条数据搬迁 + CRUD，barrel 追加）③ `InputFormat.vue` 重写为纯列表（Table+useVxeTable cacheKey `setting-input-format`、#search-action 名称搜索、适用范围 NTag 三色、NPopconfirm 删除）④ 新增 `input-format-drawer.vue`（FormWrap 名称/状态/适用范围三项 checkbox/备注 + FieldMapping 五组 22 字段非 fill 随抽屉滚动，打开时编辑回显/新建清空）⑤ i18n 三处 inputFormat 字符串→对象 + setting/index.vue 引用改 `.title`；坑=`scopeTagTypes[s]` 因表格插槽 row 为 any 报 TS7053 → key 放宽 `Record<string, ...>`）](./changelog/录单格式改纯列表.md)

- [tab 栏添加刷新与国际化按钮（用户截图红框标 tab 栏右侧按钮区："这里把刷新和国际化都加上"；确认=只加刷新+国际化、全屏保持注释下线，顺序「国际化→主题样式→主题配置→刷新」；**仅改 `global-tab/index.vue`**：script 取消 useAppStore/refresh 注释，模板原「刷新/全屏」注释块收窄为只留 FullScreen + ThemeSchemaSwitch 前插 `LangSwitch`（:lang/:lang-options/@change-lang）+ ThemeButton 后加 `ReloadButton`（:loading="!appStore.reloadFlag"）；LangSwitch/ReloadButton 在 components/common 自动注册、按 ThemeSchemaSwitch 惯例不写 import；不加 multilingual.visible 条件（header 已下线开关无入口）；i18n 零改动（复用 icon.lang/icon.reload）；验证 typecheck 0 / read_lints 0 / 单文件 oxfmt 无改动 / playwright 连 dev(9111) 实测=4 按钮顺序+语言下拉[中文/English]切换菜单·页签·标题联动+刷新时内容区 MutationObserver removed→readded、图标 animate-spin、URL 不变、console 0 errors）](./changelog/tab栏添加刷新与国际化按钮.md)

- [系统设置页左右布局竖向tab（用户："系统设置帮我把ttab栏和内容改成左右布局tab栏改成竖向的"；方案三选一（A 自定义菜单卡片/MasterDetail 风、B 原生竖向 NTabs、C 单张大卡片内部分栏但子模块本身全高卡片会套卡片），用户选 **B 原生 NTabs placement="left"**；设计确认后仅改 setting/index.vue 模板：外层 flex-col→flex gap-8px overflow-hidden（保留用户手动加的 p-16px）、左侧 NCard 包 NTabs placement="left" type="line"、tab 项用 NTab（纯 tab 头无 pane 内容区）替代空壳 NTabPane、右侧 min-w-0 min-h-0 flex-1 KeepAlive+component 原样；script（tabs/activeKey/?tab= 同步/componentMap）零改动、子模块/i18n/路由不涉及；**随后用户反馈"右侧没有显示"→ playwright 连 dev（实际端口 9111）实测修复两处：① 根因=naive `.n-card` 自带 width:100%，直接当 flex item（basis auto→容器全宽）占满整行把 flex-1 右侧（basis 0 收缩权重 0）挤成 0 宽，修复=左侧 NCard 外包 `<div class="h-full w-fit">`（跟随 MasterDetail 范式，NCard 133px/右侧 886px）；② 连带=右侧变窄后 FieldMapping「字段映射」卡片标题被 header-extra 按钮组挤成 0 宽竖排（naive `.n-card-header__main` flex:1 1 0 可被压 0），修复=FieldMapping scoped `:deep(.n-card-header__main){flex:none!important}`（直接 :deep 特异性 0,2,0 低于 naive 三连类 0,3,0 不生效；`:deep(.n-card > …)` 因 .n-card 自身才是带 data-v 的根、不是其祖先也不匹配 → 必须 !important）；复测 6 tab 全部 886 宽+无零宽标题；验证 read_lints 0 / typecheck 0；**再迭代=用户问"左侧如果不用 card 呢"→ 澄清后选定带背景/分割线的贴边侧栏：去掉 NCard 改 `<div class="h-full w-fit shrink-0 border-r border-#e5e7eb bg-container px-8px py-8px dark:border-#2a2a2a">`（label-designer 侧栏范式）+ base-layout `contentShowPadding` 数组加 `system-manage_setting` 关掉框架注入的页面 p-16px（GlobalContent `showPadding` 机制）；实测侧栏 680 撑满/贴顶贴左贴底、右侧 927、6 tab 正常、回归 user 16px / print-design 0px 不变；**再微调=用户红框标"分割线右侧 16px 留白"要求贴边 → 右侧 div `p-16px`→`py-16px pr-16px`（去左内边距，卡片左边缘=分割线 gap=0）**）](./changelog/系统设置tab栏左右布局.md)

- [注释 Form 展开收起按钮（用户："那就先注释掉吧，暂时不需要这个了"（承接上一条 Form 组件展开收起判断逻辑的定位）；仅改 `src/components/Form/index.vue` 两处连锁注释：模板 NButton 整块包 HTML 注释 + script `toggleExpand()` 函数注释（仅按钮引用，避免 unused），**保留** `expanded`/`showToggle`（visibleFieldItems 与操作区 v-if 仍在用，expanded 默认 true 即恒全展开、行为零变化）；验证 read_lints 0 / typecheck 0）](./changelog/注释Form展开收起按钮.md)

- [Form 组件展开收起判断逻辑（用户截图角色新增抽屉红框标"收起"按钮问显示隐藏依据；定位=`src/components/Form/index.vue` 三层协同：① 按钮显示由 `showToggle = actionItems.length > 0 && totalRows > 1` 决定——actionItems 是 items 配置中带 slot 的操作项、totalRows 按 24 栅格累加 span 算行数，即"有 slot 操作项且字段超 1 行"才出现按钮；② 字段显隐由 `expanded` ref（默认 true）经 `visibleFieldItems` 控制，收起时按 span 累加只保留第一行字段，其余项不进 v-for 彻底不渲染（非 CSS 隐藏）；③ 文案图标 `expanded ? 收起+上箭头 : 展开+下箭头`（i18n common.collapseFilter/expandFilter）；纯问答零代码改动）](./changelog/Form组件展开收起判断逻辑.md)

- [角色管理对接接口（用户贴 tms-user RoleSchema 要求对接真实接口；调研 /role/query|get|create|update|delete 五接口（query 为 queryAllCommon **全量无分页**固定按 order 升序、create 必填 name 全局唯一且传 refId 时后端复制被套用角色 auths、update refId 变更时后端差集/并集合并 auths、delete **单条** deleteOne、另有无分页的 updateAuths 本轮未接）；Schema 无 status/roleCode，字段=name/roleType(0-5)/desc/auths/refId/dataAuths(0|1)/orderTemplateIds/四 Ctrl 开关/order/审计/毫秒时间戳；**范围决策=权限抽屉保持 mock 菜单树现状（后端真实权限=auths 码 + updateAuths 且无权限码查询接口），只对接基础 CRUD，权限另起设计**；实施：① typings Role 五件套重写真实 Schema（+RoleType/RoleDataAuth/RoleCtrl 联合类型，RoleQueryItem/List 保留别名）+ RoleAssignMenuParams.roleId 改 string；② role/index.ts 去 mock 接 5 真实接口 + fetchGetRoleQueryList 别名留用户模块 + 权限树两函数保留 DEV mock 分支；③ mock.ts 删角色 CRUD mock、权限树 mock 不再依赖角色数字 id；④ 列表页按 group 页范式（keyword 模糊 name + where.roleType、全量本地分页、角色类型 NTag、时间戳 dayjs、批量删逐条、cacheKey 升 v2）；⑤ 抽屉重写（name/roleType/refId 下拉排除自身/dataAuths NCheckboxGroup 插槽/四 Ctrl switch/ desc，提交 \_id）；⑥ 权限抽屉仅字段适配 roleName→name、id→_id；⑦ i18n zh/en/app.d.ts 三处同步 role 段（I18nKey 由 app.d.ts Schema 派生只改 lang 会 TS2561）；遗留=权限对接需先定权限码清单来源、orderTemplateIds 待录单格式接口；验证 typecheck 0，lint/fmt/联调待复核）](./changelog/角色管理对接接口.md)

- [侧边栏底部用户区主题适配（用户截图红框标侧栏底部"超级管理员"："这一块没有自适应主题"；根因=浅色主题 + inverted 深色侧栏时 `DarkModeContainer` 强制 `text-#1f1f1f` 深色文字，而用户区 `UserAvatar`→`ButtonIcon` 是 Naive quaternary `NButton`、文字色由浅色主题内联 CSS 变量 `--n-text-color` 控制 → 深色文字落深色背景几乎不可见，且 `border-gray-200` 在深色侧栏上是突兀亮线；修复=仅改 `global-sider/index.vue`：底部用户区按 `darkMenu` 动态切换——inverted 加 `sider-footer--inverted`（浅色文字 rgba(255,255,255,.82)，scoped `:deep(.n-button)` + `!important` 强制覆盖内联变量）+ 分隔线 `border-white/10`，非 inverted 保持原样零回归；不动公共组件；验证 read_lints 0，视觉待复核）](./changelog/侧边栏底部用户区主题适配.md)

- [用户管理对接接口（用户贴 tms-user UserSchema 要求对接真实接口；调研 /user/query|get|create|update|delete 五接口（query 为 queryCommon 分页 ret:{list,total}、fillName 回填 group/role/site 名、delete 单条且管理员/本人/有客户绑定拒删、update 改自己密码后端强制 logout、create 必填 account/password/siteId 且 account+name 唯一）+ 字段映射（account/name/roleIds[]/groupIds[]/siteId 字符串 MongoId/sex 0-1-2/entryDate·birthday 毫秒时间戳/file+fileUrl/qrCodeUrl/note/\_id）；**实施完成**：① typings User 重写真实实体 + UserSearchParams/UserCreateParams/UserUpdateParams 对齐后端（password update 缺省不改密）+ RoleQueryItem/RoleQueryList；② user/index.ts 去 mock 接 5 真实接口（flat 解包 {data,error}）；③ role api 加真实 fetchGetRoleQueryList（/role/query 全量）供角色多选；④ mock.ts 删用户三函数与 site/group 专用 fixture（教训：原文件本有同名 mock 块，插入+删除两步编辑致残留一份，靠 typecheck 抓出）；⑤ 列表页按 group 页范式改造（searchItems=keyword/siteId/status、useVxeTable api 闭包解包、真实列、createDate dayjs 格式化、批量删逐条、cacheKey 升 system-manage-user-v2、保留 TableExportAction 大 size 全量导出）；⑥ 抽屉重写（角色/组别多选走 FormWrap type:'custom' 插槽渲染 NSelect multiple、sex 0/1/2、birthday/entryDate 字符串编辑提交转 dayjs.valueOf、idCard 字符串编辑提交转 Number、编辑/详情先 /user/get 回填、create 才必填 password、update 密码留空不传）；⑦ i18n zh/en/app.d.ts 三处加 keyword/idCard/unknown/passwordEditPlaceholder 等并更新占位文案（I18nKey 由 app.d.ts Schema 派生，漏改会 TS2561）；风险=idCard 后端 Number 存 18 位丢精度待后端改 String、/user/manage/query 站点分组视图本轮不用；验证 typecheck 0 / lint 0 error / fmt 已跑，联调待用户复核）](./changelog/用户管理对接接口.md)

- [分组管理对接接口（用户贴 tms-user GroupSchema 要求对接真实接口，且**接口按目录隔离生成模块文件夹**；确认=全部按模块拆目录 + status 整体移除 + 第一版不做删除迁移；**实施完成**：① service/api 全量目录化——9 个旧扁平文件删除，改为 18 个 `<模块>/index.ts`（auth/route/user/role/menu/site/group/customer/init-data/trace-config/trace-transform/trace-keyword/operation-trace/op-log/data-manage/channel-quote/print-format/export-format），channel-quote/data-manage 的 mock 移入各自目录，barrel 逐模块 export \*，视图 22 处 import 同步（目录 index 解析使 print-format 等 4 个路径不变）；② group/index.ts 真实接口——query 为 queryAllCommon **全量无分页**（where.siteId+keyword 模糊 name）→ useVxeTable 拉全量本地分页、create/update `{name,siteId,desc}`、delete **单条 `_id`** 批量循环、另加 get；③ Group 类型重写对齐后端（\_id/name/desc/siteId/creator/updateBy/时间戳），status 全删；④ 页面/抽屉改真实字段+flat 解包 `{error}`、缓存 key 升 v2、user-operate-drawer 组别下拉同步（label:name value:\_id）；⑤ i18n zh/en/app.d.ts 三处加 creator/updateBy 删 status；验证 typecheck 0 / lint 0 error / fmt 已跑，联调待用户复核）](./changelog/分组管理对接接口.md)

## 2026-09-16

- [表格分页默认改成 50（用户："表格分页默认改成50"；现状=use-vxe-table.ts 兜底 `?? 20` + 17 处页面显式 `defaultPageSize: 20`（user/role/group/site/customer/log/渠道报价×2/master-data-archive/setting 9 子表），且 Table 分页条 `page-sizes=[50,100,200,500,1000,2000,5000]` 50 已是首项而旧默认 20 不在选项里（页长显示与下拉选中态脱节）；方案对比用户选 **A=删 17 处显式值+兜底改 50**（B=全改 50 否决，重复声明依旧）——今后调默认只改一处，page-sizes 零改动，分页 size 无 localStorage 持久化改完即生效；`hooks/common/table.ts`（模板 naive useTable）零引用死代码不动；坑=删行 old_str 带猜测 cacheKey 4 处失配重读补齐；验证 typecheck 0 / lint 0 error / fmt 已跑）](./changelog/表格分页默认改成50.md)

- [快速搜索栏加阴影并跟随主题圆角（用户："快速搜索栏能不能加点boxshaw"+"没有使用到全局border-radius"；定位=`card-wrapper` shortcut=`rd-8px shadow-sm`，其 `rd-8px` 同特异性覆盖 NCard 主题圆角（themeRadius:6 → naive common.borderRadius，与 NInput width 同覆盖机制）⇒ 固定 8px 不跟随全局；方案确认=search-action 区域包 `NCard :bordered="false" class="shadow-sm"`（不用 card-wrapper、圆角交给主题）+ `content-style 12px 16px` 收紧 padding，业务插槽内容零改动；附带说明=card-wrapper 全项目均被 rd-8px 覆盖未跟随 themeRadius，统一改 shortcut 影响面大本轮不动；验证 read_lints 0 / typecheck 0）](./changelog/快速搜索栏加阴影.md)

- [NInput 设置 width 不生效（用户："NInput class=\"w-100px\" 这个设置width怎么不生效"；根因=naive `.n-input` 根元素自带 `width:100%`，与 `.w-100px` 同特异性（单类 0,1,0）看先后，而本项目 `uno.css` 在 `plugins/assets.ts` 启动早期静态 import、naive 样式组件挂载时动态插 head 恒更晚 ⇒ 必然覆盖，非偶发；解法=内联 `:style` 或 UnoCSS important 后缀 `w-100px!` 或外包 div；适用面=所有根元素 width:100% 的 naive 组件（NSelect/NDatePicker 同理）；flex 容器内固定宽度注意 shrink-0）](./changelog/NInput设置width不生效.md)

- [表格组件 search-action 快速搜索栏插槽（用户："表格组件帮我加一个slot：search-action，配置部分快速搜索条件"；澄清关键=初版误解为 SearchBar 按钮区插槽，被纠正"跟searchBar.vue没关系，是配置在表格组件里面的单独快速搜索栏"，与 searchItems/SearchBar 完整搜索栏互不依赖、可并存可二选一；设计确认=仅改 `table.vue`，模板最顶部（SearchBar 区块前）加 `v-if="$slots['search-action']"` 区块 + slot props 暴露 `refresh`（与 operation-left 惯例一致），内容与容器样式全由业务自定义、不加默认卡片不生成默认按钮——页面不传 searchItems 即彻底无默认搜索/重置按钮（顺带回应上一条被取消的"主动隐藏操作按钮"诉求）；SearchBar 组件零改动；验证 read_lints 0 / typecheck 0）](./changelog/表格组件search-action插槽.md)

- [系统管理菜单重构与系统日志（用户："把用户管理、角色管理从权限管理移到系统管理，菜单顺序为用户：1，角色：2，分组：3，站点：4，系统设置：5，在新增一个系统日志界面，顺序为6"；澄清确认=客户管理独立成一级菜单「客户管理」下挂「客户列表」、权限管理保留只留菜单管理、日志=操作+登录合并单列表、查询=时间范围/操作类型/操作端/关键字(refNames) 4 项、数据源=真实接口 `POST /op-log/query`（tms-user 通用 queryCommon：page/size/startDate/endDate/keyword/where，默认近 12 个月，ret:{list,total}，OpLog schema 含 name/refIds/refNames/client(0-TMS 1-PC 2-PDA 3-OMS)/opType(0-登录 1-修改 2-删除 3-退出)/logs 变更明细/ip/desc/creator/createDate）；方案=Move-Item 三目录（user/role 回 system-manage、customer 出 customer-manage）+ router.ts routeIcons/routeOrders 集中调整（二级 用户1/角色2/分组3/站点4/设置5/日志6、一级对齐 routes.ts 现值 2/3+客户管理4）+ 删 routes.ts 两父条目清 .vite-temp 重生成（旧值优先坑）+ 先生成后补 i18n（I18nRouteKey 派生顺序）+ 日志页独立列表页范式（Table+useVxeTable+FormWrap 查询+明细抽屉，无 mock 分支，cacheKey system-manage-log）+ user/index.vue 2 处 $t 导出文件名换 key；**实施完成=设计获用户「可以」后执行：Move-Item 三目录迁移（user/role→system-manage、customer→customer-manage）+ router.ts routeIcons/routeOrders 重排 + routes.ts 删旧值条目（menu order3/group5/setting7）后由运行中的 dev watcher 重生成重建（新值生效：二级 1-6/权限菜单1/一级对齐 2,3+客户管理4）、touch customer 页面补齐 customer-manage 条目（New-Item+Move 的 fs 事件未被 watcher 接住）+ i18n 三处 route 段增删+opLog 段+app.d.ts 类型段+user/index.vue 2 处 $t 换 key+日志页 Table #search-action 自定义搜索卡片（SearchBar 不透传自定义 slot，dateRange 非原生控件）+fetchGetOpLogList 无 mock 分支；顺手修复 update-env-ip.mjs 既有 oxlint error（prefix→_prefix）；验证 typecheck 0/oxlint+eslint 0 error/fmt 已跑，视觉效果待用户复核**](./changelog/系统管理菜单重构与系统日志.md)

- [dev 启动自动同步 .env.test 的 IP（用户："写个脚本每次启动项目都自动更新.env.test中的VITE_SERVICE_BASE_URL的ip"；定位=后端 tms-user 跑在本机、该 URL 的 IP 即本机局域网 IPv4（192.168.2.28 实测吻合）随 DHCP 变化，本机另有 WSL 虚拟网卡 172.27.0.1 必须排除；方案对比采纳 A=独立脚本+dev 串联（B=Vite 插件与构建链耦合否决、C=predev 钩子 pnpm 默认不执行否决）；新建零依赖 `scripts/update-env-ip.mjs`：网卡名黑名单（WSL/vEthernet/VMware/Hyper-V…）+ 过滤 internal/127/169.254 + 私网段优先级 192.168>10>172.16-31 取首个候选，正则按行只替换主机段保留 scheme/端口/路径，IP 未变不写文件、找不到行只警告 exit 0 不阻断启动；`package.json` dev 改为 `node scripts/update-env-ip.mjs && vite --mode test`；不动 build:test/dev:prod/.env.prod；验证=当前 IP「无需更新」+ 临时改假值 10.255.255.1 后正确写回 192.168.2.28）](./changelog/自动更新VITE_SERVICE_BASE_URL的IP.md)

- [登录页底部区域注释（用户截图红框标密码登录表单底部："这块内容也注释一下"，即验证码登录/注册账号切换按钮+其他账号登录分割线+三快捷账号按钮；仅改 pwd-login.vue=模板三块包单个 HTML 注释块（满足 vue/html-comment-content-newline）+ script 连锁注释 loginModuleRecord 导入与 Account/accounts/handleAccountLogin；**保留** toggleLoginModule/useRouterPush（上方忘记密码按钮仍在用，首轮误注释被 vue/no-undef-properties 抓到后恢复）；验证 read_lints 0）](./changelog/登录页底部区域注释.md)

- [刷新全屏按钮先注释（用户截图红框标标签栏前两个图标："这两个可以先注释一下"；背景=上一轮刚加主题模式/主题配置按钮，右侧操作区暂变「刷新→全屏→主题模式→主题配置」，现只保留主题类两按钮；改动仅 `global-tab/index.vue`=ReloadButton+FullScreen 两行包进单个 HTML 注释块（格式满足 vue/html-comment-content-newline）+ 三级 unused 连锁串行注释：`refresh()` 函数→`const appStore` 声明→`useAppStore` import，各处标注「恢复按钮时一并取消注释」；GlobalContent 对 reloadFlag 的使用不受影响；验证 read_lints 0 / typecheck 0）](./changelog/刷新全屏按钮先注释.md)

- [tab 栏占位高度调高（用户截图红框标页签栏："这个的占位高度太低了，能不能调高一点"；**两轮更正**：第一轮误解为占位栏高度——`src/theme/settings.ts` `tab.height` 44→50（用户确认）；第二轮用户澄清"不是绿色框的占位栏、是红色框的页签本体"——chrome 页签=page-tab/chrome-tab.vue 根 div 高度由内容+py-6px 撑开仅约 36px、不随占位栏变高，背景 SVG h-full 等比拉伸不变形，`PageTab` attrs fallthrough 可业务侧透传 class 免动 packages ⇒ global-tab/index.vue `<PageTab class="h-48px">`（用户确认 48px，占位栏 50px 维持）；dev 无主题缓存即改即生效、`overrideThemeSettings` 为空无覆盖；验证 typecheck 0 ×2，视觉待复核）](./changelog/tab栏占位高度调高.md)

- [标签栏右侧加主题模式切换与主题配置入口（用户截图红框标刷新/全屏按钮处："主题设置和主题配置放这里"；背景=header 已整体下线致主题入口完全消失，ThemeDrawer 完好但被注释；澄清确认=两个按钮：主题模式切换（ThemeSchemaSwitch 三态循环）+ 主题配置抽屉入口（ThemeButton）；方案对比后采纳 A=global-tab 直接追加复用（B 移文件转全局/C 内联重写均否决）；注册方式=ThemeSchemaSwitch 在 src/components/common 自动注册直接用、ThemeButton 是 layout 局部组件需显式 import（`../global-header/components/theme-button.vue`）；改动 2 文件=global-tab/index.vue 右侧操作区变「刷新→全屏→主题模式→主题配置」+ base-layout/index.vue 取消 ThemeDrawer import/渲染两处注释（注释文案同步）；零新文件、零 i18n（tooltip 复用 icon.themeSchema/icon.themeConfig）；验证 read_lints 0 / typecheck 0，浏览器效果待复核）](./changelog/主题设置和主题配置放这里.md)

- [最后更新列只显示时间（用户："最后更新就显示时间不要其他的"；改动=site/index.vue 的 #updateDate 插槽去掉 `updateBy - ` 前缀，只渲染 dayjs 格式化时间；updateBy 字段仍在实体，日后需要可单独加列；验证 typecheck 0 / fmt 已跑）](./changelog/最后更新列只显示时间.md)

- [站点类型隐藏与总公司保护（用户："新增的时候站点类型默认为0，不要显示，并且不允许修改和删除总公司"；改动=抽屉删 siteTypeOptions 与表单 siteType 项（model 固定 0 注释说明、fillFormByRow 仍回填原值保 update 字段不丢）+ index.vue 新增 isHeadquarters 判定：操作列总公司行编辑/删除按钮 disabled（详情不受限）、批量删除改用 deletableRows=computed(checkedRows 滤掉总公司)（选中混选时只删分公司、全为总公司时按钮禁用）+ i18n placeholder 留存（表格 siteType 标签列仍用另三 key）；**过程事故**=同文件 3 处编辑并行执行触发写入冲突致批量删除 NPopconfirm 整块丢失（eslint deletableRows unused 暴露），已串行补回——再证「同一文件多处编辑必须串行」；验证 typecheck 0 / lint 0 error / fmt 已跑）](./changelog/站点类型隐藏与总公司保护.md)

- [站点管理接入真实接口（用户："站点管理开始改，去掉mock数据改成调用真实接口/site/query"；确认=范围全部 CRUD（query/create/update/delete + get），状态/站点类型筛选本轮不做，编辑先 /site/get 取数；**关键定位**=`request` 是 `createFlatRequest` 返回 `{data,error,response}` 扁平结构而非裸数据，旧 system-manage PROD 分支直接 `return request(...)` 而页面 transform 读 `r.records` 恒 undefined ⇒ 真实接口路径此前从未跑通（DEV 走 mock 掩盖），本次站点改为页面 api 闭包正确解包、transform 读 `ret.list`；实体=后端 Mongo Schema（`_id`字符串/code/name/concat/phone/workTime/note/startPlace/address/siteType(0分公司·1总公司)/毫秒时间戳，无 status 字段，返回 `{errcode,errmsg,ret:{list,total}}`）；改动=Site 类型五件套重写 + system-manage.ts 站点 4 接口去 mock 分支接 `/site/query|create|update|delete` 并新增 `/site/get` + mock.ts 删站点 mock（保留 SiteFixture 最小引用数组供组别/用户 mock 解析 siteName）+ site/index.vue 重写（keyword 单筛选、真实列、siteType NTag、时间戳 dayjs 格式化、cacheKey 换 v2 防旧列配置残留）+ site-operate-drawer.vue 重写（真实字段表单、编辑先 get 再回填、提交解包 error）+ 连带适配组别/用户 3 处站点下拉（page/size、list/name/\_id、Option<string>）+ i18n 三处换 key + `.oxlintrc.json` no-underscore-dangle allow `_id`；**过渡影响**=组别/用户 siteId 仍 number 与字符串 \_id 失配（siteName 反查空、下拉不回显），`/site/get` body `{_id}` 与 `/site/delete` body `{ids}` 为惯例推断待联调核对；验证 typecheck 0 / oxlint+eslint 0 error / fmt 已跑）](./changelog/站点管理接入真实接口.md)

- [无滚动条时隐藏滚动条端点（用户截图角色管理页："没有滚动条的时候这个能不能也不显示呢？"；现象=hover 表格时滚动条浮层两端（横向层左下/右下、纵向层右端上方）各有一个小色块，此时表格并无滑块；定位=① 用户实测鼠标移出表格色块消失 ⇒ 属滚动条浮层 ② 用户检查元素得 `.vxe-table--scroll-x-handle > .vxe-table--scroll-x-space{width:1720px}`，1720 与表格实际宽度一致 ⇒ 列宽总和==容器宽、无横向溢出（据此排除"四角补块 corner"——vxe 与项目 CSS 均已是 display:none）③ 真因=vxe 给两条 handle 设的是 `overflow-x/y: scroll`（`vxe-table/es/table/style.css:2540-2557`），scroll 语义为"无论是否溢出都渲染滚动条"，无滑块时浏览器仍画出轨道端点 ④ 真溢出场景不受影响；修复=`scrollbar.css` 新增 0.4 节：`.vxe-table .vxe-table--scroll-x-handle { overflow-x: auto !important }` + `.vxe-table .vxe-table--scroll-y-handle { overflow-y: auto !important }`（auto=溢出才渲染，真滚动时滑块照常出现，vxe 的 overflowX/overflowY 判定基于 scrollXWidth 与主区宽度、与此无关）；与上一处修复互补=handle 宽度补偿消除"伪滑块"、overflow auto 消除"无滑块时的轨道残留"；验证 read_lints 无诊断 / 纯 CSS 无类型影响，视觉效果待用户复核）](./changelog/无横向滚动时隐藏滚动条端点.md)

- [表格不需要横向滚动也显示滚动条修复（用户截图："为什么不需要横向滚动也会显示横向滚动条呢？"；定位=① `x: { visible: true }` 传的是布尔值，vxe `calcScrollbar()` 只识别 `'visible'/'hidden'/false` → 不命中任何分支、走自动判定，本页列宽由 `autoCellWidth()` 按主区宽度分配（无宽度列走 remainList 均分兜底）→ `overflowX=false`，vxe 内联写的是 `visibility: hidden` ② `scrollbar.css` 的 `.vxe-table:hover .vxe-table--scroll-x-virtual { visibility: visible !important }` 覆盖内联把它显示出来（刻意保留的 hover 浮现交互）③ 滑块必然出现的真因=handle 恒有 16px 伪溢出：JS 内联轨道宽 `el.clientWidth - osbWidth` 扣掉 8px 纵向滚动条宽（本项目纵向滚动条已改绝对定位浮动、不占布局，主区是容器满宽）+ handle 自身 `scrollbar-width: thin` 滚动条占位 8px，而填充 `--scroll-x-space` 宽 = `scrollXWidth` = 容器满宽 → 可视区恒少 16px → 滑块长度≈98% ④ 浮动化后 `.vxe-table--scroll-x-virtual`（absolute 无 width，子元素又全是 absolute）宽度塌成 0，handle 的 `width:100%` 失去基准；修复（用户确认「必须保留 hover 模式」+ 选运行时测量）=`src/plugins/vxe-table.ts` 新增 `measureScrollbarSize()`（离屏探针 `overflow:scroll;scrollbar-width:thin` 实测占位写入 `:root --x-scrollbar-size`；写死 8px 在 thin 非 8px 的环境会残留偏差致滑块复现）+ `src/styles/css/scrollbar.css` 三条：x-virtual `width:100%!important`、x-wrapper `width:100%!important`（覆盖 JS 的「容器宽−osbWidth」）、x-handle `width: calc(100% + var(--x-scrollbar-size, 8px))!important`（只补滚动条自身占位）；效果=handle 可视区==容器宽==space 宽 → 不溢出、无滑块，真溢出时滑块仍按比例出现可拖动；取舍=轨道右端外溢 sbw 不裁剪（与 Naive 浮动滚动条观感一致）、全局生效、只改横向（纵向 space 宽仅 1px 且 overflow-x:hidden，无此偏差）；验证 oxlint 0 error / read_lints 0 / typecheck exit 0，浏览器效果待用户复核）](./changelog/不需要横向滚动也显示横向滚动条修复.md)

## 2026-09-15

- [图片上传控件选图后不显示预览修复（用户截图："上传后看不到也不能预览"；根因=naive `NUpload` 在 `default-upload=false` 时选择文件生成 `{status:'pending', url:null, thumbnailUrl:null}`（Upload.mjs:442-453），而 image-card 缩略图/预览按钮判定条件是「`status==='finished'` 且 mergedThumbnailUrl 非空」（UploadFile.mjs:117-129/282）、预览弹层用 `file.url`（:300）；`FormWrap.handleUploadChange` 原仅在 `last.url` 已是 blob 时才记录预览 → url 恒空、只显示占位卡片；修复=新增 `resolveUploadPreview()`（已有地址优先，否则 `URL.createObjectURL(file.file)`）并改写 `handleUploadChange` 写回 `uploadPreview[key]` + 换图 revoke 旧 blob + `onBeforeUnmount` 兜底回收；受益面=所有 image/file 控件（公司 LOGO、用户管理微信二维码/附件等）；`uploadFileList`/模板/校验零改动；验证 typecheck 0 / 单文件 oxfmt+oxlint+eslint 0）](./changelog/图片上传预览修复.md)

- [基础信息初始化公司 LOGO 改用上传组件（用户："基础信息初始化的公司logo要使用上传组件"；现状=「系统设置 → 初始化数据 → 基本信息初始化」的公司 LOGO 为文本输入框（placeholder「LOGO 图片 URL」）；改动=仅 `BasicInfoForm.vue` 的 `companyLogo` 字段 `type: 'input'` → `type: 'image'` 并删除 placeholder；复用 `FormWrap` 既有 image 控件（NUpload + image-card + max 1 + default-upload false，与用户管理微信二维码同款，本地选择即 blob 预览、model 存文件名，后端就绪后接真实上传）；`FormWrap`/i18n/保存逻辑零改动；验证 typecheck 0 / 单文件 oxfmt+oxlint+eslint 0）](./changelog/公司logo使用上传组件.md)

- [按钮文案统一为「新增」（用户："按钮都不要叫添加一行都要叫新增"；背景=「初始化数据」与「轨迹抓取配置 → 轨迹改造 / 轨迹关键词」工具栏按钮此前沿用原型用语「添加一行」；改动=仅改两个 i18n key 的值（key 名 `addRow` 与使用点零改动）：zh-cn 的 `page.manage.setting.traceCapture.addRow` / `page.manage.setting.initData.addRow` 由「添加一行」→「新增」，en-us 同两 key 由 `Add Row` → `Add`；生效按钮 3 处=InitDataCategory / TraceTransformTable / TraceKeywordTable，新值与既有 `common.add`（新增 / Add）一致；不动组件引用、key 命名与 `typings/app.d.ts`；验证 typecheck 0 / 两 i18n 文件 oxfmt+oxlint 0 / 全项目检索「添加一行|Add Row」零残留）](./changelog/按钮文案统一新增.md)

- [抓取时间模块先注释隐藏（用户："抓取时间模块先注释一下"；原因=该子 tab 字段未定、先从前端隐藏；改动=仅 `TraceCapture.vue` 的 `configTabs` 注释掉 `capture-time` 一项 + 中文注释说明恢复方式（tab 栏 5→4 项：追踪网络/轨迹改造/轨迹关键词/操作轨迹）、数组上方注释同步为「3 个子页」；不改 `TraceCaptureCategory`/`SubTabKey`（保留 capture-time 成员便于恢复）、`TraceConfigTable`（categoryTabKey 映射保留）、i18n `subTab.captureTime`、mock 数据；验证 typecheck 0 / 单文件 oxfmt+oxlint+eslint 0）](./changelog/抓取时间模块先注释.md)

- [轨迹关键词字段改造（用户附原型截图："这是轨迹关键词的字段"+运单状态下拉图；确认=① 只改「轨迹关键词」子 tab、其余 4 tab 不动 ② 使用范围按初版枚举（全局通用/指定站点/指定客户，只存枚举值不联动）③ 运单状态按截图（转运中/已送达/异常件/已退件，必填）④ 关键词组=自由文本中文逗号分隔 ⑤ 操作列=编辑+删除二次确认（省略号按原型示意）；截图字段=规则名称/使用范围/关键词组/运单状态/启用状态/最后编辑/编辑时间+操作；方案=类型新增 `TraceKeywordScope`/`TraceKeywordWaybillStatus`/`TraceKeywordItem/List/SearchParams/Create|UpdateParams`（enabled 用 EnableStatus、create 不含审计）→ 新增 `TraceKeywordTable.vue`（cacheKey trace-keyword；列=序号/规则名称/使用范围/关键词组/运单状态/启用状态 NTag/最后编辑/编辑时间/操作）/`TraceKeywordDrawer.vue`（NFormWrap：规则名称+关键词组必填、使用范围 select 默认 global、运单状态 select 默认 in-transit、启用 switch 默认 1）→ `TraceCapture.vue` configTabs 循环加 `track-keyword` 分支 → `mock.ts` 2 条截图数据（安达配送完成/规则）+ 4 个 mock 函数（create/update 自动填审计 lastEditor/editTime）→ `system-manage.ts` 4 个 fetch*TraceKeyword（DEV mock，PROD `/system/trace-keyword/*`，删除 ids:number[]）→ i18n 三处同步（col/form 新键 + scopeOption + waybillStatusOption）；不改其余 4 tab](./changelog/轨迹关键词字段改造.md)

- [轨迹改造字段改造（用户附原型截图："这是轨迹改造的字段"+第二图时间格式下拉；澄清 4 问=① 只改「轨迹改造」子 tab，其余 4 个 tab 保持现状 ② 交互沿用抽屉表单范式（工具栏「添加一行」→抽屉新增，行内「编辑/删除」+NPopconfirm 二次确认）③「抓取轨迹关键词判断定义」为自由文本、中文逗号分隔 ④「排序」=表格自动序号 show-seq、不落库；截图字段=排序/状态名称/时间格式（下拉三选一：年月日·年-月-日 时分·年-月-日 时分:秒）/服务地点/详细描述/抓取轨迹关键词判断定义 + 8 条异常状态示例；方案=新增 `TraceTransformItem/List/SearchParams/Create|UpdateParams` + `TraceTransformTimeFormat`（枚举 key 存值、i18n 展示）→ 新增 `TraceTransformTable.vue`（useVxeTable+Table，cacheKey trace-transform）/`TraceTransformDrawer.vue`（NFormWrap：状态名称必填、时间格式 select 默认 ymd-hm、其余可空）→ `TraceCapture.vue` configTabs 循环内加 `t.key==='track-transform'` 分支渲染 → `mock.ts` 8 条截图数据 + 4 个 mock 函数 → `system-manage.ts` 4 个 fetch\*TraceTransform（DEV mock `as unknown as`，PROD `/system/trace-transform/...`，删除 ids:number[]）→ i18n 三处同步（col.statusName/keywordDefinition、form 同名字段+Placeholder、addRow、timeFormatOption.{ymd,ymdHm,ymdHms}）；不做勾选列/批量删除/审计字段、不动其余 4 tab](./changelog/轨迹改造字段改造.md)

- [系统设置两个子模块改名（用户附截图要求将「通知配置」「站点扫描配置」改为「轨迹抓取配置」「操作轨迹配置」；确认=同步改显示名+目录+组件名+变量名+i18n key，英文命名 trace-capture/operation-trace；改动=移动并重命名目录 modules/notification-config→modules/trace-capture（NotificationConfig.vue→TraceCapture.vue）、modules/station-scan→modules/operation-trace（StationScan.vue→OperationTrace.vue，git 识别为 R 保留历史）+ setting/index.vue 改 import/tabs/componentMap（每模块3处）+ 两组件 list-title 中文改「轨迹抓取配置列表」「操作轨迹配置列表」+ zh-cn/en-us 的 notificationConfig→traceCapture(轨迹抓取配置/Trace Capture Config)、stationScan→operationTrace(操作轨迹配置/Operation Trace Config) + typings/app.d.ts i18n 类型两 key 同步；不改其它5个tab、不改 docs/superpowers 与 changelog 历史文档；验证：read_lints 6 文件 0 错误）](./changelog/系统设置通知配置站点扫描配置改名.md)

- [数据初始化界面（用户附截图："数据初始化界面按这个开发"；定位=「系统设置 → 初始化数据」标签页 `setting/modules/init-data/InitData.vue` 当前为占位组件（左侧列表+「敬请期待」）；澄清四问确认：① 在现有初始化数据标签页内替换 InitData.vue（不动路由）；② 四标签页（渠道类别/承运网络/计泡规则/操作配置）先用统一默认结构（序号/中文名称/英文名称/备注/操作 编辑·删除 + 添加一行）；③ 数据先本地 mock（与 site/customer 一致）；④ 顶部「基本信息」区域只读展示（LOGO 占位）；方案 A=通用表格子组件 `InitDataCategory.vue`（接收 category prop 区分四类 mock+cacheKey）+ 基本信息只读 NCard + `init-data-form-drawer.vue`（NFormWrap 中文名/英文名必填、备注 textarea）+ `init-data-info.ts`（基本信息静态 mock+分类枚举）；改动=InitData.vue 重写 + 新增 3 文件 + `typings/api/system-manage.d.ts` 增 InitDataItem/List/SearchParams/Create|UpdateParams/InitDataCategory + `mock.ts` 四类数组与四个 mock 函数 + `system-manage.ts` 四个 fetch（DEV 走 mock `as unknown as`）+ i18n 三处同步（zh-cn/en-us 的 `page.manage.setting.initData` 增 info.\* / channel|network|bubble|operation / cnName|enName|remark 及占位）；验证 typecheck/lint/build；不手改 elegant 生成文件、不新增路由无需 gen）](./changelog/数据初始化界面.md)

- [渠道报价菜单图标缺失修复（用户："渠道报价怎么不加图标"；定位=菜单图标由 `build/plugins/router.ts` 的 `onRouteMetaGen→routeIcons` 写入生成 `routes.ts` 的 `meta.icon`，新增渠道报价模块时漏登记 `channel-quote`/`channel-quote_receive`/`channel-quote_ship` 三键，致 meta 无 icon、菜单回退默认占位三条横线；确认=① 图标用推荐 `ic` 组：`channel-quote`=`ic:baseline-sell`/`channel-quote_receive`=`ic:baseline-move-to-inbox`/`channel-quote_ship`=`ic:baseline-local-shipping` ② 顺序不变（channel-quote order:1 首页之后第一）；改动=手写 `build/plugins/router.ts` 的 `routeIcons` 补三行 + 清 `.vite-temp` + `pnpm build:test` 重生成 `routes.ts`（禁止手改生成文件）；验证 typecheck 0；**顺带发现**：elegant 重生成对已存在 meta 字段「旧值优先」（data-manage 旧 order:1 未被 routeOrders 的 2 覆盖），仅新字段按配置补，故改已有路由 icon/order 须先删对应条目再重生成）](./changelog/渠道报价菜单图标.md)

- [轨迹抓取配置：子 tab 与表格融合（用户附两张截图对比："要跟后面的这样，前面这样还是太丑了"；澄清两轮排除工具栏/勾选列/删除方向（用户："跟工具栏没关系"），确认诉求 = **把 tab 栏和表格融合在一起** —— 同一张卡片、tab 下仅一条分隔线、无灰色间隙，5 个子页全部生效；根因=`TraceCapture.vue` 用独立 `NCard`（padding 8px + mb-16px）单独装 5 个子 tab、`NTabPane` 全空占位、表格由外层 `v-if` 渲染在另一个 div，两块之间露 16px 灰色页面背景；方案=单文件重构为「单卡片 + 全高 NTabs」（照搬初始化数据页 `InitData.vue` 已验证结构）：NCard(h-full/padding 0/flex column) → 内容容器(flex-1 min-h-0 overflow-hidden px-16px py-16px) → NTabs(line) → 各 NTabPane 内直接渲染表格 + 全高 CSS 三件套（.trace-capture-tabs flex column / .n-tabs-pane-wrapper flex:1 / .n-tab-pane height:100%）；前 4 个子页拆 `configTabs`（key 即 TraceCaptureCategory）+ 操作轨迹独立 pane，避开模板里 `t.key as Api.X` 断言（eslint `vue/no-undef-properties` 对模板 `Api.` 误报的已知坑）；不改 TraceConfigTable/OperationTraceTable、service/mock/i18n、设置页外层 segment tab 间隙；验证 typecheck 0 / oxfmt+oxlint+eslint 单文件 0 / build successful）](./changelog/轨迹抓取配置tab与表格融合.md)

## 2026-09-14

- [渠道报价模块-收货/发货渠道（用户："新增渠道报价-收货渠道、发货渠道模块"；确认=① 两个独立一级路由（各自独立页面、互不切换、不套 MasterDataArchive 通用组件）② 新建独立一级模块 channel-quote（与 data-manage 平级）③ 先通用骨架 code/name/status/remark、业务字段后续补；方案=建 `views/channel-quote/{receive,ship}/index.vue` 两个自包含列表页（直接用 Table+useVxeTable+Drawer+NFormWrap），共用 `service/api/channel-quote.ts`（fetchGet/Create/Update/DeleteChannelQuote，按 archive key `receive|ship` 区分，DEV mock / PROD `/channel-quote/:archive/xxx`）+ `mock-channel-quote.ts`（factories 造数）+ `typings/api/channel-quote.d.ts`；i18n 三处同步（zh-cn/en-us route 加 channel-quote/\_receive/\_ship、page.channelQuote；app.d.ts Schema 加 channelQuote）；路由由 `pnpm dev` 自动生成；验证 typecheck/lint/fmt/build）](./changelog/渠道报价模块.md)

- [表格虚拟滚动阈值 200 行（用户："表格组件行数超过200条才使用虚拟滚动"；澄清=列表 NSelect >15 选项部分本次不做，只改表格；方案=给封装的 `Table` 组件 `<vxe-table>` 加 `scroll-y` 门控 `{ enabled: true, gt: virtualScrollRowThreshold }`，`gt` 表示行数超过阈值才启用纵向虚拟滚动、否则普通渲染（避免小数据量虚拟滚动开销且支持动态行高）；新增 props `virtualScrollRowThreshold` 默认 200 供页面覆盖；类型用 `VxeTablePropTypes.ScrollY`（非 `ScrollYConfig`，当前 vxe-pc-ui@4.17.19 无后者）；改动=table.vue 三处（Props + computed + 模板绑定）；验证 typecheck 0 / lint 0 / fmt 已跑，无浏览器端到端）](./changelog/表格虚拟滚动阈值200行.md)

- [权限管理独立顶级菜单重组（用户："把用户管理、角色管理单独提到权限管理文件夹下面，新建一个文件夹"；澄清三问=①位置：建 `views/permission-manage/` 作顶级菜单（与系统管理同级）；②菜单：新增「权限管理」父级菜单分组，user/role 作子菜单；③范围：用户/角色/菜单一并归入（权限三件套）；方案=整体移动三个页面目录（含 modules/）到 `views/permission-manage/{user,role,menu}`（git 识别为 R 重命名、保留历史）；路由 meta 集中在 `build/plugins/router.ts` 的 `onRouteMetaGen`：routeIcons 移除 system-manage 的 user/role/menu、新增 `permission-manage`(`ic:round-lock`)/`permission-manage_user`(`ic:round-person`)/`permission-manage_role`(`ic:round-supervisor-account`)/`permission-manage_menu`(`ic:baseline-menu`)，routeOrders 同移且 `permission-manage:3`（顺序=首页0/资料管理1/系统管理2/权限管理3）、子项 1/2/3 保持原相对序；i18n 的 zh-cn/en-us `route` 段删除 system-manage_user/role/menu、新增 permission-manage 及三子项；`user/index.vue` 两处 `$t('route.system-manage_user')`→`permission-manage_user`；严禁手改 `src/router/elegant/**`，由 `pnpm dev` 重新生成（routes.ts/imports.ts/transform.ts/elegant-router.d.ts/components.d.ts 自动加入 permission-manage 并移除 system-manage 三项、RouteKey 类型同步）；`mock.ts` 的 system-manage 演示目录节点属 mock 数据未动；验证：pnpm dev 重新生成成功、路由父级带 icon/order、定向 eslint 0 error、typecheck 仅余与本次无关的 data-manage 既有 i18n 类型错误）](./changelog/权限管理菜单重组.md)

- [系统管理新增客户管理模块（用户："系统管理新增客户管理模块"；确认=system-manage 独立菜单页，与 site/group 同构、DEV mock 先行、不采用 MasterData 归档；字段=基础客户档案：客户编号(唯一查重)/客户名称(必填)/客户等级(普通·重要·VIP 静态)/客户来源(官网·转介绍·广告 静态)/联系人/电话/邮箱/地址/状态/备注+审计；改动=新建 customer 页面+抽屉 + Customer 五件套类型 + mock 数据/4接口(customerCodeExists 查重) + 路由 icon `ic:round-business`/order 6 + i18n + 按钮权限 `system:customer:*`(permission `system:customer:list` routePath `/system-manage/customer`) + MENU_TREE/menus 菜单 id 27；生成前清 `.vite-temp`；验证 typecheck/lint/fmt/build）](./changelog/系统管理新增客户管理模块.md)

- [业务资料子模块改造（用户要求将「业务资料」子模块由仓库/库位/承运商/门店 4 个**完全替换**为 9 个：单号资料管理/地址簿管理/申报物品/问题类别/物品类别/报关类型/出口原因/清关方式/销售条款；确认：① 沿用「基础资料」——`data-manage_business` 单路由 + 页内 `ArchiveSwitch` 子导航（子档案不生成路由/菜单）；② 删 4 旧、新增 9；③ 先按通用字段「编码+名称+状态+备注」实现、后续补；④ 继续 DEV mock；改动=删 4 目录 + 建 9 个 `archives/business/<dir>/index.vue` + `data-manage/business/index.vue` 的 items 改 9 + `data-manage.d.ts` 删 4 接口/改联合体/加 9 接口 + `mock-data-manage.ts` factories 删 4 加 9 + i18n 三处（business 段抽通用键 code/name/form.\*、9 模块各加 .title、删旧 4）；路由无关不跑 gen-route；验证 typecheck 0 / 单文件 lint 0，无浏览器端到端）](./changelog/业务资料子模块改造.md)

- [财务资料子模块调整（用户截图要求「财务资料」改为 费用类型 / 结算方式 / 银行账户 / 结算币种及汇率 4 个子模块；现状为单页 + 左侧 `ArchiveSwitch` 切换，原 4 子模块=结算账户/币种/税率/结算方式）；设计确认（用户："按你的工作字段先实现一下"）= 移除「税率(tax)」新增「费用类型(expense-type)」（字段 code+name+状态+备注），顺序严格按截图，标题文案 account「结算账户→银行账户」、currency「币种→结算币种及汇率」；改动=`finance/index.vue` 重排 items + 删 `tax/index.vue` 新建 `expense-type/index.vue` + `data-manage.d.ts`（`tax`→`expense-type`、FinanceTax→FinanceExpenseType{code;name}）+ `mock-data-manage.ts` 工厂 + zh-cn/en-us `finance.tax`→`expenseType` 及 account/currency 标题 + `typings/app.d.ts` i18n 类型 + `typings/components.d.ts` 自动生成 `Tax`→`ExpenseType` 引用（删组件后悬空，下次 `pnpm dev` 覆盖）；按钮权限 `dataFinance` 整页共用不变；验证：typecheck 过滤 finance/expense/tax/Finance 0 错误；仓库 `basic/*` 一批 pre-existing i18n 类型缺失错误与本次无关未处理）](./changelog/财务资料子模块调整.md)

- [基础资料子模块改造（用户截图要求「基础资料」改为国家地区 / 邮政路由码 / FBA仓库 / 客户等级 / 客户来源 共 5 个子模块；当前 `data-manage/basic` 为单页 + 左侧子导航 4 档案客户/供应商/商品/商品分类；待确认：① 展示形式（独立路由菜单 vs 页内导航）② 是否完全替换旧档案 ③ 数据接口与字段），详情见 `changelog/基础资料子模块改造.md`）](./changelog/基础资料子模块改造.md)

- [详情态隐藏表单反馈区（用户："查看详情的时候，显示错误信息的那个地方能不能隐藏？"；所指 = 每个 `NFormItem` 底部反馈区（`.n-form-item-feedback-wrapper`：正常时空白占位、出错时红字）；核查 naive 源码：`FormItem.mjs` 第 460 行 `mergedShowFeedback ? h(wrapper) : null`（false 时整块不渲染）+ `form-item.cssr.mjs` 第 104-107 行 wrapper `min-height: var(--n-feedback-height)`（**无错误也占一行高度**）+ `utils.mjs` 第 95-102 行 `NFormItem.showFeedback ?? NForm.showFeedback ?? true`；确认：**仅 view 态隐藏**（详情态 `mergedRules` 恒为 `{}`、反馈区永远空白，零副作用；编辑态保留错误红字显示位、避免行高跳动）；改动=`src/components/Form/index.vue` 模板 `NFormItem` 加 `:show-feedback="!isView"` + 注释；验证：typecheck 0 / oxfmt+oxlint+eslint 单文件 0，无浏览器端到端；**追加（用户："有点太紧凑了，能不能加10px"）**：view 态行距 +10px —— `NGrid` 加 `:y-gap="isView ? 10 : 0"`（naive `Grid.mjs` `rowGap: pxfy(...)` = CSS `row-gap`，只在行间生效、末行下方不多余；备选「保留 feedback 占位压到 `min-height:10px`」因末行也会多 10px 未采用），编辑态 `y-gap` 仍 0）](./changelog/详情态隐藏表单反馈区.md)
- [详情表单默认标签位置（用户："查看详情的时候 form 组件默认这样显示：label-placement=\"left\" label-width=\"auto\"；现状=`FormWrap` 的 `labelPlacement` 静态默认 `'top'`、5 处详情调用点都没显式传，故详情态标签在顶部；澄清确认：① **方案 A**——组件内部按 `mode` 决定默认值（view → left、edit → top），父级显式传值优先；② 宽度用 `auto`（每个表单项各自算宽度、多列允许参差）；改动=`src/components/Form/index.vue` 3 处（props 默认值 `labelPlacement: 'top'` → `undefined` + 注释、新增 `resolvedLabelPlacement` computed、模板 `:label-placement` 改绑）；生效面=user/site/group/role 四抽屉 + PrintFormat 的 view 态自动左标签，`MasterData/master-data-archive.vue` 显式传 `top` 不受影响；验证：typecheck 0 / oxfmt+oxlint+eslint 单文件 0，无浏览器端到端）](./changelog/详情表单默认标签位置.md)
- [列设置「重置」不生效修复（用户附列设置弹窗截图："列设置点重置，为什么没有更新为当前界面的表格最新配置？"；定位=三处断点：① `table-column-config.vue` 的 `handleReset()` 只是把 `props.columns`（正是表格当前在用的配置）重新 clone 回本地副本 + `emit('reset')`，值相同故弹窗与表格都无变化；② 9 个调用点只写了 `@confirm="persistColumns"`、无人监听 `@reset`，事件被丢弃；③ `useVxeTable.resetColumns()`（恢复 `options.columns()` 默认 + 清 `localStorage['vxe-table-column:<cacheKey>']`）全项目零调用、属死代码；确认方案=语义 A「点击即生效」（立即恢复页面默认列配置 + 清缓存，弹窗内列表与背后表格同步、关掉弹窗不回滚） + 显式接线（各调用点解构 `resetColumns` 并补 `@reset`，与 `@confirm` 对称，不用 provide/inject）；改动=table-column-config.vue（handleReset 收敛为「清 pendingDrag + emit('reset')」+ 新增 `watch(() => props.columns)` 在弹窗打开期间同步本地副本）+ 9 个调用点各 2 行（user/site/group/role/menu + setting 下 waybill-rule/print-format/export-format + MasterData 壳），`use-vxe-table.ts` 零改动；已知取舍=重置后再点「确认」会把等于默认的配置写回缓存（等价、无功能差异）；验证：typecheck / lint）](./changelog/列设置重置不生效.md)
- [详情态表单改为文本展示（用户附用户管理「详情」抽屉截图："如果是查看详情，form 表单能不能直接显示为文本不显示这种禁用效果"；澄清确认：① **直接改封装的 `FormWrap` 组件**（用户："能不能直接改封装的 form 组件？"）；② 空值统一显示 `-`；③ 密码保持脱敏 `••••••`；设计=`FormWrap` 新增 `mode?: 'edit' | 'view'`（默认 edit，**与既有 `disabled` 正交**），view 态沿用同一套 `FormItemConfig`/24 栅格/`section` 分区/label 位置、只把值区域换成文本（不渲染任何控件、`mergedRules` 返回 `{}` 故不校验也不显示必填星号）；值映射 `viewText()`：select 用 options 反查 label（`3`→普通用户）、switch 取 checkedText/uncheckedText（缺省回退 i18n `common.enable`/`common.disable`）、checkbox 按 `、` 拼接、password 固定 `••••••`、image 值是 URL 时 `NImage` 缩略图否则文件名、color 色块+色值、icon-picker 复用 `IconRenderer`、custom 仍走插槽；空值判定 `null/undefined/''/空数组`（0、false 为有效值）、灰色占位；改动=Form/index.vue（`mode` + isView/isEmptyValue/isUrlValue/optionLabel/viewText/viewTextClass，模板拆 isView/else 两支、原控件链零改动）+ 5 处调用点 `:disabled` → `:mode="isDetail ? 'view' : 'edit'"`（user/site/group/role 四个抽屉 + PrintFormat 的 `drawerMode === 'view'`）+ `components.d.ts` 自动补 `NImage` 登记（生成产物）；**有意不动** InputFormat 的 `:disabled="!isEditing"`（紧邻 FieldMapping 仍是禁用态、单独改会风格割裂）；验证：typecheck 0 / oxlint 0 / eslint 0 / oxfmt 已跑，无浏览器端到端）](./changelog/详情态表单文本展示.md)
- [分配权限界面隐藏三列（用户附「角色分配权限」抽屉截图红框圈出「菜单类型 / 路由地址 / 权限标识」三列："分配权限界面不显示这些"；改动=`role-permission-drawer.vue` 的 columns 由 5 列缩为 2 列（菜单名称 + 操作权限）+ 删对应自定义插槽 `#menuType/#routePath/#permission` + 删仅其使用的 `menuTypeTag/menuTypeLabel`；行数据字段（routePath/permission 仍供按钮匹配与「首页必选」判定）与搜索匹配保留；验证：typecheck 0 / oxlint 0 / eslint 0 / fmt 已跑）](./changelog/分配权限界面隐藏三列.md)
- [去掉「部门管理」显示（用户附「角色分配权限」抽屉截图红框圈出「部门管理」行："去掉部门管理显示"；该行无路由/权限/操作列——`views/system-manage/dept/` 空目录、页面从未实现；改动=mock.ts 的 MENU_TREE 删 `{id:24,title:'部门管理'}` + ALL_MENU_IDS 去掉 24（角色默认勾选自动同步）；menus 里的 dept 记录与 button-permissions 的 dept 条目已由用户自行删除（核对无残留）；验证：typecheck 0 / oxlint 0 / 全项目检索 `dept|部门管理` 仅剩 `depth` 误匹配）](./changelog/去掉部门管理显示.md)
- [用户管理字段更新（用户附表单截图："用户管理的字段更新为这些"；截图为两区块表单——基本信息（用户账号/用户名称/用户角色/用户密码/所属站点/所属组别/状态）+ 个人档案（姓名/联系电话/职位/性别/邮箱/入职时间/出生日期/微信/附件/家庭住址/其他联系方式/备注/微信二维码）；澄清确认：① 附件与微信二维码=给 FormWrap 新增 file/image 控件（NUpload 封装，本地选择保留文件名/预览，后端就绪后接入）；② 密码编辑也回显且必填；③ 表格列同步更新为关键 8 列；④ 字段映射按设计表；方案=`FormItemType` 新增 `section`（区块标题行）/`date`（NDatePicker，yyyy-MM-dd）/`password`/`file`/`image`；User 类型扩至 20+ 字段（移除 userPhone/userEmail/role，改 contactPhone/email/roleId，roleName/siteName/groupName 由 mock 解析）；新增 `mockUpdateUser`；抽屉重构为两区块三列、站点/角色/组别下拉全量选项）](./changelog/用户管理字段更新.md)
- [Form 组件下拉框支持搜索（用户："form 组件里面的下拉框需要可以搜索"；设计确认：**默认开启、可单独关闭**——`FormItemConfig` 新增 `filterable?: boolean`，NSelect 绑定 `:filterable="item.filterable ?? true"`；生效范围=所有 FormWrap 表单 + 表格搜索栏（SearchBar 复用同组件，天然生效）；Naive 默认按 label 模糊匹配）](./changelog/Form下拉框支持搜索.md)
- [系统管理新增组别管理模块（用户："新增组别管理模块"，样例=组别名称 岳阳 / 组别备注 上海销售二组 / 创建 测试 - 2026-07-15 / 所属站点 上海 / 最后更新 测试 - 2026-07-15；澄清确认：① 所属站点=下拉关联站点管理（存 siteId，siteName 由数据层按 siteId 实时解析，站点改名后同步）；② 要状态（启用/禁用）；③ 组别名称 mock 内查重（重复抛「组别名称已存在」）；方案=与站点管理完全同构（useVxeTable + Table + modules/ 抽屉），搜索=名称/所属站点/状态，表格「创建」「最后更新」两列均按「人名 - 日期」插槽渲染；改动=新建 group 页面与抽屉 + Group 五件套类型 + mock 数据与 4 接口（含站点名解析）+ 路由 icon/order + i18n + 按钮权限 `system:group:*` + menus/MENU_TREE 补菜单 id 26；生成路由前先清 `.vite-temp`（吸取站点管理踩坑经验，一次生成到位：icon `ic:round-groups` / 文件完整）；**追加：按用户调整后的 order 重建**（site=4 / group=5 / label-designer=6 / setting=7——生成器保留已存在条目旧 meta，处理=从 routes.ts 删这 4 个条目→清 .vite-temp→重跑 dev；meta、定制段、其它路由均不受影响，最终菜单顺序=用户→角色→菜单→站点→组别→系统设置）；验证：typecheck 0 / oxlint 0 / eslint(.vue) 0 / fmt 已跑，无浏览器端到端）](./changelog/系统管理新增组别管理模块.md)
- [系统管理新增站点管理模块（用户："系统管理新增站点管理模块"；澄清确认：① 系统管理下独立菜单页与用户/角色/菜单管理平级 `views/system-manage/site/index.vue`；② 后端未就绪先走 DEV mock；③ 字段按用户样例（站点编号 75501 / 深圳 / 测试 / 18621663103 / 工作时间 / 默认出发地 / 仓库地址 / 备注 / 最后更新「测试 - 2026-07-04」）+ 状态（启用/禁用）；④ 电话不校验格式；⑤ 站点编号 mock 内查重（重复抛「站点编号已存在」）；方案=照抄 system-manage 现有 CRUD 范式（useVxeTable + Table + modules/ 抽屉），不采用 MasterData 通用归档组件（其接口写死 data-manage 归档体系）；改动=新建 site 页面与抽屉 + Site 五件套类型 + mock 数据与 4 接口 + 路由 icon/order + i18n + 按钮权限 `system:site:*` + menus/MENU_TREE 补菜单；**⚠️ elegant-router 生成踩坑**：① 改 `build/plugins/router.ts` 后须先删 `node_modules/.vite-temp` 再跑 dev，否则用缓存配置生成（site 无 icon/order、setting order 旧值）；② 生成器对已存在条目保留旧 meta（配置改 order 不生效）、仅新条目按配置写;③ order 与已有值重复会顺移（配置 site:5 撞 label-designer:5 → 实际生成 order:6，已把配置对齐为 6）；④ 一次生成把 routes.ts 末行写坏（重复残片）→ 下次 dev 启动 Babel `MissingSemicolon (229:31)` 失败，修复=git checkout 该文件+清 .vite-temp+重跑 dev；验证：typecheck 0 / oxlint 0 / eslint(.vue) 0 / fmt 已跑，无浏览器端到端）](./changelog/系统管理新增站点管理模块.md)
- [打印格式「设计」跳自研标签设计器 + 隐藏菜单入口（用户："打印格式点设计-跳转到新增手写的标签设计界面，把菜单隐藏"；追问确认「把菜单隐藏」= **隐藏菜单里的「标签设计」入口**（不是进入设计器时收起侧边栏）；补充要求"进入标签设计，也要保留「系统设置」高亮"；方案=① `PrintFormat.vue` `openDesign` 路由 key 改 `system-manage_label-designer`、query 收敛 `{id,name}`（categoryId/paperSize 不传，纸张取详情单一来源）；② `label-designer/index.vue` 读 `route.query.id` 直接 `loadTemplate(id)`（无 id 保留「分类首个模板」兜底）、工具栏标题 computed → `currentName` ref（详情回填 name）；③ `build/plugins/router.ts` `hideInMenuRoutes` 加该键 + 新增 `menuActiveKeys` 写 `meta.activeMenu='system-manage_setting'`（**菜单选中 = hideInMenu ? activeMenu : name，故必须二者同时给**）；④ `pnpm dev` 触发路由重新生成（`gen-route` 仍是交互式"新建路由"向导、`customRoutes` 经源码核对只生成类型声明不做 meta 合并 → 均不可用）；⚠️ **生成前把 `routeOrders` 的 role/user 对齐为 2/1**，否则会冲掉工作区 `routes.ts` 里未提交的「用户在前」顺序；⚠️ 生成器会保留配置中已删的 meta 旧值（icon/order），故两者保留在配置中保持「配置=生成结果」；验证：typecheck 0 / 3 文件 oxfmt+oxlint 0 / eslint 0 error / 临时 dev(9113) 已关）**追加（同日，用户："点返回要自动跳转到打印格式的 tab"）**：设计器「返回」由 `router.back()` 改 `routerPushByKey('system-manage_setting', { query: { tab: 'print-format' } })`（用户选方案 A；根因＝设置页 `syncTabFromQuery()` 早已支持 `?tab=` 但返回从未带上该 query、且该路由无 keepAlive → 返回后重挂载落回默认分页「录单格式」；备选 B「切分页写 URL」未采用；设置页零改动）；验证 typecheck 0 / 单文件 oxfmt+oxlint+eslint 0）](./changelog/打印格式设计跳转标签设计器.md)
- [字段示例内容兜底（用户截图红框圈出「日期 date」："我把这个改成空内容，拖出来也是空的"；现状=`onDrop` 的 `payload.sample ?? payload.field` 兜不住**空字符串** → sample 清空后拖出空白；方案=`core/constant.ts` 新增 `fieldSampleOf()` 统一兜底——文本/长文本/二维码回退 `title||label||key`（中文名优先）、**条码回退 `key`**（Code128 不支持中文），`field-panel.vue` 预览构建 + dragstart payload 两处共用（预览与落纸同源）；tsx 实测 date→「日期」/ orderNoCode→「orderNoCode」/ qty→「100」；单文件 lint 0 / typecheck 0；**⚠️ 同日已按用户要求撤销**（用户报告「点击元素直接卡死」后要求撤销该批改动：`constant.ts` 删 `fieldSampleOf`、`field-panel.vue` 两处恢复 `f.sample`、`design-canvas.vue` 恢复 `??`；保留基础元素示例））](./changelog/字段示例内容兜底.md)
- [基础元素补默认示例值（用户截图圈出「文本/长文本/图片」："这三个能不能加个示例，拖出来都是空的，或者如果是空内容，能不能按之前那样显示呢？"；现状=三者 `sample: ''` 拖出即空白，条码/二维码本就有示例；方案=与条码/二维码对齐补 `sample`：文本「文本示例」/ 长文本「长文本示例，可输入多行内容」/ 图片用内嵌 SVG 占位图常量 `IMAGE_PLACEHOLDER_SRC`（`data:image/svg+xml` 纯 ASCII、`#`→`%23`）；**不采用 placeholder 占位方案**（与真实内容无视觉区分、打印端需隔离、且导出格式 placeholder 有独立语义）；tsx 实测五类基础元素 `resolveDisplayText` 全非空；单文件 lint 0 / typecheck 0）](./changelog/基础元素默认示例值.md)
- [左侧条目加拖拽把手图标（用户附参考截图："每个元素前面能不能像这个一样加个拖拽的图标？"；`panels/field-panel.vue` 两组条目（业务字段/基础元素）文字前加 `icon-mdi-drag`（沿用 `table-column-config.vue` 既有先例，六点把手 + `cursor-move` 语义）；容器改 `flex items-center`、图标 `mr-6px shrink-0 text-14px text-#c0c4cc dark:text-#555`；**整条仍可拖**（图标仅视觉提示）；单文件 oxfmt+oxlint+eslint 0 / typecheck 0；**二次调整（用户：图标看不清楚 + hover 加 border 类似截图）**：图标 `text-#c0c4cc` → `text-#8c8c8c dark:text-#999`，容器加 `group` + `hover:border-primary dark:hover:border-primary`（暗黑单独写保证特异性胜出）+ 条目名 `group-hover:text-primary` + `transition-colors`；**三次修正（用户对比截图："跟前面的效果不一样"— hover 文字未变蓝）**：`group-hover:text-primary` 与 `text-#999`/`dark:text-#777` 叠加未能稳定生效（项目内无 group-hover 先例、UnoCSS 变体优先级/源顺序不可控）→ 改用 scoped CSS `.field-entry:hover .entry-title { color: rgb(var(--primary-color, 32 128 240)) }`（特异性 (0,4,0) 稳定胜出、与 `text-primary` 同源口径））](./changelog/左侧条目加拖拽把手图标.md)
- [选中态 z-index 拦截重叠点击（用户："矩形置于最底层，然后选中了矩形，再点中间的元素还是无法选中"；根因=`design-canvas.vue` `.is-selected { z-index: 10 }` 把选中元素提升到所有元素之上——矩形透明背景（`bgColor:'transparent'`）视觉可见但 hit-test 优先命中它 → 反复选中矩形、文本永远选不中；修复=删除该 z-index（附防复发注释），命中恢复 DOM 堆叠顺序；`.resize-handle { z-index:5 }` 为定位子元素（父不创建堆叠上下文）不受影响仍可拖；**教训=选中态不能提升 z-index**；验证：单文件 oxfmt+oxlint+eslint 0 + typecheck 0；浏览器端到端因无后端（登录接口 500）不可达，临时 dev(9112)/`.playwright-cli` 产物已清理、src/router 零 diff）](./changelog/选中态z-index拦截重叠点击.md)
- [左侧拖元素光标改十字箭头（用户："左侧拖元素能不能使用十字剪头？不要使用手"；`panels/field-panel.vue` 两组条目 `cursor-grab`（张开手）+ `active:cursor-grabbing`（握拳）→ `cursor-move`（四向箭头），删按压态变体；画布平移的 `grab`/`grabbing` 保持不动（不同语义）；单文件 oxfmt+oxlint+eslint 0 / typecheck 0）](./changelog/左侧拖拽光标改十字箭头.md)
- [标签设计器字段类型可修改（用户截图圈出属性面板「字段类型」："这个类型需要能修改"；现状=该项 `disabled` 只读展示、store 无切换能力；设计确认：① 保留共有 + 内容互迁（共有配置保留、`text` ↔ `value` 自动互迁、新类型独有键补默认）；② 尺寸重置为新类型默认（位置 x/y 不变）；③ 限 5 种数据类（矩形/线条不参与）；实现=store 新增 `updateElementType`（`SWITCH_CONTENT_KEY` 内容互迁表 + `EXTRA_OPTION_KEYS` 兜底 `placeholder`，合并顺序 migrated > kept > defaults，入撤销栈）+ 面板 `fieldTypeDisplay` → `fieldTypeSelect`（UI-only、存 ElementType 值、5 选项复用既有 i18n 键），`watch(model)` 拦截类型变化后 `buildModel()` 重建受控值；关键约束=迁移后只能有一个内容键（`resolveDisplayText` 按键优先级取，残留空 text 会遮蔽 value）；**追加修正（用户截图：切换到文本类型高度还是有问题）**：切到 `text` 时高度按内容自适应——新增 `canvas/measure-element.ts` 离屏实测（`createApp` 挂 `ElementRenderer`、与 drag-preview 同口径：宽=元素宽 / height auto / 单行下限），异步实测后只写回 height（不额外入撤销栈，undo 一步回退；longText 与拖入行为一致不实测））](./changelog/标签设计器字段类型可修改.md)
- [文本类型落纸尺寸：宽 50mm + 高度按内容自适应（用户："文本类型高度按内容自适应宽度定为50mm"；方案=① `defaultSize('text')` 宽 40→50mm、8mm 高度降为兜底/最小；② 预览容器文本类型 `height:auto` + `minHeight=默认高度`（空内容不塌 0、实测天然带底线），drag-ghost 在 nextTick 后实测 `offsetHeight/PX_PER_PT` 存会话并导出 `getPreviewHeight()`；③ `addElement(desc, at, size?)` 新增可选尺寸覆盖（不传行为不变）；④ `onDrop` 经 `textDropSize()` 给文本落纸传 `{width:50mm, height:实测}`，拿不到回退默认；typecheck 0 / 4 文件 0 error 0 warning；**同日修正**（用户："这不没什么变化吗"）：文本预览 `minHeight` 由「类型默认高度 8mm」改为「单行高度 `fontSize × lineHeight`（≈3.4mm，缺省 8pt/1.2）」—— 原下限比单行内容还高、把元素撑到 8mm 掩盖了自适应；空内容仍不塌 0）](./changelog/文本类型高度自适应宽度50mm.md)
- [按住即显示真实渲染预览（用户："还有这个能不能做，鼠标按住就直接显示？"；设计确认：按下即显示 / 真实渲染（条码图+编码值、文本「标题: 值」）/ 点击不拖立即消失 / 不加装饰；架构=`drag-ghost.ts` 重写为**会话模型**（pointerdown 建立 → dragstart 接管 → pointerup/dragend 收尾）+ 新增 `drag-preview.vue`（Teleport body，复用画布 `ElementRenderer` 1:1 真实渲染，位置直写 DOM）+ store 导出 `defaultOptionsFor`/`defaultSize` 保证预览与落纸元素同源；关键坑=HTML5 拖拽期间 pointerup 不触发，dragstart 必须摘掉点击判定监听否则残留误触发（pointercancel 兜底）；typecheck 0 / 4 文件 oxfmt+oxlint+eslint 0）](./changelog/按住即显示真实渲染预览.md)
- [拖入元素按预览位置创建（用户："能不能拖进来是在什么位就显示在什么位置，不要以鼠标为起始点画"；根因=`onDrop` 把鼠标落点当元素左上角、而预览是「跟手偏移」定位，两者差一个抓取偏移导致松手瞬间元素跳位；方案=`drag-ghost` 新增导出 `getDragOffset()`（drop 早于 dragend，state 仍在），`onDrop` 按「预览左上角 = 指针 − 抓取偏移」换算坐标并 `Math.max(0,…)` 钳纸张左上边界；落点有效性判定不变；注意元素与预览尺寸不同、是左上角对齐非完全重合；typecheck 0 / oxfmt+oxlint+eslint 0）](./changelog/拖入元素按预览位置创建.md)
- [标签设计拖拽预览看不清（用户："从左侧拖出来，为什么看不清"；定位=浏览器 drag image 默认用源条目快照（白底+浅灰字 #999/#bbb+浅灰边框）再叠半透明，落灰底画布上对比度极低；已与用户确认排除误诊——浅色框是拖动中的预览（松手即消失）、拖到纸张内元素能正常创建；方案（用户选 A）=`dataTransfer.setDragImage()` 自定义预览卡片：Teleport 到 body 的离屏常驻 div（避免 transform 祖先使 fixed 退化）、白底深字+主色描边+阴影、dragstart 同步写 textContent（setDragImage 只在 dragstart 内生效，Vue 响应式来不及）+ 强读 offsetWidth 触发重排、光标居中；drag-ghost.ts 注释同步；typecheck 0 / 2 文件 oxfmt+oxlint+eslint 0；备注=本轮临时起的 dev 实例(9112)与 Playwright 脚本已清理，无后端故未做端到端验证；**同日回退**（用户："不要自定义了，左侧还是改成透明背景吧"）——撤销自定义预览恢复原生 drag image + 左侧面板移除 `bg-container` 改回透明（右侧保持白底）；**同日三改（用户对比截图："为什么鼠标松开就是正常的，鼠标按住是这样的"）**：根因=原生影子被浏览器强制半透明 vs 松手后幻影不透明、两套机制观感不一 → 重构 `drag-ghost.ts` 统一为「跟手预览」：1×1 透明图顶掉原生影子 + dragstart 克隆源项做跟手预览（初始位置与源项重合零跳变）+ dragover 更新 left/top + dragend 同一元素原地转场（淡出/回位），`createGhost` 删除；样式与面板条目天然一致，非自定义卡片）](./changelog/标签设计拖拽预览看不清.md)
- [标签设计器左右两侧面板背景改白（用户："左右两侧的背景能不能做成白色的"；现状=左 260px 字段面板/右 300px 属性面板容器无背景、透出页面 layout 底色 rgb(247,250,252) 发灰；方案=用主题 token 类 `bg-container`（浅色=纯白 rgb(255,255,255)、暗黑自动 rgb(28,28,28)，无需 dark: 变体，项目先例 dark-mode-container.vue）给两个侧栏容器加背景；分隔线/中间画布不动；typecheck 0 / 单文件 oxfmt+oxlint+eslint 0）](./changelog/标签设计器左右两侧背景改白.md)
- [标签设计器水平垂直对齐并排一行（用户截图："水平对齐和垂直对齐放在同一行"；根因=样式卡两个对齐是 span12 标准项，「关联标题」开启多出 3 个标题样式项使前序累计 84≡12(mod24)，水平对齐落行尾、垂直对齐被挤下一行；`titleStyleItems` 为奇数项导致 title 开/关奇偶翻转，纯排序无法两全（差 36≡12 mod24）；方案=把两个对齐合并为一个 span24 的 custom item（`alignGroup`），slot 内嵌套两个 NFormItem（各带 label、样式天然一致）+ NSelect，外层 `label:''`（naive `!labelText` 不渲染 label 且挂 --no-label，已核对源码），选项抽 alignOptions/valignOptions，「行高」移到对齐组后使 title 开启时整卡满行；已知取舍=title 关闭时「字重」半行留空；typecheck 0 / 单文件 oxfmt+oxlint+eslint 0；**追加修正（用户：为什么上下间距没有统一）**：对齐组上下间距偏大 → 根因是 naive `mergedShowLabel`/`mergedShowFeedback` 默认 true（空 label 仍留一行 label 高度、嵌套 NFormItem feedback 占位叠加双倍），修法=FormWrap 新增 `showLabel?: boolean` 透传 `:show-label` + alignGroup 显式 `showLabel:false` + 内层 `:show-feedback="false"`）](./changelog/水平对齐和垂直对齐放同一行.md)
- [标签对齐拆分与字重中文化（用户截图：属性面板「对齐」拆为水平/垂直两个下拉 + 对齐/字重选项中文；**已实施**：① `types.ts` 新增 `TextOptions.verticalAlign?: 'top'|'middle'|'bottom'`（缺省 top，取值对齐导出格式 `style.verticalAlign` 的 CSS 语义、直传不映射）；② 渲染两端（`canvas/element-renderer.vue` + `render/render-element-html.ts`）文本分支改「外层 flex column 定纵向（justify-content）+ 内层 100% 宽 div 保持标题行内前缀与内容同一文本流」，`vAlignToJustify()` 映射收敛在 `render/render-utils.ts`，缺省 `?? top`；③ `export-format.ts` 导出真实值（原写死 'top'）+ 回读新增 `pickVAlign`，旧数据/缺字段兜底 top；④ 属性面板样式项重排（字号|颜色、字重|行高、水平对齐|垂直对齐、显示边框），字重/对齐选项中文走 i18n（新增 `propAlignH`/`propAlignV`/`weightNormal`/`weightBold`/`alignLeft|alignCenter|alignRight`/`valignTop|valignMiddle|valignBottom`，删旧 `propAlign`，zh/en/typings 三处同步），buildModel 按类型兜底受控 `verticalAlign`；⑤ 范围仅文本/长文本，条码/二维码/图片/矩形线条不动；验证：typecheck 通过、`propAlign` 0 残留、tsx 实测打印三分支输出 `justify-content:flex-start/center/flex-end` 且标题内容同文本流、导出→纯 style 回读 `bottom` 往返一致 + 缺字段兜底 top）](./changelog/标签对齐拆分与字重中文化.md)
- [录单格式字段映射必填配置（用户："不只是配置是否显示也要配置是否必填"；设计已确认：① 数据结构 `Record<string, string[]>` → `Record<string, { show, required }>`，两数组独立不自动联动；② UI 每字段两个复选框（显示 / 必填），必填框在未勾显示时置灰禁用、已勾状态保留可见（"必填但不显示"只可能来自历史数据）；③ 分组一键全选按钮完全不变、仍只操作 show 数组；④ 地址/公司 span 4→8 容纳双框；i18n 新增 `fieldMappingRequired` 三处同步；**二次调整（用户截图反馈双复选框太丑）**：改「复选框 + 必填星标」——未勾显示不渲染星标（数据保留）、★主题红/☆灰点击切换 + NTooltip、星标固定宽防抖动、默认 span 8→6 一行 4 个；**三次调整（用户：星号太小不好点 + 勾显示要默认必填）**：星标放大 16px + 24×24 固定热区（hover 浅灰底/变红）、`toggleField` 勾选显示时自动并入 required（取消显示不清除）；**四次微调**：显示复选框补 `NTooltip`（复用 `common.show`，与星标「必填」tooltip 对称）；**五次修正**：分组一键全选同步默认设为必填（`toggleGroup` 合并 required，取消全选不清除））](./changelog/录单格式字段映射必填配置.md)
- [标签设计器目录整理（modules 下 16 个文件平铺混放 → 按职责拆分 core/render/canvas/panels 四组；方案 A + 只移动不重命名，零逻辑改动、29 条相对 import 改写；**已完成**：`git mv` 全 R 状态保留历史、typecheck 通过、路由零 diff 双重确认（源码级 `pagePatterns` 只匹配 index.vue/[param].vue + 实测启动 dev 后 router/typings 无 diff）；附带产出模块中文 `README.md`（使用方法/目录结构/关键约定/扩展指南/可维护性）；备注=property-panel.vue 用户先前未提交的 3 处 propTestData 文案改动原样保留）](./changelog/标签设计器目录整理.md)

## 2026-09-13

- [标签设计画布平移缺少抓手光标（用户报「标签拖动的时候鼠标没有显示拖动的样式」，澄清后=**拖整块画布平移**时无抓手手势；根因：`design-canvas.vue` 的 `.label-designer-canvas`（即 `viewportRef`）样式块只有 `touch-action:none`、**完全没有 `cursor` 规则**，平移链路只管坐标不动光标；对照组=姊妹页 `print-design/index.vue` 同款平移有 `cursor: grab` + `.panning{cursor:grabbing}`，label-designer 漏了；**已实施（用户确认方案 1-4 全做）**：① 容器 `cursor: grab`；② `.label-designer-canvas.panning, .label-designer-canvas.panning * { cursor: grabbing !important }`——`cursor` 是每元素独立生效、继承值一律输给元素自身声明，平移途中指针会滑到纸张/元素/手柄上，不带 `*` + !important 会出现「握拳↔箭头↔缩放」来回闪；③ `panning` 类命令式 `viewportRef.value.classList.add/remove`（`onViewportPointerDown`/`onPanUp`，类名抽 `PANNING_CLASS` 常量），与现有「平移高频帧只直写 DOM」写法一致、不为光标触发画布响应式重渲染，并补 `pointercancel` 监听 + `onBeforeUnmount` 兜底摘类；④ 语义错位顺带修：容器变手掌后元素会继承手掌、但拖元素是「移动」，故 `.element-item { cursor: move }`（`.resize-handle` 自带缩放光标不受影响）；验证：typecheck 通过、改动文件 oxlint+eslint 0/0、`pnpm fmt` 后 `git diff --stat` 仅 design-canvas.vue 24 行新增）](./changelog/标签设计画布平移缺少抓手光标.md)
- [提交被precommit与commitmsg钩子拦截修复（用户报「提交不了」：① `pnpm typecheck` 报 TS7016 缺 rimraf 类型——手工铺的 hoisted node_modules 里 rimraf 实际是 2.7.1，而 `packages/scripts` 声明 6.1.3（v6 才自带类型、且 `cleanup.ts` 用的是具名导出），`npm pack rimraf@6.1.3` 解包拷入 `packages/scripts/node_modules/rimraf` 且**不动根目录 v2**（避免影响其他按 v2 API 使用的依赖），d.ts 内对 glob 的引用由 `--skipLibCheck` 兜住；② commit-msg 的 `pnpm sa` 报 ERR_UNKNOWN_FILE_EXTENSION——`bin.ts` shebang 是 `#!/usr/bin/env tsx`，但 `.bin` 下 `sa`/`sa.cmd`/`sa.ps1` 三个 shim 都被生成成 node 直跑 `.ts`，改为经 `node ../tsx/dist/cli.mjs` 启动；③ 中文提交信息改用 `git commit -F .git/COMMIT_MSG_TMP.txt` 传入以规避 Windows 终端编码；最终 `cbafcd6` 完整通过 typecheck→lint→fmt→git diff→commit-msg 全链；**追记｜彻底修复**：改 `.bin` shim 只是治标——shim 被依赖工具重新生成后又复发（`sa.cmd` 变回 `node bin.ts`），故把 `packages/scripts` 的 `bin.sa` 由 `./bin.ts`（`#!/usr/bin/env tsx` shebang）改为新增的 `./bin.mjs`（纯 JS：`require.resolve('tsx/cli')` + `spawnSync` 委托执行 `src/index.ts`，规避 tsx 以 CJS 模式加载 TS 时的 `Cannot find module './commands'`），删除 `bin.ts`、同步拷入**真实拷贝形态**的 `node_modules/@sa/scripts`、三个 shim 改为指向 `bin.mjs`——此后无论 shim 怎么生成都正确；rimraf 侧因落在真实目录 `packages/scripts/node_modules/rimraf` 亦不会被重装清掉）](./changelog/提交被precommit与commitmsg钩子拦截修复.md)
- [标签设计左侧面板改用组件库滚动条（用户："不要使用系统滚动条，使用组件库的滚动条"——上一步基础元素改单列后面板变高露出系统滚动条；先核查范围：同页右侧 property-panel 早已用 NScrollbar、画布视口 overflow-hidden、预览弹窗刻意隐藏滚动条、姊妹页 print-design 左侧面板也是 NScrollbar，**全页仅左侧 field-panel 一处系统滚动条**；改动=根节点 `div.h-full.overflow-auto.p-12px` → `<NScrollbar class="h-full">` + 内层 `div.p-12px`（padding 放内层让滚动条贴右缘，与右侧面板同款），其余内容零改动；naive 内部容器仍原生滚动、仅 CSS 隐藏系统滚动条，故滚轮/触控/HTML5 拖拽边缘自动滚动行为不变；Playwright 实测：全页 .n-scrollbar=1 且父级为左侧面板容器、container `overflow-y:scroll` + `scrollbar-width:none`、799/843 可滚、hover 后 rail opacity=1 且 rail.right=475≈面板右缘 480、滑块 height=761；左下角小三角经 elementFromPoint 确认是 Vue DevTools 悬浮按钮与本次无关；另备注 index.vue 左侧面板宽度已被用户自行改为 w-260px）](./changelog/标签设计左侧面板改用组件库滚动条.md)
- [标签设计基础元素改单列并下移（用户截图要求左侧面板「基础元素」从两列网格+居中改为与「业务字段」同款（单列整行、左对齐圆角描边条目），并把整个基础元素区块移到最下面；**已实施（用户确认文案样式取 A：左对齐+灰色 text-#999）**：`field-panel.vue` 仅模板改动——两个 NCollapseItem 顺序对调（fields 在前）+ `default-expanded-names` 同步 `['fields','basic']` + 容器 `grid grid-cols-2` → `flex flex-col gap-8px` + 条目去 `text-center`、类名与字段条目逐字对齐、文案包 `text-#999 dark:text-#777`；未动 draggable/onDragStart/startDragGhost/i18n/清单；`scripts/panel-shot.cjs` 截图+DOM dump 核对：分组顺序为业务字段→基础元素，条目 `textAlign=start`，两组均单列整行灰字同款描边，与需求截图一致）](./changelog/标签设计基础元素改单列并下移.md)
- [标签设计面板拖拽未落纸回到原位动画（**需求澄清/认错**：用户截图确认「需要回到原位的动画」指的是**从左侧面板拖出来、没落进纸张的那一项**要回到它在面板里的原位，此前实现的 `replayBack` 是画布内已有元素出界回弹，认错了需求；根因=原生 HTML5 影子由浏览器绘制销毁、JS 无法动画，且画布 `onDrop` 对灰底所有落点都 `preventDefault()` → 浏览器判定拖放已接受、松手直接抹掉影子不播放回位；**已实施方案 A**：新增 `modules/drag-ghost.ts`——`dragstart` 记源项与抓取偏移，拖拽期间以 capture 阶段临时接管 document 级 dragover/drop（仅对 `application/x-label` preventDefault，令任何落点都算「已接受」，浏览器就不会自己再播一次回位动画），`dragend` 时 `cloneNode` 出幻影摆到松手位置 → 未创建元素则 rAF 补间（首帧 rAF 时间戳为 0 点）平移回源项 rect、已创建则 120ms 淡出；画布 `onDrop` 两个成功分支调 `markDragCreated()`；曲线/时长抽到 `constant.ts`（`BACK_MS=260` + `backRemain` cubic ease-in-out）；**探针踩坑**：`backRemain` 返回「剩余比例」而幻影要「已走完比例」，首版直接用导致幻影第二帧跳到原位再倒着飘回落点，改 `1 - backRemain(t)` 才对；**同时按用户 Q2 恢复画布 move 纸界钳制**：恢复 `clampInPaper`、删除 `outAmount`/`replayBack`/`stopBack`/`backRaf`/`backDom` 与临时屏显徽标 `debugInfo`，保留 `DragState.lastGeo` 作为结算权威；Playwright 帧级实测：幻影 79 帧内从 `(1172,399)` 平移回源项 `(232,200)` 且终点重合、无残留节点、落纸外不创建元素；画布元素拖向纸外时右边缘精确停在纸张右边缘、松手不跳变）](./changelog/标签设计面板拖拽未落纸回到原位动画.md)

- [标签设计单位规范（用户口径：纸张尺寸必须 mm、元素「位置与尺寸」用 pt，其余项暂不动；核对后当前实现已完全合规——纸张侧 PAPER_SIZES/parsePaper/PX_PER_MM/预览/导出 paper 全 mm，元素几何存储+画布+面板(已带 (pt) 后缀)+打印导出全 pt，无 mm/pt 混用；**零业务代码变更**）；追记：`DragState.paperW/paperH` 的「纸张尺寸（pt…）」注释易误读为纸张改单位，实为 `paperPt()` 经 mmToPt 得到的**换算值**（仅与 pt 元素几何同单位比较钳纸界），注释已改写澄清）](./changelog/标签设计单位规范.md)
- [标签设计元素拖拽越界回到原位动画（**⚠️ 方向已认错更正**：用户截图澄清「回到原位的动画」指的是左侧面板被拖项，本文 `replayBack`/`outAmount` 链路已整段删除、画布 move 恢复纸界钳制，仅保留「CSS transition 无效 / rAF 补间时间轴基线」两个结论；用户否掉「每帧硬钳制」手感：move 去掉实时钳制自由出界，松手越界则本次移动作废+动画回到拖动前位置（不落库/不记撤销），`transition: transform 180ms` + 强制 reflow 起步 + transitionend 清理 + begin 清残留 transition；删 clampInPaper 改 outAmount 越界量判定（元素超纸的轴不计、按「比拖动前更差才算越界」以允许把溢出元素往里拖）；resize 仍保留纸界钳制不在本次范围）；追代：手测「松手瞬间跳回、看不到动画」——内联 transition + 强制 reflow 写法在本项目不产生动画（根因未定性：elementStyle 不含 transform、Vue style patch 不碰 transform、全局无 transition:none !important），按用户方案改 rAF 手动补间（同步钉起始位移 → 每帧 translate(dx\*(1-t)³)、220ms、backRaf/backDom + stopBack 生命周期，replayBack/begin 两处取消），彻底不依赖 transition 与 reflow；追代 2：手感仍报「松手直接消失、无回位动画」——根因是动画时钟 0 点取错（`performance.now()` 墙钟 − rAF 首帧时间戳），pointerup 任务里的属性面板重渲染把首帧推迟，首帧 t 已跨过 20%~100%，低帧率下直接 t>=1 当场收尾（CSS transition 版同理：计时 0 点是 `void offsetWidth` 提交样式的时刻），改为以「首个 rAF 帧时间戳」为基线 + `t` 夹 [0,1] + `k` 上限锁 1，动画必播满 BACK_MS 且位移只向原位单调收缩；追代 3：仍报「每次都一样」，改用 Playwright 帧级实测（`scripts/drag-probe.cjs`，系统 Chrome + 本机后端 LINFLY/123）证明补间每帧都在写 DOM（78 帧 / 260ms、transform 逐帧插值到原位），根因是机器上留了 4 个 dev server 僵尸实例 + `FSWatcher` 崩溃后 Vite 模块缓存永不失效，浏览器一直在跑旧模块——已清掉只留一个干净实例；同时把结算/回弹起点改为「最后一帧真正写进 DOM 的几何」`lastGeo`（不再依赖 pointerup 坐标），回弹曲线由 ease-out 改 cubic ease-in-out、220→260ms（ease-out 前 3 帧就走完 61% 距离，观感等同瞬移））](./changelog/标签设计元素拖拽越界回到原位动画.md)
- [标签设计元素拖拽越出右下边界（move 只钳下界 + resizeGeometry 无纸界约束 → 拖拽/8向缩放可越出右/下、w/n 手柄还能把 x/y 推负；新增 clampInPaper 位置贴边钳制、resize 上限取 max(原尺寸,纸界余量) 不强制缩小、rAF 预览与落库同源；onDrop 落点不在纸张内则不创建）](./changelog/标签设计元素拖拽越出右下边界.md)
- [安装依赖报错→IDE内无法启动（终案：CodeBuddy 文件过滤层禁止一切 junction/symlink 穿透读取，pnpm 链接模型在 IDE 内不可用；解法=.npmrc 加 node-linker=hoisted + 手动删悬空 junction + @sa 包真实拷入 node_modules + 补齐 rolldown/oxc binding 的 package.json；IDE 内 vite 启动验证通过。装依赖起服务仍建议 IDE 外）](./changelog/安装依赖报错.md)

## 2026-09-12

- [标签预览突出显示（iframe 白纸贴白卡无对比；**最终定稿：弹窗宽高零约束完全随纸张；naive 遮罩层默认 fixed+overflow:visible 不可滚（超高直接溢出视口），覆盖 wrapper overflow-y:auto+隐藏滚动条视觉、padding 40px 放 .n-modal-scroll-content**——超高时整个弹窗滚轮上滚，滚到底 footer 下留 40px 空白，bootstrap/hiprint 模式；教训：naive 类名横/双下混用必须查源码、`flex:1 1 0` 在自适应高度容器压扁内容](./changelog/标签预览突出显示.md)
- [标签设计拖出元素尺寸过小（非代码 bug：Vite HMR 半新半旧——.vue 组件热替换生效、store 模块未重载，裸 50×15 被 pt 渲染；整页刷新即恢复；教训：store+组件同批改动后先刷新再排查）](./changelog/标签设计拖出元素尺寸过小.md)
- [标签设计拖放落点跳到上方（pt 迁移漏改：onDrop 落点按 mm 算出被当 pt 存，坐标缩小 2.83 倍；同类修复 moveSelected 钳制 mm/pt 混减、duplicate/paste 偏移 mmToPt(4)；教训——单位迁移须按 parsePaper 数据流核对而非字面量搜索）](./changelog/标签设计拖放落点跳到上方.md)
- [标签设计元素几何单位mm改pt（x/y/width/height 全链路 pt，纸张保留 mm；designJson 加 unit 字段旧数据自动 ×2.8346 迁移，默认尺寸/最小边/方向键步长按物理尺寸等比换算手感不变）](./changelog/标签设计元素几何单位mm改pt.md)
- [标签设计designJson目标格式改造（B方案：存储整体换 unit:pt+paper+key/label/style 目标格式，原生字段以独立 options 子对象全保留；export-format.ts 序列化边界转换，loadFromJson 三输入兼容（目标/旧原生 mm 迁移/非法），几何 pt 直传适配并行 pt 化；保存成功行为改 window.open Blob URL 新标签预览 JSON；typecheck+oxlint 过）](./changelog/标签设计保存导出JSON.md)
- [标签设计默认字体大小改为8（store 新建默认 text 12→8、barcode 10→8、titleFontSize 10→8；画布/打印两端缺省回退 ??10/||10 同步 8，旧模板显式字号不受影响）](./changelog/标签设计默认字体大小改为8.md)
- [标签条码标题与编码值同行（alttext 中文乱码实证废弃→DOM 文本行方案：条码 PNG 只画条形，「标题:编码值」DOM 行居中条形下方、统一用「字号」；画布/打印两端同步，displayValue 关且无标题时不渲染行）](./changelog/标签条码标题与编码值同行.md)
- [标签设计默认不显示网格（store showGrid 初始值 true→false，纯内存无持久化改默认即生效；打印设计页独立状态不受影响）](./changelog/标签设计默认不显示网格.md)
- [标签设计缩放连体与移除垂直对齐（缩放三独立控件收敛为 NButtonGroup 连体 [-|100%|+]，点击百分比仍重置；删「垂直对齐」按钮并清理 store.verticalCenterAll 与 i18n 键三处死代码；追代：quaternary 连体不可见，改 default 带边框共享边线）](./changelog/标签设计缩放连体与移除垂直对齐.md)
- [标签设计快捷键（Ctrl+S/Z/Y/Shift+Z、X/C/V 剪切复制粘贴、Delete 删除、方向键 1/10/0.1mm 移动带长按会话与纸界钳制；工具栏加快捷键按钮 + Popover Windows/Mac 两列对照弹窗，输入框聚焦不拦截）](./changelog/标签设计快捷键.md)
- [标签设计工具栏模板下拉改纯文本（去 NSelect 选择器，props 收敛为 templateName，页面内不再提供模板切换入口，默认仍加载列表第一个模板）](./changelog/标签设计工具栏模板下拉改纯文本.md)
- [标签条码编码值间距样式（BarcodeOptions.textGap 透传 bwip-js textyoffset，新拖入默认 2pt、旧数据紧贴零影响，样式卡「字号」旁可调，画布/打印单点生效）](./changelog/标签条码编码值间距样式.md)
- [标签设计工具栏按截图改造（新增返回上一页/垂直对齐=全元素整体垂直居中/清空带 dialog 确认；缩放改独立 — 100% + 三元素，网格文案改「显示网格」，预览改白底描边；撤销/重做保留）](./changelog/标签设计工具栏按截图改造.md)
- [标签设计工具栏两行合一（去「标签设计」标题文字，模板选择+保存经 props/events 并入 tool-bar.vue 单行：模板选择|撤销重做|纸张|网格|缩放……预览 保存）](./changelog/标签设计工具栏两行合一.md)
- [标签设计 Shift 等比缩放（Shift+角手柄锁定拖拽起始宽高比、以宽驱动高，dragState 记 shiftKey 实时切换，边手柄不参与；预览与落库同源 resizeGeometry）](./changelog/标签设计Shift等比缩放.md)
- [标签条码编码值文本不显示（旧 designJson 缺 displayValue 字段 loadFromJson 原样还原 → includetext undefined；渲染层 displayValue!==false 兜底 + buildModel 补默认，node 实测 PNG 对比定位）](./changelog/标签条码编码值文本不显示.md)
- [标签设计元素 hover 虚线与选中实线（hover 未选中显示 1px 虚线提示可交互、选中改 2px 实线，纯 CSS :hover 零 JS 状态；追代：缩放手柄 8px 蓝底方块改 6px 白底蓝边圆点并修正与描边线错位）](./changelog/标签设计元素hover虚线与选中实线.md)
- [标签业务字段标题显示规则（拖入不再回退 label 当标题；FieldDef/ElementSeed 全链路增 showTitle，showTitle && title 同时为真才开关联标题，缺省全部不显示）](./changelog/标签业务字段标题显示规则.md)
- [标签设计右侧紧凑化+字段类型自定义（FormWrap 增 size prop 透传 NForm，属性面板 small+label 12px；FieldDef 增 elementType 按字段定义创建元素，废除拖入一律 longText 硬编码）](./changelog/标签设计右侧紧凑化与字段类型自定义.md)
- [标签选中/hover框横竖外扩不一致（outline 外扩本身均匀，扁宽元素占比差+scale 非整数缩放亚像素舍入造成观感差；offset 归零贴元素边界彻底消除）](./changelog/标签选中框横竖外扩不一致.md)
- [标签设计器缩放控件优化（四元素松散混排改 NButtonGroup 紧凑组合 [-|80%|+]，点击百分比重置 100% 带 title 提示，删独立重置按钮）](./changelog/标签设计器缩放控件优化.md)
- [标签设计器移除刻度（删 .ruler 元素与样式、工具栏切换按钮、store showRuler、i18n ruler 键，打印设计页不受影响）](./changelog/标签设计器移除刻度.md)
- [标签样式移除标题对齐+新增显示边框（titleAlign 在行内流下无意义全链路删除；BorderExtras 给 text/longText/barcode/qrcode 加开关+宽度+颜色边框，默认关，画布/打印两端同步）](./changelog/标签移除标题对齐与新增显示边框.md)
- [标签设计切换元素后输入框残留旧值（根因：新元素 title/field 为 undefined → naive NInput 走非受控分支回落实例内部值，组件实例跨元素复用残留上一元素输入；已实施方案 A：buildModel 预置受控空串）](./changelog/标签设计切换元素后输入框残留旧值.md)
- [标签设计器左右面板折叠样式统一（右侧属性面板三区块合并为单个裸 NCollapse 与左侧同款，header flex-1 箭头贴右、去加粗；二次迭代：各折叠内容区包 NCard size=small 白底描边卡）](./changelog/标签设计器左右面板折叠样式统一.md)
- [标签标题与内容垂直错位→行内连续排版（分栏布局被否定：baseline 对齐后用户明确要标题+内容填进同一文本流不分块；文本改行内前缀 span 排版，折行行首顶格，基线天然对齐，画布与打印两处同步）](./changelog/标签标题与内容垂直错位.md)
- [标签设计左侧面板折叠（field-panel 两组改为 NCollapse + arrow-placement right，默认全展开，组内结构与拖拽零改动）](./changelog/标签设计左侧面板折叠.md)
- [标签设计点击后高度被压缩（按下时 will-change:transform 提升合成层导致非整数 px 高度被栅格化舍入，视觉矮 1~2px 松手恢复；删除 will-change 两行，拖动性能不受影响）](./changelog/标签设计点击后高度被压缩.md)
- [标签设计点击元素跑到左上角（拖动优化回归：pointerup 无条件清空内联样式后纯点击不写 store、Vue 不重渲染无人恢复；改为清空后按 store 权威值恢复定位样式）](./changelog/标签设计点击元素跑到左上角.md)
- [标签设计数据预览栏（删「内容与绑定」卡并入数据预览：标题名称/字段类型/关联标题开关/占位文本/绑定信息条，标题渲染画布与打印同步，placeholder 回退链）](./changelog/标签设计数据预览栏.md)
- [标签设计表单超出NCard（FormWrap 的 NGrid cols=24 产生 23×16=368px 列间隙开销压塌窄面板轨道，属性面板 grid-x-gap 调小为 8）](./changelog/标签设计表单超出NCard.md)

## 2026-09-12

- [标签设计器拖动卡顿优化（根因：pointermove 无 rAF 合帧全链路响应式重渲染；方案 A 设计已确认：拖动期直写 DOM transform/几何、pointerup 一次写回 store，点击不拖不产生撤销点，待实施）](./changelog/标签设计器拖动卡顿优化.md)

## 2026-09-11

- [标签设计器新建独立组件（不修改现有 print-design：新建 label-designer 目录 + 路由，纯数据驱动 Vue 组件树 + bwip-js，与 hiprint 版并存）](./changelog/标签设计器新建独立组件.md)
- [标签设计器自研方案（完全替代 vue-plugin-hiprint：纯数据驱动 Vue 组件树 + 手写 pointer 交互 + bwip-js 条码，修复暗黑/撤销重做，v1 砍 table）](./changelog/%E6%A0%87%E7%AD%BE%E8%AE%BE%E8%AE%A1%E5%99%A8%E8%87%AA%E7%A0%94%E6%96%B9%E6%A1%88.md)
- [重写打印设计右侧属性面板（修正选中事件名 + 混合：简单字段 Naive 控件 + 复杂字段 hiprint 原生控件兜底，字段零遗漏）](./changelog/%E9%87%8D%E5%86%99%E6%89%93%E5%8D%B0%E8%AE%BE%E8%AE%A1%E5%8F%B3%E4%BE%A7%E5%B1%9E%E6%80%A7%E9%9D%A2%E6%9D%BF.md)
- [标签设计工作区无限画布改造（中间区改造成无限工作台+浮层纸张，拖动平移/滚轮缩放、刻度显隐按钮，缩放围绕纸张中心、范围0.2–4.0）](./changelog/%E6%A0%87%E7%AD%BE%E8%AE%BE%E8%AE%A1%E5%B7%A5%E4%BD%9C%E5%8C%BA%E6%97%A0%E9%99%90%E7%94%BB%E5%B8%83%E6%94%B9%E9%80%A0.md)
- [禁用标签设计框选（hiprint 在 .hiprint-printPaper 冒泡监听 mousedown 置 rectDraging 并创建 mouseRect 选框；改为捕获阶段拦截纸张空白 mousedown）](./changelog/%E7%A6%81%E7%94%A8%E6%A0%87%E7%AD%BE%E8%AE%BE%E8%AE%A1%E6%A1%86%E9%80%89.md)

## 2026-09-09

- [去掉标签设计界面最外层 padding（base-layout 按路由关掉 content 的 p-16px，仅打印设计页占满可视区）](./changelog/%E5%8E%BB%E6%8E%89%E6%A0%87%E7%AD%BE%E8%AE%BE%E8%AE%A1%E7%95%8C%E9%9D%A2%E6%9C%80%E5%A4%96%E5%B1%82padding.md)
- [预览弹窗宽度贴合标签纸（容器去 min-w-400px 改 w-fit + NModal 透传 content-style 去 NCard 左右内边距）](./changelog/%E9%A2%84%E8%A7%88%E5%BC%B9%E7%AA%97%E5%AE%BD%E5%BA%A6%E8%B4%B4%E5%90%88%E6%A0%87%E7%AD%BE%E7%BA%B8.md)
- [预览弹窗左右留白边（content-style 左右 padding 从 0 改回 16px，让纸张两侧各留一点白边）](./changelog/%E9%A2%84%E8%A7%88%E5%BC%B9%E7%AA%97%E5%B7%A6%E5%8F%B3%E7%95%99%E7%99%BD%E8%BE%B9.md)
- [标签预览弹窗不限制宽度（移除 w-800px 固定宽度，改为自适应 + maxWidth 95vw）](./changelog/%E6%A0%87%E7%AD%BE%E9%A2%84%E8%A7%88%E5%BC%B9%E7%AA%97%E4%B8%8D%E9%99%90%E5%88%B6%E5%AE%BD%E5%BA%A6.md)
- [打印预览测试数据不更新（getHtml 用固定 sample 覆盖 testData，改为读取设计器当前 testData）](./changelog/%E6%89%93%E5%8D%B0%E9%A2%84%E8%A7%88%E6%B5%8B%E8%AF%95%E6%95%B0%E6%8D%AE%E4%B8%8D%E6%9B%B4%E6%96%B0.md)
- [打印设计标尺不显示（设计画布未加载 hiprint 全局 CSS，显式补充 .hiprint_rul_wrapper 样式）](./changelog/%E6%89%93%E5%8D%B0%E8%AE%BE%E8%AE%A1%E6%A0%87%E5%B0%BA%E4%B8%8D%E6%98%BE%E7%A4%BA.md)

## 2026-09-08

- [通用 Table 新增 headerCellConfig prop（模板已引用但 Props 未声明，补齐声明与默认值）](./changelog/%E6%96%B0%E5%A2%9EheaderCellConfig.md)

## 2026-09-07

- [标签打印模板设计器实现（vue-plugin-hiprint：三栏设计器 + 字段 provider + 预览打印，已实现待手测）](./changelog/%E6%A0%87%E7%AD%BE%E6%89%93%E5%8D%B0%E6%A8%A1%E6%9D%BF%E8%AE%BE%E8%AE%A1%E5%99%A8%E5%AE%9E%E7%8E%B0.md)
- [标签打印模板设计器设计方案（vue-plugin-hiprint：独立路由页 + 自定义字段 provider + 浏览器预览打印）](./changelog/%E6%A0%87%E7%AD%BE%E6%89%93%E5%8D%B0%E6%A8%A1%E6%9D%BF%E8%AE%BE%E8%AE%A1%E5%99%A8%E8%AE%BE%E8%AE%A1%E6%96%B9%E6%A1%88.md)
- [字段映射卡片头部加各模块「一键全选」按钮（每个模块一个，点击切换全选/清空，新增 disabled prop）](./changelog/%E5%AD%97%E6%AE%B5%E6%98%A0%E5%B0%84%E5%8D%A1%E7%89%87%E5%A4%B4%E9%83%A8%E5%8A%A0%E5%90%84%E6%A8%A1%E5%9D%97%E4%B8%80%E9%94%AE%E5%85%A8%E9%80%89%E6%8C%89%E9%92%AE.md)
- [阿里巴巴普惠体字体子集化（自动提取项目文字生成 woff2，6.53MB→116KB）](./changelog/%E9%98%BF%E9%87%8C%E5%B7%B4%E5%B7%B4%E6%99%AE%E6%83%A0%E4%BD%93%E5%AD%97%E4%BD%93%E5%AD%90%E9%9B%86%E5%8C%96.md)
- [表格 detail 列复制图标改为「行 hover 显示」且移出 Link 组件（图标常占位 + rowid 关联固定列）](./changelog/%E8%A1%A8%E6%A0%BCdetail%E5%88%97%E5%A4%8D%E5%88%B6%E5%9B%BE%E6%A0%87%E6%94%B9%E4%B8%BA%E8%A1%8Chover%E6%98%BE%E7%A4%BA.md)

## 2026-09-05

- [分配权限界面改为表格模式（抽屉内 vxe 树形表格 + 双接口拼接列数据，已实现待手测）](./changelog/%E5%88%86%E9%85%8D%E6%9D%83%E9%99%90%E7%95%8C%E9%9D%A2%E6%94%B9%E4%B8%BA%E8%A1%A8%E6%A0%BC%E6%A8%A1%E5%BC%8F.md)
- [导出组件增加「数据范围」选项（全部 / 当前页 / 勾选数据），仅作用于 exceljs 字段选择弹窗（已实现，待手测）](./changelog/%E5%AF%BC%E5%87%BA%E8%8C%83%E5%9B%B4%E9%80%89%E9%A1%B9%E8%AE%BE%E8%AE%A1.md)
- [导出字段弹窗升级为「可新增字段」的编辑表格（可编辑列名 + 拖拽排序 + 自定义字段取数据/固定值）](./changelog/%E5%AF%BC%E5%87%BA%E5%AD%97%E6%AE%B5%E5%BC%B9%E7%AA%97%E5%8D%87%E7%BA%A7%E4%B8%BA%E5%8F%AF%E6%96%B0%E5%A2%9E%E5%AD%97%E6%AE%B5%E7%9A%84%E7%BC%96%E8%BE%91%E8%A1%A8%E6%A0%BC.md)
- [Table action-export 改为打开 vxe-table 原生高级导出弹窗](./changelog/Table%20action-export%20%E6%89%93%E5%BC%80%20vxe%20%E9%AB%98%E7%BA%A7%E5%AF%BC%E5%87%BA%E5%BC%B9%E7%AA%97.md)
- [共用导出 v3：Table 右上 action-export 回归 vxe 原生一键导出 + exceljs 字段选择导出放表格左侧操作栏（两套并存）](./changelog/共用导出组件设计.md)

## 2026-09-04

- [导出格式界面实现（左侧 13 项固定分类 + 右侧模板表格 + 新建/编辑含上传文件/删除，下载为占位）](./changelog/导出格式界面实现.md)
- [打印格式界面实现（左侧固定分类 + 右侧模板表格 + 新建/查看/复制/删除/设为默认）](./changelog/打印格式界面实现.md)
- [菜单「状态」选了仍报必填（FormWrap 必填规则带 trigger 导致数字值被判空）](./changelog/菜单状态必填校验失败.md)
- [录单格式「字段映射」高度自适应占满（MasterDetail slot 容器撑满 + FieldMapping fill）](./changelog/录单格式字段映射高度自适应占满.md)
- [录单格式列表启用/禁用不明显（MasterDetail 状态改实心彩色徽标）](./changelog/录单格式列表启用禁用不明显.md)
- [设置页整理：字段映射项改为 key/label/span 结构 + 清空其余 5 页占位内容（LookForward 空状态）](./changelog/设置页整理字段映射结构与占位清理.md)

## 2026-09-03

- [设置页 MasterDetail：启用/禁用与操作栏改为可选（showStatus/showActions）](./changelog/设置页MasterDetail状态与操作栏改为可选.md)
- [设置页操作按钮样式统一（工具栏 ghost 一致）](./changelog/设置页操作按钮样式统一.md)
- [系统设置点开卡死：路由重定向死循环修复（子页移出自动路由目录）](./changelog/系统设置点开卡死-路由重定向死循环修复.md)
- [录单格式列表 hover 样式修正（hover 与选中态区分）](./changelog/录单格式列表hover样式修正.md)
- [系统设置 tab 栏不够突出：NCard 包裹增强视觉层次](./changelog/系统设置tab栏不够突出.md)
- [系统设置右侧内容区补充背景卡片（MasterDetail 右侧 NCard 包裹）](./changelog/系统设置右侧内容区补充背景卡片.md)
- [系统设置：顶层 tab 固定显示「系统设置」+ 页内 tab 切子模块](./changelog/系统设置顶层tab显示系统设置.md)
- [系统设置：单一菜单项 + tab 切换（hiddenRoutes 配置 + 默认进入录单格式）](./changelog/系统设置单一菜单项与tab切换.md)
- [录单格式列表 hover 与选中高亮失效修复（改用 useThemeVars）](./changelog/录单格式列表hover与选中高亮修复.md)
- [pnpm release 提交被 pre-commit 钩子拦截（git diff --exit-code 失败）](./changelog/pnpm-release 提交被 pre-commit 拦截.md)
- [侧边栏菜单顺序 vs elegant-router 自动重排路由文件](./changelog/路由菜单顺序与elegant路由生成.md)
- [资料管理子档案按需懒加载（KeepAlive 缓存）](./changelog/资料管理子档案按需懒加载.md)
- [搜索栏独立封装为 SearchBar 组件（通用 Table 配置委托、各界面独立配置）](./changelog/搜索栏内置通用Table组件.md)

## 2026-09-01

- [菜单管理样式对齐](./changelog/菜单管理样式对齐.md)
- [菜单管理多级测试数据](./changelog/菜单管理多级测试数据.md)（已撤销）
- [菜单管理树形子级不显示](./changelog/菜单管理树形子级不显示.md)
- [图标选择器封装](./changelog/图标选择器封装.md)
- [图标选择器接入 xicons（@vicons）](./changelog/图标选择器接入xicons.md)
- [菜单排序必填校验不通过（number 字段缺 type）](./changelog/菜单排序必填校验不通过.md)
- [菜单管理操作列「增加子菜单」](./changelog/增加子菜单.md)
- [资料管理模块设计](./changelog/资料管理模块设计.md)（基础/财务/业务资料，方案 A 通用配置驱动组件）
- [pnpm release 命令报错诊断](./changelog/pnpm-release命令报错诊断.md)
- [资料管理子档案左侧子导航（不作为路由/菜单）](./changelog/资料管理子档案左侧子导航.md)

## 2026-08-31

- [重写 AGENTS 文档结构](./changelog/重写AGENTS文档结构.md)
- [菜单管理模块设计](./changelog/菜单管理模块设计.md)
- [角色管理搜索区改用 FormWrap](./changelog/角色管理搜索区改用FormWrap.md)
- [FormWrap 展开/收起 + 右侧独立按钮区 设计与实现](./changelog/FormWrap展开收起设计.md)
